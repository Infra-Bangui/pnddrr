/* PNDDRR engine bundle — generated by scripts/build-engine.mjs — DO NOT EDIT */
/* Source of truth: src/modules/ — run: npm run engine:build */
/* module: core/header.js — PNDDRR engine (classic globals) */

/* ================================================================
   PNDDRR — Application de suivi du processus DDR en RCA
   Données conservées en mémoire pendant la session.
   Utiliser « Sauvegarde » pour exporter/restaurer le fichier JSON.
   ================================================================ */

/* ---------- Emblème (simplifié — remplacer ARM_SVG par le SVG
   officiel des armoiries de la RCA lorsqu'il sera disponible) ---------- */
/* Armoiries officielles de la République Centrafricaine (SVG fourni) */
const ARM_SVG = `<svg viewBox="0 0 750 713" xml:space="preserve" xmlns="http://www.w3.org/2000/svg"> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" stroke="#000"> <path d="m217.58 374.75 42.92 1.93s-1.45-2.16-9.09-5.82-10.16-10.16-10.16-10.16 4.8 2.6 11.22 5.2 10.8 2.76 10.8 2.76-29.98-26.49-29.82-26.24c0.16 0.24 38.52 18.77 38.52 18.77s-1.46-2.93-6.74-6.75c-5.29-3.82-3.74-12.59-3.74-12.59s11.05 14.22 17.14 13.97c-2.44-8.69-14.06-34.37-14.06-34.37l24.06 30.96s1.14-3.9-0.16-10.16c-1.3-6.25 2.27-12.19 2.27-12.19s0.89 6.59 3.82 11.22c2.92 4.63 4.63 5.93 4.06 8.78 2.68-3.99 6.83-36.65 6.83-36.65s3.33 27.06 3.33 35.26c0.49 3.25 2.76-0.4 4.15-5.2 1.38-4.79 3.81-7.88 9.42-12.1-2.92 4.71-3.09 11.21-3.01 14.7 0.09 3.5-0.32 5.53-0.32 5.53l22.99-30.72-12.92 34.78s3.99-2.03 8.46-6.34c4.46-4.3 8.12-6.82 13.73-5.93-5.53 3.33-8.45 9.27-11.46 15.2 14.63-6.59 32.75-17.72 32.75-17.72l-28.44 27.06s5.2 1.38 12.35-1.87 13.41-2.92 15.93 0.33c-8.21-0.25-17.56 5.93-22.59 10.72 20.96-3.25 41.12-4.95 41.12-4.95l-39.58 13.24s6.82 1.71 12.35 2.12c5.53 0.4 12.11 2.52 13.17 7.06-7.72-2.92-17.72-1.3-25.93-0.97 8.29 2.52 32.18 11.54 32.18 11.54l-33.31-4.39s-1.71 0.81-2.36 3.33-77.68 6.99-83.7 5.36c-6.01-1.62-9.42-0.48-8.93-0.81 0.48-0.32 5.12-5.52 5.2-6.74s-39.33 3.25-39.58 3.25c-0.24 0 37.71-11.54 37.71-11.54s-3.98-2.2-9.59-2.28-13.16-0.16-15.68-3.82c8.77-0.65 11.94-0.73 15.52-1.87 3.57-1.13 7.07-0.81 7.07-0.81z" fill="#ffd200" stroke-linecap="round" stroke-linejoin="round"/> <g fill="none"> <g stroke-linecap="round" stroke-linejoin="round"> <path d="M262.87 397.10001 268.66 396.23001"/> <path d="M259.69 389.38 267.79001 389.25"/> <path d="m260 381.4c0.25 0.13 8.16 1.37 8.16 1.37"/> <path d="m263.99 373.8c0.31 0.19 5.98 2.55 5.98 2.55"/> <path d="M285.54999 356.16 288.17001 361.45999"/> </g> <ellipse transform="translate(-2.5474)" cx="306.51999" cy="389.28" rx="32.5" ry="28.381001" stroke-width="1.5"/> <g stroke-linecap="round" stroke-linejoin="round"> <path d="M294.45999 352.98001 295.70999 358.87"/> <path d="m304.74 351.86c-0.06 0.38-0.31 5.3-0.31 5.3"/> <path d="M313.03 353.42001 311.72 357.60001"/> <path d="M322.56 355.73001 320.01001 360.28"/> <path d="M342.75 373.60999 337.82999 376.35001"/> <path d="M346.42999 380.64999 340.13 381.89999"/> <path d="M348.29999 389.44 341.13 389.19"/> <path d="M346.42999 395.79999 340.76001 395.67001"/> <path d="M268.45001 365.01999 274.31 370.48999"/> <path d="M276.35999 359.89001 279.92999 364.62"/> <path d="M339.39999 365.45001 333.84 369.88"/> <path d="M331.79999 360.29999 328.14999 364.53"/> </g> </g> <path stroke-linecap="round" stroke-linejoin="round" d="M125.77 325.39 137.13 325.24 121.32 310.96z"/> <path fill="#fff" stroke-linecap="round" stroke-linejoin="round" d="M121.32 310.96 137.13 325.24 138.04 315.74z"/> </g> <path d="m94.513864 440.3452c-1.103754 0.54275-2.534544 1.41948-4.026655 2.73461 0.06131-0.0626 1.53299-1.27336 4.026655-2.73461z" fill="none"/> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"> <path d="m167.06 489.8c0.61-0.3 1.01-0.4 1.01-0.4s7.31 11.82 12.18 23.13c7.75-6.03 8.37-12.23 6.27-19.41-7.52-8.55-15.77-5.44-19.46-3.32z" fill="#005cab"/> <path fill="none" d="M165.09 491.11 165.08 491.12z"/> <path d="m239.26 468.84c-24.75-14.64-64.89-47.71-74.23-55.5 0.17 0.99 0.32 1.89 0.43 2.66 1.36 9.27 3.38 23.13 7.19 38.4 9.36 5.68 39.29 24.29 64.2 45.59 0.92-10.23 1.72-20.9 2.41-31.15z" fill="#d21034"/> </g> <path d="m126.87018 427.84114c-3.94489 14.69592-8.11463 29.55884-12.5092 44.44263 2.47322 5.05174 4.90557 10.29133 7.11307 15.51005 22.2999-17.72278 15.75915-36.15532 5.39613-59.95268z" fill="none"/> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" stroke="#000" stroke-linecap="round" stroke-linejoin="round"> <g stroke-width="1.5"> <path d="m168.07 489.4s-9.44 2.43-6.1 17.52c2.95 5.06 6.42 8.66 10.59 11.74 1.45-4.51 2.85-9.03 4.21-13.56-4.26-8.52-8.7-15.7-8.7-15.7z" fill="#fff"/> <path d="m176.25 467.31c1.89 6.11 4.44 11.55 6.64 16.5 1.83-6.65 3.56-13.24 5.2-19.69-6.82-4.44-12.29-7.81-15.44-9.72 1.06 4.22 2.24 8.54 3.6 12.91z" fill="#fff"/> <path d="m205.82 381.75c-2.26 12.84-6.02 32.45-11.24 55.38 8.89 6.93 18.64 14.27 27.82 20.69 5.85-22.01 10.37-40.02 12.76-51.78-6.68-5.53-17.37-14.39-29.34-24.29z" fill="#289728"/> <path d="m242.34 412s-2.68-2.22-7.18-5.96c-2.39 11.76-6.91 29.77-12.76 51.78 6 4.2 11.76 8.01 16.86 11.02 2.04-30.31 3.08-56.84 3.08-56.84z" fill="#ffd200"/> <path d="m228.08 554.27c3.59-6.73 6.52-29.15 8.77-54.28-6.89-5.89-14.17-11.58-21.3-16.85-4.4 16.1-9.25 33.55-14.32 51.72 14.25 8.84 26.85 19.41 26.85 19.41z" fill="#ffd200"/> <path d="m182.89 483.81c5.07 11.4 8.27 20.23-2.64 28.72-1.08-2.5-2.27-5.01-3.48-7.43-1.36 4.53-2.76 9.05-4.21 13.56 4.93 3.64 10.85 6.54 18.12 10.22 3.51 1.77 7.08 3.82 10.55 5.98 5.07-18.17 9.92-35.62 14.32-51.72-9.96-7.36-19.61-13.9-27.46-19.02-1.64 6.45-3.37 13.04-5.2 19.69z" fill="#289728"/> <path d="m176.39 357.44c-2.35 21.24-6.42 41.56-11.44 55.46 0.02 0.15 0.05 0.3 0.08 0.44 4.47 3.73 16.02 13.26 29.55 23.79 5.22-22.93 8.98-42.54 11.24-55.38-9.46-7.83-19.73-16.31-29.43-24.31z" fill="#fff"/> <path d="m162.39 400.43c-3.67-16.53-9.5-39.28-13.1-48.61-3.18-8.22-6.08-17.34-7.79-22.96-0.7-0.56-1.36-1.08-1.96-1.56-0.17 6.26 4.53 34.78 6.61 49.56 0.88 6.22 1.79 15.47 2.56 25.34 4.19-2.58 8.96-3.58 13.68-1.77z" fill="#005cab"/> <path d="m156.24 493.22c1.7 5.57 3.58 10.02 5.73 13.7-2.04-9.2 0.68-13.69 3.11-15.8l0.01-0.01c0.58-0.51 1.15-0.87 1.63-1.13-3.37-6.34-9.6-18.75-16-31.98 0.22 10.27 1.23 21.15 5.52 35.22z" fill="#fff"/> <path d="m169.02 437.76c-1.62-8.51-2.71-16.02-3.56-21.76-0.49-3.37-1.61-8.98-3.07-15.57-4.72-1.81-9.49-0.81-13.68 1.77 1.06 13.61 1.87 28.39 1.97 37.98v0.68c6.05-3.56 12.68-5.26 18.34-3.1z" fill="#d21034"/> <path d="m176.25 467.31c-3.23-10.39-5.52-20.53-7.23-29.55-5.66-2.16-12.29-0.46-18.34 3.09 0.05 5.83-0.08 11.4 0.04 17.15 6.4 13.23 12.63 25.64 16 31.98 0.12-0.06 0.23-0.13 0.34-0.18 3.69-2.12 11.94-5.23 19.46 3.32-2.14-7.31-7.11-15.63-10.27-25.81z" fill="#005cab"/> <path d="m176.39 357.44c-14.25-11.74-27.31-22.46-34.89-28.58 1.71 5.62 4.61 14.74 7.79 22.96 0.67 1.74 1.43 3.96 2.23 6.52l4.65 2.24c1.95-2.94 3.7-5.64 3.7-5.64l0.54 7.68 7.09 3.42-6.8 0.8 0.59 8.34-5.48-7.76-1.58 0.18c4.24 15.22 8.89 35.18 10.72 45.31 5.02-13.91 9.09-34.23 11.44-55.47z" fill="#005cab"/> <path d="m153.08 363.54c0.11 0.38 0.22 0.76 0.33 1.14 0.08-0.11 0.15-0.22 0.23-0.34z" fill="none"/> <path d="m161.29 375.18-0.59-8.34 6.8-0.8-7.09-3.42-0.54-7.68s-1.75 2.7-3.7 5.64l-4.65-2.24c0.5 1.61 1.02 3.35 1.56 5.2l0.56 0.8c-0.08 0.12-0.15 0.23-0.23 0.34 0.27 0.95 0.55 1.93 0.82 2.92l1.58-0.18z" fill="#ffd200"/> </g> <path d="m243.75 414.75c-0.34 0-0.68-0.11-0.96-0.34l-105.3-87.11c-0.63-0.52-0.72-1.47-0.2-2.11 0.53-0.64 1.48-0.73 2.12-0.2l105.3 87.1c0.63 0.53 0.72 1.48 0.2 2.11-0.3 0.36-0.73 0.55-1.16 0.55z" fill="#414142"/> <circle cx="138.45" cy="326.14999" r="3.3929999" fill="#414142"/> <path d="M487.25 310.96 482.8 325.39 471.44 325.24z"/> <path fill="#fff" d="M470.52 315.74 487.25 310.96 471.44 325.24z"/> </g> <path d="m655.48614 440.3452c1.10376 0.54275 2.53456 1.41948 4.02666 2.73461-0.0613-0.0626-1.53301-1.27336-4.02666-2.73461z" fill="none"/> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"> <path d="m441.51 489.8c-0.61-0.3-1.01-0.4-1.01-0.4s-7.31 11.82-12.18 23.13c-7.75-6.03-8.38-12.23-6.28-19.41 7.53-8.55 15.78-5.44 19.47-3.32z" fill="#005cab"/> <path fill="none" d="M443.48 491.11 443.49 491.12z"/> <path d="m369.31 468.84c24.75-14.64 64.88-47.71 74.23-55.5-0.17 0.99-0.32 1.89-0.44 2.66-1.36 9.27-3.37 23.13-7.19 38.4-9.35 5.68-39.28 24.29-64.19 45.59-0.92-10.23-1.73-20.9-2.41-31.15z" fill="#d21034"/> </g> <path d="m623.1094 427.84114c3.9653 14.69592 8.13506 29.55884 12.50918 44.44263-2.45278 5.05174-4.88512 10.29133-7.09264 15.51005-22.2999-17.72278-15.75913-36.15532-5.41654-59.95268z" fill="none"/> <g stroke="#000" stroke-linecap="round" stroke-linejoin="round"> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" stroke-width="1.5"> <path d="m440.5 489.4s9.44 2.43 6.1 17.52c-2.96 5.06-6.42 8.66-10.6 11.74-1.44-4.51-2.85-9.03-4.21-13.56 4.27-8.52 8.71-15.7 8.71-15.7z" fill="#fff"/> <path d="m432.32 467.31c-1.9 6.11-4.44 11.55-6.65 16.5-1.82-6.65-3.55-13.24-5.19-19.69 6.82-4.44 12.29-7.81 15.44-9.72-1.06 4.22-2.24 8.54-3.6 12.91z" fill="#fff"/> <path d="m402.75 381.75c2.26 12.84 6.02 32.45 11.24 55.38-8.89 6.93-18.65 14.27-27.82 20.69-5.85-22.01-10.37-40.02-12.76-51.78 6.68-5.53 17.37-14.39 29.34-24.29z" fill="#289728"/> <path d="m366.23 412s2.68-2.22 7.18-5.96c2.39 11.76 6.9 29.77 12.76 51.78-6 4.2-11.76 8.01-16.86 11.02-2.04-30.31-3.08-56.84-3.08-56.84z" fill="#ffd200"/> <path d="m380.49 554.27c-3.59-6.73-6.53-29.15-8.77-54.28 6.89-5.89 14.17-11.58 21.29-16.85 4.4 16.1 9.25 33.55 14.33 51.72-14.25 8.84-26.85 19.41-26.85 19.41z" fill="#ffd200"/> <path d="m425.67 483.81c-5.06 11.4-8.26 20.23 2.65 28.72 1.07-2.5 2.27-5.01 3.47-7.43 1.36 4.53 2.77 9.05 4.21 13.56-4.92 3.64-10.84 6.54-18.11 10.22-3.51 1.77-7.08 3.82-10.55 5.98-5.08-18.17-9.93-35.62-14.33-51.72 9.96-7.36 19.61-13.9 27.47-19.02 1.64 6.45 3.37 13.04 5.19 19.69z" fill="#289728"/> <path d="m432.18 357.44c2.35 21.24 6.42 41.56 11.44 55.46-0.02 0.15-0.05 0.3-0.08 0.44-4.48 3.73-16.02 13.26-29.55 23.79-5.23-22.93-8.98-42.54-11.24-55.38 9.46-7.83 19.72-16.31 29.43-24.31z" fill="#fff"/> <path d="m446.18 400.43c3.67-16.53 9.49-39.28 13.1-48.61 3.18-8.22 6.08-17.34 7.79-22.96l1.95-1.56c0.18 6.26-4.52 34.78-6.61 49.56-0.87 6.22-1.78 15.47-2.55 25.34-4.2-2.58-8.96-3.58-13.68-1.77z" fill="#005cab"/> <path d="m452.32 493.22c-1.69 5.57-3.57 10.02-5.72 13.7 2.03-9.2-0.68-13.69-3.11-15.8l-0.01-0.01c-0.59-0.51-1.15-0.87-1.63-1.13 3.36-6.34 9.6-18.75 16-31.98-0.22 10.27-1.24 21.15-5.53 35.22z" fill="#fff"/> <path d="m439.55 437.76c1.62-8.51 2.71-16.02 3.56-21.76 0.49-3.37 1.61-8.98 3.07-15.57 4.72-1.81 9.48-0.81 13.68 1.77-1.06 13.61-1.87 28.39-1.97 37.98v0.68c-6.06-3.56-12.69-5.26-18.34-3.1z" fill="#d21034"/> <path d="m432.32 467.31c3.22-10.39 5.52-20.53 7.23-29.55 5.65-2.16 12.28-0.46 18.34 3.09-0.05 5.83 0.08 11.4-0.04 17.15-6.4 13.23-12.64 25.64-16 31.98-0.12-0.06-0.23-0.13-0.34-0.18-3.69-2.12-11.94-5.23-19.47 3.32 2.15-7.31 7.12-15.63 10.28-25.81z" fill="#005cab"/> <path d="m432.18 357.44c14.25-11.74 27.31-22.46 34.89-28.58-1.71 5.62-4.61 14.74-7.79 22.96-0.67 1.74-1.43 3.96-2.23 6.52l-4.65 2.24c-1.95-2.94-3.7-5.64-3.7-5.64l-0.54 7.68-7.09 3.42 6.8 0.8-0.59 8.34 5.48-7.76 1.58 0.18c-4.24 15.22-8.9 35.18-10.72 45.31-5.02-13.91-9.09-34.23-11.44-55.47z" fill="#005cab"/> <path d="m455.49 363.54c-0.11 0.38-0.22 0.76-0.33 1.14-0.08-0.11-0.16-0.22-0.24-0.34z" fill="none"/> <path d="m447.28 375.18 0.58-8.34-6.79-0.8 7.09-3.42 0.54-7.68s1.74 2.7 3.7 5.64l4.65-2.24c-0.5 1.61-1.03 3.35-1.56 5.2l-0.57 0.8c0.08 0.12 0.16 0.23 0.24 0.34-0.28 0.95-0.55 1.93-0.83 2.92l-1.58-0.18z" fill="#ffd200"/> </g> <g stroke-width="2.06562"> <path d="m498.73277 283.67922c0.67451 0 1.36946-0.22964 1.94179-0.70977l215.23185-181.84109c1.30813-1.08549 1.4921-3.068602 0.40879-4.404595-1.08332-1.335992-3.00467-1.523866-4.31281-0.417498l-215.23184 181.82023c-1.30818 1.10636-1.49213 3.08948-0.40881 4.40459 0.6132 0.7515 1.4921 1.14813 2.37103 1.14813z" fill="#414142"/> <ellipse cx="713.9646" cy="98.727745" rx="6.9352484" ry="7.0828471" fill="#414142"/> <path d="m220.91402 259.15123v257.78392c0 28.51508 31.57962 29.9972 65.42803 29.9972h61.52402c10.75138 0 23.05617 8.12031 27.14415 15.15517 4.08797-7.03486 16.39278-15.15517 27.12371-15.15517h61.52403c33.84841 0 65.44845-1.48212 65.44845-29.9972v-257.78392z"/> </g> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" stroke-width=".9963"> <path fill="#fff" d="M304.29001 408H374.164011V471.921001H304.29001z"/> <path d="m304.29 471.92v68.95c3.54-2.83 8.4-5.01 13.17-5.01h29.89c20.55 0 26.81-2.19 26.81-9.37v-54.57z" fill="#005cab"/> <path fill="#289728" d="M234.41 408H304.284001V471.921001H234.41z"/> <path d="m234.41 471.92v54.57c0 7.18 6.27 9.37 26.82 9.37h29.88c4.77 0 9.63 2.18 13.18 5.01v-68.95z" fill="#ffd200"/> <path d="m279.19 449.25v41.69c0 4.61 5.14 4.85 10.66 4.85h10.02c1.75 0 3.75 1.31 4.42 2.45 0.66-1.14 2.67-2.45 4.42-2.45h10.02c5.51 0 10.66-0.24 10.66-4.85v-41.69z" fill="#fff"/> </g> <path d="m375.01022 450.13552c-2.30969-1.87874-5.45745-3.33997-8.54387-3.33997h-19.35656c-13.30635 0-17.3739-1.44038-17.3739-6.19984v-78.6357h90.5282v78.6357c0 4.75946-4.04708 6.19984-17.37389 6.19984h-19.35654c-3.08643 0.0208-6.23418 1.46123-8.52344 3.33997z" fill="#d21034" stroke-width="1.94872"/> <ellipse cx="374.11783" cy="402.29031" rx="39.289524" ry="34.518703" fill="#fff" stroke-width="2.05797"/> </g> <path transform="matrix(2.0439872,0,0,2.0874883,-251.26048,-582.10653)" d="M244.14 514.92 252.6 513.18 255.27 504.87 256.86 513.18 265.58 515.73 256.86 517.6 254.45 525.78 252.6 517.6z"/> <ellipse cx="269.6701" cy="493.61792" rx="6.820785" ry="6.7905993" fill="#fff" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width="2.05797"/> <path transform="matrix(2.0439872,0,0,2.0874883,-251.26048,-582.10653)" d="M276.03 514.92 284.49 513.18 287.17 504.87 288.75 513.18 297.48 515.73 288.75 517.6 286.34 525.78 284.49 517.6z"/> <ellipse cx="334.87332" cy="493.61792" rx="6.8228292" ry="6.7905993" fill="#fff" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width="2.05797"/> <path transform="matrix(2.0439872,0,0,2.0874883,-251.26048,-582.10653)" d="M258.8 494.02 267.26 492.28 269.93 483.97 271.52 492.28 280.24 494.82 271.52 496.7 269.11 504.87 267.26 496.7z"/> <ellipse cx="299.63492" cy="449.98941" rx="6.8228292" ry="6.7905993" fill="#fff" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width="2.05797"/> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" stroke="#000" stroke-linecap="round" stroke-linejoin="round"> <path d="m361.32 431.91c0.96-1.81-0.09-2.96-2.29-2.96 0.47-2.59-0.2-5.07-1.92-4.97 0.96-1.63 0.39-4.98-4.01-4.6 1.05-3.82-0.58-5.06-4.69-4.59-0.86-2.58-2.77-1.24-2.77-1.24s-0.39-3.06-4.12-3.06-4.59 2.77-4.59 2.77-3.06 0.19-4.4 3.64c-1.91-0.19-4.01 0-5.83 4.2-1.63 0.2-2.3 1.73-2.3 1.73s-4.39 0.57-4.39 3.15 1.81 2.78 1.81 2.78 0.1 1.81 3.35 1.72c-1.05 0.38-4.11-0.67-5.64 1.34-3.26 0.28-2.2 5.54 0.09 5.64-0.57 1.72 2.39 1.91 3.45 1.34-0.29 1.15 1.52 0.96 3.15-0.48 1.82 1.53 3.16 0.57 3.16 0.57s2.87 0.96 4.68 0c0.48 1.73 2.49 0 2.49 0s-0.1 1.63 0.96 2.3c0.25 0.16 0 12.62 0 12.62s-6.32 5.93-6.51 10.14c3.45-2.68 7.08-3.83 7.08-3.83s0.29 5.46 0.86 6.32c0.96-3.35 1.34-4.69 1.34-4.69s4.11 4.49 4.78 6.31c0.38-2-0.48-7.07-0.48-7.07s8.04 1.33 10.24 4.11c-0.96-3.64-10.62-11-10.62-11l-0.77-13.68s0.67-1.72 2.2-2.1 2.68-0.67 2.78-1.91c0.19 2.19 1.53 4.68 4.49 3.53 2.3 2.3 3.83 0.1 4.21-1.72 2.01-0.48 1.91-2.58 1.91-2.58s2.2 0.1 2.3-1.34c1.15-0.38 1.53-1.91 0-2.39zm-21.99 5.93c-0.32 0.16-0.49-1.33-1.72-0.94 0-0.17-0.52-1.37-2.04-1.04 0-0.85-0.72-1.82-2.18-0.91 1.88-1.14 0.71-3.18 0.71-3.18 1.4 0.09 3.09-1.01 3.09-1.01s0.84 0.23 2.24 0.06c0 0.78 1.72 1.98 1.72 1.98s-0.13 1.08 0.06 2.21c-0.97 0.23-1.55 2.67-1.88 2.83z" fill="#289728"/> <g fill="none"> <path d="m326.13 435.36c0.19-0.48-0.29-2.78 1.91-3.35 2.2-0.58 2.68 1.15 2.68 1.15"/> <path d="m327.56 426.85s0.67 1.91 3.25 1.24"/> <path d="m330.33 422.54s-0.38 4.31 4.78 2.2 0.2 4.02 4.21 1.53"/> <path d="m334.35 422.73c0.19 0.48 1.34 2.39 2.3 2.49"/> <path d="m339.13 419.58c0.29 0 6.03-0.58 5.26 4.11 3.44-2.87 5.84 0.38 6.22 2.87"/> <path d="m342.38 413.74c-0.19 0.86-1.05 2.49 0.58 3.06"/> <path d="m345.06 416.04s1.82 1.53 1.63 3.06"/> <path d="m352.62 425.22c0.57 0.19 6.41 3.25 1.24 6.6"/> <path d="m355.87 431.91c0.57 0.1 2.2 3.83-3.35 4.69"/> <path d="m333.95 429s2 3.2 5.54 0.55"/> <path d="m341.9 430.2c0.45 0.38 0.33 0.85 1.45 0.82"/> <path d="m343.75 428.31c0 0.1-0.3 1.49 0.7 1.69"/> <path d="m347.96 429.23c0.07-0.05-0.05 2.27-2.74 2.14"/> <path d="m340.46 431.75c0.25 0.05 1.62 0.29 2.39-0.73 0 1.22 1.35 1.32 1.45 1.3 0.09-0.02-1.8 1.59 0.1 3.11-0.45-0.19-1.6-0.19-2.1 1.5"/> </g> <path d="m271.22 465.22s-7.31 3.44-8.67-8.31-1.29-15.05-1.29-15.05l-3.58 0.14-1.07 2.01s-11.76-0.5-16.77-9.6c5.01-9.31 8.6-16.48 10.75-17.05 2.15-0.58 3.65 1.36 7.16 0.28 3.51-1.07 4.66-4.58 10.18-3.36 5.51 1.22 8.59 3.29 11.75 3.01 3.15-0.29 9.6-2.37 12.25 3.94 2.65 6.3 2.86 8.67 3.37 10.03 0.5 1.36-1.44 1.79-3.16 3.44-1.72 1.64-2.51 4.94-3.65 5.94-1.15 1.01-11.54 4.37-11.54 4.37s-1.79-3.36-2.15-3.36h-4.08s-1.29 2.22-2.29 6.87c-1 4.66-1.36 11.9 0.14 13.11 1.5 1.22 3.58-0.49 2.65 3.59z" fill="#fff"/> <g fill="none"> <path d="m257.32 442.22s-0.5-4.23-0.36-6.02c0.15-1.79-2.5-3.29-2.58-6.44-0.07-3.16 1.94-5.95 2.58-7.38"/> <path d="m261.26 441.86s0-3.51-0.14-3.72c-0.15-0.22-1.15 0.14-1.15 0.14"/> <path d="m270 442.94c0.72-3.08 0.29-5.16 1.36-5.45"/> <path d="m275.09 441.86c-0.29-1.57-0.43-3.72 0.57-5.3 1.01-1.58 3.01-4.66 3.23-7.31 0.21-2.65-1.79-3.87-1.72-7.95"/> <path d="m283.26 431.62c0.57 0.43 2.22 1.64 1.72 4.44"/> <path d="m286.12 428.32c0.22 0 2.87 2.22 2.15 5.16"/> <path d="m278.96 419.94c2.58 1.07 6.73-1.79 8.17 1"/> <path d="m263.13 420.15c2.5-0.57 5.73-1.14 6.94 0.22"/> <path d="m249.51 423.02c1.94-1.58 4.66-1.93 5.23-0.86"/> <path d="m246.14 429.04s-2.65 4.15-2.93 4.44"/> <path d="m249.58 430.47s-2.72 3.37-3.44 4.52"/> <path d="m251.59 433.19s-2.8 1.87-2.72 3.44"/> </g> <path d="m256.72 444.1s-1.74 10.73-3.49 16.15c4.42-5.08 8.08-15.5 8.08-15.5l0.04-2.89-3.37 0.14z" fill="#fff"/> <path d="m274.4 441.5s-0.17 14.08 1.5 20c-5.09-6.58-5.92-18.25-5.92-18.25l1.25-1.58z" fill="#fff"/> <path d="m258.26 432.44c1.1-0.39 1.5-0.73 2.33-0.24 0.82 0.49 1.37 1.71 1.37 1.71" fill="none"/> <path d="m260.31 432.29s-0.52 0.18-0.43 1.25c0.1 1.08 1.81 0.86 1.72-0.18-0.06-0.7-1.29-1.07-1.29-1.07z"/> <path d="m275.79 431.16c-1.1-0.4-1.5-0.73-2.33-0.24-0.82 0.48-1.37 1.71-1.37 1.71" fill="none"/> <path d="m273.74 431.01s0.52 0.18 0.43 1.25-1.81 0.86-1.71-0.18c0.06-0.7 1.28-1.07 1.28-1.07z"/> <path d="m156.15 513.23s11.54 7.54 3.99 19.98c-7.54 12.43-11.32 14.98-7.88 21.09 3.44 6.1 18.21 8.21 26.31 6.77 8.11-1.45 50.18-5.11 70.94 2.11 5.43 4.22 16.98 21.31 16.31 23.86-0.66 2.56-2.66 3.78-7.88 5-5.21 1.22-5.99 5.22-2.55 6.66s28.76 6.82 53.12 6.38c24.37-0.44 45.88-5.93 51.15-9.88 0.88-3.29-11.43-1.66-12.38-9.18s6.85-20.83 11.53-23.06 25.07-5.21 39.82-6.09c14.75-0.87 43.25 4.25 55.25-2s9.75-13.5 1.75-19.75-9.5-19.12-1.13-24.75c-2.12 9 5 17.13 11 21.88s13.75 14.5 4.38 24.5c-9.38 10-28.38 12.37-28.38 12.37s10.5 3.5 7.62 16.38c-2.87 12.88-21.12 19.38-36.87 5.12 10.63 3.38 23.63 4.38 25.25-3.12s-8.25-12-34.12-12.75c-25.88-0.75-40.5 1.75-43.63 4.5 2 5 11 4.5 11.75 8.63 0.75 4.12-1.88 15.24-5.88 19.87-3.24 3.13-21 11.25-48.87 11.75s-56-1.75-66.75-6.12c-9-11.38-11.12-19.88-11.12-22 0-2.13 1.62-4.63 6.24-5.76 4.63-1.12 6.76-2.12 7-5.62-8.37-3.25-27.74-5.88-49.5-5-21.74 0.88-32.5 8.62-27.24 17 5.24 8.38 19.62 4.38 22 2.75-5.88 5-20.26 8.87-28.76 2.13-8.5-6.76-5.87-18.88 0.26-22.26-9.63-2.87-30.88-13.87-31.5-20.87-0.63-7 3-11.13 11.5-18.87 8.5-7.76 10.29-16.3 7.27-21.65z" fill="#fff" stroke-width="1.25"/> <g fill="none" stroke-width="1.25"> <path d="m244.12 577.75s20.13 4.13 21.13 7.13"/> <path d="m238.75 591.88s3.13 4.5 19.25 7.5"/> <path d="m347.5 586.62c-0.12-1.87 1-4.24 14-8.37"/> <path d="m371.12 587.38c-0.12 2.24-6.37 5.5-12.74 8.62"/> </g> </g> <g stroke-width="2.06562"> <path d="m139.72686 597.03207 1.57387 9.87382c0.4088 2.56762 1.38991 3.63224 4.43545 3.11036 2.88202-0.48014 3.47478-1.81612 3.06598-4.36285l-1.57387-9.87382 3.1273-0.52188 1.57387 9.85295c0.24528 1.60737 0.32704 2.88073-0.44967 4.0706-1.06288 1.62825-2.80027 2.60937-5.31437 3.04773-2.69806 0.43838-4.63985 0.1461-6.27504-1.08547-1.26727-0.98112-1.47167-2.23363-1.69651-3.73663l-1.57387-9.85295z"/> <path d="m159.4309 610.14149-0.57232-15.71879 3.2295-0.12528 11.13973 11.50205-0.59276-11.89869 3.10686-0.10446 0.59276 15.71879-3.04554 0.10447-11.36457-11.83607 0.65408 12.23268z"/> <path d="m182.73235 609.49438v-15.73966h3.14774v15.73966z"/> <path d="m194.71012 609.59874 0.36792-13.58954-4.08798-0.10447 0.0613-2.12923 11.30324 0.31314-0.0409 2.10834-4.08796-0.10447-0.36794 13.58956z"/> <path d="m206.66744 610.03712 1.00154-15.69792 11.2828 0.7515-0.12266 2.08749-8.13506-0.54275-0.30659 4.67598 8.15549 0.54275-0.1431 2.06661-8.13507-0.54275-0.30661 4.69687 8.15551 0.54272-0.1431 2.171z"/> <path d="m308.88723 699.15199 1.41035-15.65617 6.99046 0.64713c6.60206 0.62625 10.60827 4.27935 10.17902 8.93445-0.24526 2.69287-2.00309 4.78035-4.94643 5.97022-2.43235 1.00199-4.6603 0.96024-7.54233 0.68887zm7.05177-1.44036c4.88512 0.48013 8.05329-1.44038 8.3599-4.9056 0.32702-3.54874-2.53455-6.07459-7.91023-6.57557l-3.2295-0.31314-1.04244 11.43945z"/> <path d="m331.18715 701.11425 1.02199-15.69794 3.14773 0.20877-1.02199 15.69791z"/> <path d="m362.41925 694.72652c-0.51099 4.98908-5.25304 8.03681-11.44633 7.78634-3.24993-0.12529-5.98888-1.14814-8.13507-3.04773-1.77825-1.54475-2.67762-3.50699-2.59584-5.678 0.0818-2.19184 1.32857-4.15408 3.63829-5.65708 2.16662-1.44037 4.80337-2.06661 7.99199-1.94136 4.43544 0.18789 7.80803 1.89961 9.38191 4.67598l-3.88358-0.14611c-1.38992-1.39862-3.39303-2.17098-5.84581-2.25448-2.04399-0.10446-3.96533 0.3966-5.43701 1.39861-1.5943 1.06462-2.49367 2.56762-2.57542 4.0706-0.0613 1.66998 0.67451 3.21475 2.04399 4.3211 1.47167 1.18988 3.49522 1.92049 5.51877 2.00399 3.27037 0.12528 5.88668-1.02285 7.6445-3.46524l-9.85202-0.41749 0.0818-2.21274z"/> <path d="m366.48679 702.74248 0.18399-15.73967 3.24995 0.0208 10.56743 12.04481-0.0204-11.91956 3.12729 0.0418-0.18398 15.73967-3.0251-0.0418-10.8127-12.37881 0.0818 12.25355z"/> <path d="m390.0744 702.86772-0.12265-15.73964 3.16819-0.0208 0.10222 15.73965z"/> <path d="m403.66692 702.65898-0.47011-13.58956-4.06755 0.14611-0.0613-2.12926 11.30325-0.39663 0.0613 2.12925-4.06753 0.14611 0.44968 13.58953z"/> <path d="m418.62891 701.8866-1.57386-15.65614 11.24192-1.16901 0.22483 2.08748-8.11462 0.85588 0.47011 4.6551 8.11463-0.85587 0.20439 2.04574-8.11462 0.85587 0.47013 4.69683 8.11461-0.85585 0.20439 2.15011z"/> <path d="m506.07067 612.56299-2.06442-13.44343-4.02666 0.64712-0.32703-2.08748 11.1806-1.79525 0.30661 2.08749-4.02667 0.64713 2.08488 13.44341z"/> <path d="m518.10975 610.85123-1.22638-15.67703 6.27505-0.52186c5.29392-0.41749 8.17595 1.16897 8.44165 4.42547 0.18399 2.50497-2.12574 4.6551-5.82537 5.09347l6.64297 5.51097-3.94489 0.31312-7.78759-6.67997 0.5723 7.26446zm4.19019-7.93246c4.31282-0.35487 6.35679-1.52386 6.21371-3.61132-0.16354-2.08749-2.12573-2.77637-5.94799-2.46325l-2.3506 0.18787 0.47013 6.01197z"/> <path d="m538.59052 609.41089-3.35215 0.0835 8.13509-15.94841 3.06598-0.0835 8.91175 15.48917-3.45432 0.10446-2.51411-4.50896-8.4621 0.20874zm6.37722-12.94243-2.96377 6.05371 6.19327-0.16711z"/> <path d="m562.83219 609.056-8.29859-15.80229 3.35217 0.0208 5.88666 11.79431 6.09107-11.68994 3.31126 0.0208-8.48252 15.67702z"/> <path d="m574.05369 609.18125-3.33171-0.12529 9.07532-15.40567 3.04554 0.12528 7.97154 16.03189-3.45432-0.1461-2.22796-4.65508-8.46211-0.31312zm7.15396-12.50405-3.33172 5.84497 6.19331 0.22962z"/> <path d="m595.31116 610.20412 1.18551-15.69792 3.14774 0.25051-1.18552 15.69792z"/> <path d="m605.51066 611.10174 1.98265-15.59353 3.12731 0.41749-1.69652 13.44342 7.09264 0.93939-0.28615 2.15011z"/> </g> <path d="m375.04605 579.459c-21.07559 0-38.19481 17.00665-38.19481 37.96965s17.11922 37.96965 38.19481 37.96965c21.07558 0 38.1573-17.00665 38.1573-37.96965s-17.08172-37.96965-38.1573-37.96965zm0 6.5064c17.49423 0 31.65087 14.02533 31.65087 31.35075 0 17.32541-14.15664 31.40699-31.65087 31.40699-17.47549 0-31.70713-14.08158-31.70713-31.40699 0-17.32542 14.23164-31.35075 31.70713-31.35075z" fill="#289728" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width="2.58194"/> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" fill="#fff" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.6372782"> <path d="m293.48 598.61c0.37-0.05 10.83-22.99 10.83-22.99l10.82 22.99-10.82-4.29z"/> <path d="m293.47 550.64c0.37 0.05 10.82 22.99 10.82 22.99l10.83-22.99-10.83 4.29z"/> <path d="m279.37 585.24c0.05-0.36 23.48-10.6 23.48-10.6l-23.48-10.6 4.38 10.6z"/> <path d="m329.22 564.02c-0.05 0.36-23.48 10.6-23.48 10.6l23.48 10.6-4.38-10.6z"/> </g> <ellipse cx="375.02713" cy="617.38434" rx="23.230194" ry="22.910196" fill="#40cfff" stroke="#bf9b30" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.87502"/> <ellipse cx="374.87561" cy="617.53552" rx="17.791874" ry="17.54615" stroke="#bf9b30" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.78568"/> <path transform="matrix(1.9245817,0,0,1.9647913,-213.23925,-511.31516)" fill="#bf9b30" d="M305.51 566.16 307.44 572.09 313.66 572.09 308.62 575.75 310.55 581.68 305.51 578.01 300.47 581.68 302.4 575.75 297.35 572.09 303.58 572.09z"/> <g transform="matrix(2.0439872,0,0,2.0874883,-246.95464,-582.10653)" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.35"> <path d="m246.48 305.73c-0.4 2.37 4.34 7.77 3.16 10.93-1.19 3.16-7.77 3.95-24.88 7.89-17.11 3.95-16.19 15.93-14.61 23.57-13.69-10.01-6.19-25.15-1.19-30.02-8.16 1.19-21.06-1.84-29.35-5.26-8.3-3.43-11.85-7.24-18.96-6.06 0 0 5.92-2.89 21.72 0 15.8 2.9 36.73 4.21 53.05-4.08-6.05-10.53-6.97-11.98 10.93-16.58 17.9-4.61 39.62-6.59 67.79-6.59s52.26 7.9 59.37 10.4 0.53 7.77-1.05 10.67c6.32 3.02 19.22 10.79 34.75 8.16 15.53-2.64 26.46-6.85 41.33-3.69-10.13 1.32-15.4 3.29-22.24 7.51-6.85 4.21-23.17 7.76-32.12 5.79 11.45 8.95 10.92 21.98 3.03 29.09 1.97-10.66 0.52-19.35-18.7-24.62-19.22-5.26-26.98-2.24-19.87-17.24-6.98-4.35-36.34-7.51-53.32-7.64s-47.39 3.56-58.84 7.77z" fill="#fff"/> <path d="m239.31 309.9c-0.22-2.02 4.73-4.05 9.2-4.94-2.32 1.83-2.46 0.58-1.43 2.95s2.06 5.19 2.06 5.19-2.15-1.52-4.34-1.97-3.7-0.29-5.49-1.23z"/> <path d="m372.46 300.6s-5.31 7.71-7.41 10.07c-2.11 2.36-7.92 0.08-9.01 3.37" fill="none"/> <path d="m356.29 304.52 2.44 1.1c-2.14 3.6-2.41 7.74-2.44 8.46 0.04-0.45 0.33-2.06 2.86-2.06 2.95 0 5.31-0.84 5.31-0.84l1.77-2.19c-2.28-2.36-9.94-4.47-9.94-4.47z"/> <path d="m356.29 314.2s0-0.04 0.01-0.12c-0.01 0.08-0.01 0.12-0.01 0.12z"/> </g> <path d="m434.0074 143.40089c0 126.57108-100.34371 229.1728-224.12456 229.1728-123.78086 0-224.12456-102.60172-224.12456-229.1728 0-126.57108 100.3437-229.1728 224.12456-229.1728 123.78085 0 224.12456 102.60173 224.12456 229.1728z" color="#000" fill="none"/> <g transform="matrix(2.0439872,0,0,2.0874883,-265.16494,-358.78705)"> <path d="m263.59 193.47-0.31-1.4 4.2-8.63q0.45-0.91 0.88-1.62l-6.37 1.43-0.3-1.34 8.17-1.84 0.31 1.35-4.63 9.35-0.51 0.96 7.28-1.64 0.3 1.35z"/> <path d="m272.86 185.56q-0.48-2.87 0.79-4.75t3.71-2.29q1.59-0.27 3 0.28 1.42 0.55 2.32 1.8 0.9 1.26 1.19 2.99 0.29 1.75-0.19 3.26-0.48 1.5-1.65 2.43-1.18 0.93-2.69 1.18-1.63 0.27-3.04-0.3t-2.3-1.83q-0.89-1.25-1.14-2.77zm1.57-0.23q0.35 2.08 1.67 3.09 1.32 1.02 3.02 0.73 1.72-0.28 2.63-1.68t0.54-3.63q-0.23-1.41-0.89-2.38-0.65-0.97-1.66-1.39-1.01-0.43-2.16-0.24-1.62 0.27-2.6 1.58t-0.55 3.92z"/> <path d="m295.75 188.15-0.75-11.67 1.55-0.1 0.37 5.79 5.42-6.16 2.1-0.13-4.6 5.04 5.56 6.61-2.04 0.13-4.53-5.64-1.79 1.99 0.26 4.04z"/> <path d="m308.75 187.46-3.11-11.7h1.59l1.78 7.67q0.29 1.2 0.5 2.39 0.45-1.87 0.53-2.16l2.21-7.9h1.87l1.68 5.92q0.63 2.2 0.92 4.14 0.22-1.11 0.58-2.55l1.82-7.52h1.56l-3.2 11.7h-1.49l-2.47-8.91q-0.31-1.11-0.37-1.37-0.18 0.81-0.34 1.37l-2.48 8.92z"/> <path d="m322.16 187.6 0.73-11.68 8.44 0.53-0.09 1.38-6.9-0.43-0.22 3.57 6.46 0.41-0.09 1.37-6.45-0.41-0.25 3.98 7.16 0.44-0.08 1.38z"/> <path d="m341.1 189.26 0.22-1.42 7.09-6.46q0.75-0.69 1.41-1.18l-6.44-1.02 0.21-1.37 8.28 1.31-0.22 1.37-7.75 6.98-0.83 0.7 7.37 1.17-0.21 1.36z"/> <path d="m352.62 185.33q0.61-2.85 2.49-4.13t4.29-0.76q1.59 0.35 2.7 1.38 1.1 1.03 1.48 2.52 0.37 1.5 0 3.22-0.37 1.74-1.37 2.96t-2.44 1.65q-1.44 0.42-2.93 0.1-1.61-0.35-2.71-1.4-1.11-1.05-1.47-2.54t-0.04-3zm1.56 0.36q-0.45 2.07 0.4 3.5 0.86 1.43 2.54 1.79 1.7 0.37 3.07-0.6 1.36-0.96 1.84-3.17 0.3-1.39 0.05-2.54-0.25-1.14-1.03-1.91-0.79-0.77-1.92-1.01-1.61-0.35-3 0.51-1.4 0.85-1.95 3.43z"/> </g> <g stroke-width="2.06562"> <path d="m325.03539 254.09591h-0.951v-8.17043q-0.34342 0.44164-0.90346 0.88328-0.55474 0.44165-0.99854 0.66247v-1.23946q0.79778-0.50574 1.3948-1.2252 0.59701-0.71946 0.84533-1.39616h0.61287z"/> <path d="m327.57826 248.26049v-6.78792h3.64028v0.80103h-2.97404v2.07898h2.78516v0.79639h-2.78516v2.3105h3.09081v0.80102z" baseline-shift="super"/> <path d="m332.26598 248.26049v-6.78792h2.23225q0.67311 0 1.0234 0.1852 0.35029 0.18059 0.55978 0.64361 0.20949 0.46303 0.20949 1.02328 0 0.72232-0.34686 1.21775-0.34686 0.49544-1.07148 0.62971 0.26444 0.17133 0.40181 0.33801 0.2919 0.36117 0.55291 0.9029l0.87572 1.84746h-0.83795l-0.66624-1.41222q-0.29191-0.61119-0.48079-0.9353-0.18888-0.32412-0.33999-0.45376-0.14767-0.12966-0.30221-0.18059-0.11333-0.0324-0.3709-0.0324h-0.7727v3.01429zm0.66624-3.79216h1.43207q0.45676 0 0.71432-0.12502 0.25757-0.12964 0.3915-0.40746 0.13394-0.28244 0.13394-0.61119 0-0.48154-0.261-0.79176-0.25757-0.31024-0.81735-0.31024h-1.59348z" baseline-shift="super"/> <path d="m340.63327 254.09591v-10.44277h2.66808q0.90345 0 1.37895 0.14959 0.6657 0.20658 1.13592 0.74795 0.61287 0.69808 0.91402 1.78794 0.30643 1.08275 0.30643 2.47891 0 1.1896-0.20605 2.1085-0.20606 0.91891-0.52834 1.52439-0.32228 0.59836-0.70796 0.94741-0.3804 0.34191-0.92458 0.51999-0.53891 0.17809-1.24159 0.17809zm1.02497-1.23234h1.65368q0.76608 0 1.19931-0.19232 0.43852-0.19233 0.69741-0.54137 0.36454-0.49152 0.56531-1.31782 0.20605-0.83343 0.20605-2.01589 0-1.63836-0.40153-2.51454-0.39626-0.88328-0.96686-1.18246-0.41209-0.2137-1.32611-0.2137h-1.62726z"/> <path d="m348.46845 254.09591v-10.44277h5.60033v1.23233h-4.57537v3.19837h4.28478v1.22521h-4.28478v3.55452h4.755v1.23234zm2.11332-11.08387 0.70269-1.99452h1.24686l-1.16232 1.99452z"/> <path d="m361.1907 250.43452 1.02496 0.34905q-0.32228 1.70246-1.16233 2.60001-0.83476 0.89041-2.04464 0.89041-1.25215 0-2.03937-0.68384-0.78193-0.69096-1.19403-1.99453-0.40682-1.30356-0.40682-2.79945 0-1.63124 0.45965-2.8422 0.46493-1.21809 1.31555-1.84493 0.8559-0.63398 1.88086-0.63398 1.16234 0 1.95484 0.7978 0.79249 0.79781 1.10421 2.24385l-1.00912 0.32054q-0.26945-1.13972-0.78193-1.65973-0.51248-0.51999-1.28913-0.51999-0.89288 0-1.49518 0.57698-0.59701 0.57699-0.84005 1.55288-0.24303 0.96877-0.24303 2.00166 0 1.33205 0.2853 2.32932 0.29058 0.99013 0.89816 1.48164 0.60759 0.4915 1.31555 0.4915 0.86118 0 1.4582-0.66958 0.59702-0.66959 0.80835-1.98741z"/> <path d="m363.49952 254.09591v-10.44277h5.60032v1.23233h-4.57536v3.19837h4.28478v1.22521h-4.28478v3.55452h4.75499v1.23234z"/> <path d="m370.6637 254.09591v-10.44277h1.54274l1.8333 7.39399q0.25361 1.03288 0.36984 1.54575 0.13208-0.56985 0.4121-1.67396l1.85445-7.26578h1.37895v10.44277h-0.98798v-8.7403l-2.2507 8.7403h-0.92459l-2.24013-8.88989v8.88989z"/> <path d="m379.66649 254.09591v-10.44277h2.90584q0.88759 0 1.42121 0.32055 0.5389 0.31342 0.84005 0.97589 0.30643 0.65535 0.30643 1.3748 0 0.66959-0.26945 1.26083-0.26945 0.59123-0.81363 0.95452 0.70268 0.2778 1.0778 0.9474 0.3804 0.66959 0.3804 1.58137 0 0.7337-0.23246 1.36768-0.22719 0.62685-0.56533 0.96876-0.33813 0.34193-0.85061 0.52001-0.5072 0.17096-1.24686 0.17096zm1.02497-6.05481h1.67481q0.68155 0 0.97742-0.1211 0.39096-0.15671 0.58645-0.52 0.20077-0.3633 0.20077-0.91179 0-0.51999-0.18492-0.91177-0.18491-0.39892-0.52833-0.54138-0.34342-0.14959-1.17818-0.14959h-1.54802zm0 4.82247h1.92842q0.49663 0 0.69739-0.0498 0.35399-0.0855 0.59174-0.28494 0.23774-0.19945 0.39096-0.57698 0.15322-0.38467 0.15322-0.8833 0-0.5841-0.2219-1.01151-0.2219-0.43451-0.61815-0.60548-0.39096-0.17807-1.13063-0.17807h-1.79105z"/> <path d="m386.94163 254.09591v-10.44277h3.43417q1.03552 0 1.57443 0.28493 0.5389 0.27781 0.86118 0.99014t0.32228 1.57425q0 1.11124-0.53361 1.87343-0.53362 0.7622-1.6484 0.96877 0.40682 0.26357 0.61815 0.52001 0.44908 0.55562 0.85062 1.38904l1.34724 2.8422h-1.28913l-1.02497-2.17261q-0.44908-0.94028-0.73966-1.43891-0.29059-0.49863-0.52305-0.69808-0.22718-0.19946-0.46493-0.27781-0.17436-0.0498-0.5706-0.0498h-1.18875v4.63727zm1.02497-5.83399h2.20315q0.70268 0 1.09893-0.19233 0.39625-0.19945 0.6023-0.62686 0.20605-0.43451 0.20605-0.94027 0-0.74082-0.40154-1.21809-0.39624-0.47725-1.25742-0.47725h-2.45147z"/> <path d="m394.76095 254.09591v-10.44277h5.60033v1.23233h-4.57536v3.19837h4.28478v1.22521h-4.28478v3.55452h4.75499v1.23234z"/> <path d="m408.15948 254.09591h-0.951v-8.17043q-0.34342 0.44164-0.90345 0.88328-0.55475 0.44165-0.99855 0.66247v-1.23946q0.79778-0.50574 1.3948-1.2252t0.84533-1.39616h0.61287z"/> <path d="m410.73774 251.6811 0.91402-0.11402q0.11623 0.86905 0.44379 1.26083 0.32757 0.39178 0.84005 0.39178 0.43852 0 0.76609-0.27068 0.33284-0.27069 0.54418-0.71946 0.21133-0.45589 0.35398-1.22521t0.14265-1.56713q0-0.0855-5e-3 -0.25644-0.2853 0.61261-0.78193 0.99727-0.49135 0.37753-1.06723 0.37753-0.96157 0-1.62727-0.94027-0.6657-0.94028-0.6657-2.47891 0-1.5885 0.69212-2.55726 0.69739-0.96877 1.7435-0.96877 0.75551 0 1.37895 0.54849 0.62871 0.54849 0.951 1.56713 0.32756 1.01151 0.32756 2.9348 0 2.00165-0.32228 3.19123-0.32229 1.18248-0.96157 1.80221-0.634 0.61972-1.4899 0.61972-0.90873 0-1.48461-0.67671-0.57589-0.68384-0.69212-1.91617zm3.89382-4.60877q0-1.10411-0.43853-1.75233-0.43322-0.64823-1.04609-0.64823-0.63401 0-1.10422 0.69809-0.47022 0.69808-0.47022 1.80932 0 0.99726 0.4438 1.6241 0.44909 0.61973 1.10422 0.61973 0.66042 0 1.08308-0.61973 0.42796-0.62684 0.42796-1.73095z"/> <path d="m416.6128 251.36056 0.99855-0.11403q0.11094 0.98302 0.51248 1.48165 0.40682 0.49151 0.97742 0.49151 0.68683 0 1.16233-0.69808 0.4755-0.69809 0.4755-1.85206 0-1.09699-0.45965-1.73097-0.45437-0.63397-1.19403-0.63397-0.45965 0-0.82949 0.28493-0.36983 0.27782-0.58116 0.72658l-0.89288-0.15671 0.75022-5.36385h3.85155v1.22521h-3.09075l-0.41738 2.80658q0.6974-0.65535 1.46349-0.65535 1.01439 0 1.7118 0.9474 0.69739 0.94741 0.69739 2.43617 0 1.41754-0.61286 2.45043-0.74495 1.26794-2.03408 1.26794-1.05667 0-1.72765-0.79781-0.6657-0.79781-0.7608-2.11562z"/> <path d="m424.09399 248.43288q-0.59174-0.29205-0.87703-0.83343-0.28531-0.54137-0.28531-1.29644 0-1.13973 0.60759-1.91617 0.60758-0.77643 1.6167-0.77643 1.0144 0 1.63255 0.7978 0.61815 0.79069 0.61815 1.93041 0 0.72658-0.2853 1.26795-0.28002 0.53426-0.8559 0.82631 0.71325 0.31343 1.08308 1.01151 0.37511 0.69808 0.37511 1.66685 0 1.33918-0.70267 2.25096-0.70269 0.91179-1.84918 0.91179-1.14647 0-1.84916-0.91179-0.70268-0.91891-0.70268-2.28657 0-1.01864 0.3804-1.70248 0.38568-0.69096 1.09365-0.94027zm-0.1902-2.17261q0 0.74082 0.35398 1.21096t0.9193 0.47014q0.54947 0 0.89817-0.46301 0.35398-0.47014 0.35398-1.14685 0-0.70522-0.36455-1.18248-0.35927-0.48439-0.89816-0.48439-0.54419 0-0.90346 0.47015-0.35926 0.47014-0.35926 1.12548zm-0.30643 4.82247q0 0.54851 0.1902 1.06138 0.19548 0.51288 0.57588 0.79782 0.3804 0.2778 0.81891 0.2778 0.68155 0 1.12536-0.59123 0.4438-0.59124 0.4438-1.50303 0-0.92602-0.45966-1.5315-0.45436-0.60548-1.14119-0.60548-0.67098 0-1.11479 0.59835-0.43851 0.59836-0.43851 1.49589z"/> </g> <path d="m348.96469 396.407c0.12979 0.10635 0.324 0.17255 0.30417 0.46517 0.21057 0.10972 0.41701 0.22317 0.49148 0.43045l0.66954 0.34007h4e-3l2.57218 2.10718 0.73284 0.25818 0.32851 0.0317 2.59328-0.73408 0.93056 0.10372 4.3e-4 -9.6e-4 0.9883 0.36179c0.78258-0.30613 1.22544-0.78217 2.34773-0.91837 0.22433 0.13091 0.23624-0.0921 0.31629-0.20237l0.61418-0.17348 0.78861-0.0979h1.22378c0.37418 0.12415 0.72747 0.30702 0.9458 0.87054 0.28709 1.00936 0.84365 1.08093 1.54618 0.64347 0.29413 0.10746 0.58818 0.1386 0.88235 0.13147l0.0565-0.30042 0.17161 0.0131h1e-3c1e-3 0.2341 0.20132 0.21564 0.34429 0.26945 0.0626 0.21884 0.15792 0.40768 0.42486 0.44081l0.21597-0.0677 0.0812 0.12153-0.12135 0.14854 0.32398 0.41853-0.12529 0.94497 0.015-2.2e-4 -0.41383 0.79434 0.25152 0.18623-0.14366 0.54186 0.37476 0.19209c0.0231 0.0503-0.015 0.069-0.0407 0.0947l-0.43195-0.0947-0.0945 0.5265-0.33754 0.32397 0.36621 0.93543 1.53365 1.5869h-4e-3l0.85683 1.10591h-2e-3c0.10841 0.0979 0.10691 0.21475 0.10991 0.33053-0.0279 0.12228 0.0527 0.22292 0.10521 0.33986l0.0741 0.31418c0.26709 0.76188 0.82661 1.19476 0.80769 2.32169l-0.18193 0.17442c0.15023 0.50898 0.28403 1.0414 0.61832 1.28437 0.033 0.14254-0.0392 0.24603-0.0484 0.35307-0.0341 0.39164-0.24016 1.50155-0.33054 1.30372 0 0-0.37281 0.0818-0.40623 0.38165-0.0169 0.14611-0.19673 0.16692-0.2638 0.34722-0.18755 0.50384-0.38506 1.30307-0.52747 2.02955-0.0169 0.084-0.12641 0.0467-0.13972 0.12753-0.0392 0.23131 6e-3 0.39576-0.0113 0.64132-6e-3 0.0733-0.0975-0.0503-0.11571 0.0409-0.0221 0.11216 0.0683 0.43021 0.0454 0.50712 0.0383 0.47522 0.0825 0.94767 0.51819 1.28306l1.80031 3.20603 0.0458 1.47121 0.32129 0.681c0.0188 0.26503-0.0797 0.53021 0.0833 0.79537l0.22367 1.25226 0.97767 1.08868h4e-3c-1e-3 1e-3 -2e-3 0-4e-3 0l1.36918 2.75514 0.0188 0.62989c-0.0694 0.15154-0.18287 0.15623-0.30969 0.13147-0.0516 0.0722-0.0998 0.16561 0.015 0.23611 0.16973 0.30124 0.29241 0.66757 0.50858 0.92574 0.17761 0.21204 0.44107 0.32294 0.64992 0.50593l0.54362 0.0431c0.11197-0.0782 0.0113-0.18567 0.41569-0.22523h0.79636c0.2514-0.0992-0.0317-0.34746 0.78849-0.28652 0.13466 0.0169 0.19308 0.16467 0.40436 0.0503 0.38065-0.0619 0.74407 0.084 1.11494 0.13954 0.1099-0.0876 0.069-0.23716 0.68646-0.11797l0.0634-0.2402 0.72372 0.0131 4.29023-3.98689c0.68011-0.28231 0.83072-1.1057 1.02127-1.88835l1e-3 1.3e-4 0.14723-0.51094-0.22175 0.0829c0.034-0.20212-0.015-0.40426 0.19954-0.60641l1.77266-0.7008c0.21957-0.1613 0.35594-0.40547 0.50866-0.63353l-0.20704-0.0713 0.3671-1.38582-0.18857-0.0643-0.15266-1.16623c-0.5023-0.308-0.21162-0.78686-0.0113-0.89879l1.07169-0.91069 0.0885 0.18155 0.39747-0.43917-0.0878-0.13542 0.25839-0.0478 0.0767-0.22244 1.31339-0.64623h0.32795c0.24869-0.18837 0.44851-0.39074 0.81108-0.54665l-0.0231-0.1947 0.43753-0.33844c0.10502-0.10746 0.19524-0.26735 0.33262-0.26176l0.0193-0.21698-0.0835-0.0923 0.23939-0.35901 0.0604-0.23971-0.0383-0.15679-0.14667-0.22291 0.0508-0.87791c-0.11197-0.10859-0.0501-0.18286 0.0112-0.25711l-0.0289-0.96925 0.35207-0.593c0.0113-0.16261 0.0223-0.32504-0.0369-0.48761-0.057 0.0338-0.11966 0.0713-0.18342 0.10859 0.0613-0.0373 0.12022-0.0733 0.16917-0.10503-0.11009-0.19873-0.311-0.3068-0.49746-0.42912 0.015-0.22596-0.0761-0.47325-0.15755-0.71845-0.0951-0.15004-0.16861-0.29423-0.10559-0.40464l-0.22204-0.48526 0.13954-0.0456c-0.0362-0.36995-0.034-0.71904 0.1868-0.9507-0.15154-0.31325-0.66684-0.56183-0.63726-0.93343l0.44138-1.24003c0.42089-0.51307 0.67641-1.2105 1.01237-1.81739 0.30511-0.0748 0.42356-0.29632 0.51533-0.53942 0.24643-0.0131 0.36918-0.16899 0.48126-0.34395h-4e-3l1.14461-1.28194 1.92454-1.81655 0.22137-0.0268 0.80652-0.76294 0.033-0.15248c0.44262-0.33624 0.99386-0.90043 1.13116-1.17902 0.24335-0.49283 0.55675-0.91043 0.90633-1.31039l0.75784-2.1884c0.14741-0.14198 0.2579-0.33345 0.35063-0.54844 0.0384-0.22957 0.17554-0.31066 0.28095-0.43951l0.0863-1.09601c0.24549 0.0349 0.12716-0.45095 0.14104-0.74751-0.0949-0.084-0.20061-0.22396-2e-3 -0.3359l-2e-3 -0.27321c-0.17836 0.015-0.35777-0.18872-0.53294-0.0514-0.2194 0.20646-0.51196 0.35408-0.84569 0.38161-0.37107 0.0302-0.6735 0.24534-0.82904 0.19271-0.25143 0.20856-0.57186 0.27925-0.98772 0.15904-0.27757 0.24693-0.45954 0.56767-1.35095 0.34112l-0.56819 0.4063c-0.87673 0.23759-1.10511-0.47209-1.54651-0.87043l-0.0112 0.015c-0.17818-0.14722-0.35654-0.0701-0.53477-0.0782 0.0489-0.22033 0.29622-0.27063 0.47199-0.38234 0.1296-0.4011-0.14591-0.65694-0.31469-0.73421l-8.6e-4 8.1e-4c-0.0707-0.18024-0.16993-0.34065-0.49231-0.34849-0.0169-0.15192-0.0812-0.28673-0.29419-0.36856-0.0454-0.18249-0.2022-0.17911-0.32583-0.23097-0.1041-0.29001-0.3584-0.44909-0.56694-0.64786-0.23513 0.0548-0.34368-0.10052-0.45498-0.2516l-0.17873-0.0349-0.12791 0.0782c-0.13373-0.10972-0.17011-0.26166-0.20755-0.4129l-0.19154-0.0514 0.0486 0.26552c-0.11591 0.0713-0.17949-0.1206-0.23988-0.32679l-0.39009-0.56639c-0.25418-1.18469-0.47595-1.33083-0.70322-1.65396 1e-3 -0.0131 6e-3 -0.021 8e-3 -0.0328-0.0771-0.17236-0.18777-0.14104-0.2901-0.16279-0.16805-0.32259-0.44662-0.35095-0.68856-0.47654l-0.33791-2.04496-0.26779-0.28371-0.0315-0.34827h-0.18736c0.0773-0.015 0.12641-0.0291 0.12847-0.0478l-1.14234-0.91499-0.14704-0.72723 0.23549 0.0131-0.57291-0.51002-1.57157-2.5957-8e-3 -0.31499c-0.3719-0.24382-0.64704-0.5523-0.81312-0.93333l-0.015-0.24185-0.2372-0.17255 0.0621-0.38384 0.7045 0.94149c0.0632 0.0724-9e-3 0.12491 0.0491 0.2037 0.18418 0.25488 0.50118 0.51222 0.68805 0.66149 0.0955-0.0328 0.20736-0.0131 0.25474-0.2019l0.1838-1.22283-0.82602-1.68318c-0.55783 0.39033-1.1157 0.54641-1.6736-0.0345l-0.68169 0.0525c-0.15604 0.067-0.32163 0.16899-0.41151 7e-3l-1.09993 0.64276-1.82152-0.40577c-0.27371-0.0564-0.56577-0.1491-0.79169-0.10953-0.22587 0.13785-0.3882 0.0373-0.54887-0.0694-0.0508-0.28844-0.64389-0.2664-0.96224-0.28273-0.43541-0.0223-0.728-0.082-0.65222-0.40814 0 0-1.13822-0.40873-1.57878-0.13935-0.23404 0.14329-0.72755 0.37188-0.81735 0.66514-0.0606 0.19717-7e-3 0.33086 0.11122 0.53111 0.20576 0.35053-0.48904 1.55416-1.28317 0.5909 0 0-1.04314-0.28722-1.63237-0.41598-0.57667-0.12622-0.97452-0.63165-0.73519-0.9377 0.0233-0.0298-1.56513-0.48947-1.56513-0.48947-0.73082 0.11309-1.26637-0.0994-1.39944-0.24224-2e-3 -0.0112-4e-3 -0.0169-6e-3 -0.027-0.0251-0.0876-0.24418 0.0169-0.2797-0.14291-0.0799-0.35661-0.0698-0.44433-0.20513-0.49791-0.0683-0.0268-0.34972 0.24355-0.40695 0.20897-0.12772-0.0769-0.28712-0.25682-0.21816-0.34576 0.11891-0.15323 0.97715-0.68533 0.9857-0.97864 0.015-0.43656-0.14835-0.47859-0.36237-0.59484-0.12153-0.0658-0.0778-0.27281-0.0386-0.3908 0.024-0.0724 0.4875-0.23011 0.43407-0.39767-0.0362-0.11384-0.10353-0.13128-0.1281-0.22005-0.0188-0.0655-0.36833 0.30018-0.39041 0.24365-0.0585-0.1491-0.0761-0.24012-0.1461-0.34678-0.0865 4e-3 -0.3152-0.0953-0.55896-0.0942v-1e-5c-0.18962 8.6e-4 -0.38836 0.0621-0.53655 0.27767 0 0-9e-3 6e-3 -0.0113 7e-3 -0.42233 0.11234-0.8613-0.2294-0.98395-0.14592-0.3318 0.22576-0.40517-0.0396-0.64841-0.0551-0.24156-0.015-0.78569 0.2694-1.20338 0.14704-0.41778-0.12228-1.6963 0.25415-2.68586 0.25095-0.54024-1e-3 -0.91435 0.29939-0.93961 0.4362-0.0236 0.12792-0.40973 0.0878-0.61123 0.0236-0.17274-0.0544-0.30496 0.22523-0.40845 0.3639-0.19745 0.26441-0.51986 0.21662-0.59773 0.24466-1e-3 -9e-3 -4e-3 -0.015-6e-3 -0.024-0.21216-0.0392-0.47258-0.0441-0.56127-0.17124-0.30794-0.0529-0.61311-0.0812-0.88851-0.0533-0.50417 0.0508-0.71162-0.1311-0.91917-0.60866-0.48557-0.0317-1.00031 1.01176-1.37485 1.52766-0.90568 0.25184-1.56119 0.66011-2.01151 1.19662 0.0602 0.2181-0.0456 0.30212-0.26527 0.44609-0.14197 0.0932-0.64184 0.89077-0.47302 1.3361 1e-3 0.34794-0.26904 0.92796-1.04849 1.31387 0 0-0.36342 0.33962-0.93821 0.52715-0.24248 0.0793-0.80467 0.14217-0.97474 0.29902h0.0274c-0.31704 0.48889-0.50551 0.90214-0.98772 1.09344-0.45922 0.18211-0.63886 0.74878-0.76876 1.23966l-1.36695 1.56414c-0.0493 0.35515-0.25569 0.59244-0.53191 0.77729-0.24973 0.32679-0.40224 0.73269-0.44058 1.09356l0.20032-0.18155-0.0467 0.0467 0.15698 0.47334s0.1326-0.0398 0.16017 0.034c0.24294 0.64384-0.12866 0.84227-0.25045 1.05736l0.22754 0.36345-0.0381 1.2099-0.36824 1.02272c-0.14854 0.25407-0.24948 0.3911-0.25381 0.60046-0.0113 0.48646-0.49564 0.64156-0.73856 0.99519 0.22609 0.32958 0.47224 0.65332 0.50868 1.03939l-0.1673 0.424h-8.4e-4c-0.048 0.22129-0.0926 0.44256-0.0467 0.66909l0.0621 0.16279c0.11872 0.0818 0.34205-0.0793 0.32864 0.30648 0.10803 0.0561 0.24135-0.0572 0.31879 0.20213 0.13259 0.0666 0.46781-0.55623 0.34638 0.37369l0.29348 0.32154c-1e-3 0-6e-3 0.0131-7e-3 0.0131-0.0294 0.0923-0.0217 0.13729 0.0973 0.13748 0.048 0.0585 0.21788-0.34977 0.19174 0.0619l0.15154 0.0169c-0.0516 0.2284 0.10409 0.50909 0.25261 0.55976 0.19108 0.0653 0.29676 0.28628 0.40177 0.38396l0.26145 0.48842z"/> <path transform="matrix(2.0439872,0,0,2.0874883,-251.26048,-582.10653)" fill="#ffd200" stroke="#000" stroke-linecap="round" stroke-linejoin="round" stroke-width=".9623" d="M317.17 484.43 305.94 477.64 294.7 484.43 298.99 473.44 287.75 466.64 301.64 466.64 305.94 455.64 310.23 466.64 324.12 466.64 312.88 473.44z"/> <g transform="matrix(.46480504 0 0 .46480504 412.04888 434.73067)"> <path d="m34.06 20.07c-0.03-3.15 5.56-5.68 6.83-3.1-0.56 5.03 7.11 13.06 10.5 6.5 6.13 0.8 12.12 7.43 18.47 9.75 18.34 6.73 26.67 27.28 45.24 33.61 6.28 1.85 9.07 7.91 11.67 13.31 3.48 23.59 19.74 42.38 32.22 61.87 1.48 2.1 5.74 4.33 3.24 7.23-8.95 9.63-17.15 20.41-26.86 29.16-13.33 0.5-27.27-2.54-39.99-6.84-8.93-6.87-22.47-10.22-25.42-22.55-3.08-15.12-17.57-23.46-24.5-36.5-5.27-6.29-14.82-15.95-8.25-24.31 4.2-6.26 12.46-4.54 18.18-1.69 5.18 3.25 5.26-3.15 8.51-4.61 2.92-3.09 11.93-1.21 11.1-6.82-1.63-2.91-4.07-5.43-6.93-7.18-8.75-4.78-8.11-17.47-17.2-21.71-8.03-6.53-18.32-14.64-16.81-26.12m7.25 70.1c-4.84 4.15-0.27 15.45 5.54 16.76 5.16-4.43 4.04-19.34-5.54-16.76m40.03 5.1c-5.45 1.78-5.19 9.4-4.54 13.85 1.76 5.9 14.17 13.98 14.26 2.91 2.99-6.92 0.46-19.97-9.72-16.76z"/> <path d="m40.89 16.97c4.48-0.3 7.11 4.26 10.5 6.5-3.39 6.56-11.06-1.47-10.5-6.5z"/> <path d="m41.31 90.17c9.58-2.58 10.7 12.33 5.54 16.76-5.81-1.31-10.38-12.61-5.54-16.76z"/> </g> <path d="m433.82499 484.43229 0.28088-0.26223 0.35888-0.46144 0.38088-0.75442 0.19042-0.63721 0.15379-0.84964 0.0585-0.98144-0.0512-0.76908-0.20509-0.94486-0.30029-0.73243-0.43214-0.64454-0.542-0.46143-0.40286-0.20509-0.54932-0.19044-0.7178-0.0293-0.79771 0.13354-0.35221 0.37197-0.25636 0.43214-0.22705 0.57132-0.10259 0.57861 0.015 0.82033 0.0807 0.63723 0.15379 0.55665 0.21974 0.68117 0.30031 0.62258 0.29297 0.56398 0.38819 0.5127 0.57862 0.62991 0.53469 0.38817z" fill="#fff" stroke="#000" stroke-width="1.87505px"/> <path d="m435.93521 445.63965-0.35715 0.55341-0.35157 0.34425-0.3589 0.18324-0.4248 0.066-0.26368 8e-3 -0.24172-0.0439-0.3296-0.12453-0.36621-0.17574-0.32226-0.20509-0.33693-0.27832-0.29298-0.31496-0.28566-0.34423-0.26368-0.38088-0.24169-0.43945-0.18324-0.40287-0.12453-0.35887-0.10991-0.49073-0.0219-0.30031-4e-3 -0.31529 0.32643 3.3e-4 0.3589 0.022 0.32227 0.0733 0.37354 0.15379 0.38088 0.19778 0.46144 0.28564 0.31493 0.24903 0.35892 0.32227 0.50538 0.46876 0.55663 0.51269 0.48343 0.41751z" fill="#fff" stroke="#000" stroke-width=".187505"/> <path d="m214.25908 590.56971 2.88229 0.0999-3.04426 2.69023-2.34046-0.1412z"/> <path d="m422.32224 681.32737 2.8642-0.33758-2.60192 3.12007-2.33486 0.21474z"/> <g fill="#bf9b30"> <circle cx="322.80835" cy="593.67206" r="2.5"/> <circle cx="323.79913" cy="639.9563" r="2.5"/> <circle cx="426.3605" cy="594.76178" r="2.5"/> <circle cx="427.35129" cy="641.04602" r="2.5"/> <circle transform="rotate(90)" cx="566.28748" cy="-397.70447" r="2.5"/> <circle transform="rotate(90)" cx="565.9682" cy="-352.29358" r="2.5"/> <circle transform="rotate(90)" cx="667.65619" cy="-397.4881" r="2.5"/> <circle transform="rotate(90)" cx="667.77362" cy="-352.51392" r="2.5"/> </g> </svg>`;
document.getElementById('embLogin').innerHTML = ARM_SVG;
document.getElementById('embSide').innerHTML = ARM_SVG;
document.querySelector('main').style.setProperty('--wm', `url("data:image/svg+xml,${encodeURIComponent(ARM_SVG)}")`);


/* module: referentials/data.js — PNDDRR engine (classic globals) */
/* ---------- Référentiels ---------- */
/* Découpage administratif issu de la loi 21-001 du 21 janvier 2021 :
   7 régions, 20 préfectures, 85 sous-préfectures. */
const PREFECTURES = ["Bangui","Bamingui-Bangoran","Basse-Kotto","Haut-Mbomou","Haute-Kotto","Kémo","Lim-Pendé","Lobaye","Mambéré","Mambéré-Kadéï","Mbomou","Nana-Grébizi","Nana-Mambéré","Ombella-M'Poko","Ouaka","Ouham","Ouham-Fafa","Ouham-Pendé","Sangha-Mbaéré","Vakaga"];
const CHEF_LIEUX = {"Bangui":"Bangui","Bamingui-Bangoran":"Ndélé","Basse-Kotto":"Mobaye","Haut-Mbomou":"Obo","Haute-Kotto":"Bria","Kémo":"Sibut","Lim-Pendé":"Paoua","Lobaye":"Mbaïki","Mambéré":"Carnot","Mambéré-Kadéï":"Berbérati","Mbomou":"Bangassou","Nana-Grébizi":"Kaga-Bandoro","Nana-Mambéré":"Bouar","Ombella-M'Poko":"Boali","Ouaka":"Bambari","Ouham":"Bossangoa","Ouham-Fafa":"Batangafo","Ouham-Pendé":"Bozoum","Sangha-Mbaéré":"Nola","Vakaga":"Birao"};
const REGIONS = {
  "Plateaux":       {chef:"Boali",     coord:[18.12,4.80], prefs:["Ombella-M'Poko","Lobaye"], num:1},
  "Équateur":       {chef:"Berbérati", coord:[15.79,4.26], prefs:["Nana-Mambéré","Mambéré","Mambéré-Kadéï","Sangha-Mbaéré"], num:2},
  "Yadé":           {chef:"Bossangoa", coord:[17.45,6.49], prefs:["Ouham","Ouham-Fafa","Ouham-Pendé","Lim-Pendé"], num:3},
  "Kagas":          {chef:"Sibut",     coord:[19.08,5.72], prefs:["Kémo","Nana-Grébizi","Ouaka"], num:4},
  "Fertit":         {chef:"Bria",      coord:[21.99,6.54], prefs:["Vakaga","Bamingui-Bangoran","Haute-Kotto"], num:5},
  "Haut-Oubangui":  {chef:"Bangassou", coord:[22.82,4.74], prefs:["Basse-Kotto","Mbomou","Haut-Mbomou"], num:6},
  "Bas-Oubangui":   {chef:"Bangui",    coord:[18.55,4.36], prefs:["Bangui"], num:7}
};
function regionOf(pref){ for(const [r,v] of Object.entries(REGIONS)) if(v.prefs.includes(pref)) return r; return "—"; }
/* Référentiel des sous-préfectures et communes — d'après la carte officielle
   « Découpage administratif - Centrafrique », Laboratoire de Cartographie, ICASEES. */
const SOUS_PREFS={"Ombella-M'Poko":["Boali","Damara","Bogangolo","Yaloké","Bossembélé"],"Lobaye":["Mbaïki","Mongoumba","Boda","Boganangone","Boganda","Moboma"],"Mambéré-Kadéï":["Berbérati","Gamboula","Sosso-Nakombo","Dédé-Mokouba"],"Nana-Mambéré":["Bouar","Baoro","Baboua","Abba"],"Sangha-Mbaéré":["Nola","Bambio","Bayanga"],"Mambéré":["Carnot","Amada-Gaza","Gadzi","Senkpa-Mbaéré"],"Ouham-Pendé":["Bozoum","Bocaranga","Koui","Bossemptélé"],"Ouham":["Bossangoa","Nana-Bakassa","Markounda","Nanga-Boguila"],"Ouham-Fafa":["Batangafo","Bouca","Kabo","Sido"],"Lim-Pendé":["Paoua","Ndim","Ngaoundaye","Kodi"],"Kémo":["Sibut","Dékoa","Mala","Ndjoukou"],"Nana-Grébizi":["Kaga-Bandoro","Mbrès","Nana-Outa"],"Ouaka":["Bambari","Bakala","Grimari","Kouango","Ippy"],"Bamingui-Bangoran":["Ndélé","Bamingui"],"Haute-Kotto":["Bria","Ouadda","Yalinga","Ouandja-Kotto"],"Vakaga":["Birao","Ouanda-Djallé","Ouandja","Am-Dafock"],"Basse-Kotto":["Mobaye","Alindao","Kembé","Mingala","Zangba","Satéma"],"Mbomou":["Bangassou","Ouango","Gambo","Rafaï","Bakouma"],"Haut-Mbomou":["Obo","Bambouti","Zémio","Djémah","Mboki"],"Bangui":["Bangui-Rapides","Bangui-Fleuve","Bangui-Centre","Bangui-Kagas"]};
const COMMUNES={"Vakaga":["Am-Dafock","Ouandja","Ridina","Vokouma"],"Haute-Kotto":["Daba-Nydou","Daho-Mboutou","Ouadda","Ouandja-Kotto","Samba-Boungou","Yalinga"],"Haut-Mbomou":["Djémah","Zémio"],"Kémo":["Dékoa","Galabadja","Galafondo","Guifa","Mala","Ngoumbélé","Sibut","Tilo"],"Ouaka":["Azéngué-Mindou","Baïdou-Ngoumbrou","Cochio-Toulou","Danga-Gboudou","Haute-Baïdou","Ippy","Kobadja","Kouango","Koudoubégo","Lissa","Ngougbia","Pladama-Ouaka","Pouyamba","Yéngou"],"Basse-Kotto":["Bakou","Bangui-Ketté","Guiligui","Kotto","Kotto-Oubangui","Kémbé","M'boui","Mbélima","Mobaye","Ouambe","Siriki","Séliba","Yabongo","Yambélé"],"Mbomou":["Bakouma","Bazouma","Gambo","Ngandou","Ouango","Ouara","Rafaï/Chinko","Sayo-Niakari","Vougba-Balifondo","Zangandou-Mada"],"Ombella-M'Poko":["Boali","Bogangolo","Bossembélé","Damara","Guézéli","La Mbi","Yaloké"],"Bangui":["Arrondissement 1","Arrondissement 2","Arrondissement 3","Arrondissement 4","Arrondissement 5","Arrondissement 6","Arrondissement 7","Arrondissement 8","Arrondissement 9","Bimbo","Bégoua"],"Lobaye":["Baléloko","Boganda","Bogongo-Gaza","Boutélossi","Lessé","Lobaye","Mbaiki","Mbata","Moboma","Mongoumba","Nola","Pissa"],"Bamingui-Bangoran":["Dar-El-Kouti","Mbolo-Kpata","Vassako"],"Ouham":["Bakassa","Ben-Zambé","Bouca-Bobo","Bédé","Hama","Koro-M'poko","Nana-Bakassa","Nana-Markounda","Nanga-Boguila","Ndoro-Mboli","Ouham-Bac","Soumbe"],"Ouham-Pendé":["Binon","Birvan-Bolé","Bocaranga","Dan-Gbabiri","Daneyérin","Kouazo","Koui","Malé"],"Sangha-Mbaéré":["Basse-Kadéï","Bilolo","Mbaéré","Nola","Salo"],"Nana-Mambéré":["Abba","Baboua","Bawi-Tédoua","Bingué","Béa-Nana","Doaka-Koursou","Goudrot","Herman-Brousse","Nadziboro","Niem-Yéléwa","Yenga","Yoro-Samba-Bougoulou","Zotoua-Banguérèm"],"Mambéré-Kadéï":["Basse-Batouri","Basse-Boumbé","Basse-Mambéré","Haute-Batouri","Haute-Kadéï","Ouakanga","Senkpa-M'baéré"],"Nana-Grébizi":["Botto","Grevaï","M'brès","Nana","Ndenga"],"Lim-Pendé":["Bah-Bessar","Banh","Bimbi","Dilouki","Kodi","Loura","Mbili","Mia-Péndé","Nana-Barya","Paoua","Péndé","Yémé"],"Ouham-Fafa":["Fafa-Boungou","Kabo","Ladi-Gbawi","Ouaki","Ouassi","Ouham-Fafa","Sido"],"Mambéré":["Carnot","Haute-Boumbé","Mbali","Topia"]};

const DEFAULT_GROUPES = ["Ex-Séléka / FPRC","UPC","MPC","3R","Anti-Balaka (aile Mokom)","Anti-Balaka (aile Ngaïssona)","RJ (Révolution et Justice)","MLCJ","Siriri","Autre"];
var GROUPES = DEFAULT_GROUPES.slice();  /* pointe vers DB.groupes après initialisation */
const CORPS = ["Forces Armées Centrafricaines (FACA)","Gendarmerie Nationale","Police Nationale","Douanes","Eaux et Forêts","Protection Civile"];
const FILIERES = ["Agriculture","Élevage","Pêche / pisciculture","Menuiserie","Maçonnerie","Couture","Mécanique","Soudure","Coiffure","Commerce / AGR","Boulangerie","Conduite / transport","Autre"];
const TYPES_ARMES = ["Fusil d'assaut AK-47 / dérivés","Fusil PMAK","Fusil de chasse artisanal","Pistolet automatique","Pistolet mitrailleur","Fusil mitrailleur (PKM…)","Lance-roquettes RPG-7","Grenade","Munitions (lot)","Machette / arme blanche","Autre"];
const STATUTS = {
  enregistre:{lbl:"Enregistré",ord:1},
  desarme:{lbl:"Désarmé",ord:2},
  demobilise:{lbl:"Démobilisé",ord:3},
  reintegration_militaire:{lbl:"Réintégration militaire",ord:4},
  reintegration_socio:{lbl:"Réintégration socio-éco.",ord:4},
  reintegre:{lbl:"Réintégré",ord:5},
  rapatrie:{lbl:"Rapatrié",ord:5},
  abandon:{lbl:"Abandon",ord:0}
};


/* module: core/state.js — PNDDRR engine (classic globals) */
/* ---------- État ---------- */
var DB = {
  seq:{comb:0, dem:0},
  groupes:GROUPES,
  users:[
    {id:"u1",login:"admin",pass:"admin2026",nom:"Administrateur système",role:"admin",actif:true},
    {id:"u2",login:"agent",pass:"agent2026",nom:"Agent DDR — Bangui",role:"agent",actif:true},
    {id:"u3",login:"suivi",pass:"suivi2026",nom:"Chargé de suivi & évaluation",role:"suivi",actif:true}
  ],
  combattants:[],
  journal:[]
};
var CUR = null;          // utilisateur connecté
var VIEW = "dashboard";  // vue courante
var FICHE_ID = null;     // fiche ouverte

const ROLES = {admin:"Administrateur", agent:"Agent DDR", suivi:"Chargé de suivi", superviseur:"Superviseur (lecture seule)"};
const PERMS = {
  enregistrer:"Enregistrer / modifier les dossiers",
  desarmer:"Procès-verbaux de désarmement",
  demobiliser:"Prononcer les démobilisations",
  orienter:"Orienter en réintégration (militaire / socio-éco.)",
  visites:"Réaliser les visites de suivi",
  cloturer:"Clôturer les parcours et déclarer les abandons",
  importer:"Importer des données (dossiers, registres d'armes)",
  referentiels:"Gérer les référentiels (groupes armés)"
};
const ROLE_PERMS = {
  admin: Object.keys(PERMS),
  agent: ["enregistrer","desarmer","demobiliser","orienter","visites","cloturer","importer"],
  suivi: ["visites"],
  superviseur: []
};
function userPerms(u){ return u.role==="admin"?Object.keys(PERMS):(u.perms||ROLE_PERMS[u.role]||[]); }
function hasPerm(p){ return !!CUR && userPerms(CUR).includes(p); }
const $ = id => document.getElementById(id);
const esc = s => (s==null?"":String(s)).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const fmtD = d => { if(!d) return "—"; const [a,m,j]=d.split("-"); return `${j}/${m}/${a}`; };
const today = () => new Date().toISOString().slice(0,10);
const fmtN = n => (n==null||n==="")?"—":Number(n).toLocaleString("fr-FR");
function toast(msg){ const t=$("toast"); t.textContent=msg; t.style.display="block"; clearTimeout(t._h); t._h=setTimeout(()=>t.style.display="none",2600); }
/* ---------- Mode hors ligne : persistance locale automatique ----------
   L'application est un fichier unique qui fonctionne sans serveur ni connexion.
   Chaque opération est enregistrée automatiquement dans le stockage local du
   navigateur de l'appareil (localStorage) : en zone éloignée, les données
   survivent à la fermeture du navigateur et aux coupures. La sauvegarde JSON
   reste le moyen de transfert entre appareils (fusion multi-postes ci-dessous).
   Nota : dans certains aperçus intégrés (bac à sable), le stockage local est
   indisponible — l'application bascule alors en mémoire de session. */
const LS_KEY="pnddrr_db", LS_TS="pnddrr_ts";
function storageOK(){ try{ localStorage.setItem("__t","1"); localStorage.removeItem("__t"); return true; }catch(e){ return false; } }
var HAS_LS = storageOK();
function persist(){
  if(HAS_LS){
    try{ localStorage.setItem(LS_KEY, JSON.stringify(DB)); localStorage.setItem(LS_TS, new Date().toISOString()); updNetBadge(); }catch(e){}
  }
  scheduleServerSave();
}
var _saveT=null, _pulling=false;
function dbFingerprint(){
  return (DB.combattants||[]).length+":"+(DB.combattants||[]).map(c=>c.id||c.num).join(",");
}
function refreshViewSafe(){
  if(!CUR) return;
  const app=$("app"); if(!app||!app.classList.contains("on")) return;
  const modal=$("modalBack"); if(modal&&modal.classList.contains("on")) return;
  if(VIEW==="nouveau") return;
  try{ go(VIEW, VIEW==="fiche"?FICHE_ID:undefined); }catch(e){}
}
function scheduleServerSave(){
  if(!window.__PNDDRR_SERVER || !window.__PNDDRR_SYNCED) return;
  clearTimeout(_saveT);
  _saveT=setTimeout(pushServer, 500);
}
function pushServer(){
  _saveT=null;
  const before=dbFingerprint();
  fetch("/api/db",{method:"PUT",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify(DB)})
    .then(r=>{
      if(r.status===401){ toast("Session expirée — reconnectez-vous."); return null; }
      if(!r.ok) throw new Error("save");
      return r.json();
    })
    .then(db=>{
      if(!(db&&db.combattants&&db.users)) return;
      delete DB._replace;
      applyServerDb(db);
      if(CUR && dbFingerprint()!==before) refreshViewSafe();
    })
    .catch(()=>{});
}
function pullServer(){
  if(!window.__PNDDRR_SERVER || !window.__PNDDRR_SYNCED || !CUR || _pulling || _saveT) return;
  _pulling=true;
  const before=dbFingerprint();
  fetch("/api/db",{credentials:"include"})
    .then(x=>{ if(!x.ok) throw new Error("db"); return x.json(); })
    .then(db=>{
      if(!applyServerDb(db)) return;
      if(dbFingerprint()!==before) refreshViewSafe();
    })
    .catch(()=>{})
    .finally(()=>{ _pulling=false; });
}
function lastPersist(){ try{ return localStorage.getItem(LS_TS); }catch(e){ return null; } }
/* SHA-256 pure JS (synchrone, hors ligne) — implémentation standard FIPS 180-4 */
function sha256(ascii){
  function rr(v,c){return (v>>>c)|(v<<(32-c));}
  ascii=unescape(encodeURIComponent(ascii));
  const K=[0x428a2f98,0x71374491,0xb5c0fbcf,0xe9b5dba5,0x3956c25b,0x59f111f1,0x923f82a4,0xab1c5ed5,0xd807aa98,0x12835b01,0x243185be,0x550c7dc3,0x72be5d74,0x80deb1fe,0x9bdc06a7,0xc19bf174,0xe49b69c1,0xefbe4786,0x0fc19dc6,0x240ca1cc,0x2de92c6f,0x4a7484aa,0x5cb0a9dc,0x76f988da,0x983e5152,0xa831c66d,0xb00327c8,0xbf597fc7,0xc6e00bf3,0xd5a79147,0x06ca6351,0x14292967,0x27b70a85,0x2e1b2138,0x4d2c6dfc,0x53380d13,0x650a7354,0x766a0abb,0x81c2c92e,0x92722c85,0xa2bfe8a1,0xa81a664b,0xc24b8b70,0xc76c51a3,0xd192e819,0xd6990624,0xf40e3585,0x106aa070,0x19a4c116,0x1e376c08,0x2748774c,0x34b0bcb5,0x391c0cb3,0x4ed8aa4a,0x5b9cca4f,0x682e6ff3,0x748f82ee,0x78a5636f,0x84c87814,0x8cc70208,0x90befffa,0xa4506ceb,0xbef9a3f7,0xc67178f2];
  let H=[0x6a09e667,0xbb67ae85,0x3c6ef372,0xa54ff53a,0x510e527f,0x9b05688c,0x1f83d9ab,0x5be0cd19];
  const l=ascii.length*8, words=[];
  ascii+='\x80'; while(ascii.length%64-56) ascii+='\x00';
  for(let i=0;i<ascii.length;i++) words[i>>2]=(words[i>>2]||0)|(ascii.charCodeAt(i)<<((3-i%4)*8));
  words.push(0, l|0);
  for(let j=0;j<words.length;j+=16){
    const w=words.slice(j,j+16); let [a,b,c,d,e,f,g,h]=H;
    for(let i=0;i<64;i++){
      const wi = i<16?w[i]:(w[i]=(w[i-16]+(rr(w[i-15],7)^rr(w[i-15],18)^(w[i-15]>>>3))+w[i-7]+(rr(w[i-2],17)^rr(w[i-2],19)^(w[i-2]>>>10)))|0);
      const t1=(h+(rr(e,6)^rr(e,11)^rr(e,25))+((e&f)^(~e&g))+K[i]+wi)|0;
      const t2=((rr(a,2)^rr(a,13)^rr(a,22))+((a&b)^(a&c)^(b&c)))|0;
      h=g;g=f;f=e;e=(d+t1)|0;d=c;c=b;b=a;a=(t1+t2)|0;
    }
    H=[(H[0]+a)|0,(H[1]+b)|0,(H[2]+c)|0,(H[3]+d)|0,(H[4]+e)|0,(H[5]+f)|0,(H[6]+g)|0,(H[7]+h)|0];
  }
  return H.map(x=>(x>>>0).toString(16).padStart(8,"0")).join("");
}
function hashPwd(p){ return "sha256:"+sha256("PNDDRR|"+p); }
function pwdOK(u, saisie){ return !!(u.pass && u.pass.startsWith("sha256:") && u.pass===hashPwd(saisie)); }
function migrateDB(){
  if(!DB.journal) DB.journal=[];
  if(!DB.groupes||!DB.groupes.length) DB.groupes=DEFAULT_GROUPES.slice();
  if(!DB.groupes.includes("Autre")) DB.groupes.push("Autre");
  GROUPES=DB.groupes;
  if(!DB.seq) DB.seq={comb:0,dem:0};
  if(DB.poste===undefined) DB.poste="";
  if(!DB.syncs) DB.syncs=[];
  /* Sécurité : hachage des mots de passe encore en clair */
  DB.users.forEach(u=>{ if(u.pass && !String(u.pass).startsWith("sha256:")) u.pass=hashPwd(u.pass); });
  if(!DB.secret) DB.secret=null; /* secret d'installation (défini par l'administrateur) */
  if(DB.posteCode===undefined) DB.posteCode="";
  /* Configuration du programme (onglet Paramètres → Configuration) */
  const C_DEF={villeSignature:"Bangui", signataireCarte:"Le Coordonnateur de l'UEPNDDR",
    seuilOrientation:90, seuilVisite:60, seuilFormation:270,
    verrouMin:15, pageTaille:100, rappelJours:7,
    carteQR:true, carteCodeBarres:true};
  DB.config=Object.assign({}, C_DEF, DB.config||{});
}
function cfg(k){ return (DB.config||{})[k]; }
function applyConfig(){ PAGE_TAILLE=+cfg("pageTaille")||100; }
function posteSlug(){ return (DB.poste||"poste").normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^A-Za-z0-9]+/g,"-").replace(/^-|-$/g,"").toLowerCase()||"poste"; }
function addSync(type, fichier, detail){
  DB.syncs.unshift({date:new Date().toISOString(), type, fichier, poste:DB.poste||"(non nommé)", detail});
  if(DB.syncs.length>50) DB.syncs.length=50;
  persist();
}
function loadPersisted(){
  if(!HAS_LS) return false;
  try{
    const raw=localStorage.getItem(LS_KEY); if(!raw) return false;
    const d=JSON.parse(raw); if(!d.combattants||!d.users) return false;
    DB=d; migrateDB(); return true;
  }catch(e){ return false; }
}
function clearPersisted(){ try{ localStorage.removeItem(LS_KEY); localStorage.removeItem(LS_TS); }catch(e){} }
if(!loadPersisted()) migrateDB();
/* Premier démarrage : pré-charger la simulation (60 ex-combattants fictifs) pour découvrir le programme.
   L'effacement des données locales laisse ensuite l'appareil vierge (marqueur pnddrr_skip_demo). */
var DEMO_PRELOADED=false;
function updNetBadge(){
  const el=$("netBadge"); if(!el) return;
  const online=typeof navigator!=="undefined"&&"onLine" in navigator?navigator.onLine:true;
  const ts=lastPersist();
  el.innerHTML=`<span style="display:inline-flex;align-items:center;gap:6px">
    <span style="width:9px;height:9px;border-radius:50%;background:${online?"var(--ok)":"var(--warn)"}"></span>
    ${online?"En ligne":"Hors ligne"} · ${HAS_LS?(ts?"enregistré à "+new Date(ts).toLocaleTimeString("fr-FR",{hour:"2-digit",minute:"2-digit"}):"stockage local prêt"):"mémoire de session"}</span>`;
}
if(typeof window!=="undefined"){ window.addEventListener("online",()=>updNetBadge()); window.addEventListener("offline",()=>updNetBadge()); }
function log(action, detail){
  const date=new Date().toISOString(), user=CUR?CUR.login:"—";
  const ph=DB.journal.length&&DB.journal[0].h?DB.journal[0].h:"GENESE";
  const h=sha256([ph,date,user,action,detail].join("|"));
  DB.journal.unshift({date, user, action, detail, h, ph});
  persist();
}
function verifJournal(){
  let ok=0, ko=[], sans=0, ruptures=0;
  for(let i=0;i<DB.journal.length;i++){
    const j=DB.journal[i];
    if(!j.h){ sans++; continue; }
    if(j.h===sha256([j.ph||"GENESE",j.date,j.user,j.action,j.detail].join("|"))) ok++;
    else ko.push(i+1);
    const suivant=DB.journal[i+1];
    if(j.ph&&j.ph!=="GENESE"&&suivant&&suivant.h&&j.ph!==suivant.h) ruptures++;
  }
  return {ok, ko, sans, ruptures};
}
function mVerifJournal(){
  const r=verifJournal();
  const corps = r.ko.length
    ? `<div style="background:#FDF1F1;border:1px solid #E3B4B4;border-radius:8px;padding:12px"><b style="color:var(--danger)">✘ Altération détectée.</b> ${r.ko.length} entrée(s) du journal ne correspondent plus à leur empreinte (position(s) : ${r.ko.slice(0,15).join(", ")}${r.ko.length>15?"…":""}). Le contenu de ces entrées a été modifié après leur écriture.</div>`
    : `<div style="background:#EAF7EE;border:1px solid #9ED0AC;border-radius:8px;padding:12px"><b style="color:var(--ok)">✔ Journal intègre.</b> ${r.ok} entrée(s) vérifiée(s) par leur empreinte chaînée (SHA-256) — aucune altération de contenu détectée.</div>`;
  openModal("Vérification de l'intégrité du journal", `${corps}
    <p class="small muted" style="margin-top:10px">Chaque entrée est scellée par une empreinte SHA-256 qui enchaîne l'empreinte précédente : toute modification a posteriori d'une entrée rend son empreinte invalide.${r.sans?` ${r.sans} entrée(s) antérieures à l'activation du chaînage ne portent pas d'empreinte.`:""}${r.ruptures?` ${r.ruptures} raccord(s) de chaîne constatés — normaux après une fusion multi-postes ou une restauration.`:""}</p>`);
  log("Journal","Vérification d'intégrité — "+(r.ko.length?r.ko.length+" altération(s)":"intègre"));
}
function age(dn){ if(!dn) return "—"; const d=new Date(dn), n=new Date(); let a=n.getFullYear()-d.getFullYear(); if(n<new Date(n.getFullYear(),d.getMonth(),d.getDate())) a--; return a; }

function posteCode(){ return (DB.posteCode||"").toUpperCase().replace(/[^A-Z0-9]/g,"").slice(0,5); }
function numDossier(){ DB.seq.comb++; const p=posteCode(); return `DDR-${p?p+"-":""}${new Date().getFullYear()}-${String(DB.seq.comb).padStart(4,"0")}`; }
function numDem(){ DB.seq.dem++; const p=posteCode(); return `DEM-${p?p+"-":""}${new Date().getFullYear()}-${String(DB.seq.dem).padStart(4,"0")}`; }


/* module: auth/auth.js — PNDDRR engine (classic globals) */
/* ---------- Authentification ---------- */
function doLogin(){
  const login=$("loginUser").value.trim(), pass=$("loginPass").value;
  $("loginErr").style.display="none";
  fetch("/api/login",{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify({login,password:pass})})
    .then(r=>{
      if(r.status===429){ $("loginErr").textContent="Trop de tentatives. Réessayez plus tard."; $("loginErr").style.display="block"; return null; }
      if(r.status===401){ $("loginErr").textContent="Identifiant ou mot de passe incorrect."; $("loginErr").style.display="block"; return null; }
      if(!r.ok) throw new Error("login");
      window.__PNDDRR_SERVER=true;
      return fetch("/api/db",{credentials:"include"}).then(x=>{ if(!x.ok) throw new Error("db"); return x.json(); });
    })
    .then(db=>{
      if(!db) return;
      applyServerDb(db, {mergeLocal:true});
      const u=DB.users.find(x=>x.login===login&&x.actif);
      if(!u){ $("loginErr").textContent="Identifiant ou mot de passe incorrect."; $("loginErr").style.display="block"; return; }
      enterSession(u);
    })
    .catch(()=>{
      /* Hors ligne / pas d'API : repli local uniquement si le serveur n'est pas en face */
      if(window.__PNDDRR_SERVER){ $("loginErr").textContent="Impossible de charger le registre."; $("loginErr").style.display="block"; return; }
      const u=DB.users.find(x=>x.login===login && x.actif && pwdOK(x,pass));
      if(!u){ $("loginErr").textContent="Identifiant ou mot de passe incorrect."; $("loginErr").style.display="block"; return; }
      enterSession(u);
    });
}
function applyServerDb(db, opts){
  if(!(db&&db.combattants&&db.users)) return false;
  const login=CUR&&CUR.login;
  const local=opts&&opts.mergeLocal?{combattants:DB.combattants||[],journal:DB.journal||[],groupes:DB.groupes||[],seq:DB.seq}:null;
  DB=db; migrateDB();
  if(local && typeof mergeDB==="function" && local.combattants.length){
    mergeDB(local);
  }
  window.__PNDDRR_SYNCED=true;
  if(login){
    const u=DB.users.find(x=>x.login===login&&x.actif);
    if(u) CUR=u;
  }
  if(HAS_LS){ try{ localStorage.setItem(LS_KEY, JSON.stringify(DB)); localStorage.setItem(LS_TS, new Date().toISOString()); }catch(e){} }
  return true;
}
function enterSession(u, opts){
  CUR=u; $("loginErr").style.display="none";
  $("loginScreen").style.display="none"; $("app").classList.add("on");
  $("uName").textContent=u.nom; $("uRole").textContent=ROLES[u.role];
  $("todayLbl").textContent=new Date().toLocaleDateString("fr-FR",{weekday:"long",day:"numeric",month:"long",year:"numeric"});
  if(!(opts&&opts.resume)) log("Connexion",`Ouverture de session (${ROLES[u.role]})`);
  buildNav(); updNetBadge(); go("dashboard");
}
function resumeSession(user){
  fetch("/api/db",{credentials:"include"})
    .then(x=>{ if(!x.ok) throw new Error("db"); return x.json(); })
    .then(db=>{
      applyServerDb(db, {mergeLocal:true});
      const u=DB.users.find(x=>x.login===user.login&&x.actif);
      if(u) enterSession(u,{resume:true});
    })
    .catch(()=>{});
}
["loginUser","loginPass"].forEach(id=>$(id).addEventListener("keydown",e=>{ if(e.key==="Enter"){ e.preventDefault(); doLogin(); } }));
$("loginForm").addEventListener("submit",e=>{ e.preventDefault(); doLogin(); });
function mMonPass(){
  openModal("Changer mon mot de passe", `
    <div class="field"><label>Mot de passe actuel</label><input type="password" id="mp_old"></div>
    <div class="grid2"><div class="field"><label>Nouveau mot de passe</label><input type="password" id="mp_new"></div>
    <div class="field"><label>Confirmation</label><input type="password" id="mp_new2"></div></div>
    <p class="small muted">Au moins 6 caractères. Les mots de passe sont conservés hachés (SHA-256) — ils ne peuvent pas être relus, seulement réinitialisés par l'administrateur.</p>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveMonPass()">Changer</button>`);
}
function saveMonPass(){
  if(!pwdOK(CUR,$("mp_old").value)){ toast("Mot de passe actuel incorrect."); return; }
  const nv=$("mp_new").value;
  if(nv.length<6){ toast("Le nouveau mot de passe doit compter au moins 6 caractères."); return; }
  if(nv!==$("mp_new2").value){ toast("La confirmation ne correspond pas."); return; }
  CUR.pass=hashPwd(nv); CUR.passUpdated=true; log("Mot de passe","Changement du mot de passe personnel");
  closeModal(); toast("Mot de passe changé.");
}
/* Verrouillage automatique après 15 minutes d'inactivité */
var LOCK_T=Date.now(), LOCKED=false;
["click","keydown","input"].forEach(ev=>document.addEventListener(ev,()=>{ LOCK_T=Date.now(); },true));
function checkLock(){
  const min=+cfg("verrouMin");
  if(!min||!CUR||LOCKED) return;
  if(Date.now()-LOCK_T>min*60*1000) lockSession();
}
setInterval(checkLock, 30000);
function lockSession(){
  LOCKED=true;
  $("lockName").textContent=CUR.nom;
  $("lockPass").value=""; $("lockErr").style.display="none";
  $("lockScreen").style.display="flex";
  log("Session","Verrouillage automatique (inactivité)");
}
function unlockSession(){
  if(!pwdOK(CUR,$("lockPass").value)){ $("lockErr").style.display="block"; return; }
  LOCKED=false; LOCK_T=Date.now(); $("lockScreen").style.display="none";
  log("Session","Déverrouillage");
}
function logout(){
  fetch("/api/logout",{method:"POST",credentials:"include"}).catch(()=>{});
  log("Déconnexion","Fermeture de session"); CUR=null; $("app").classList.remove("on"); $("loginScreen").style.display="flex"; $("loginForm").reset();
}
function bootSession(){
  if($("embLogin")&&typeof ARM_SVG!=="undefined") $("embLogin").innerHTML=ARM_SVG;
  if($("embSide")&&typeof ARM_SVG!=="undefined") $("embSide").innerHTML=ARM_SVG;
  fetch("/api/config",{credentials:"include"}).then(r=>r.json()).then(c=>{
    window.__PNDDRR_SERVER=!!c.server;
    window.__PNDDRR_DEMO=!!c.demo;
    const h=$("demoHint"); if(h&&c.demo) h.style.display="block";
    if(c.user&&c.user.login) resumeSession(c.user);
  }).catch(()=>{ window.__PNDDRR_SERVER=false; });
}
bootSession();
document.addEventListener("pnddrr-bind", bootSession);
document.addEventListener("visibilitychange", ()=>{ if(document.visibilityState==="visible") pullServer(); });
setInterval(pullServer, 20000);
if(typeof window!=="undefined"){
  window.addEventListener("storage", e=>{
    if(e.key!==LS_KEY || !e.newValue || !CUR || _saveT) return;
    try{
      const d=JSON.parse(e.newValue);
      const before=dbFingerprint();
      if(!applyServerDb(d)) return;
      if(dbFingerprint()!==before) refreshViewSafe();
    }catch(err){}
  });
}


/* module: shell/nav.js — PNDDRR engine (classic globals) */
/* ---------- Navigation ---------- */
const NAV = [
  {grp:"Pilotage"},
  {id:"dashboard", lbl:"Tableau de bord", ic:"◫", roles:["admin","agent","suivi","superviseur"]},
  {id:"stats", lbl:"Statistiques", ic:"◔", roles:["admin","agent","suivi","superviseur"]},
  {grp:"1 · Désarmement"},
  {id:"nouveau", lbl:"Nouvel enregistrement", ic:"✚", perm:"enregistrer"},
  {id:"registre", lbl:"Registre des ex-combattants", ic:"☰", roles:["admin","agent","suivi","superviseur"]},
  {id:"armes", lbl:"Registre des armes", ic:"⌖", roles:["admin","agent","suivi","superviseur"]},
  {id:"docs", lbl:"Cartes & attestations", ic:"▤", roles:["admin","agent","suivi","superviseur"]},
  {grp:"2 · Réintégration"},
  {id:"reintegration", lbl:"Suivi des réintégrations", ic:"⇄", roles:["admin","agent","suivi","superviseur"]},
  {id:"jalons", lbl:"Formation & intégration", ic:"✓", roles:["admin","agent","suivi","superviseur"]},
  {grp:"3 · Cartographie"},
  {id:"carto", lbl:"Carte des zones de désarmement", ic:"◉", roles:["admin","agent","suivi","superviseur"]},
  {grp:"Administration"},
  {id:"parametres", lbl:"Paramètres", ic:"⚙", roles:["admin","agent","suivi","superviseur"]}
];
/* Outils regroupés dans la page Paramètres */
const OUTILS = [
  {id:"recherche", lbl:"Recherche", ic:"⌕", roles:["admin","agent","suivi","superviseur"]},
  {id:"import", lbl:"Importer", ic:"⇪", perm:"importer"},
  {id:"referentiels", lbl:"Référentiels", ic:"⚑", perm:"referentiels"},
  {id:"config", lbl:"Configuration", ic:"⚙", roles:["admin"]},
  {id:"comptes", lbl:"Comptes", ic:"♟", roles:["admin"]},
  {id:"journal", lbl:"Journal", ic:"✎", roles:["admin"]},
  {id:"sauvegarde", lbl:"Sauvegarde", ic:"⇆", roles:["admin","agent","suivi","superviseur"]}
];
function navAllowed(it){ return it.perm?hasPerm(it.perm):it.roles.includes(CUR.role); }
function buildNav(){
  let h="";
  for(const it of NAV){
    if(it.grp){ h+=`<div class="grp">${it.grp}</div>`; continue; }
    if(!navAllowed(it)) continue;
    h+=`<a href="#" data-v="${it.id}" onclick="go('${it.id}');return false;">${it.lbl}</a>`;
  }
  $("mainNav").innerHTML = h;
  $("bannerRCA").innerHTML = `<div class="b-emb">${ARM_SVG}</div>
    <div class="b-tx">
      <div class="r">RÉPUBLIQUE CENTRAFRICAINE</div>
      <div class="d">Unité — Dignité — Travail</div>
      <div class="u">Unité d'exécution du Programme national de désarmement, démobilisation, réintégration et rapatriement</div>
    </div>`;
}
function viewAllowed(v){
  if(!CUR) return false;
  if(v==="fiche"||v==="parametres") return true;
  const it=[...NAV,...OUTILS].find(x=>x.id===v);
  return it?navAllowed(it):true;
}
function go(v, arg){
  if(!viewAllowed(v)){ toast("Accès non autorisé pour ce compte."); return; }
  VIEW=v;
  const OUTIL_IDS=OUTILS.map(o=>o.id);
  document.querySelectorAll("#mainNav a").forEach(a=>a.classList.toggle("on",a.dataset.v===v||(a.dataset.v==="parametres"&&OUTIL_IDS.includes(v))));
  const titles={dashboard:"Tableau de bord",stats:"Statistiques du programme",nouveau:"Désarmement — nouvel enregistrement",registre:"Désarmement — registre des ex-combattants",armes:"Désarmement — registre des armes",docs:"Cartes de démobilisé & attestations de désarmement",import:"Désarmement — importation de données",referentiels:"Référentiels — groupes armés",reintegration:"Réintégration — suivi des parcours",jalons:"Réintégration — formation & intégration à la vie militaire ou civile",carto:"Cartographie des zones de désarmement",recherche:"Recherche multicritère",comptes:"Gestion des comptes utilisateurs",journal:"Journal des opérations",sauvegarde:"Sauvegarde & synchronisation",config:"Configuration du programme",parametres:"Paramètres",fiche:"Dossier individuel"};
  $("pageTitle").textContent = titles[v]||"";
  const R={dashboard:rDash,stats:rStats,nouveau:rNouveau,registre:()=>rRegistre(arg),armes:rArmes,docs:rDocs,import:()=>rImport(arg),referentiels:rReferentiels,reintegration:rReint,jalons:rJalons,carto:rCarto,recherche:rRecherche,comptes:rComptes,journal:rJournal,sauvegarde:rSauvegarde,config:rConfig,parametres:rParametres,fiche:()=>rFiche(arg)};
  (R[v]||rDash)();
  $("view").scrollTop=0;
}


/* module: shell/modal.js — PNDDRR engine (classic globals) */
/* ---------- Modale ---------- */
function openModal(title, body, footer){
  $("modalBox").innerHTML = `<div class="mh"><h3>${esc(title)}</h3><button onclick="closeModal()">×</button></div><div class="mb">${body}</div><div class="mf">${footer||'<button class="btn ghost" onclick="closeModal()">Fermer</button>'}</div>`;
  $("modalBack").classList.add("on");
}
function closeModal(){ $("modalBack").classList.remove("on"); }
$("modalBack").addEventListener("click",e=>{ if(e.target.id==="modalBack") closeModal(); });



/* module: dashboard/dashboard.js — PNDDRR engine (classic globals) */
/* ================= TABLEAU DE BORD ================= */
function countBy(arr, fn){ const m={}; arr.forEach(x=>{ const k=fn(x)||"—"; m[k]=(m[k]||0)+1; }); return Object.entries(m).sort((a,b)=>b[1]-a[1]); }
function barChart(pairs, clickFn){
  if(!pairs.length) return `<div class="empty">Aucune donnée.</div>`;
  const max=Math.max(...pairs.map(p=>p[1]));
  return pairs.map(([k,v])=>`<div class="bar-row${clickFn?" click":""}" ${clickFn?`onclick="${clickFn(k)}" title="Cliquer pour voir les dossiers"`:""}><span class="lbl">${esc(k)}</span><div class="bar" style="width:${Math.round(v/max*100)*0.7}%"></div><span class="val">${v}</span></div>`).join("");
}
var DASH_F={reg:"",pref:"",grp:"",sexe:"",d1:"",d2:""};
function joursDepuis(d){ if(!d) return 0; return Math.floor((Date.now()-new Date(String(d).slice(0,10)).getTime())/86400000); }
function alertesATraiter(){
  const al=[];
  for(const c of DB.combattants){
    if(["desarme","demobilise"].includes(c.statut)){
      const ref=(c.desarmement&&c.desarmement.date)||c.creele.slice(0,10);
      const j=joursDepuis(ref);
      if(j>(+cfg("seuilOrientation")||90)) al.push({c, type:"Orientation en retard", detail:`désarmé depuis ${j} jours sans orientation (souhait : ${c.souhait})`, grav:2});
    }
    if(c.statut==="reintegration_socio"&&c.reintSocio){
      const v=c.reintSocio.visites||[];
      const derniere=v.length?v.map(x=>x.date).sort().pop():null;
      const j=joursDepuis(derniere||c.reintSocio.date);
      if(j>(+cfg("seuilVisite")||60)) al.push({c, type:"Visite de suivi en retard", detail:`${derniere?`dernière visite il y a ${j} jours`:`aucune visite depuis l'orientation (${j} jours)`}`, grav:derniere?1:2});
    }
    if(["reintegration_militaire","reintegration_socio"].includes(c.statut)){
      const r=reintOf(c);
      if(r&&!r.formFin){
        const j=joursDepuis(r.date);
        if(j>(+cfg("seuilFormation")||270)) al.push({c, type:"Formation anormalement longue", detail:`en formation depuis ${j} jours sans validation de fin`, grav:1});
      }
    }
  }
  return al.sort((a,b)=>b.grav-a.grav);
}
function dashFiltered(){
  return DB.combattants.filter(c=>
    (!DASH_F.reg||regionOf(c.prefecture)===DASH_F.reg)&&
    (!DASH_F.pref||c.prefecture===DASH_F.pref)&&
    (!DASH_F.grp||c.groupe===DASH_F.grp)&&
    (!DASH_F.sexe||c.sexe===DASH_F.sexe)&&
    (!DASH_F.d1||c.creele.slice(0,10)>=DASH_F.d1)&&
    (!DASH_F.d2||c.creele.slice(0,10)<=DASH_F.d2));
}
function dashSet(k,v){ DASH_F[k]=v; if(k==="reg") DASH_F.pref=""; rDash(); }
function dashReset(){ DASH_F={reg:"",pref:"",grp:"",sexe:"",d1:"",d2:""}; rDash(); }
function rappelSauvegarde(){
  if(!DB.combattants.length) return "";
  const dernierExport=(DB.syncs||[]).find(x=>x.type==="Export");
  const j=dernierExport?Math.floor((Date.now()-new Date(dernierExport.date).getTime())/86400000):null;
  const freq=+cfg("rappelJours");
  if(!freq) return "";
  if(j!==null&&j<freq) return "";
  return `<div class="small" style="background:#EFF4FB;border:1px solid #B9CCE8;border-radius:8px;padding:9px 13px;margin-bottom:13px">⇆ <b>Rappel de sauvegarde :</b> ${j===null?"aucun fichier de synchronisation JSON n'a encore été exporté depuis ce poste":"le dernier export de synchronisation date de "+j+" jour(s)"}. Exportez régulièrement le fichier JSON (clé USB) pour consolider les données au niveau central. <span class="link" onclick="go('sauvegarde')">Ouvrir la sauvegarde</span></div>`;
}
function rDash(){
  const C=dashFiltered();
  const n=s=>C.filter(c=>c.statut===s).length;
  const armes=C.reduce((a,c)=>a+(c.desarmement?c.desarmement.armes.length:0),0);
  const enCours=C.filter(c=>["reintegration_militaire","reintegration_socio"].includes(c.statut)).length;
  const actif=Object.values(DASH_F).some(x=>x);
  const kpi=(cls,val,lbl,click)=>`<div class="kpi ${cls} ${click?"click":""}" ${click?`onclick="${click}" title="Cliquer pour ouvrir la liste"`:""}><div class="n">${val}</div><div class="l">${lbl}</div></div>`;
  const goReg=(st)=>`go('registre',{st:'${st}',pref:DASH_F.pref,grp:DASH_F.grp})`;
  const prefList=DASH_F.reg?REGIONS[DASH_F.reg].prefs:PREFECTURES;
  const demoN=DB.combattants.filter(x=>x.agent==="Poste de démonstration").length;
  $("view").innerHTML = `
  ${rappelSauvegarde()}
  ${demoN?`<div class="small" style="background:#FFF9E6;border:1px solid #E8D48A;border-radius:8px;padding:9px 13px;margin-bottom:13px">▸ <b>Mode simulation :</b> ${demoN} dossier(s) fictifs de démonstration sont chargés pour découvrir le programme. Pour repartir de zéro : ${CUR.role==="admin"?`onglet Sauvegarde → « Effacer les données locales »`:`demandez à l'administrateur d'effacer les données locales`}.</div>`:""}
  <div class="toolbar">
    <div class="field"><label>Région</label><select onchange="dashSet('reg',this.value)"><option value="">Toutes</option>${Object.entries(REGIONS).map(([r,v])=>`<option value="${esc(r)}" ${DASH_F.reg===r?"selected":""}>${v.num}. ${esc(r)}</option>`).join("")}</select></div>
    <div class="field"><label>Préfecture</label><select onchange="dashSet('pref',this.value)"><option value="">Toutes</option>${prefList.map(p=>`<option ${DASH_F.pref===p?"selected":""}>${esc(p)}</option>`).join("")}</select></div>
    <div class="field"><label>Groupe armé</label><select onchange="dashSet('grp',this.value)"><option value="">Tous</option>${GROUPES.map(g=>`<option ${DASH_F.grp===g?"selected":""}>${esc(g)}</option>`).join("")}</select></div>
    <div class="field"><label>Sexe</label><select onchange="dashSet('sexe',this.value)"><option value="">Tous</option><option value="M" ${DASH_F.sexe==="M"?"selected":""}>Masculin</option><option value="F" ${DASH_F.sexe==="F"?"selected":""}>Féminin</option></select></div>
    <div class="field"><label>Enregistrés du</label><input type="date" value="${DASH_F.d1}" onchange="dashSet('d1',this.value)"></div>
    <div class="field"><label>au</label><input type="date" value="${DASH_F.d2}" onchange="dashSet('d2',this.value)"></div>
    ${actif?`<button class="btn ghost" onclick="dashReset()">✕ Réinitialiser (${C.length}/${DB.combattants.length})</button>`:""}
  </div>
  <div class="cards">
    ${kpi("",C.length,"Ex-combattants enrôlés","go('registre',{pref:DASH_F.pref,grp:DASH_F.grp})")}
    ${kpi("c-des",n("desarme")+n("demobilise")+enCours+n("reintegre"),"Désarmés",goReg("desarme"))}
    ${kpi("c-dem",n("demobilise")+enCours+n("reintegre"),"Démobilisés",goReg("demobilise"))}
    ${kpi("c-rm",n("reintegration_militaire"),"Réint. militaire en cours",goReg("reintegration_militaire"))}
    ${kpi("c-rs",n("reintegration_socio"),"Réint. socio-éco. en cours",goReg("reintegration_socio"))}
    ${kpi("c-ok",n("reintegre"),"Réintégrés (achevé)",goReg("reintegre"))}
    ${n("rapatrie")?kpi("c-des",n("rapatrie"),"Rapatriés",goReg("rapatrie")):""}
    ${kpi("c-ab",n("abandon"),"Abandons",goReg("abandon"))}
    ${kpi("",armes,"Armes & lots collectés","go('armes')")}
  </div>
  ${(()=>{ const AL=alertesATraiter().filter(a=>C.includes(a.c)); if(!AL.length) return "";
    return `<div class="panel" style="border-left:4px solid var(--warn)"><div class="ph"><h3>⚠ À traiter (${AL.length})</h3><span class="muted small">orientations &gt; ${cfg("seuilOrientation")} j · visites &gt; ${cfg("seuilVisite")} j · formations &gt; ${cfg("seuilFormation")} j — seuils réglables dans Paramètres → Configuration</span></div><div class="pb nopad" style="max-height:230px;overflow-y:auto"><table><thead><tr><th>Signalement</th><th>Dossier</th><th>Détail</th><th></th></tr></thead><tbody>${
      AL.map(a=>`<tr><td><span class="badge ${a.grav>1?"st-abandon":"st-demobilise"}">${a.type}</span></td>
      <td><b>${a.c.num}</b> — ${esc(a.c.nom)} ${esc(a.c.prenom)}</td>
      <td class="small">${esc(a.detail)}</td>
      <td><span class="link" onclick="go('fiche','${a.c.id}')">Ouvrir</span></td></tr>`).join("")
    }</tbody></table></div></div>`; })()}
  <div class="grid2">
    <div class="panel"><div class="ph"><h3>Répartition par statut</h3><span class="muted small">cliquable</span></div><div class="pb">${
      barChart(countBy(C,c=>STATUTS[c.statut].lbl), k=>{const key=Object.keys(STATUTS).find(x=>STATUTS[x].lbl===k);return `go('registre',{st:'${key}',pref:DASH_F.pref,grp:DASH_F.grp})`;})}</div></div>
    <div class="panel"><div class="ph"><h3>Répartition par groupe armé d'origine</h3><span class="muted small">cliquable</span></div><div class="pb">${
      barChart(countBy(C,c=>c.groupe), k=>`go('registre',{grp:'${String(k).replace(/'/g,"\\'")}',pref:DASH_F.pref})`)}</div></div>
    <div class="panel"><div class="ph"><h3>Répartition par préfecture</h3><span class="muted small">cliquable</span></div><div class="pb">${
      barChart(countBy(C,c=>c.prefecture), k=>`go('registre',{pref:'${String(k).replace(/'/g,"\\'")}',grp:DASH_F.grp})`)}</div></div>
    <div class="panel"><div class="ph"><h3>Derniers dossiers ${actif?"(sélection filtrée)":""}</h3></div><div class="pb nopad">${C.length?`<table><thead><tr><th>N° dossier</th><th>Nom & prénom</th><th>Statut</th><th></th></tr></thead><tbody>${
      [...C].sort((a,b)=>b.creele.localeCompare(a.creele)).slice(0,8).map(c=>`<tr><td><b>${c.num}</b></td><td>${esc(c.nom)} ${esc(c.prenom)}</td><td><span class="badge st-${c.statut}">${STATUTS[c.statut].lbl}</span></td><td><span class="link" onclick="go('fiche','${c.id}')">Ouvrir</span></td></tr>`).join("")
    }</tbody></table>`:DB.combattants.length?`<div class="empty">Aucun dossier ne correspond aux filtres.<br><span class="link" onclick="dashReset()">Réinitialiser les filtres</span></div>`:`<div class="empty">Aucun ex-combattant enregistré pour le moment.<br>Utilisez « Nouvel enregistrement » pour ouvrir un premier dossier.${CUR&&CUR.role==="admin"?`<br><br><button class="btn sec" onclick="askSeedDemo()">Charger des données de démonstration</button>`:""}</div>`}</div></div>
  </div>`;
}


/* module: combattants/nouveau.js — PNDDRR engine (classic globals) */
/* ================= NOUVEL ENREGISTREMENT ================= */
function selOpts(arr,sel){ return arr.map(x=>`<option ${x===sel?"selected":""}>${esc(x)}</option>`).join(""); }
function rNouveau(edit){
  const c = edit ? DB.combattants.find(x=>x.id===edit) : null;
  $("view").innerHTML = `
  <div class="panel"><div class="ph"><h3>${c?`Modification du dossier ${c.num}`:"Fiche d'enregistrement individuel"}</h3></div><div class="pb">
  <form id="fEnr" onsubmit="return false">
    <div style="display:flex;gap:20px;align-items:flex-start;margin-bottom:14px">
      <div>
        <div class="photo-box" id="phBox">${c&&c.photo?`<img src="${c.photo}">`:"Photo<br>d'identité"}</div>
        <input type="file" id="phFile" accept="image/*" capture="environment" style="margin-top:7px;font-size:11.5px;border:none;padding:0">
        <div class="small muted" style="margin-top:3px">La photo est automatiquement compressée (≈300 px) pour préserver la capacité de stockage.</div>
      </div>
      <div style="flex:1">
        <div class="grid3">
          <div class="field"><label>Nom *</label><input id="e_nom" required value="${c?esc(c.nom):""}"></div>
          <div class="field"><label>Prénom(s) *</label><input id="e_prenom" required value="${c?esc(c.prenom):""}"></div>
          <div class="field"><label>Alias / nom de guerre</label><input id="e_alias" value="${c?esc(c.alias||""):""}"></div>
          <div class="field"><label>Sexe *</label><select id="e_sexe"><option ${c&&c.sexe==="M"?"selected":""} value="M">Masculin</option><option ${c&&c.sexe==="F"?"selected":""} value="F">Féminin</option></select></div>
          <div class="field"><label>Date de naissance</label><input type="date" id="e_dn" value="${c?c.dn||"":""}"></div>
          <div class="field"><label>Lieu de naissance</label><input id="e_ln" value="${c?esc(c.ln||""):""}"></div>
          <div class="field"><label>Nationalité</label><input id="e_nat" value="${c?esc(c.nat):"Centrafricaine"}"></div>
          <div class="field"><label>Téléphone</label><input id="e_tel" value="${c?esc(c.tel||""):""}"></div>
          <div class="field"><label>Situation familiale</label><select id="e_fam">${selOpts(["Célibataire","Marié(e)","Veuf(ve)","Divorcé(e)"],c?c.fam:"")}</select></div>
        </div>
      </div>
    </div>
    <h3 style="color:var(--teal-dark);margin:8px 0 10px;font-size:13.5px;text-transform:uppercase">Localisation</h3>
    <div class="grid3">
      <div class="field"><label>Région</label><select id="e_region" onchange="fillPref()">${Object.entries(REGIONS).map(([r,v])=>`<option value="${esc(r)}" ${c&&regionOf(c.prefecture)===r?"selected":""}>${v.num}. ${esc(r)}${r==="Bas-Oubangui"?" (Bangui)":""}</option>`).join("")}</select></div>
      <div class="field"><label>Préfecture *</label><select id="e_pref" onchange="fillLoc()"></select></div>
      <div class="field"><label>Sous-préfecture</label><select id="e_sp"></select><input id="e_sp_libre" placeholder="Préciser la sous-préfecture" style="display:none;margin-top:5px"></div>
      <div class="field"><label>Commune</label><select id="e_commune"></select><input id="e_commune_libre" placeholder="Préciser la commune" style="display:none;margin-top:5px"></div>
      <div class="field"><label>Site de regroupement / cantonnement</label><input id="e_site" value="${c?esc(c.site||""):""}"></div>
      <div class="field"><label>Vague d'enregistrement (générée automatiquement)</label>
      <div style="display:flex;gap:6px"><input id="e_vague" list="vagueList" style="flex:1" value="${c?esc(c.vague||""):""}" oninput="this.dataset.man='1'" placeholder="Générée selon la préfecture"><button type="button" class="btn sec sm" title="Ouvrir une nouvelle vague pour cette préfecture" onclick="$('e_vague').dataset.man='';majVague(true)">↻ Nouvelle</button></div>
      <datalist id="vagueList">${promosConnues().map(p=>`<option>${esc(p)}</option>`).join("")}</datalist>
      <div class="small muted" style="margin-top:3px">Format automatique : Vague &lt;Préfecture&gt; &lt;Année&gt;-&lt;n°&gt; — les enregistrements du jour dans la même préfecture rejoignent la même vague. Modifiable si besoin, suivie jusqu'à la formation.</div></div>
    </div>
    <h3 style="color:var(--teal-dark);margin:8px 0 10px;font-size:13.5px;text-transform:uppercase">Parcours combattant</h3>
    <div class="grid3">
      <div class="field"><label>Groupe armé d'origine *</label><div style="display:flex;gap:6px"><select id="e_grp" style="flex:1">${selOpts(GROUPES,c?c.groupe:"")}</select>${hasPerm("referentiels")?`<button type="button" class="btn sec sm" title="Ajouter un groupe armé au référentiel" onclick="quickAddGroupe()">+</button>`:""}</div></div>
      <div class="field"><label>Fonction / grade dans le groupe</label><input id="e_grade" value="${c?esc(c.grade||""):""}"></div>
      <div class="field"><label>Années passées dans le groupe</label><input type="number" min="0" id="e_annees" value="${c?c.annees||"":""}"></div>
      <div class="field"><label>Zone d'opération principale</label><input id="e_zone" value="${c?esc(c.zone||""):""}"></div>
      <div class="field"><label>Souhait de réintégration</label><select id="e_souhait">${selOpts(["Socio-économique","Militaire","Indécis"],c?c.souhait:"")}</select></div>
      <div class="field"><label>Niveau d'instruction</label><select id="e_instr">${selOpts(["Aucun","Primaire","Secondaire","Supérieur","Coranique"],c?c.instr:"")}</select></div>
    </div>
    <div class="field"><label>Observations</label><textarea id="e_obs" rows="2">${c?esc(c.obs||""):""}</textarea></div>
    <div style="display:flex;gap:10px;justify-content:flex-end">
      ${c?`<button type="button" class="btn ghost" onclick="go('fiche','${c.id}')">Annuler</button>`:""}
      <button class="btn" type="button" onclick="submitEnr()">${c?"Enregistrer les modifications":"Ouvrir le dossier"}</button>
    </div>
  </form></div></div>`;
  window.vagueAuto = function(pref, fresh){
    const an=new Date().getFullYear(), jour=today();
    /* Même opération : réutiliser la vague des enregistrements du jour dans la même préfecture */
    if(!fresh){
      const rec=[...DB.combattants].reverse().find(x=>x.prefecture===pref&&x.vague&&x.creele&&x.creele.slice(0,10)===jour);
      if(rec) return rec.vague;
    }
    /* Sinon : numéro suivant pour la préfecture et l'année */
    const rx=new RegExp("^Vague "+pref.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")+" "+an+"-(\\d+)$");
    let max=0;
    DB.combattants.forEach(x=>{ const m=x.vague&&x.vague.match(rx); if(m) max=Math.max(max,+m[1]); });
    return `Vague ${pref} ${an}-${max+1}`;
  };
  window.majVague = function(force){
    const v=$("e_vague"); if(!v) return;
    if(c&&!force) return;                       /* modification d'un dossier : conserver la vague existante */
    if(v.dataset.man==="1"&&!force) return;     /* saisie manuelle : ne pas écraser */
    v.value=vagueAuto($("e_pref").value, force===true); v.dataset.man="";
  };
  window.fillPref = function(){
    const reg=$("e_region").value;
    const list=REGIONS[reg].prefs;
    const cur=(c&&list.includes(c.prefecture))?c.prefecture:list[0];
    $("e_pref").innerHTML=list.map(p=>`<option ${p===cur?"selected":""}>${esc(p)}</option>`).join("");
    fillLoc();
  };
  window.fillLoc = function(){
    const pref=$("e_pref").value;
    const mk=(sel,libre,list,cur)=>{
      const el=$(sel), lb=$(libre);
      const inList=cur&&list.includes(cur);
      el.innerHTML='<option value="">— Non précisée —</option>'
        +list.map(x=>`<option ${x===cur?"selected":""}>${esc(x)}</option>`).join("")
        +`<option value="__autre" ${cur&&!inList?"selected":""}>Autre (préciser)…</option>`;
      lb.style.display=(cur&&!inList)?"block":"none";
      if(cur&&!inList) lb.value=cur;
      el.onchange=()=>{ lb.style.display=el.value==="__autre"?"block":"none"; };
    };
    mk("e_sp","e_sp_libre",SOUS_PREFS[pref]||[], c?c.sousPref:"");
    mk("e_commune","e_commune_libre",COMMUNES[pref]||[], c?c.commune:"");
    majVague();
  };
  fillPref();
  const locVal=(sel,libre)=>{ const v=$(sel).value; return v==="__autre"?$(libre).value.trim():v; };
  let photoData = c?c.photo||null:null;
  $("phFile").addEventListener("change",e=>{
    const f=e.target.files[0]; if(!f) return;
    const r=new FileReader();
    r.onload=()=>{
      const img=new Image();
      img.onload=()=>{
        try{
          const MAX=300, k=Math.min(1, MAX/Math.max(img.width,img.height));
          const cv=document.createElement("canvas");
          cv.width=Math.max(1,Math.round(img.width*k)); cv.height=Math.max(1,Math.round(img.height*k));
          cv.getContext("2d").drawImage(img,0,0,cv.width,cv.height);
          const out=cv.toDataURL("image/jpeg",0.82);
          photoData = out && out.length>50 ? out : r.result;
        }catch(err){ photoData=r.result; }
        $("phBox").innerHTML=`<img src="${photoData}">`;
      };
      img.onerror=()=>{ photoData=r.result; $("phBox").innerHTML=`<img src="${photoData}">`; };
      img.src=r.result;
    };
    r.readAsDataURL(f);
  });
  window.submitEnr = function(){
    if(!$("e_nom").value.trim()||!$("e_prenom").value.trim()){ toast("Le nom et le prénom sont obligatoires."); return; }
    if(!$("e_vague").value.trim()){ toast("La vague d'enregistrement est obligatoire — elle permet le suivi jusqu'à la formation."); return; }
    const d={
      nom:$("e_nom").value.trim().toUpperCase(), prenom:$("e_prenom").value.trim(), alias:$("e_alias").value.trim(),
      sexe:$("e_sexe").value, dn:$("e_dn").value, ln:$("e_ln").value.trim(), nat:$("e_nat").value.trim(), tel:$("e_tel").value.trim(),
      fam:$("e_fam").value, prefecture:$("e_pref").value, sousPref:locVal("e_sp","e_sp_libre"), commune:locVal("e_commune","e_commune_libre"), site:$("e_site").value.trim(), vague:$("e_vague").value.trim(),
      groupe:$("e_grp").value, grade:$("e_grade").value.trim(), annees:$("e_annees").value, zone:$("e_zone").value.trim(),
      souhait:$("e_souhait").value, instr:$("e_instr").value, obs:$("e_obs").value.trim(), photo:photoData
    };
    if(c){ Object.assign(c,d); log("Modification dossier",`${c.num} — ${c.nom} ${c.prenom}`); toast("Dossier mis à jour."); go("fiche",c.id); }
    else{
      const nc = Object.assign({ id:"c"+Date.now(), num:numDossier(), statut:"enregistre", creele:new Date().toISOString(), agent:CUR.login,
        desarmement:null, demobilisation:null, reintMil:null, reintSocio:null, fin:null, abandon:null }, d);
      DB.combattants.push(nc);
      log("Enregistrement",`Ouverture du dossier ${nc.num} — ${nc.nom} ${nc.prenom} (${nc.groupe}, ${nc.vague})`);
      toast(`Dossier ${nc.num} ouvert.`); go("fiche",nc.id);
    }
  };
  $("fEnr").addEventListener("submit",e=>{ e.preventDefault(); submitEnr(); });
}


/* module: combattants/registre.js — PNDDRR engine (classic globals) */
/* ================= REGISTRE ================= */
function rRegistre(preset){
  preset=preset||{};
  $("view").innerHTML = `
  <div class="toolbar">
    <div class="field"><label>Filtrer par statut</label><select id="fSt" onchange="filtRegistre()"><option value="">Tous</option>${Object.entries(STATUTS).map(([k,v])=>`<option value="${k}" ${preset.st===k?"selected":""}>${v.lbl}</option>`).join("")}</select></div>
    <div class="field"><label>Préfecture</label><select id="fPref" onchange="filtRegistre()"><option value="">Toutes</option>${PREFECTURES.map(p=>`<option ${preset.pref===p?"selected":""}>${esc(p)}</option>`).join("")}</select></div>
    <div class="field"><label>Groupe armé</label><select id="fGrp" onchange="filtRegistre()"><option value="">Tous</option>${GROUPES.map(g=>`<option ${preset.grp===g?"selected":""}>${esc(g)}</option>`).join("")}</select></div>
    <div class="field"><label>Vague</label><select id="fVague" onchange="filtRegistre()"><option value="">Toutes</option>${promosConnues().map(v=>`<option ${preset.vague===v?"selected":""}>${esc(v)}</option>`).join("")}</select></div>
    <div class="field" style="flex:1"><label>Recherche rapide</label><input id="fTxt" placeholder="Nom, alias, n° dossier…" oninput="filtRegistre()"></div>
    <button class="btn sec" onclick="exportCombCSV()">Exporter CSV</button>
    <button class="btn sec" onclick="exportXLSX()">Exporter Excel</button>
    <button class="btn sec" onclick="go('import')">Importer…</button>
  </div>
  <div class="panel"><div class="pb nopad"><table><thead><tr><th>N° dossier</th><th>Nom & prénom</th><th>Sexe</th><th>Groupe armé</th><th>Préfecture</th><th>Statut</th><th>Actions</th></tr></thead><tbody id="tbReg"></tbody></table></div></div>`;
  REG_LIM=PAGE_TAILLE;
  filtRegistre();
}
var PAGE_TAILLE=100; try{ applyConfig(); }catch(e){}
var REG_LIM=PAGE_TAILLE;
function regPlus(tout){ REG_LIM=tout?Infinity:REG_LIM+PAGE_TAILLE; filtRegistre(); }
function filtRegistre(resetLim){
  if(resetLim!==false) {} 
  const st=$("fSt").value, gr=$("fGrp").value, pref=$("fPref").value, vg=$("fVague").value, q=$("fTxt").value.toLowerCase();
  const rows = DB.combattants.filter(c=>
    (!st||c.statut===st)&&(!gr||c.groupe===gr)&&(!pref||c.prefecture===pref)&&(!vg||c.vague===vg||(reintOf(c)&&reintOf(c).promo===vg))&&
    (!q||`${c.num} ${c.nom} ${c.prenom} ${c.alias||""}`.toLowerCase().includes(q))
  ).sort((a,b)=>a.num.localeCompare(b.num));
  const tronq=rows.length>REG_LIM;
  $("tbReg").innerHTML = (rows.length ? rows.slice(0,REG_LIM).map(c=>`<tr>
    <td><b>${c.num}</b></td><td>${esc(c.nom)} ${esc(c.prenom)}${c.alias?` <span class="muted small">« ${esc(c.alias)} »</span>`:""}</td>
    <td>${c.sexe}</td><td class="small">${esc(c.groupe)}</td><td class="small">${esc(c.prefecture)}</td>
    <td><span class="badge st-${c.statut}">${STATUTS[c.statut].lbl}</span></td>
    <td><span class="link" onclick="go('fiche','${c.id}')">Ouvrir le dossier</span></td></tr>`).join("")
  : `<tr><td colspan="7" class="empty">Aucun dossier ne correspond aux critères.</td></tr>`)
  + (tronq?`<tr><td colspan="7" style="text-align:center;padding:11px;background:#FBFDFC"><span class="small muted">${REG_LIM} affiché(s) sur ${rows.length} — </span><button class="btn sm sec" onclick="regPlus()">Afficher ${Math.min(PAGE_TAILLE,rows.length-REG_LIM)} de plus</button> <button class="btn sm ghost" onclick="regPlus(true)">Tout afficher</button></td></tr>`:"");
}



/* module: demo/seed.js — PNDDRR engine (classic globals) */
/* ================= DONNÉES DE DÉMONSTRATION (SIMULATION) ================= */
function seedRng(seed){ let a=seed>>>0; return function(){ a|=0; a=a+0x6D2B79F5|0; let t=Math.imul(a^a>>>15,1|a); t=t+Math.imul(t^t>>>7,61|t)^t; return ((t^t>>>14)>>>0)/4294967296; }; }
function seedDemo(n, silent){
  n=n||60;
  const rnd=seedRng(2026), pick=a=>a[Math.floor(rnd()*a.length)], chance=p=>rnd()<p;
  const NOMS=["NGOUMBA","SANZE","YAKA","DOKO","GBAZALE","KOSSI","MOKO","BAILA","WILI","MANDJEKA","KPENGO","OUAMBI","YAMBELE","NDOUBA","SEREMALE","BANGA","MBOLI","DEKO","PAMBA","GREKOMBO","NAKOMBO","ZOUNGOULA","BEFIO","MAZANGUE","KONGBO"];
  const PM=["Jean","Pierre","Fidèle","Marcel","Ghislain","Rodrigue","Éric","Serge","Boris","Aristide","Dieudonné","Christian","Armel","Brice","Sylvain","Parfait","Igor","Junior"];
  const PF=["Marie","Rosine","Alice","Chantal","Nadège","Grâce","Paméla","Sandrine","Élodie","Clarisse","Mireille","Olga"];
  const iso=j=>{ const dte=new Date(Date.now()-j*86400000); return dte.toISOString(); };
  const jour=j=>iso(j).slice(0,10);
  const grpChoix=GROUPES.filter(g=>g!=="Autre");
  let serie=1000;
  const cibles=[]; for(let i=0;i<n;i++){ const r=rnd();
    cibles.push(r<.14?"enregistre":r<.34?"desarme":r<.48?"demobilise":r<.60?"reintegration_militaire":r<.78?"reintegration_socio":r<.93?"reintegre":"abandon"); }
  let crees=0;
  for(const cible of cibles){
    const sexe=chance(.86)?"M":"F";
    const pref=pick(PREFECTURES);
    const sp=(SOUS_PREFS[pref]&&SOUS_PREFS[pref].length)?pick(SOUS_PREFS[pref]):"";
    const com=(COMMUNES[pref]&&COMMUNES[pref].length)?pick(COMMUNES[pref]):"";
    const j0=30+Math.floor(rnd()*330);   /* enregistrement : entre 1 et 12 mois */
    const c={ id:"demo"+Date.now()+"_"+(crees++)+Math.floor(rnd()*1e4), num:numDossier(), statut:"enregistre",
      creele:iso(j0), agent:"Poste de démonstration",
      nom:pick(NOMS), prenom:sexe==="M"?pick(PM):pick(PF), alias:chance(.3)?pick(["Cobra","Tigre","Rambo","Delta","Sultan","Fantôme","Scorpion","Colonel"]):"",
      sexe, dn:`${1975+Math.floor(rnd()*32)}-${String(1+Math.floor(rnd()*12)).padStart(2,"0")}-${String(1+Math.floor(rnd()*28)).padStart(2,"0")}`,
      ln:sp||pref, nat:"Centrafricaine", tel:chance(.6)?"7"+String(2000000+Math.floor(rnd()*7999999)):"",
      fam:pick(["Célibataire","Marié(e)","Marié(e)","Veuf(ve)","Union libre"]), prefecture:pref, sousPref:sp, commune:com, site:sp||pref,
      vague:pick(["Vague Bambari 2026-1","Vague Bouar 2026-2","Vague Kaga-Bandoro 2026-1","Vague Bria 2025-4","Vague Berbérati 2026-1"]),
      groupe:pick(grpChoix), grade:pick(["","","Combattant","Chef d'équipe","Chef de groupe","Commandant de zone"]),
      annees:String(1+Math.floor(rnd()*10)), zone:sp||pref, souhait:pick(["Militaire","Socio-économique","Indécis"]),
      instr:pick(["Aucun","Primaire","Primaire","Secondaire","Coranique"]), obs:"Dossier de démonstration (simulation)", photo:null,
      desarmement:null, demobilisation:null, reintMil:null, reintSocio:null, fin:null, abandon:null };
    const ordre=["enregistre","desarme","demobilise","reintegration_militaire","reintegration_socio","reintegre","abandon"];
    const avance=ordre.indexOf(cible);
    let j=j0;
    if(avance>=1&&cible!=="abandon"||cible==="abandon"&&chance(.7)){
      j-=3+Math.floor(rnd()*10);
      const armes=[]; const na=chance(.12)?0:1+Math.floor(rnd()*2);
      for(let a=0;a<na;a++){ const t=pick(TYPES_ARMES.filter(x=>x!=="Munitions (lot)"));
        armes.push({type:t, marque:t.includes("AK")?pick(["Type 56","AKM","Zastava M70"]):chance(.5)?pick(["Norinco","Baïkal","Artisanale","MAT-49"]):"", calibre:t.includes("AK")||t.includes("PKM")?"7,62 mm":chance(.5)?pick(["9 mm","12 mm","5,45 mm"]):"", serie:chance(.75)?"SN-"+(serie++):"", etat:pick(["Fonctionnelle","Fonctionnelle","Fonctionnelle","Défectueuse","Hors d'usage"]), mun:""}); }
      const munitions=[]; if(chance(.45)) munitions.push({nature:pick(["7,62×39 mm","5,45 mm","9 mm","12,7 mm"]), qte:10+Math.floor(rnd()*290), unite:"cartouches", obs:""});
      if(chance(.15)) munitions.push({nature:"Grenades défensives", qte:1+Math.floor(rnd()*5), unite:"grenades", obs:""});
      c.desarmement={date:jour(j), lieu:sp||pref, agent:"Poste de démonstration", armes, munitions};
      c.statut="desarme";
    }
    if(avance>=2&&avance<=5&&cible!=="reintegration_socio"||cible==="reintegration_socio"&&chance(.6)||cible==="reintegre"&&true){
      if(c.desarmement&&(avance>=2)&&cible!=="abandon"&&!(cible==="reintegration_socio"&&chance(.3))){
        j-=5+Math.floor(rnd()*20);
        c.demobilisation={date:jour(j), lieu:pick(["Bangui","Bouar","Bambari","Kaga-Bandoro","Bria"]), carte:numDem()};
        c.statut="demobilise";
      }
    }
    if(["reintegration_militaire","reintegration_socio","reintegre"].includes(cible)&&c.desarmement){
      j-=5+Math.floor(rnd()*25);
      const mil=cible==="reintegration_militaire"||cible==="reintegre"&&chance(.4);
      if(mil){
        c.reintMil={corps:pick(CORPS), unite:pick(["1er BIT","2e BIT","BSS","Zone de défense n°3","Compagnie de Bouar","Escadron de Bambari"]), matricule:"FACA-"+(1000+Math.floor(rnd()*9000)), date:jour(j), formation:pick(["Formation initiale de 3 mois","Recyclage militaire","Formation commune de base"])};
        c.statut="reintegration_militaire";
      } else {
        c.reintSocio={filiere:pick(FILIERES.filter(f=>f!=="Autre")), centre:pick(["Centre de Bangui","Centre de Bouar","Centre de Bambari","Centre de Berbérati","ONG partenaire"]), duree:pick(["3 mois","6 mois","9 mois"]), date:jour(j), kit:chance(.7), kitDate:chance(.7)?jour(j-20):"", appui:chance(.8)?String(50000+Math.floor(rnd()*9)*25000):"", visites:[]};
        c.statut="reintegration_socio";
        const nv=Math.floor(rnd()*3);
        for(let v=0;v<nv;v++) c.reintSocio.visites.push({date:jour(Math.max(2,j-10-v*25)), agent:"Chargé de suivi & évaluation", appr:pick(["Satisfaisant","Satisfaisant","Moyen","Préoccupant"]), obs:pick(["Activité en progression","Assiduité correcte","Difficultés d'approvisionnement","Bonne insertion dans la communauté"])});
      }
      const r=reintOf(c);
      r.promo=c.vague;
      if(chance(.6)){ r.formFin=jour(Math.max(2,j-15)); r.formNote="Formation achevée avec succès"+(r.promo?` (promotion ${r.promo})`:""); }
      if(r.formFin&&chance(.6)){ r.vieDate=jour(Math.max(1,j-30)); r.vieDetail=c.reintMil?c.reintMil.unite:("Activité : "+c.reintSocio.filiere); }
      if(cible==="reintegre"){
        c.fin=jour(Math.max(1,j-35)); c.statut="reintegre";
        if(!r.formFin){ r.formFin=c.fin; r.formNote="Considérée achevée à la clôture du parcours"; }
        if(!r.vieDate){ r.vieDate=c.fin; r.vieDetail=c.reintMil?"Intégration confirmée à la clôture":"Intégration à la vie civile confirmée à la clôture"; }
      }
    }
    if(cible==="abandon"){ c.statut="abandon"; c.abandon={date:jour(Math.max(1,j-10)), motif:pick(["Départ volontaire du site","Absence prolongée non justifiée","Retour dans la zone d'origine","Refus de l'orientation proposée"])}; }
    DB.combattants.push(c);
  }
  if(silent){ persist(); return; }
  log("Simulation",`${n} dossier(s) de démonstration générés`);
  toast(`${n} dossiers de démonstration ajoutés — la simulation est prête.`);
  go('dashboard');
}
/* Premier démarrage : pré-charger la simulation (60 ex-combattants fictifs) pour découvrir le programme.
   L'effacement des données locales laisse ensuite l'appareil vierge (marqueur pnddrr_skip_demo). */
try{
  if(window.__PNDDRR_DEMO && DB.combattants.length===0 && !(HAS_LS&&localStorage.getItem("pnddrr_skip_demo"))){
    seedDemo(60,true); DEMO_PRELOADED=true;
  }
}catch(e){}
function askSeedDemo(){
  openModal("Charger des données de démonstration", `
    <p>Cette action va générer <b>60 dossiers fictifs</b> couvrant tout le circuit — enregistrements, désarmements (armes et munitions), démobilisations, réintégrations militaires et socio-économiques avec jalons et visites, parcours clôturés et abandons — répartis sur les préfectures et les 12 derniers mois.</p>
    <p class="small muted" style="margin-top:8px">Les dossiers portent la mention « Dossier de démonstration (simulation) » et s'ajoutent aux données existantes${DB.combattants.length?` (${DB.combattants.length} dossier(s) actuellement)`:""}. Pour repartir à zéro ensuite : Sauvegarde → Effacer les données locales.</p>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="closeModal();seedDemo(60)">Générer la simulation</button>`);
}


/* module: reintegration/jalons.js — PNDDRR engine (classic globals) */
/* ================= FORMATION & INTÉGRATION (JALONS) ================= */
var JAL_F={voie:"",etat:"",promo:"",q:""};
function jalSearch(v){
  JAL_F.q=v; rJalons();
  const el=$("jal_q"); if(el){ el.focus(); const n=el.value.length; try{el.setSelectionRange(n,n);}catch(e){} }
}
function promosConnues(){
  const set=new Set();
  DB.combattants.forEach(c=>{ if(c.vague) set.add(c.vague); const r=reintOf(c); if(r&&r.promo) set.add(r.promo); });
  return [...set].sort();
}
function jalCoches(){ return [...document.querySelectorAll(".jal_ck:checked")].map(cb=>cb.value); }
function affecterPromo(){
  const ids=jalCoches();
  const nom=$("jal_promo_nom").value.trim();
  if(!ids.length){ toast("Cochez au moins un ex-combattant."); return; }
  if(!nom){ toast("Saisissez le nom de la promotion / vague."); return; }
  ids.forEach(id=>{ const c=DB.combattants.find(x=>x.id===id); const r=reintOf(c); if(r) r.promo=nom; });
  log("Promotion",`${ids.length} parcours affecté(s) à « ${nom} »`);
  toast(`${ids.length} parcours affecté(s) à la promotion « ${nom} ».`);
  rJalons();
}
function mPromoValider(promo){
  const membres=DB.combattants.filter(c=>["reintegration_militaire","reintegration_socio"].includes(c.statut)&&reintOf(c).promo===promo);
  const attente=membres.filter(c=>!reintOf(c).formFin);
  openModal(`Valider la formation — ${esc(promo)}`, `
    <p class="small muted" style="margin-bottom:10px">${membres.length} parcours dans cette promotion, dont <b>${attente.length}</b> en attente de validation de la formation${membres.length-attente.length?` (${membres.length-attente.length} déjà validé(s), inchangés)`:""}.</p>
    <div class="pb nopad" style="max-height:180px;overflow:auto;border:1px solid var(--line);border-radius:8px;margin-bottom:12px">
      <table><tbody>${attente.map(c=>`<tr><td><b>${c.num}</b></td><td>${esc(c.nom)} ${esc(c.prenom)}</td><td class="small">${c.reintMil?"Militaire":"Socio-éco. — "+esc(c.reintSocio.filiere)}</td></tr>`).join("")||'<tr><td class="empty">Aucun parcours en attente.</td></tr>'}</tbody></table></div>
    <div class="grid2">
      <div class="field"><label>Date d'achèvement</label><input type="date" id="pv_date" value="${today()}"></div>
      <div class="field"><label>Résultat</label><select id="pv_res"><option>Formation achevée avec succès</option><option>Formation achevée — attestation délivrée</option><option>Formation validée partiellement</option></select></div>
    </div>
    <div class="field"><label>Observations</label><textarea id="pv_obs" rows="2" placeholder="Ex. : cérémonie de fin de formation du ${new Date().toLocaleDateString("fr-FR")}"></textarea></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" ${attente.length?"":"disabled"} onclick="savePromoValider('${esc(promo).replace(/'/g,"\\'")}')">Valider la formation des ${attente.length} parcours</button>`);
}
function savePromoValider(promo){
  const date=$("pv_date").value||today();
  const note=$("pv_res").value+(($("pv_obs").value.trim())?" — "+$("pv_obs").value.trim():"")+` (promotion ${promo})`;
  let n=0;
  DB.combattants.forEach(c=>{
    if(!["reintegration_militaire","reintegration_socio"].includes(c.statut)) return;
    const r=reintOf(c);
    if(r&&r.promo===promo&&!r.formFin){ r.formFin=date; r.formNote=note; n++; }
  });
  log("Validation par promotion",`« ${promo} » — ${n} formation(s) validée(s) le ${fmtD(date)}`);
  closeModal(); toast(`Formation validée pour ${n} parcours de la promotion « ${promo} ».`);
  rJalons();
}
function jalSet(k,v){ JAL_F[k]=v; rJalons(); }
function rJalons(){
  const enCours=DB.combattants.filter(c=>["reintegration_militaire","reintegration_socio"].includes(c.statut));
  const acheves=DB.combattants.filter(c=>c.statut==="reintegre"&&reintOf(c));
  const fForm=enCours.filter(c=>reintOf(c).formFin);
  const fVie=enCours.filter(c=>reintOf(c).vieDate);
  const P=hasPerm("orienter");
  const q=JAL_F.q.trim().toLowerCase();
  const filt=list=>list.filter(c=>{
    const r=reintOf(c), mil=!!c.reintMil;
    if(q){
      const txt=[c.num,c.nom,c.prenom,c.alias,c.vague,r.promo,c.prefecture,
        mil?c.reintMil.corps:"",mil?c.reintMil.unite:"",mil?c.reintMil.matricule:"",
        !mil&&c.reintSocio?c.reintSocio.filiere:"",!mil&&c.reintSocio?c.reintSocio.centre:""].join(" ").toLowerCase();
      if(!txt.includes(q)) return false;
    }
    if(JAL_F.promo&&r.promo!==JAL_F.promo) return false;
    if(JAL_F.voie==="mil"&&!mil) return false;
    if(JAL_F.voie==="socio"&&mil) return false;
    if(JAL_F.etat==="formEnCours"&&r.formFin) return false;
    if(JAL_F.etat==="formOk"&&!r.formFin) return false;
    if(JAL_F.etat==="vieAttente"&&r.vieDate) return false;
    if(JAL_F.etat==="vieOk"&&!r.vieDate) return false;
    return true;
  });
  const ligne=(c,fini)=>{
    const mil=!!c.reintMil, r=reintOf(c);
    const [bF,bV]=jalonBadge(c);
    const det=mil?`${esc(c.reintMil.corps||"")}${c.reintMil.unite?" · "+esc(c.reintMil.unite):""}`:`Filière ${esc(c.reintSocio.filiere||"")}${c.reintSocio.centre?" · "+esc(c.reintSocio.centre):""}`;
    return `<tr>${fini?"":`<td>${P?`<input type="checkbox" class="jal_ck" value="${c.id}" style="width:auto">`:""}</td>`}<td><b>${c.num}</b></td>
    <td><span class="link" onclick="go('fiche','${c.id}')">${esc(c.nom)} ${esc(c.prenom)}</span></td>
    <td><span class="badge st-${mil?"reintegration_militaire":"reintegration_socio"}">${mil?"Militaire":"Socio-éco."}</span></td>
    <td class="small">${det}</td>
    <td class="small">${r.promo?`<span class="tag">${esc(r.promo)}</span>`:'<span class="muted">—</span>'}</td>
    <td>${bF}${r.formFin&&r.formNote?`<div class="small muted">${esc(r.formNote)}</div>`:""}</td>
    <td>${bV}${r.vieDate&&r.vieDetail?`<div class="small muted">${esc(r.vieDetail)}</div>`:""}</td>
    ${fini?`<td>${fmtD(c.fin)}</td>`:`<td class="actions-cell">${!r.formFin&&P?`<button class="btn sm sec" onclick="mFormationFin('${c.id}')">Valider la fin de formation</button>`:""}${r.formFin&&!r.vieDate&&P?`<button class="btn sm" onclick="mIntegration('${c.id}')">Confirmer l'intégration</button>`:""}${r.formFin&&r.vieDate?`<span class="small muted">Jalons atteints — clôture possible depuis le suivi</span>`:""}</td>`}</tr>`;
  };
  const eC=filt(enCours), aC=filt(acheves);
  $("view").innerHTML = `
  <div class="cards">
    <div class="kpi"><div class="n">${enCours.length}</div><div class="l">Parcours en cours</div></div>
    <div class="kpi c-dem"><div class="n">${enCours.length-fForm.length}</div><div class="l">En formation</div></div>
    <div class="kpi c-ok"><div class="n">${fForm.length}</div><div class="l">Formations achevées</div></div>
    <div class="kpi c-rm"><div class="n">${fForm.length-fVie.length}</div><div class="l">En attente d'intégration</div></div>
    <div class="kpi c-rs"><div class="n">${fVie.length}</div><div class="l">Intégrés vie militaire / civile</div></div>
    <div class="kpi"><div class="n">${acheves.length}</div><div class="l">Parcours clôturés</div></div>
  </div>
  <div class="toolbar">
    <div class="field"><label>Voie de réintégration</label><select onchange="jalSet('voie',this.value)"><option value="">Toutes</option><option value="mil" ${JAL_F.voie==="mil"?"selected":""}>Militaire</option><option value="socio" ${JAL_F.voie==="socio"?"selected":""}>Socio-économique</option></select></div>
    <div class="field" style="flex:1;min-width:220px"><label>Recherche</label><input id="jal_q" value="${esc(JAL_F.q)}" oninput="jalSearch(this.value)" placeholder="Nom, n° dossier, matricule, filière, unité, vague…"></div>
    <div class="field"><label>Promotion / vague</label><select onchange="jalSet('promo',this.value)"><option value="">Toutes</option>${promosConnues().map(p=>`<option ${JAL_F.promo===p?"selected":""}>${esc(p)}</option>`).join("")}</select></div>
    <div class="field"><label>État des jalons</label><select onchange="jalSet('etat',this.value)"><option value="">Tous</option>
      <option value="formEnCours" ${JAL_F.etat==="formEnCours"?"selected":""}>Formation en cours</option>
      <option value="formOk" ${JAL_F.etat==="formOk"?"selected":""}>Formation achevée</option>
      <option value="vieAttente" ${JAL_F.etat==="vieAttente"?"selected":""}>Intégration en attente</option>
      <option value="vieOk" ${JAL_F.etat==="vieOk"?"selected":""}>Intégrés</option></select></div>
    ${(JAL_F.voie||JAL_F.etat||JAL_F.promo||JAL_F.q)?`<button class="btn ghost" style="align-self:end" onclick="JAL_F={voie:'',etat:'',promo:'',q:''};rJalons()">✕ Réinitialiser</button>`:""}
  </div>
  ${(()=>{ const promos={}; enCours.forEach(c=>{const p=reintOf(c).promo; if(p){(promos[p]=promos[p]||[]).push(c);} });
    const noms=Object.keys(promos).sort();
    return `<div class="panel"><div class="ph"><h3>Validation par promotion / vague</h3></div><div class="pb">
    ${P?`<div class="toolbar" style="margin-bottom:${noms.length?"13px":"0"}">
      <div class="field" style="max-width:320px"><label>Affecter les parcours cochés à une promotion</label><input id="jal_promo_nom" list="promoList" placeholder="Ex. : Vague 2026-A"><datalist id="promoList">${promosConnues().map(p=>`<option>${esc(p)}</option>`).join("")}</datalist></div>
      <button class="btn sec" style="align-self:end" onclick="affecterPromo()">Affecter la sélection</button>
      <div class="small muted" style="align-self:end;padding-bottom:9px">Cochez des parcours dans le tableau ci-dessous, nommez la vague, puis validez la formation de toute la promotion en une fois.</div>
    </div>`:""}
    ${noms.length?`<table><thead><tr><th>Promotion / vague</th><th>Parcours</th><th>Formations validées</th><th>En attente</th><th>Actions</th></tr></thead><tbody>${
      noms.map(p=>{const L=promos[p], ok=L.filter(c=>reintOf(c).formFin).length, att=L.length-ok;
        return `<tr><td><b>${esc(p)}</b></td><td>${L.length}</td><td>${ok}</td><td>${att?`<span class="badge st-demobilise">${att}</span>`:'<span class="badge st-reintegre">0</span>'}</td>
        <td class="actions-cell">${att&&P?`<button class="btn sm" onclick="mPromoValider('${esc(p).replace(/'/g,"\\'")}')">Valider la formation de la promotion</button>`:att?"":'<span class="small muted">Promotion validée</span>'}</td></tr>`;}).join("")
    }</tbody></table>`:`<div class="small muted">${P?"Aucune promotion constituée pour l'instant — cochez des parcours ci-dessous et affectez-les à une vague.":"Aucune promotion constituée pour l'instant."}</div>`}
  </div></div>`; })()}
  <div class="panel"><div class="ph"><h3>Parcours en cours — avancement des jalons (${eC.length})</h3></div><div class="pb nopad">${
    eC.length?`<table><thead><tr>${P?"<th></th>":"<th></th>"}<th>N° dossier</th><th>Nom & prénom</th><th>Voie</th><th>Détail</th><th>Promotion</th><th>Formation</th><th>Intégration</th><th>Actions</th></tr></thead><tbody>${eC.map(c=>ligne(c,false)).join("")}</tbody></table>`
    :`<div class="empty">Aucun parcours ne correspond aux critères.</div>`}</div></div>
  <div class="panel"><div class="ph"><h3>Parcours clôturés — jalons atteints (${aC.length})</h3></div><div class="pb nopad">${
    aC.length?`<table><thead><tr><th>N° dossier</th><th>Nom & prénom</th><th>Voie</th><th>Détail</th><th>Promotion</th><th>Formation</th><th>Intégration</th><th>Clôturé le</th></tr></thead><tbody>${aC.map(c=>ligne(c,true)).join("")}</tbody></table>`
    :`<div class="empty">Aucun parcours clôturé.</div>`}</div></div>`;
}


/* module: combattants/fiche.js — PNDDRR engine (classic globals) */
/* ================= FICHE INDIVIDUELLE ================= */
function stepsHtml(c){
  const order=["enregistre","desarme","demobilise","reint","reintegre"];
  const cur = c.statut==="reintegration_militaire"||c.statut==="reintegration_socio" ? "reint" : c.statut;
  const idx = c.statut==="abandon" ? -1 : order.indexOf(cur);
  const lbl = {enregistre:"1. Enregistrement",desarme:"2. Désarmement",demobilise:"3. Démobilisation",reint:"4. Réintégration",reintegre:"5. Réintégré"};
  let h = order.map((s,i)=>`<div class="step click ${idx>i?"done":idx===i?"now":""}" title="Cliquer pour le détail de cette étape" onclick="stepInfo('${c.id}','${s}')">${lbl[s]}</div>`).join("");
  if(c.statut==="abandon") h+=`<div class="step ab click" title="Cliquer pour le détail" onclick="stepInfo('${c.id}','abandon')">Abandon</div>`;
  return `<div class="steps">${h}</div>`;
}
function stepInfo(id, step){
  const c=DB.combattants.find(x=>x.id===id); if(!c) return;
  const order=["enregistre","desarme","demobilise","reint","reintegre"];
  const cur = c.statut==="reintegration_militaire"||c.statut==="reintegration_socio" ? "reint" : c.statut;
  const idx = c.statut==="abandon" ? -1 : order.indexOf(cur);
  const i=order.indexOf(step);
  const etat = step==="abandon" ? "" : idx>i?`<span class="badge st-reintegre">Étape accomplie</span>` : idx===i?`<span class="badge st-demobilise">Étape en cours</span>` : `<span class="badge st-enregistre">Étape à venir</span>`;
  const L=(k,v)=>`<tr><td style="width:210px;color:var(--muted);font-weight:600">${k}</td><td>${v}</td></tr>`;
  const T=rows=>`<table style="background:transparent">${rows.join("")}</table>`;
  let titre="", corps="", pied=`<button class="btn ghost" onclick="closeModal()">Fermer</button>`;
  if(step==="enregistre"){
    titre="1. Enregistrement";
    corps=T([L("Dossier ouvert le",fmtD(c.creele.slice(0,10))+" par "+esc(c.agent)),
      L("Vague d'enregistrement",c.vague?`<span class="tag">${esc(c.vague)}</span>`:"—"),
      L("Localisation",`${esc(c.prefecture)} (région ${regionOf(c.prefecture)})${c.sousPref?" — "+esc(c.sousPref):""}${c.commune?" — "+esc(c.commune):""}${c.site?" · site : "+esc(c.site):""}`),
      L("Groupe armé d'origine",esc(c.groupe)+(c.grade?" · "+esc(c.grade):"")),
      L("Souhait exprimé",esc(c.souhait))]);
  } else if(step==="desarme"){
    titre="2. Désarmement";
    corps=c.desarmement?T([L("Désarmé le",fmtD(c.desarmement.date)+" à "+esc(c.desarmement.lieu)),
      L("Matériel réceptionné par",esc(c.desarmement.agent)),
      L("Armes remises",c.desarmement.armes.length?c.desarmement.armes.map(a=>`${esc(a.type)}${a.serie?" ("+esc(a.serie)+")":""} — ${esc(a.etat)}`).join("<br>"):"Aucune"),
      L("Munitions / explosifs",(c.desarmement.munitions||[]).length?c.desarmement.munitions.map(m=>`${fmtN(m.qte)} ${esc(m.unite)}${m.nature?" — "+esc(m.nature):""}`).join("<br>"):"—")])
      :`<p class="muted">Le désarmement n'a pas encore eu lieu. Il consiste en la remise des armes et munitions contre attestation, et fait passer le dossier au statut « Désarmé ».</p>`;
    if(c.desarmement) pied=`<button class="btn ghost" onclick="closeModal()">Fermer</button><button class="btn sec" onclick="closeModal();printAttestation('${c.id}')">Attestation de désarmement</button>`;
  } else if(step==="demobilise"){
    titre="3. Démobilisation";
    corps=c.demobilisation?T([L("Prononcée le",fmtD(c.demobilisation.date)+" à "+esc(c.demobilisation.lieu)),
      L("Carte de démobilisé",`<b>${esc(c.demobilisation.carte)}</b>`)])
      :`<p class="muted">La démobilisation n'a pas encore été prononcée. Elle acte la sortie officielle du groupe armé et donne lieu à la délivrance d'une carte de démobilisé (l'orientation en réintégration reste possible dès le désarmement).</p>`;
    if(c.demobilisation) pied=`<button class="btn ghost" onclick="closeModal()">Fermer</button><button class="btn sec" onclick="closeModal();printCarte('${c.id}')">Carte de démobilisé</button>`;
  } else if(step==="reint"){
    titre="4. Réintégration";
    const r=reintOf(c);
    if(!r) corps=`<p class="muted">Aucune orientation prononcée pour l'instant. L'ex-combattant sera orienté vers la voie militaire (incorporation dans les forces) ou socio-économique (formation professionnelle et appui à l'installation).</p>`;
    else{
      const rows=[L("Voie",c.reintMil?"Militaire":"Socio-économique"),L("Orientation prononcée le",fmtD(r.date))];
      if(c.reintMil){ rows.push(L("Corps / unité",esc(c.reintMil.corps)+(c.reintMil.unite?" · "+esc(c.reintMil.unite):"")),L("Matricule",esc(c.reintMil.matricule)||"—"),L("Formation initiale",esc(c.reintMil.formation)||"—")); }
      else{ rows.push(L("Filière",esc(c.reintSocio.filiere)+(c.reintSocio.centre?" · "+esc(c.reintSocio.centre):"")),L("Kit / appui",(c.reintSocio.kit?"Kit remis":"Kit non remis")+(c.reintSocio.appui?" · "+fmtN(c.reintSocio.appui)+" FCFA":"")),L("Visites de suivi",String((c.reintSocio.visites||[]).length))); }
      if(r.promo) rows.push(L("Promotion / vague",`<span class="tag">${esc(r.promo)}</span>`));
      const [bF,bV]=jalonBadge(c);
      rows.push(L("Formation",bF+(r.formNote?` <span class="small muted">${esc(r.formNote)}</span>`:"")),L("Intégration à la vie "+(c.reintMil?"militaire":"civile"),bV+(r.vieDetail?` <span class="small muted">${esc(r.vieDetail)}</span>`:"")));
      corps=T(rows);
    }
  } else if(step==="reintegre"){
    titre="5. Réintégré";
    corps=c.statut==="reintegre"?T([L("Parcours achevé le",fmtD(c.fin)),
      L("Situation",c.reintMil?"Intégré à la vie militaire"+(reintOf(c).vieDetail?" — "+esc(reintOf(c).vieDetail):""):"Intégré à la vie civile"+(reintOf(c).vieDetail?" — "+esc(reintOf(c).vieDetail):""))])
      :`<p class="muted">La clôture interviendra au terme du parcours de réintégration, une fois la formation achevée et l'intégration à la vie militaire ou civile confirmée.</p>`;
    if(c.statut==="reintegre") pied=`<button class="btn ghost" onclick="closeModal()">Fermer</button><button class="btn sec" onclick="closeModal();printParcours('${c.id}')">Relevé du parcours</button>`;
  } else if(step==="abandon"){
    titre="Abandon du processus";
    corps=T([L("Déclaré le",fmtD(c.abandon.date)),L("Motif",esc(c.abandon.motif)||"—")]);
  }
  openModal(`${titre} — dossier ${c.num}`, `<div style="margin-bottom:10px">${etat}</div>${corps}`, pied);
}
function parcoursEvents(c){
  const ev=[];
  const add=(date,titre,detail,cls)=>{ if(date) ev.push({date:String(date).slice(0,10),titre,detail,cls}); };
  add(c.creele,"Enregistrement (désarmement — étape 1)",`Dossier ${c.num} ouvert par ${c.agent}${c.vague?" · vague "+c.vague:""}${c.site?" · site de "+c.site:""}`,"enregistre");
  if(c.desarmement){
    const d=c.desarmement, nm=(d.munitions||[]).length;
    add(d.date,"Désarmement — remise du matériel",`${d.armes.length} arme(s)${nm?` et ${nm} lot(s) de munitions`:""} réceptionnée(s) à ${d.lieu}${d.armes.length?" : "+d.armes.map(a=>a.type+(a.serie?" ("+a.serie+")":"")).join(", "):""}`,"desarme");
  }
  if(c.demobilisation) add(c.demobilisation.date,"Démobilisation prononcée",`Carte ${c.demobilisation.carte} délivrée à ${c.demobilisation.lieu}`,"demobilise");
  if(c.reintMil) add(c.reintMil.date,"Orientation — réintégration militaire",`${c.reintMil.corps}${c.reintMil.unite?" · "+c.reintMil.unite:""}${c.reintMil.matricule?" · matricule "+c.reintMil.matricule:""}${c.reintMil.promo?" · promotion "+c.reintMil.promo:""}${c.reintMil.formation?" · "+c.reintMil.formation:""}`,"reintegration_militaire");
  if(c.reintSocio){
    add(c.reintSocio.date,"Orientation — réintégration socio-économique",`Filière ${c.reintSocio.filiere}${c.reintSocio.centre?" · "+c.reintSocio.centre:""}${c.reintSocio.duree?" · "+c.reintSocio.duree:""}${c.reintSocio.promo?" · promotion "+c.reintSocio.promo:""}`,"reintegration_socio");
    if(c.reintSocio.kit) add(c.reintSocio.kitDate||c.reintSocio.date,"Kit de réinstallation remis","","reintegration_socio");
    if(c.reintSocio.appui) add(c.reintSocio.date,"Appui financier accordé",fmtN(c.reintSocio.appui)+" FCFA","reintegration_socio");
    (c.reintSocio.visites||[]).forEach(v=>add(v.date,"Visite de suivi",`Appréciation : ${v.appr}${v.obs?" — "+v.obs:""} (${v.agent})`,"reintegration_socio"));
  }
  const r=reintOf(c);
  if(r){
    if(r.formFin) add(r.formFin,"Fin de la formation "+(c.reintMil?"initiale":"professionnelle"),r.formNote||"","reintegre");
    if(r.vieDate) add(r.vieDate,c.reintMil?"Intégration à la vie militaire":"Intégration à la vie civile",r.vieDetail||"","reintegre");
  }
  if(c.rapatriement) add(c.rapatriement.date,"Rapatriement vers le pays d'origine",`${c.rapatriement.pays}${c.rapatriement.convoi?" · "+c.rapatriement.convoi:""}${c.rapatriement.autorites?" · remise : "+c.rapatriement.autorites:""}`,"rapatrie");
  if(c.fin&&c.statut!=="rapatrie") add(c.fin,"Clôture — réintégration achevée","Fin du parcours de réintégration","reintegre");
  if(c.abandon) add(c.abandon.date,"Abandon du processus",c.abandon.motif||"","abandon");
  return ev.sort((a,b)=>a.date.localeCompare(b.date));
}
function rFiche(id){
  const c = DB.combattants.find(x=>x.id===id); if(!c){ go("registre"); return; }
  FICHE_ID=id;
  $("pageTitle").textContent = `Dossier ${c.num}`;
  const A = hasPerm("enregistrer");
  const rowsInfo = [
    ["Nom & prénom(s)", `${esc(c.nom)} ${esc(c.prenom)}${c.alias?` — alias « ${esc(c.alias)} »`:""}`],
    ["Sexe / âge", `${c.sexe==="M"?"Masculin":"Féminin"} · ${age(c.dn)} ans (né(e) le ${fmtD(c.dn)} à ${esc(c.ln)||"—"})${estMineur(c)?' <span class="badge" style="background:#FCEBD8;color:#9A5B00" title="Enfant associé aux forces et groupes armés — la voie militaire est bloquée">Mineur (EAFGA)</span>':""}`],
    ["Nationalité / téléphone", `${esc(c.nat)} · ${esc(c.tel)||"—"}`],
    ["Situation familiale / instruction", `${esc(c.fam)} · ${esc(c.instr)}`],
    ["Localisation", `${esc(c.prefecture)} (région ${regionOf(c.prefecture)})${c.sousPref?" — S/P "+esc(c.sousPref):""}${c.commune?" — commune de "+esc(c.commune):""}${c.site?" · Site : "+esc(c.site):""}`],
    ["Vague d'enregistrement", c.vague?esc(c.vague):"—"],
    ["Groupe armé d'origine", `${esc(c.groupe)}${c.grade?" · "+esc(c.grade):""}${c.annees?" · "+c.annees+" an(s)":""}`],
    ["Zone d'opération", esc(c.zone)||"—"],
    ["Souhait de réintégration", esc(c.souhait)],
    ["Observations", esc(c.obs)||"—"],
    ["Dossier ouvert", `${fmtD(c.creele.slice(0,10))} par ${esc(c.agent)}`]
  ];
  let h = stepsHtml(c);
  h += `<div class="panel"><div class="ph"><h3>Identité — <span class="badge st-${c.statut}">${STATUTS[c.statut].lbl}</span></h3>
    <div style="display:flex;gap:7px">
      ${A?`<button class="btn sm sec" onclick="rNouveau('${c.id}')">Modifier</button>`:""}
      <button class="btn sm sec" onclick="printFiche('${c.id}')">Imprimer la fiche</button>
      ${hasPerm("cloturer")&&c.statut!=="abandon"&&c.statut!=="reintegre"?`<button class="btn sm danger" onclick="markAbandon('${c.id}')">Déclarer abandon</button>`:""}
    </div></div>
    <div class="pb" style="display:flex;gap:18px">
      <div class="photo-box">${c.photo?`<img src="${c.photo}">`:"Photo"}</div>
      <table style="background:transparent">${rowsInfo.map(r=>`<tr><td style="width:240px;color:var(--muted);font-weight:600">${r[0]}</td><td>${r[1]}</td></tr>`).join("")}</table>
    </div></div>`;

  /* --- Étape désarmement --- */
  h += `<div class="panel"><div class="ph"><h3>Étape 2 — Désarmement</h3>${
    c.statut==="enregistre"&&hasPerm("desarmer")?`<button class="btn sm" onclick="mDesarmement('${c.id}')">Procéder au désarmement</button>`:
    c.desarmement?`<button class="btn sm sec" onclick="printAttestation('${c.id}')">Attestation de désarmement</button>`:""
  }</div><div class="pb${c.desarmement?" nopad":""}">${
    c.desarmement ? `<table><thead><tr><th>Type</th><th>Marque / modèle</th><th>Calibre</th><th>N° de série</th><th>État</th><th>Munitions</th></tr></thead><tbody>${
      c.desarmement.armes.map(a=>`<tr><td>${esc(a.type)}</td><td>${esc(a.marque)||"—"}</td><td>${esc(a.calibre)||"—"}</td><td>${esc(a.serie)||"—"}</td><td>${esc(a.etat)}</td><td>${esc(a.mun)||"—"}</td></tr>`).join("")
    }</tbody></table>${(c.desarmement.munitions||[]).length?`<div style="padding:8px 16px 0"><b class="small" style="color:var(--teal-dark);text-transform:uppercase">Munitions et explosifs</b></div>
    <table><thead><tr><th>Nature / calibre</th><th>Quantité</th><th>Unité</th><th>Observations</th></tr></thead><tbody>${
      c.desarmement.munitions.map(m=>`<tr><td>${esc(m.nature)||"—"}</td><td>${fmtN(m.qte)}</td><td>${esc(m.unite)}</td><td class="small">${esc(m.obs)||"—"}</td></tr>`).join("")
    }</tbody></table>`:""}<div style="padding:10px 16px" class="small muted">Désarmé le ${fmtD(c.desarmement.date)} à ${esc(c.desarmement.lieu)} — matériel réceptionné par ${esc(c.desarmement.agent)}.</div>`
    : `<span class="muted">En attente — l'intéressé n'a pas encore remis d'armement.</span>`
  }</div></div>`;

  /* --- Étape démobilisation --- */
  h += `<div class="panel"><div class="ph"><h3>Étape 3 — Démobilisation</h3>${
    c.statut==="desarme"&&hasPerm("demobiliser")?`<button class="btn sm" onclick="mDemobilisation('${c.id}')">Prononcer la démobilisation</button>`:
    c.demobilisation?`<button class="btn sm sec" onclick="printCarte('${c.id}')">Carte de démobilisé</button>`:""
  }</div><div class="pb">${
    c.demobilisation ? `Démobilisé le <b>${fmtD(c.demobilisation.date)}</b> — carte n° <span class="tag">${c.demobilisation.carte}</span> délivrée à ${esc(c.demobilisation.lieu)}.`
    : `<span class="muted">En attente — la démobilisation peut être prononcée après la remise des armes${["reintegration_militaire","reintegration_socio"].includes(c.statut)?" (l'intéressé a été orienté directement en réintégration)":""}.</span>`
  }</div></div>`;

  /* --- Étape réintégration --- */
  h += `<div class="panel"><div class="ph"><h3>Étape 4 — Réintégration</h3>${
    (c.statut==="demobilise"||c.statut==="desarme")&&hasPerm("orienter")?`<span style="display:flex;gap:7px"><button class="btn sm" onclick="mReintMil('${c.id}')">Orientation militaire</button><button class="btn sm sec" onclick="mReintSocio('${c.id}')">Orientation socio-économique</button><button class="btn sm ghost" title="Combattant étranger : retour vers le pays d'origine" onclick="mRapatriement('${c.id}')">Rapatriement</button></span>`:""
  }</div><div class="pb">`;
  if(c.reintMil){
    const r=c.reintMil;
    h+=`<b style="color:#5A2E96">Réintégration militaire</b><table style="background:transparent;margin-top:6px">
      <tr><td style="width:240px;color:var(--muted);font-weight:600">Corps d'incorporation</td><td>${esc(r.corps)}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Unité d'affectation</td><td>${esc(r.unite)||"—"}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Matricule</td><td>${r.matricule?`<span class="tag">${esc(r.matricule)}</span>`:"—"}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Date d'incorporation</td><td>${fmtD(r.date)}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Formation initiale</td><td>${esc(r.formation)||"—"}</td></tr></table>
    ${jalonsFiche(c)}`;
  } else if(c.reintSocio){
    const r=c.reintSocio;
    h+=`<b style="color:#8A5400">Réintégration socio-économique</b><table style="background:transparent;margin-top:6px">
      <tr><td style="width:240px;color:var(--muted);font-weight:600">Filière choisie</td><td>${esc(r.filiere)}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Centre / structure de formation</td><td>${esc(r.centre)||"—"}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Durée de formation</td><td>${esc(r.duree)||"—"}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Kit de réinstallation</td><td>${r.kit?"Remis le "+fmtD(r.kitDate):"Non remis"}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Appui financier</td><td>${r.appui?fmtN(r.appui)+" FCFA":"—"}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Début du parcours</td><td>${fmtD(r.date)}</td></tr></table>
    ${jalonsFiche(c)}
      <h3 style="font-size:13px;color:var(--teal-dark);margin:14px 0 8px;text-transform:uppercase">Visites de suivi (${r.visites.length})</h3>
      ${r.visites.length?`<table><thead><tr><th>Date</th><th>Agent</th><th>Appréciation</th><th>Observations</th></tr></thead><tbody>${
        r.visites.map(v=>`<tr><td>${fmtD(v.date)}</td><td>${esc(v.agent)}</td><td>${esc(v.appr)}</td><td>${esc(v.obs)}</td></tr>`).join("")
      }</tbody></table>`:`<span class="muted small">Aucune visite de suivi enregistrée.</span>`}
      ${(c.statut==="reintegration_socio"&&hasPerm("visites"))?`<div style="margin-top:10px"><button class="btn sm sec" onclick="mVisite('${c.id}')">Ajouter une visite de suivi</button></div>`:""}`;
  } else if(c.rapatriement){
    const r=c.rapatriement;
    h+=`<b style="color:#274B8F">Rapatriement — combattant étranger</b><table style="background:transparent;margin-top:6px">
      <tr><td style="width:240px;color:var(--muted);font-weight:600">Pays de retour</td><td>${esc(r.pays)}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Rapatrié le</td><td>${fmtD(r.date)}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Convoi / point de passage</td><td>${esc(r.convoi)||"—"}</td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Remise aux autorités</td><td>${esc(r.autorites)||"—"}</td></tr>
      ${r.obs?`<tr><td style="color:var(--muted);font-weight:600">Observations</td><td>${esc(r.obs)}</td></tr>`:""}</table>`;
  } else {
    h+=`<span class="muted">En attente — l'orientation est possible dès le désarmement${c.souhait&&c.souhait!=="Indécis"?` (souhait exprimé : ${esc(c.souhait)})`:""}.</span>`;
  }
  h+=`</div></div>`;

  /* --- Clôture --- */
  h += `<div class="panel"><div class="ph"><h3>Étape 5 — Clôture du parcours</h3>${
    ["reintegration_militaire","reintegration_socio"].includes(c.statut)&&hasPerm("cloturer")?`<button class="btn sm" onclick="markReintegre('${c.id}')">Déclarer la réintégration achevée</button>`:""
  }</div><div class="pb">${
    c.statut==="rapatrie"?`<b style="color:#274B8F">Rapatrié le ${fmtD(c.fin)}</b> vers ${esc(c.rapatriement.pays)} — dossier clos au titre du rapatriement.`
    : c.statut==="reintegre"?`<b style="color:var(--ok)">Parcours achevé le ${fmtD(c.fin)}.</b> L'intéressé est considéré comme pleinement réintégré.`
    : c.statut==="abandon"?`<b style="color:var(--danger)">Abandon déclaré le ${fmtD(c.abandon.date)}.</b> Motif : ${esc(c.abandon.motif)}`
    : `<span class="muted">Le dossier sera clôturé au terme du parcours de réintégration.</span>`
  }</div></div>`;

  /* --- Chronologie complète --- */
  const ev=parcoursEvents(c);
  h += `<div class="panel"><div class="ph"><h3>Parcours complet — du désarmement à la fin de la réintégration (${ev.length} étape(s))</h3>
    <button class="btn sm sec" onclick="printParcours('${c.id}')">Imprimer le parcours</button></div>
    <div class="pb"><div class="chrono">${
      ev.map(e=>`<div class="ch-item"><span class="ch-dot st-${e.cls}"></span>
        <div class="ch-date">${fmtD(e.date)}</div>
        <div class="ch-body"><b>${esc(e.titre)}</b>${e.detail?`<div class="small muted">${esc(e.detail)}</div>`:""}</div></div>`).join("")
    }</div></div></div>`;
  $("view").innerHTML = h;
}
function printParcours(id){
  const c=DB.combattants.find(x=>x.id===id); const ev=parcoursEvents(c);
  doPrint(docWrap(`${docEntete("Relevé chronologique du parcours DDR")}
    <h2 class="titre">Parcours complet — dossier ${c.num}</h2>
    <p><b>${esc(c.nom)} ${esc(c.prenom)}</b>${c.alias?` (alias « ${esc(c.alias)} »)`:""}, ${c.sexe==="M"?"né":"née"} le ${fmtD(c.dn)}, groupe d'origine ${esc(c.groupe)}, ${esc(c.prefecture)}${c.vague?", vague "+esc(c.vague):""}. Statut actuel : <b>${STATUTS[c.statut].lbl}</b>. Relevé arrêté au ${new Date().toLocaleDateString("fr-FR")}.</p>
    <table class="dt"><tr><th style="width:90px">Date</th><th>Étape du processus</th><th>Détail</th></tr>${
      ev.map(e=>`<tr><td>${fmtD(e.date)}</td><td><b>${esc(e.titre)}</b></td><td>${esc(e.detail)||"—"}</td></tr>`).join("")}</table>
    <div class="sig"><div class="c"></div><div class="c">Fait à ${esc(cfg("villeSignature")||"Bangui")}, le ${new Date().toLocaleDateString("fr-FR")}<br>Pour le PNDDRR<div class="ligne">${esc(CUR.nom)}</div></div></div>`));
  log("Impression",`Relevé de parcours ${c.num}`);
}

/* ---------- Actions du circuit ---------- */
const UNITES_MUN=["cartouches","chargeurs","bandes","caisses","grenades","roquettes","obus","kg d'explosif","autre"];
function munRow(){
  return `<tr class="munRow"><td><input class="m_nature" placeholder="Nature / calibre (ex. : 7,62×39 mm)"></td>
  <td><input class="m_qte" type="number" min="0" style="width:90px" placeholder="Qté"></td>
  <td><select class="m_unite">${UNITES_MUN.map(u=>`<option>${u}</option>`).join("")}</select></td>
  <td><input class="m_obs" placeholder="Observations"></td>
  <td><button type="button" class="btn sm ghost" onclick="this.closest('tr').remove()">✕</button></td></tr>`;
}
function armeRow(i){
  return `<tr class="armeRow"><td><select class="a_type">${TYPES_ARMES.map(t=>`<option>${t}</option>`).join("")}</select></td>
  <td><input class="a_marque" placeholder="Marque / modèle"></td><td><input class="a_calibre" placeholder="Calibre" style="width:90px"></td>
  <td><input class="a_serie" placeholder="N° série"></td>
  <td><select class="a_etat"><option>Fonctionnelle</option><option>Défectueuse</option><option>Hors d'usage</option></select></td>
  <td><input class="a_mun" placeholder="Munitions" style="width:90px"></td>
  <td><button type="button" class="btn sm ghost" onclick="this.closest('tr').remove()">✕</button></td></tr>`;
}
function mDesarmement(id){
  openModal("Procès-verbal de désarmement", `
    <div class="grid2"><div class="field"><label>Date de remise</label><input type="date" id="d_date" value="${today()}"></div>
    <div class="field"><label>Lieu / site de collecte</label><input id="d_lieu"></div></div>
    <label>Armement et matériel remis</label>
    <table id="tArmes"><thead><tr><th>Type</th><th>Marque</th><th>Calibre</th><th>N° série</th><th>État</th><th>Munitions</th><th></th></tr></thead>
    <tbody>${armeRow(0)}</tbody></table>
    <button type="button" class="btn sm sec" style="margin-top:8px" onclick="document.querySelector('#tArmes tbody').insertAdjacentHTML('beforeend', armeRow())">+ Ajouter une arme</button>
    <h3 style="color:var(--teal-dark);margin:16px 0 8px;font-size:13px;text-transform:uppercase">Munitions et explosifs remis</h3>
    <table id="tMun"><thead><tr><th>Nature / calibre</th><th>Quantité</th><th>Unité</th><th>Observations</th><th></th></tr></thead>
    <tbody></tbody></table>
    <button type="button" class="btn sm sec" style="margin-top:8px" onclick="document.querySelector('#tMun tbody').insertAdjacentHTML('beforeend', munRow())">+ Ajouter des munitions</button>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveDesarmement('${id}')">Valider le désarmement</button>`);
}
function saveDesarmement(id){
  const c=DB.combattants.find(x=>x.id===id);
  const armes=[...document.querySelectorAll("#tArmes tbody tr")].map(tr=>({
    type:tr.querySelector(".a_type").value, marque:tr.querySelector(".a_marque").value.trim(),
    calibre:tr.querySelector(".a_calibre").value.trim(), serie:tr.querySelector(".a_serie").value.trim(),
    etat:tr.querySelector(".a_etat").value, mun:tr.querySelector(".a_mun").value.trim()
  }));
  const munitions=[...document.querySelectorAll("#tMun tbody tr")].map(tr=>({
    nature:tr.querySelector(".m_nature").value.trim(), qte:+tr.querySelector(".m_qte").value||0,
    unite:tr.querySelector(".m_unite").value, obs:tr.querySelector(".m_obs").value.trim()
  })).filter(m=>m.nature||m.qte);
  if(!armes.length&&!munitions.length){ toast("Ajoutez au moins une arme ou un lot de munitions."); return; }
  c.desarmement={date:$("d_date").value||today(), lieu:$("d_lieu").value.trim()||c.site||c.prefecture, agent:CUR.nom, armes, munitions};
  c.statut="desarme";
  log("Désarmement",`${c.num} — ${armes.length} arme(s) et ${munitions.length} lot(s) de munitions réceptionnés`);
  closeModal(); toast("Désarmement enregistré."); rFiche(id);
}
function mDemobilisation(id){
  const c=DB.combattants.find(x=>x.id===id);
  openModal("Démobilisation", `
    <p class="small muted" style="margin-bottom:12px">La validation attribue automatiquement un numéro de carte de démobilisé.</p>
    <div class="grid2"><div class="field"><label>Date de démobilisation</label><input type="date" id="dm_date" value="${today()}"></div>
    <div class="field"><label>Lieu de délivrance</label><input id="dm_lieu" value="${esc(c.prefecture)}"></div></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveDemob('${id}')">Prononcer la démobilisation</button>`);
}
function saveDemob(id){
  const c=DB.combattants.find(x=>x.id===id);
  c.demobilisation={date:$("dm_date").value||today(), lieu:$("dm_lieu").value.trim(), carte:numDem()};
  c.statut="demobilise";
  log("Démobilisation",`${c.num} — carte ${c.demobilisation.carte}`);
  closeModal(); toast(`Carte ${c.demobilisation.carte} délivrée.`); rFiche(id);
}
function estMineur(c){ const a=age(c.dn); return a!=="—" && a<18; }
function mReintMil(id){
  const cM=DB.combattants.find(x=>x.id===id);
  if(cM && estMineur(cM)){
    openModal("Orientation militaire impossible — mineur (EAFGA)", `
      <p><b>${esc(cM.nom)} ${esc(cM.prenom)}</b> a <b>${age(cM.dn)} ans</b>. Conformément aux standards du DDR et à la protection de l'enfance, un mineur (enfant associé aux forces et groupes armés — EAFGA) <b>ne peut pas être orienté vers la voie militaire</b>.</p>
      <p class="small muted" style="margin-top:8px">Orientez ce dossier vers la voie socio-économique (scolarisation, formation, réunification familiale), en liaison avec les services de protection de l'enfance.</p>`,
      `<button class="btn ghost" onclick="closeModal()">Fermer</button><button class="btn" onclick="closeModal();mReintSocio('${cM.id}')">Orientation socio-économique</button>`);
    return;
  }
  openModal("Orientation — réintégration militaire", `
    <div class="grid2">
    <div class="field"><label>Corps d'incorporation</label><select id="rm_corps">${CORPS.map(x=>`<option>${x}</option>`).join("")}</select></div>
    <div class="field"><label>Unité d'affectation</label><input id="rm_unite"></div>
    <div class="field"><label>Matricule attribué <span class="muted" style="font-weight:400">(optionnel)</span></label><input id="rm_mat" placeholder="Laissé vide en formation"></div>
    <div class="field"><label>Date d'incorporation</label><input type="date" id="rm_date" value="${today()}"></div>
    <div class="field"><label>Formation initiale prévue</label><input id="rm_form" placeholder="Ex. : formation commune de base — camp Kassaï"></div>
    <div class="field"><label>Promotion / vague de formation</label><input id="rm_promo" list="promoList" value="${esc((DB.combattants.find(x=>x.id===id)||{}).vague||"")}" placeholder="Ex. : Vague 2026-A"><datalist id="promoList">${promosConnues().map(p=>`<option>${esc(p)}</option>`).join("")}</datalist></div></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveReintMil('${id}')">Valider l'orientation</button>`);
}
function saveReintMil(id){
  const c=DB.combattants.find(x=>x.id===id);
  c.reintMil={corps:$("rm_corps").value, unite:$("rm_unite").value.trim(), matricule:$("rm_mat").value.trim(), date:$("rm_date").value||today(), formation:$("rm_form").value.trim(), promo:$("rm_promo").value.trim()||c.vague||""};
  c.statut="reintegration_militaire";
  log("Réintégration militaire",`${c.num} — ${c.reintMil.corps}${c.reintMil.matricule?", matricule "+c.reintMil.matricule:""}`);
  closeModal(); toast("Orientation militaire enregistrée."); rFiche(id);
}
function mReintSocio(id){
  openModal("Orientation — réintégration socio-économique", `
    <div class="grid2">
    <div class="field"><label>Filière</label><select id="rs_fil">${FILIERES.map(x=>`<option>${x}</option>`).join("")}</select></div>
    <div class="field"><label>Centre / structure de formation</label><input id="rs_centre"></div>
    <div class="field"><label>Durée de formation</label><input id="rs_duree" placeholder="Ex. : 6 mois"></div>
    <div class="field"><label>Début du parcours</label><input type="date" id="rs_date" value="${today()}"></div>
    <div class="field"><label>Kit de réinstallation remis ?</label><select id="rs_kit"><option value="">Non</option><option value="1">Oui</option></select></div>
    <div class="field"><label>Date de remise du kit</label><input type="date" id="rs_kitdate"></div>
    <div class="field"><label>Appui financier (FCFA)</label><input type="number" min="0" id="rs_appui"></div>
    <div class="field"><label>Promotion / vague de formation</label><input id="rs_promo" list="promoList" value="${esc((DB.combattants.find(x=>x.id===id)||{}).vague||"")}" placeholder="Ex. : Promotion Couture 2026-1"><datalist id="promoList">${promosConnues().map(p=>`<option>${esc(p)}</option>`).join("")}</datalist></div></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveReintSocio('${id}')">Valider l'orientation</button>`);
}
function saveReintSocio(id){
  const c=DB.combattants.find(x=>x.id===id);
  c.reintSocio={filiere:$("rs_fil").value, centre:$("rs_centre").value.trim(), duree:$("rs_duree").value.trim(),
    date:$("rs_date").value||today(), kit:!!$("rs_kit").value, kitDate:$("rs_kitdate").value, appui:$("rs_appui").value, promo:$("rs_promo").value.trim()||c.vague||"", visites:[]};
  c.statut="reintegration_socio";
  log("Réintégration socio-économique",`${c.num} — filière ${c.reintSocio.filiere}`);
  closeModal(); toast("Orientation socio-économique enregistrée."); rFiche(id);
}
function mRapatriement(id){
  const c=DB.combattants.find(x=>x.id===id);
  openModal("Rapatriement — combattant étranger", `
    <p class="small muted" style="margin-bottom:12px">Voie prévue pour les combattants étrangers : retour vers le pays d'origine, en liaison avec les autorités de ce pays et les partenaires (MINUSCA, OIM…). Nationalité déclarée : <b>${esc(c.nat)}</b>.</p>
    <div class="grid2">
      <div class="field"><label>Pays de retour *</label><input id="rp_pays" value="${normTxt(c.nat).includes("centrafric")?"":esc(c.nat)}" placeholder="Ex. : Tchad, Soudan…"></div>
      <div class="field"><label>Date de rapatriement</label><input type="date" id="rp_date" value="${today()}"></div>
      <div class="field"><label>Convoi / vol / point de passage</label><input id="rp_convoi" placeholder="Ex. : convoi OIM n° 12 — Am-Dafock"></div>
      <div class="field"><label>Autorités de remise</label><input id="rp_autorites" placeholder="Ex. : autorités tchadiennes — poste de Sido"></div>
    </div>
    <div class="field"><label>Observations</label><textarea id="rp_obs" rows="2"></textarea></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveRapatriement('${id}')">Confirmer le rapatriement</button>`);
}
function saveRapatriement(id){
  const c=DB.combattants.find(x=>x.id===id);
  if(!$("rp_pays").value.trim()){ toast("Le pays de retour est obligatoire."); return; }
  c.rapatriement={pays:$("rp_pays").value.trim(), date:$("rp_date").value||today(), convoi:$("rp_convoi").value.trim(), autorites:$("rp_autorites").value.trim(), obs:$("rp_obs").value.trim()};
  c.fin=c.rapatriement.date; c.statut="rapatrie";
  log("Rapatriement",`${c.num} — vers ${c.rapatriement.pays}${c.rapatriement.convoi?" ("+c.rapatriement.convoi+")":""}`);
  closeModal(); toast("Rapatriement enregistré — dossier clos."); rFiche(id);
}
function mVisite(id){
  openModal("Visite de suivi", `
    <div class="grid2"><div class="field"><label>Date de la visite</label><input type="date" id="v_date" value="${today()}"></div>
    <div class="field"><label>Appréciation</label><select id="v_appr"><option>Satisfaisant</option><option>Moyen</option><option>Préoccupant</option></select></div></div>
    <div class="field"><label>Observations</label><textarea id="v_obs" rows="3"></textarea></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveVisite('${id}')">Enregistrer la visite</button>`);
}
function saveVisite(id){
  const c=DB.combattants.find(x=>x.id===id);
  c.reintSocio.visites.push({date:$("v_date").value||today(), agent:CUR.nom, appr:$("v_appr").value, obs:$("v_obs").value.trim()});
  log("Visite de suivi",`${c.num} — appréciation : ${$("v_appr").value}`);
  closeModal(); toast("Visite enregistrée."); rFiche(id);
}
function reintOf(c){ return c.reintMil||c.reintSocio||null; }
function jalonsFiche(c){
  const r=reintOf(c); if(!r) return "";
  const mil=!!c.reintMil;
  const [bF,bV]=jalonBadge(c);
  const enCours=["reintegration_militaire","reintegration_socio"].includes(c.statut);
  const P=hasPerm("orienter");
  return `<h3 style="font-size:13px;color:var(--teal-dark);margin:14px 0 8px;text-transform:uppercase">Jalons du parcours</h3>
  <table style="background:transparent">
    ${r.promo?`<tr><td style="width:240px;color:var(--muted);font-weight:600">Promotion / vague</td><td colspan="2"><span class="tag">${esc(r.promo)}</span></td></tr>`:""}
    <tr><td style="width:240px;color:var(--muted);font-weight:600">Formation ${mil?"initiale":"professionnelle"}</td>
      <td>${bF}${r.formFin&&r.formNote?` <span class="small muted">${esc(r.formNote)}</span>`:""}</td>
      <td style="text-align:right">${enCours&&!r.formFin&&P?`<button class="btn sm sec" onclick="mFormationFin('${c.id}')">Valider la fin de formation</button>`:""}</td></tr>
    <tr><td style="color:var(--muted);font-weight:600">Intégration à la vie ${mil?"militaire":"civile"}</td>
      <td>${bV}${r.vieDate&&r.vieDetail?` <span class="small muted">${esc(r.vieDetail)}</span>`:""}</td>
      <td style="text-align:right">${enCours&&!r.vieDate&&P?`<button class="btn sm sec" onclick="mIntegration('${c.id}')">Confirmer l'intégration</button>`:""}</td></tr>
  </table>
  ${enCours&&(!r.formFin||!r.vieDate)?`<div class="small muted" style="margin-top:6px">La clôture du parcours est recommandée une fois la formation achevée et l'intégration à la vie ${mil?"militaire":"civile"} confirmée.</div>`:""}`;
}
function jalonBadge(c){
  const r=reintOf(c); if(!r) return ["",""];
  const mil=!!c.reintMil;
  const form=r.formFin?`<span class="badge st-reintegre" title="${esc(r.formNote||"")}">Achevée le ${fmtD(r.formFin)}</span>`:`<span class="badge st-demobilise">En cours</span>`;
  const vie=r.vieDate?`<span class="badge st-reintegre" title="${esc(r.vieDetail||"")}">${mil?"Vie militaire":"Vie civile"} · ${fmtD(r.vieDate)}</span>`:`<span class="badge st-enregistre">En attente</span>`;
  return [form,vie];
}
function mFormationFin(id){
  const c=DB.combattants.find(x=>x.id===id); const r=reintOf(c); const mil=!!c.reintMil;
  openModal("Fin de la formation", `
    <p class="small muted" style="margin-bottom:12px">${mil?`Formation initiale : ${esc(c.reintMil.formation)||"—"} (${esc(c.reintMil.corps)})`:`Formation professionnelle — filière ${esc(c.reintSocio.filiere)}${c.reintSocio.centre?" · "+esc(c.reintSocio.centre):""}`}</p>
    <div class="grid2">
      <div class="field"><label>Date d'achèvement</label><input type="date" id="ff_date" value="${today()}"></div>
      <div class="field"><label>Résultat</label><select id="ff_res"><option>Formation achevée avec succès</option><option>Formation achevée — attestation délivrée</option><option>Formation validée partiellement</option><option>Formation interrompue</option></select></div>
    </div>
    <div class="field"><label>Observations</label><textarea id="ff_obs" rows="2"></textarea></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveFormationFin('${id}')">Valider la fin de formation</button>`);
}
function saveFormationFin(id){
  const c=DB.combattants.find(x=>x.id===id); const r=reintOf(c);
  r.formFin=$("ff_date").value||today();
  r.formNote=($("ff_res").value+($("ff_obs").value.trim()?" — "+$("ff_obs").value.trim():""));
  log("Fin de formation",`${c.num} — ${r.formNote}`);
  closeModal(); toast("Fin de formation enregistrée.");
  VIEW==="reintegration"?rReint():VIEW==="jalons"?rJalons():rFiche(id);
}
function mIntegration(id){
  const c=DB.combattants.find(x=>x.id===id); const mil=!!c.reintMil;
  openModal(mil?"Intégration à la vie militaire":"Intégration à la vie civile", `
    <div class="grid2">
      <div class="field"><label>Date d'intégration effective</label><input type="date" id="vi_date" value="${today()}"></div>
      <div class="field"><label>${mil?"Unité rejointe / affectation":"Activité exercée / installation"}</label><input id="vi_det" value="${mil?esc(c.reintMil.unite||""):esc(c.reintSocio.filiere||"")}"></div>
      ${mil?`<div class="field"><label>Matricule attribué</label><input id="vi_mat" value="${esc(c.reintMil.matricule||"")}" placeholder="Saisi à l'intégration, pas en formation"></div>`:""}
    </div>
    <p class="small muted">${mil?"Confirme que l'intéressé a effectivement rejoint son unité et sert au sein des forces. Le matricule peut être renseigné ici s'il n'avait pas été attribué en formation.":"Confirme que l'intéressé exerce effectivement son activité et est installé dans la vie civile."}</p>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveIntegration('${id}')">Confirmer l'intégration</button>`);
}
function saveIntegration(id){
  const c=DB.combattants.find(x=>x.id===id); const r=reintOf(c); const mil=!!c.reintMil;
  r.vieDate=$("vi_date").value||today();
  r.vieDetail=$("vi_det").value.trim();
  if(mil && $("vi_mat")) c.reintMil.matricule=$("vi_mat").value.trim();
  log("Intégration "+(mil?"vie militaire":"vie civile"),`${c.num}${r.vieDetail?" — "+r.vieDetail:""}${mil&&c.reintMil.matricule?" · matricule "+c.reintMil.matricule:""}`);
  closeModal(); toast("Intégration confirmée.");
  VIEW==="reintegration"?rReint():VIEW==="jalons"?rJalons():rFiche(id);
}
function markReintegre(id){
  const c=DB.combattants.find(x=>x.id===id);
  const r=reintOf(c);
  const alerte=r&&(!r.formFin||!r.vieDate)?`<div class="small" style="background:#FFF7E0;border:1px solid #E8D48A;border-radius:7px;padding:9px;margin-bottom:10px">⚠ ${!r.formFin?"La fin de formation n'est pas encore validée. ":""}${!r.vieDate?"L'intégration à la vie "+(c.reintMil?"militaire":"civile")+" n'est pas encore confirmée. ":""}La clôture les considérera comme atteintes à la date choisie.</div>`:"";
  openModal("Clôture du parcours", `${alerte}<p>Confirmer que <b>${esc(c.nom)} ${esc(c.prenom)}</b> (${c.num}) a achevé son parcours de réintégration ?</p>
    <div class="field" style="margin-top:12px"><label>Date d'achèvement</label><input type="date" id="fin_date" value="${today()}"></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveReintegre('${id}')">Confirmer</button>`);
}
function saveReintegre(id){
  const c=DB.combattants.find(x=>x.id===id);
  c.fin=$("fin_date").value||today(); c.statut="reintegre";
  const r=reintOf(c);
  if(r){ if(!r.formFin){ r.formFin=c.fin; r.formNote=r.formNote||"Considérée achevée à la clôture du parcours"; }
    if(!r.vieDate){ r.vieDate=c.fin; r.vieDetail=r.vieDetail||(c.reintMil?"Intégration confirmée à la clôture":"Intégration à la vie civile confirmée à la clôture"); } }
  log("Clôture",`${c.num} — réintégration achevée`);
  closeModal(); toast("Parcours clôturé — réintégré."); rFiche(id);
}
function markAbandon(id){
  const c=DB.combattants.find(x=>x.id===id);
  openModal("Déclaration d'abandon", `<p>Déclarer l'abandon du processus par <b>${esc(c.nom)} ${esc(c.prenom)}</b> (${c.num}) ?</p>
    <div class="field" style="margin-top:12px"><label>Motif</label><textarea id="ab_motif" rows="2" placeholder="Ex. : ne s'est plus présenté au site depuis 3 mois"></textarea></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn danger" onclick="saveAbandon('${id}')">Déclarer l'abandon</button>`);
}
function saveAbandon(id){
  const c=DB.combattants.find(x=>x.id===id);
  c.abandon={date:today(), motif:$("ab_motif").value.trim()||"Non précisé"}; c.statut="abandon";
  log("Abandon",`${c.num} — ${c.abandon.motif}`);
  closeModal(); toast("Abandon enregistré."); rFiche(id);
}



/* module: armes/armes.js — PNDDRR engine (classic globals) */
/* ================= REGISTRE DES ARMES ================= */
function allArmes(){
  const out=[];
  DB.combattants.forEach(c=>{ if(c.desarmement) c.desarmement.armes.forEach((a,ai)=>out.push(Object.assign({num:c.num,nom:`${c.nom} ${c.prenom}`,groupe:c.groupe,date:c.desarmement.date,lieu:c.desarmement.lieu,cid:c.id,ai},a))); });
  return out;
}
function allMunitions(){
  const out=[];
  DB.combattants.forEach(c=>{ if(c.desarmement) (c.desarmement.munitions||[]).forEach(m=>out.push(Object.assign({num:c.num,nom:`${c.nom} ${c.prenom}`,groupe:c.groupe,date:c.desarmement.date,lieu:c.desarmement.lieu,cid:c.id},m))); });
  return out;
}
const GARDE_LBL={depot:"En dépôt",scelle:"Scellée",detruit:"Détruite"};
function gardeBadge(a){
  const g=a.garde&&a.garde.etat||"depot";
  const cls=g==="detruit"?"st-abandon":g==="scelle"?"st-demobilise":"st-enregistre";
  const tip=a.garde?(a.garde.depot?("Dépôt : "+a.garde.depot+" ") : "")+(a.garde.scelle?("Scellé "+a.garde.scelle+" "):"")+(a.garde.destruction?("PV "+(a.garde.destruction.pv||"—")+" le "+fmtD(a.garde.destruction.date)):""):"";
  return `<span class="badge ${cls}" title="${esc(tip)}">${GARDE_LBL[g]}</span>`;
}
function mGarde(cid, ai){
  const c=DB.combattants.find(x=>x.id===cid); if(!c||!c.desarmement) return;
  const a=c.desarmement.armes[ai]; if(!a) return;
  const g=a.garde||{etat:"depot"};
  openModal("Garde de l'arme — cycle de vie", `
    <p class="small muted" style="margin-bottom:12px"><b>${esc(a.type)}</b>${a.serie?" — n° de série "+esc(a.serie):""} · remise par ${esc(c.nom)} ${esc(c.prenom)} (${c.num}) le ${fmtD(c.desarmement.date)}.</p>
    <div class="field"><label>Étape de garde</label><select id="gd_etat" onchange="$('gd_scelleBox').style.display=this.value==='scelle'?'grid':'none';$('gd_destBox').style.display=this.value==='detruit'?'grid':'none'">
      <option value="depot" ${g.etat==="depot"?"selected":""}>En dépôt (armurerie / conteneur)</option>
      <option value="scelle" ${g.etat==="scelle"?"selected":""}>Scellée (mise sous scellé)</option>
      <option value="detruit" ${g.etat==="detruit"?"selected":""}>Détruite (procès-verbal de destruction)</option>
    </select></div>
    <div class="field"><label>Dépôt / lieu de garde</label><input id="gd_depot" value="${esc(g.depot||"")}" placeholder="Ex. : armurerie MINUSCA Bangui — conteneur 4"></div>
    <div class="grid2" id="gd_scelleBox" style="display:${g.etat==="scelle"?"grid":"none"}">
      <div class="field"><label>N° de scellé</label><input id="gd_scelle" value="${esc(g.scelle||"")}" placeholder="Ex. : SC-2026-0145"></div>
      <div class="field"><label>Date de mise sous scellé</label><input type="date" id="gd_scelleDate" value="${g.scelleDate||today()}"></div>
    </div>
    <div class="grid2" id="gd_destBox" style="display:${g.etat==="detruit"?"grid":"none"}">
      <div class="field"><label>Date de destruction</label><input type="date" id="gd_destDate" value="${g.destruction?g.destruction.date:today()}"></div>
      <div class="field"><label>N° du procès-verbal</label><input id="gd_destPv" value="${esc(g.destruction?g.destruction.pv:"")}" placeholder="Ex. : PV-DEST-2026-07"></div>
      <div class="field" style="grid-column:1/-1"><label>Commission / lieu de destruction</label><input id="gd_destCom" value="${esc(g.destruction?g.destruction.commission:"")}" placeholder="Ex. : Commission mixte UEPNDDR-MINUSCA — Bangui"></div>
    </div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveGarde('${cid}',${ai})">Enregistrer</button>`);
}
function saveGarde(cid, ai){
  if(!hasPerm("desarmer")){ toast("Autorisation « désarmer » requise."); return; }
  const c=DB.combattants.find(x=>x.id===cid); const a=c.desarmement.armes[ai];
  const etat=$("gd_etat").value;
  a.garde={etat, depot:$("gd_depot").value.trim()};
  if(etat==="scelle"){ a.garde.scelle=$("gd_scelle").value.trim(); a.garde.scelleDate=$("gd_scelleDate").value||today(); }
  if(etat==="detruit"){ a.garde.destruction={date:$("gd_destDate").value||today(), pv:$("gd_destPv").value.trim(), commission:$("gd_destCom").value.trim()}; }
  log("Garde d'arme",`${a.type}${a.serie?" ("+a.serie+")":""} — ${GARDE_LBL[etat]}${etat==="scelle"&&a.garde.scelle?" · scellé "+a.garde.scelle:""}${etat==="detruit"&&a.garde.destruction.pv?" · "+a.garde.destruction.pv:""} (dossier ${c.num})`);
  closeModal(); toast("Garde mise à jour."); rArmes();
}
function rArmes(){
  const A=allArmes(), MU=allMunitions();
  $("view").innerHTML = `
  <div class="cards">
    <div class="kpi"><div class="n">${A.length}</div><div class="l">Armes & lots collectés</div></div>
    <div class="kpi c-ok"><div class="n">${A.filter(a=>a.etat==="Fonctionnelle").length}</div><div class="l">Fonctionnelles</div></div>
    <div class="kpi c-ab"><div class="n">${A.filter(a=>a.etat!=="Fonctionnelle").length}</div><div class="l">Défectueuses / hors d'usage</div></div>
    <div class="kpi c-dem"><div class="n">${MU.length}</div><div class="l">Lots de munitions</div></div>
    <div class="kpi"><div class="n">${A.filter(a=>!a.garde||a.garde.etat==="depot").length}</div><div class="l">En dépôt</div></div>
    <div class="kpi c-rs"><div class="n">${A.filter(a=>a.garde&&a.garde.etat==="scelle").length}</div><div class="l">Sous scellé</div></div>
    <div class="kpi c-ab"><div class="n">${A.filter(a=>a.garde&&a.garde.etat==="detruit").length}</div><div class="l">Détruites</div></div>
    <div class="kpi c-rm"><div class="n">${fmtN(MU.filter(m=>m.unite==="cartouches").reduce((a,m)=>a+m.qte,0))}</div><div class="l">Cartouches collectées</div></div>
  </div>
  <div class="toolbar">
    <div class="field"><label>Type</label><select id="wType" onchange="filtArmes()"><option value="">Tous</option>${TYPES_ARMES.map(t=>`<option>${t}</option>`).join("")}</select></div>
    <div class="field" style="flex:1"><label>Recherche</label><input id="wTxt" placeholder="N° série, nom, dossier…" oninput="filtArmes()"></div>
    <button class="btn sec" onclick="exportArmesCSV()">Exporter CSV</button>
    ${hasPerm("importer")?`<button class="btn sec" onclick="go('import','armes')">Importer un registre…</button>`:""}
    <button class="btn sec" onclick="printRegArmes()">Imprimer le registre</button>
  </div>
  <div class="panel"><div class="ph"><h3>Armes collectées</h3><span class="muted small">cycle de vie : dépôt → scellé → destruction</span></div><div class="pb nopad"><table><thead><tr><th>Type</th><th>Marque</th><th>N° série</th><th>État</th><th>Garde</th><th>Remise par</th><th>Dossier</th><th>Date / lieu</th><th>Actions</th></tr></thead><tbody id="tbArm"></tbody></table></div></div>
  <div class="panel"><div class="ph"><h3>Munitions et explosifs collectés (${MU.length} lot(s))</h3>
    <span style="display:flex;gap:7px"><button class="btn sm sec" onclick="exportMunCSV()">Exporter CSV</button></span></div>
    <div class="pb nopad">${MU.length?`<table><thead><tr><th>Nature / calibre</th><th>Quantité</th><th>Unité</th><th>Observations</th><th>Remise par</th><th>Dossier</th><th>Date / lieu</th></tr></thead><tbody>${
      MU.map(m=>`<tr><td>${esc(m.nature)||"—"}</td><td><b>${fmtN(m.qte)}</b></td><td>${esc(m.unite)}</td><td class="small">${esc(m.obs)||"—"}</td><td>${esc(m.nom)}</td><td><span class="link" onclick="go('fiche','${m.cid}')">${m.num}</span></td><td class="small">${fmtD(m.date)}${m.lieu?" · "+esc(m.lieu):""}</td></tr>`).join("")
    }</tbody></table>`:`<div class="empty">Aucune munition enregistrée — utilisez la section « Munitions et explosifs remis » du procès-verbal de désarmement.</div>`}</div></div>
  <div class="panel"><div class="ph"><h3>Synthèse des munitions par nature</h3></div><div class="pb">${
    MU.length?(()=>{const agg={};MU.forEach(m=>{const k=(m.nature||"(non précisée)")+" — "+m.unite;agg[k]=(agg[k]||0)+m.qte;});
      return Object.entries(agg).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div class="bar-row"><span class="lbl" style="width:260px">${esc(k)}</span><span class="val">${fmtN(v)}</span></div>`).join("");})()
    :`<span class="muted small">Aucune donnée.</span>`}</div></div>`;
  filtArmes();
}
function exportMunCSV(){
  const rows=[["Nature / calibre","Quantité","Unité","Observations","Remise par","N° dossier","Groupe","Date","Lieu"]];
  allMunitions().forEach(m=>rows.push([m.nature,m.qte,m.unite,m.obs,m.nom,m.num,m.groupe,m.date,m.lieu]));
  dl("pnddrr_registre_munitions.csv", csv(rows), "text/csv"); log("Export CSV","Registre des munitions");
}
var ARM_LIM=PAGE_TAILLE;
function armPlus(tout){ ARM_LIM=tout?Infinity:ARM_LIM+PAGE_TAILLE; filtArmes(); }
function filtArmes(){
  const t=$("wType").value, q=$("wTxt").value.toLowerCase();
  const rows=allArmes().filter(a=>(!t||a.type===t)&&(!q||`${a.serie} ${a.nom} ${a.num} ${a.marque}`.toLowerCase().includes(q)));
  const tronq=rows.length>ARM_LIM;
  $("tbArm").innerHTML = (rows.length ? rows.slice(0,ARM_LIM).map(a=>`<tr><td class="small">${esc(a.type)}</td><td>${esc(a.marque)||"—"}</td><td>${esc(a.serie)||"—"}</td><td>${esc(a.etat)}</td><td>${gardeBadge(a)}</td><td>${esc(a.nom)}</td><td><span class="link" onclick="go('fiche','${a.cid}')">${a.num}</span></td><td class="small">${fmtD(a.date)} · ${esc(a.lieu)}</td><td>${hasPerm("desarmer")?`<button class="btn sm sec" onclick="mGarde('${a.cid}',${a.ai})">Garde…</button>`:""}</td></tr>`).join("")
  : `<tr><td colspan="9" class="empty">Aucune arme enregistrée.</td></tr>`)
  + (tronq?`<tr><td colspan="9" style="text-align:center;padding:11px;background:#FBFDFC"><span class="small muted">${ARM_LIM} affichée(s) sur ${rows.length} — </span><button class="btn sm sec" onclick="armPlus()">Afficher ${Math.min(PAGE_TAILLE,rows.length-ARM_LIM)} de plus</button> <button class="btn sm ghost" onclick="armPlus(true)">Tout afficher</button></td></tr>`:"");
}


/* module: admin/recherche.js — PNDDRR engine (classic globals) */
/* ================= RECHERCHE MULTICRITÈRE ================= */
function rRecherche(){
  $("view").innerHTML = `
  ${outilRetour()}
  <div class="panel"><div class="ph"><h3>Critères de recherche</h3></div><div class="pb">
    <div class="grid4">
      <div class="field"><label>Nom / alias / n° dossier</label><input id="q_txt"></div>
      <div class="field"><label>Statut</label><select id="q_st"><option value="">Tous</option>${Object.entries(STATUTS).map(([k,v])=>`<option value="${k}">${v.lbl}</option>`).join("")}</select></div>
      <div class="field"><label>Groupe armé</label><select id="q_grp"><option value="">Tous</option>${GROUPES.map(g=>`<option>${g}</option>`).join("")}</select></div>
      <div class="field"><label>Préfecture</label><select id="q_pref"><option value="">Toutes</option>${PREFECTURES.map(p=>`<option>${p}</option>`).join("")}</select></div>
      <div class="field"><label>Sexe</label><select id="q_sexe"><option value="">Tous</option><option value="M">Masculin</option><option value="F">Féminin</option></select></div>
      <div class="field"><label>Type de réintégration</label><select id="q_reint"><option value="">Toutes</option><option value="mil">Militaire</option><option value="socio">Socio-économique</option></select></div>
      <div class="field"><label>Enregistré entre le</label><input type="date" id="q_d1"></div>
      <div class="field"><label>et le</label><input type="date" id="q_d2"></div>
    </div>
    <button class="btn" onclick="runRecherche()">Lancer la recherche</button>
  </div></div>
  <div id="qRes"></div>`;
}
function runRecherche(){
  const txt=$("q_txt").value.toLowerCase(), st=$("q_st").value, grp=$("q_grp").value, pref=$("q_pref").value,
    sexe=$("q_sexe").value, ri=$("q_reint").value, d1=$("q_d1").value, d2=$("q_d2").value;
  const rows=DB.combattants.filter(c=>
    (!txt||`${c.num} ${c.nom} ${c.prenom} ${c.alias||""}`.toLowerCase().includes(txt)) &&
    (!st||c.statut===st) && (!grp||c.groupe===grp) && (!pref||c.prefecture===pref) && (!sexe||c.sexe===sexe) &&
    (!ri||(ri==="mil"?!!c.reintMil:!!c.reintSocio)) &&
    (!d1||c.creele.slice(0,10)>=d1) && (!d2||c.creele.slice(0,10)<=d2)
  );
  $("qRes").innerHTML = `<div class="panel"><div class="ph"><h3>${rows.length} résultat(s)</h3>${rows.length?`<button class="btn sm sec" onclick="exportCombCSV(true)">Exporter ces résultats</button>`:""}</div><div class="pb nopad">${
    rows.length?`<table><thead><tr><th>N° dossier</th><th>Nom & prénom</th><th>Groupe</th><th>Préfecture</th><th>Statut</th><th></th></tr></thead><tbody>${
      rows.map(c=>`<tr><td><b>${c.num}</b></td><td>${esc(c.nom)} ${esc(c.prenom)}</td><td class="small">${esc(c.groupe)}</td><td class="small">${esc(c.prefecture)}</td><td><span class="badge st-${c.statut}">${STATUTS[c.statut].lbl}</span></td><td><span class="link" onclick="go('fiche','${c.id}')">Ouvrir</span></td></tr>`).join("")
    }</tbody></table>`:`<div class="empty">Aucun dossier ne correspond à ces critères.</div>`}</div></div>`;
  window._lastSearch = rows;
}


/* module: admin/comptes.js — PNDDRR engine (classic globals) */
/* ================= COMPTES ================= */
function rComptes(){
  $("view").innerHTML = `
  ${["admin2026","agent2026","suivi2026"].some(p=>DB.users.some(u=>u.actif&&u.pass===hashPwd(p)))?`<div class="small" style="background:#FDF1F1;border:1px solid #E3B4B4;border-radius:8px;padding:10px 13px;margin-bottom:13px"><b style="color:var(--danger)">⚠ Sécurité :</b> des comptes utilisent encore les mots de passe de démonstration (admin2026, agent2026, suivi2026). Changez-les avant toute mise en service réelle — via « Modifier » ci-dessous ou « Mon mot de passe » dans la barre latérale.</div>`:""}
  <div class="toolbar"><button class="btn ghost" onclick="go('parametres')">← Retour aux paramètres</button><div style="flex:1"></div><button class="btn" onclick="mCompte()">+ Nouveau compte</button></div>
  <div class="panel"><div class="pb nopad"><table><thead><tr><th>Identifiant</th><th>Nom</th><th>Profil</th><th>Autorisations</th><th>État</th><th>Actions</th></tr></thead><tbody>${
    DB.users.map(u=>`<tr><td><b>${esc(u.login)}</b></td><td>${esc(u.nom)}</td><td>${ROLES[u.role]}</td>
    <td class="small">${u.role==="admin"?'<span class="tag">Toutes</span>':userPerms(u).length?userPerms(u).map(p=>`<span class="tag" title="${esc(PERMS[p])}">${p}</span>`).join(" "):'<span class="muted">Aucune</span>'}${u.perms?' <span class="muted small">(personnalisées)</span>':""}</td>
    <td>${u.actif?'<span class="badge st-reintegre">Actif</span>':'<span class="badge st-abandon">Désactivé</span>'}</td>
    <td class="actions-cell"><button class="btn sm sec" onclick="mCompte('${u.id}')">Modifier</button>${u.login!==CUR.login?`<button class="btn sm ghost" onclick="toggleCompte('${u.id}')">${u.actif?"Désactiver":"Réactiver"}</button>`:""}</td></tr>`).join("")
  }</tbody></table></div></div>`;
}
function permBoxes(u){
  const cur=u?userPerms(u):ROLE_PERMS[u?u.role:"agent"];
  return Object.entries(PERMS).map(([k,lbl])=>`<label style="display:flex;align-items:center;gap:8px;font-weight:400;text-transform:none;font-size:13px;color:var(--ink);margin:0 0 6px"><input type="checkbox" class="c_perm" value="${k}" style="width:auto" ${cur.includes(k)?"checked":""}> ${lbl}</label>`).join("");
}
function presetPerms(){
  const role=$("c_role").value;
  $("c_permBox").style.display=role==="admin"?"none":"block";
  $("c_permAdmin").style.display=role==="admin"?"block":"none";
  document.querySelectorAll(".c_perm").forEach(cb=>cb.checked=ROLE_PERMS[role].includes(cb.value));
}
function mCompte(id){
  const u=id?DB.users.find(x=>x.id===id):null;
  openModal(u?"Modifier le compte":"Nouveau compte", `
    <div class="grid2">
    <div class="field"><label>Identifiant</label><input id="c_login" value="${u?esc(u.login):""}" ${u?"disabled":""}></div>
    <div class="field"><label>Nom complet</label><input id="c_nom" value="${u?esc(u.nom):""}"></div>
    <div class="field"><label>Profil</label><select id="c_role" onchange="presetPerms()">${Object.entries(ROLES).map(([k,v])=>`<option value="${k}" ${u&&u.role===k?"selected":""}>${v}</option>`).join("")}</select></div>
    <div class="field"><label>${u?"Nouveau mot de passe (laisser vide pour conserver)":"Mot de passe"}</label><input id="c_pass" type="password"></div></div>
    <div id="c_permBox" style="${(u?u.role:"agent")==="admin"?"display:none":""}">
      <label style="margin-bottom:8px">Autorisations du compte</label>
      <div class="small muted" style="margin-bottom:8px">Le profil pré-remplit les autorisations ; l'administrateur peut ensuite les ajuster individuellement.</div>
      <div style="border:1px solid var(--line);border-radius:8px;padding:12px;columns:2">${permBoxes(u)}</div>
    </div>
    <div id="c_permAdmin" class="small muted" style="${(u?u.role:"agent")==="admin"?"":"display:none"}">Le profil Administrateur dispose de toutes les autorisations, y compris la gestion des comptes et le journal.</div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveCompte('${id||""}')">Enregistrer</button>`);
}
function saveCompte(id){
  if(CUR.role!=="admin"){ toast("Réservé à l'administrateur."); return; }
  const role=$("c_role").value;
  const perms=role==="admin"?null:[...document.querySelectorAll(".c_perm:checked")].map(cb=>cb.value);
  if(id){
    const u=DB.users.find(x=>x.id===id);
    u.nom=$("c_nom").value.trim()||u.nom; u.role=role; u.perms=perms||undefined;
    if(role==="admin") delete u.perms;
    if($("c_pass").value){ u.pass=hashPwd($("c_pass").value); u.passUpdated=true; }
    log("Compte modifié",`${u.login} — autorisations : ${role==="admin"?"toutes":perms.join(", ")||"aucune"}`);
  } else {
    const login=$("c_login").value.trim();
    if(!login||!$("c_pass").value){ toast("Identifiant et mot de passe requis."); return; }
    if(DB.users.some(x=>x.login===login)){ toast("Cet identifiant existe déjà."); return; }
    const nu={id:"u"+Date.now(),login,pass:hashPwd($("c_pass").value),nom:$("c_nom").value.trim()||login,role,actif:true,passUpdated:true};
    if(role!=="admin") nu.perms=perms;
    DB.users.push(nu);
    log("Compte créé",`${login} (${ROLES[role]}) — autorisations : ${role==="admin"?"toutes":perms.join(", ")||"aucune"}`);
  }
  closeModal(); toast("Compte enregistré."); rComptes();
}
function toggleCompte(id){
  const u=DB.users.find(x=>x.id===id); u.actif=!u.actif;
  log(u.actif?"Compte réactivé":"Compte désactivé",u.login); rComptes();
}


/* module: admin/referentiels.js — PNDDRR engine (classic globals) */
/* ================= RÉFÉRENTIELS — GROUPES ARMÉS ================= */
function groupeUsage(g){ return DB.combattants.filter(c=>c.groupe===g).length; }
function quickAddGroupe(){
  openModal("Ajouter un groupe armé", `<div class="field"><label>Nom du groupe</label><input id="g_quick" placeholder="Ex. : CPC"></div>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveQuickGroupe()">Ajouter</button>`);
}
function saveQuickGroupe(){
  const name=$("g_quick").value.trim();
  if(addGroupe(name, nv=>{
    closeModal();
    const sel=$("e_grp");
    if(sel){ const opt=document.createElement("option"); opt.textContent=nv;
      sel.insertBefore(opt, [...sel.options].find(o=>o.value==="Autre")||null); sel.value=nv; }
  })===false){}
}
function rReferentiels(){
  $("view").innerHTML = `
  ${outilRetour()}
  <div class="panel"><div class="ph"><h3>Groupes armés (${GROUPES.length})</h3></div><div class="pb">
    <div class="toolbar" style="margin-bottom:14px">
      <div class="field" style="flex:1;max-width:380px"><label>Nouveau groupe armé</label><input id="g_new" placeholder="Ex. : CPC, Wagner Ti Azandé…"></div>
      <button class="btn" onclick="addGroupe()">+ Ajouter le groupe</button>
    </div>
    <table><thead><tr><th>Groupe armé</th><th>Dossiers rattachés</th><th>Actions</th></tr></thead><tbody>${
      GROUPES.map(g=>`<tr><td><b>${esc(g)}</b>${g==="Autre"?' <span class="muted small">(catégorie système)</span>':""}</td>
      <td>${groupeUsage(g)}</td>
      <td class="actions-cell">${g==="Autre"?"":`<button class="btn sm sec" onclick="renameGroupe('${esc(g).replace(/'/g,"\\'")}')">Renommer</button>
      <button class="btn sm ghost" ${groupeUsage(g)?'disabled title="Des dossiers utilisent ce groupe"':`onclick="delGroupe('${esc(g).replace(/'/g,"\\'")}')"`}>Supprimer</button>`}</td></tr>`).join("")
    }</tbody></table>
    <p class="small muted" style="margin-top:10px">Les groupes ajoutés sont immédiatement disponibles dans le formulaire d'enregistrement, les filtres, la recherche et l'importation, et sont conservés dans la sauvegarde JSON. Un groupe utilisé par des dossiers doit d'abord être renommé ou ses dossiers réaffectés avant suppression.</p>
  </div></div>`;
}
function addGroupe(nameArg, after){
  const name=(nameArg!==undefined?nameArg:$("g_new").value).trim();
  if(!name){ toast("Saisissez le nom du groupe."); return false; }
  if(GROUPES.some(g=>normTxt(g)===normTxt(name))){ toast("Ce groupe existe déjà."); return false; }
  GROUPES.splice(GROUPES.indexOf("Autre"),0,name);
  log("Référentiel",`Groupe armé ajouté : ${name}`);
  toast(`Groupe « ${name} » ajouté.`);
  if(after) after(name); else rReferentiels();
  return true;
}
function renameGroupe(g){
  openModal("Renommer le groupe armé", `<div class="field"><label>Nouveau nom</label><input id="g_ren" value="${esc(g)}"></div>
    <p class="small muted">${groupeUsage(g)} dossier(s) seront mis à jour automatiquement.</p>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="saveRenameGroupe('${esc(g).replace(/'/g,"\\'")}')">Renommer</button>`);
}
function saveRenameGroupe(g){
  const nv=$("g_ren").value.trim();
  if(!nv){ toast("Nom requis."); return; }
  if(nv!==g&&GROUPES.some(x=>normTxt(x)===normTxt(nv))){ toast("Ce nom existe déjà."); return; }
  GROUPES[GROUPES.indexOf(g)]=nv;
  DB.combattants.forEach(c=>{ if(c.groupe===g) c.groupe=nv; });
  log("Référentiel",`Groupe armé renommé : ${g} → ${nv}`);
  closeModal(); toast("Groupe renommé."); rReferentiels();
}
function delGroupe(g){
  if(groupeUsage(g)){ toast("Des dossiers utilisent ce groupe."); return; }
  GROUPES.splice(GROUPES.indexOf(g),1);
  log("Référentiel",`Groupe armé supprimé : ${g}`);
  toast("Groupe supprimé."); rReferentiels();
}


/* module: admin/journal.js — PNDDRR engine (classic globals) */
/* ================= JOURNAL ================= */
var JRN_LIM=PAGE_TAILLE;
function rJournal(){
  $("view").innerHTML = `${outilRetour()}<div class="panel"><div class="ph"><h3>Journal des opérations (${DB.journal.length})</h3><span style="display:flex;gap:7px"><button class="btn sm sec" onclick="mVerifJournal()">Vérifier l'intégrité</button><button class="btn sm sec" onclick="exportJournalCSV()">Exporter CSV</button></span></div><div class="pb nopad">${
    DB.journal.length?`<table><thead><tr><th>Horodatage</th><th>Utilisateur</th><th>Action</th><th>Détail</th><th title="Empreinte SHA-256 chaînée">⛓</th></tr></thead><tbody>${
      DB.journal.slice(0,JRN_LIM).map(j=>`<tr><td class="small">${new Date(j.date).toLocaleString("fr-FR")}</td><td><b>${esc(j.user)}</b></td><td>${esc(j.action)}</td><td class="small">${esc(j.detail)}</td><td class="small muted" title="${esc(j.h||"")}">${j.h?j.h.slice(0,8):"—"}</td></tr>`).join("")
    }${DB.journal.length>JRN_LIM?`<tr><td colspan="5" style="text-align:center;padding:11px;background:#FBFDFC"><span class="small muted">${JRN_LIM} sur ${DB.journal.length} — </span><button class="btn sm sec" onclick="JRN_LIM+=PAGE_TAILLE;rJournal()">Afficher plus</button> <button class="btn sm ghost" onclick="JRN_LIM=Infinity;rJournal()">Tout</button></td></tr>`:""}</tbody></table>`:`<div class="empty">Journal vide.</div>`}</div></div>`;
}


/* module: admin/parametres.js — PNDDRR engine (classic globals) */
/* ================= PARAMÈTRES & CONFIGURATION ================= */
const OUTIL_DESC={
  recherche:["Recherche multicritère","Retrouver des dossiers par identité, statut, localisation, groupe, dates et tout autre critère combiné."],
  import:["Importer des données","Charger des fichiers CSV, Excel, Word ou PDF : dossiers d'ex-combattants et registres d'armes, avec contrôle et synchronisation."],
  referentiels:["Référentiels (groupes armés)","Ajouter, renommer ou supprimer les groupes armés proposés dans les formulaires et les filtres."],
  config:["Configuration du programme","Régler les seuils d'alerte, le verrouillage, la pagination, les documents officiels et les éléments de la carte de démobilisé."],
  comptes:["Gestion des comptes","Créer les comptes des agents et attribuer leurs autorisations, profil par profil."],
  journal:["Journal des opérations","Consulter l'historique horodaté de toutes les opérations effectuées dans l'application."],
  sauvegarde:["Sauvegarde & synchronisation","Fonctionnement hors ligne, enregistrement local, échanges JSON entre postes et fusion multi-postes."]
};
function outilRetour(){ return `<div class="toolbar"><button class="btn ghost" onclick="go('parametres')">← Retour aux paramètres</button></div>`; }
function rConfig(){
  const c=DB.config, num=(id,lbl,val,min,max,note)=>`<div class="field"><label>${lbl}</label><input type="number" id="${id}" value="${val}" min="${min}" max="${max}">${note?`<div class="small muted" style="margin-top:3px">${note}</div>`:""}</div>`;
  $("view").innerHTML = `
  ${outilRetour()}
  <div class="panel"><div class="ph"><h3>Alertes « À traiter » — seuils (jours)</h3></div><div class="pb"><div class="grid3">
    ${num("cf_orient","Orientation en retard après",c.seuilOrientation,15,720,"Jours depuis le désarmement sans orientation.")}
    ${num("cf_visite","Visite de suivi en retard après",c.seuilVisite,15,720,"Jours depuis la dernière visite socio-économique.")}
    ${num("cf_form","Formation anormalement longue après",c.seuilFormation,30,1080,"Jours depuis l'orientation sans fin de formation.")}
  </div></div></div>
  <div class="panel"><div class="ph"><h3>Sécurité & affichage</h3></div><div class="pb"><div class="grid3">
    ${num("cf_verrou","Verrouillage après inactivité (minutes)",c.verrouMin,0,240,"0 = verrouillage automatique désactivé.")}
    <div class="field"><label>Lignes par page (grands tableaux)</label><select id="cf_page">${[50,100,200,500].map(n=>`<option ${+c.pageTaille===n?"selected":""}>${n}</option>`).join("")}</select><div class="small muted" style="margin-top:3px">Registres et journal — « Afficher plus » au-delà.</div></div>
    ${num("cf_rappel","Rappel de sauvegarde après (jours)",c.rappelJours,0,90,"0 = rappel désactivé sur le tableau de bord.")}
  </div></div></div>
  <div class="panel"><div class="ph"><h3>Documents officiels</h3></div><div class="pb"><div class="grid2">
    <div class="field"><label>Ville de signature des documents</label><input id="cf_ville" value="${esc(c.villeSignature)}" placeholder="Bangui"></div>
    <div class="field"><label>Signataire de la carte de démobilisé</label><input id="cf_sign" value="${esc(c.signataireCarte)}" placeholder="Le Coordonnateur de l'UEPNDDR"></div>
  </div></div></div>
  <div class="panel"><div class="ph"><h3>Carte de démobilisé — éléments du verso</h3></div><div class="pb">
    <label style="display:flex;align-items:center;gap:8px;font-weight:400;text-transform:none;font-size:13.5px;color:var(--ink);margin-bottom:7px"><input type="checkbox" id="cf_qr" style="width:auto" ${c.carteQR!==false?"checked":""}> QR code (n° de carte + code d'authentification en un scan)</label>
    <label style="display:flex;align-items:center;gap:8px;font-weight:400;text-transform:none;font-size:13.5px;color:var(--ink)"><input type="checkbox" id="cf_cb" style="width:auto" ${c.carteCodeBarres!==false?"checked":""}> Code-barres Code 39 (n° de carte, lisible à la douchette)</label>
    <p class="small muted" style="margin-top:8px">S'applique aux cartes imprimées à partir de maintenant ; le code d'authentification du recto reste toujours présent.</p>
  </div></div>
  <div style="display:flex;gap:10px">
    <button class="btn" onclick="saveConfig()">Enregistrer la configuration</button>
    <button class="btn ghost" onclick="resetConfig()">Rétablir les valeurs par défaut</button>
  </div>
  <p class="small muted" style="margin-top:10px">La configuration est propre à cette installation, conservée dans les données locales et incluse dans les sauvegardes JSON (Restaurer la propage à un autre poste).</p>`;
}
function saveConfig(){
  if(CUR.role!=="admin"){ toast("Réservé à l'administrateur."); return; }
  const b=(v,min,max,d)=>{ v=+v; return isFinite(v)?Math.min(max,Math.max(min,v)):d; };
  DB.config.seuilOrientation=b($("cf_orient").value,15,720,90);
  DB.config.seuilVisite=b($("cf_visite").value,15,720,60);
  DB.config.seuilFormation=b($("cf_form").value,30,1080,270);
  DB.config.verrouMin=b($("cf_verrou").value,0,240,15);
  DB.config.pageTaille=+$("cf_page").value||100;
  DB.config.rappelJours=b($("cf_rappel").value,0,90,7);
  DB.config.villeSignature=$("cf_ville").value.trim()||"Bangui";
  DB.config.signataireCarte=$("cf_sign").value.trim()||"Le Coordonnateur de l'UEPNDDR";
  DB.config.carteQR=$("cf_qr").checked;
  DB.config.carteCodeBarres=$("cf_cb").checked;
  applyConfig(); persist();
  log("Configuration",`Seuils ${DB.config.seuilOrientation}/${DB.config.seuilVisite}/${DB.config.seuilFormation} j · verrou ${DB.config.verrouMin} min · page ${DB.config.pageTaille} · rappel ${DB.config.rappelJours} j · ville ${DB.config.villeSignature} · carte QR:${DB.config.carteQR?"oui":"non"} CB:${DB.config.carteCodeBarres?"oui":"non"}`);
  toast("Configuration enregistrée."); rConfig();
}
function resetConfig(){
  if(CUR.role!=="admin"){ toast("Réservé à l'administrateur."); return; }
  DB.config={}; migrateDB(); applyConfig(); persist();
  log("Configuration","Rétablissement des valeurs par défaut");
  toast("Valeurs par défaut rétablies."); rConfig();
}
function rParametres(){
  const items=OUTILS.filter(navAllowed);
  $("view").innerHTML = `
  <p class="small muted" style="margin-bottom:14px">Outils et administration du programme — l'accès à chaque option dépend des autorisations du compte connecté (${esc(CUR.nom)}).</p>
  <div class="panel"><div class="ph"><h3>Options du programme</h3><span class="muted small">${items.length} option(s) disponibles</span></div>
  <div class="pb nopad"><ul class="optList">${items.map((o,i)=>{
    const [t,dsc]=OUTIL_DESC[o.id]||[o.lbl,""];
    return `<li onclick="go('${o.id}')">
      <span class="num">${i+1}</span>
      <span class="lbl"><b>${t}</b><span class="small muted">${dsc}</span></span>
      <button class="btn sm sec" onclick="event.stopPropagation();go('${o.id}')">Ouvrir</button>
    </li>`;}).join("")}
  </ul></div></div>`;
}

/* module: admin/sauvegarde.js — PNDDRR engine (classic globals) */
/* ================= SAUVEGARDE & EXPORTS ================= */
function rSauvegarde(){
  const ts=lastPersist();
  $("view").innerHTML = `
  ${outilRetour()}
  <div class="panel"><div class="ph"><h3>Fonctionnement hors ligne (zones éloignées)</h3></div><div class="pb">
    ${HAS_LS?`<p class="small" style="margin-bottom:10px"><b style="color:var(--ok)">✔ Stockage local actif sur cet appareil.</b>
      Chaque opération (enregistrement, désarmement, importation…) est <b>enregistrée automatiquement</b> dans le navigateur :
      les données survivent à la fermeture du navigateur, au redémarrage de l'appareil et aux coupures de réseau ou d'électricité.
      Aucune connexion Internet n'est nécessaire pour travailler.${ts?` Dernier enregistrement automatique : <b>${new Date(ts).toLocaleString("fr-FR")}</b>.`:""}</p>`
    :`<p class="small" style="margin-bottom:10px"><b style="color:var(--warn)">⚠ Stockage local indisponible dans cet environnement</b> (aperçu bac à sable) —
      les données sont conservées en mémoire de session uniquement. Ouvrez le fichier directement dans un navigateur (Chrome, Firefox, Edge)
      sur l'appareil de terrain pour activer l'enregistrement automatique hors ligne.</p>`}
    ${window.__PNDDRR_SERVER?`<p class="small" style="margin-bottom:10px"><b style="color:var(--ok)">✔ Registre partagé.</b> Tous les comptes (admin, agents, suivi) travaillent sur le <b>même registre</b> : un dossier saisi par un utilisateur apparaît chez les autres après quelques secondes.</p>`:""}
    <p class="small muted" style="margin-bottom:10px">L'application est un fichier unique sans serveur : copiez <b>ddr-rca.html</b> sur chaque poste de terrain (clé USB) et ouvrez-le dans le navigateur. Formats d'importation hors ligne : CSV et sauvegardes JSON ; Excel/Word/PDF nécessitent une connexion ponctuelle.</p>
    <div style="display:flex;gap:10px;flex-wrap:wrap">
      ${HAS_LS?`<button class="btn sec" onclick="persist();toast('Données enregistrées sur cet appareil.');rSauvegarde()">Enregistrer maintenant</button>`:""}
      ${CUR.role==="admin"?`<button class="btn sec" onclick="askSeedDemo()">Charger des données de démonstration (simulation)</button>`:""}
      ${HAS_LS&&CUR.role==="admin"?`<button class="btn danger" onclick="askClearLocal()">Effacer les données locales de cet appareil</button>`:""}
    </div>
  </div></div>
  <div class="panel"><div class="ph"><h3>Identification de ce poste</h3></div><div class="pb">
    <p class="small muted" style="margin-bottom:10px">Donnez un nom à chaque poste de terrain (ex. : « Antenne Bambari », « Site Paoua ») : il figure dans le nom des fichiers de synchronisation et dans l'historique, pour savoir d'où viennent les données consolidées.</p>
    <div class="toolbar" style="margin-bottom:0">
      <div class="field" style="flex:1;max-width:340px"><label>Nom de ce poste</label><input id="posteNom" value="${esc(DB.poste||"")}" placeholder="Ex. : Antenne Bambari"></div>
      <div class="field" style="max-width:190px"><label>Code du poste (numérotation)</label><input id="posteCodeInp" maxlength="5" value="${esc(DB.posteCode||"")}" placeholder="Ex. : BAM1" style="text-transform:uppercase"></div>
      <button class="btn sec" style="align-self:end" onclick="savePoste()">Enregistrer</button>
    </div>
    <p class="small muted" style="margin-top:9px">Le <b>code du poste</b> (2 à 5 lettres/chiffres) est intégré aux nouveaux numéros — <b>DDR-${posteCode()||"CODE"}-${new Date().getFullYear()}-0001</b>, <b>DEM-${posteCode()||"CODE"}-…</b> — ce qui rend toute collision impossible entre postes travaillant hors ligne. Les numéros déjà attribués ne changent pas.</p>
  </div></div>
  ${CUR.role==="admin"?`<div class="panel"><div class="ph"><h3>Secret d'installation (authentification des cartes)</h3></div><div class="pb">
    <p class="small muted" style="margin-bottom:10px">Le secret sert au calcul des codes d'authentification des cartes de démobilisé. Défini par l'administrateur et partagé entre les postes par la synchronisation, il rend les codes incalculables sans lui. ${DB.secret?`<b style="color:var(--ok)">✔ Un secret d'installation est défini.</b> Les cartes imprimées avant sa définition restent vérifiables.`:`<b style="color:var(--warn)">⚠ Aucun secret défini</b> — les codes reposent sur le réglage d'usine, commun à toutes les installations.`}</p>
    <div class="toolbar" style="margin-bottom:0">
      <button class="btn sec" onclick="genSecret()">${DB.secret?"Renouveler le secret":"Générer un secret d'installation"}</button>
    </div>
  </div></div>`:""}
  <div class="panel"><div class="ph"><h3>Sauvegarde et transfert entre postes</h3></div><div class="pb">
    <p class="small muted" style="margin-bottom:12px">Le fichier JSON sert au transfert entre appareils : <b>Restaurer</b> remplace toutes les données de ce poste ; <b>Fusionner</b> combine les données collectées hors ligne sur plusieurs postes (déduplication par n° de dossier et n° de série, le statut le plus avancé est conservé).</p>
    <div style="display:flex;gap:10px;flex-wrap:wrap">
      <button class="btn" onclick="backupJSON()">⇩ Télécharger la sauvegarde (JSON)</button>
      <label class="btn sec" style="display:inline-flex;align-items:center">⇧ Restaurer une sauvegarde (remplace tout)<input type="file" accept=".json" style="display:none" onchange="restoreJSON(this)"></label>
      <label class="btn sec" style="display:inline-flex;align-items:center">⇆ Fusionner une sauvegarde (multi-postes)<input type="file" accept=".json" style="display:none" onchange="mergeJSONFile(this)"></label>
    </div>
  </div></div>
  <div class="panel"><div class="ph"><h3>Historique des synchronisations (${DB.syncs.length})</h3></div><div class="pb nopad">${
    DB.syncs.length?`<table><thead><tr><th>Date</th><th>Opération</th><th>Fichier</th><th>Poste</th><th>Détail</th></tr></thead><tbody>${
      DB.syncs.slice(0,10).map(x=>`<tr><td class="small">${new Date(x.date).toLocaleString("fr-FR")}</td><td><span class="tag">${esc(x.type)}</span></td><td class="small">${esc(x.fichier)}</td><td class="small">${esc(x.poste)}</td><td class="small">${esc(x.detail)}</td></tr>`).join("")
    }</tbody></table>`:`<div class="empty">Aucune synchronisation pour le moment — exportez le fichier JSON de ce poste, ou fusionnez celui d'un autre poste.</div>`}</div></div>
  <div class="panel"><div class="ph"><h3>Exports CSV</h3></div><div class="pb" style="display:flex;gap:10px;flex-wrap:wrap">
    <button class="btn sec" onclick="exportXLSX()">Classeur Excel complet (.xlsx)</button>
    <button class="btn sec" onclick="exportCombCSV()">Ex-combattants (CSV)</button>
    <button class="btn sec" onclick="exportArmesCSV()">Registre des armes (CSV)</button>
    <button class="btn sec" onclick="exportJournalCSV()">Journal (CSV)</button>
  </div></div>`;
}
function dl(name, content, type){
  try{
    const b=new Blob([content],{type}); const a=document.createElement("a");
    a.href=URL.createObjectURL(b); a.download=name;
    document.body.appendChild(a); a.click(); a.remove();
    setTimeout(()=>URL.revokeObjectURL(a.href), 4000);
    toast(`Fichier « ${name} » téléchargé. S'il n'apparaît pas, ouvrez ddr-rca.html directement dans un navigateur.`);
  }catch(e){
    openModal("Téléchargement bloqué par cet aperçu", `<p class="small muted" style="margin-bottom:8px">Copiez le contenu ci-dessous et collez-le dans un fichier « ${esc(name)} », ou ouvrez ddr-rca.html directement dans Chrome/Edge/Firefox pour un téléchargement normal.</p>
      <textarea style="width:100%;height:260px;font-family:monospace;font-size:11px" onclick="this.select()">${esc(content)}</textarea>`,
      `<button class="btn ghost" onclick="closeModal()">Fermer</button>`);
  }
}
/* Écrivain XLSX minimal embarqué : archive ZIP « stored » + feuilles inlineStr */
var CRC_T=(function(){ const t=[]; for(let n=0;n<256;n++){ let c=n; for(let k=0;k<8;k++) c=c&1?0xEDB88320^(c>>>1):c>>>1; t[n]=c>>>0; } return t; })();
function crc32(u8){ let c=0xFFFFFFFF; for(let i=0;i<u8.length;i++) c=CRC_T[(c^u8[i])&0xFF]^(c>>>8); return (c^0xFFFFFFFF)>>>0; }
function xmlEsc(v){ return String(v==null?"":v).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;"); }
function xlsxBytes(sheets){
  const enc=new TextEncoder();
  const colName=i=>{ let n=""; i++; while(i){ n=String.fromCharCode(65+(i-1)%26)+n; i=Math.floor((i-1)/26); } return n; };
  const files=[["[Content_Types].xml",'<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/><Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/>'+sheets.map((sh,i)=>'<Override PartName="/xl/worksheets/sheet'+(i+1)+'.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>').join("")+'</Types>'],
  ["_rels/.rels",'<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>'],
  ["xl/workbook.xml",'<?xml version="1.0" encoding="UTF-8" standalone="yes"?><workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets>'+sheets.map((sh,i)=>'<sheet name="'+xmlEsc(String(sh.nom).slice(0,31))+'" sheetId="'+(i+1)+'" r:id="rId'+(i+1)+'"/>').join("")+'</sheets></workbook>'],
  ["xl/_rels/workbook.xml.rels",'<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">'+sheets.map((sh,i)=>'<Relationship Id="rId'+(i+1)+'" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet'+(i+1)+'.xml"/>').join("")+'</Relationships>']];
  sheets.forEach((sh,i)=>{
    const rows=sh.rows.map((r,ri)=>'<row r="'+(ri+1)+'">'+r.map((v,ci)=>{
      const ref=colName(ci)+(ri+1);
      if(typeof v==="number"&&isFinite(v)) return '<c r="'+ref+'" t="n"><v>'+v+'</v></c>';
      const t=String(v==null?"":v); if(t==="") return "";
      return '<c r="'+ref+'" t="inlineStr"><is><t xml:space="preserve">'+xmlEsc(t)+'</t></is></c>';
    }).join("")+'</row>').join("");
    files.push(["xl/worksheets/sheet"+(i+1)+".xml",'<?xml version="1.0" encoding="UTF-8" standalone="yes"?><worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><sheetData>'+rows+'</sheetData></worksheet>']);
  });
  const parts=[], central=[]; let off=0;
  for(const [name,content] of files){
    const nm=enc.encode(name), data=enc.encode(content), crc=crc32(data);
    const head=new Uint8Array(30+nm.length); const dv=new DataView(head.buffer);
    dv.setUint32(0,0x04034b50,true); dv.setUint16(4,20,true); dv.setUint16(6,0,true); dv.setUint16(8,0,true);
    dv.setUint16(10,0,true); dv.setUint16(12,0x21,true);
    dv.setUint32(14,crc,true); dv.setUint32(18,data.length,true); dv.setUint32(22,data.length,true);
    dv.setUint16(26,nm.length,true); dv.setUint16(28,0,true); head.set(nm,30);
    parts.push(head,data);
    const cd=new Uint8Array(46+nm.length); const cv=new DataView(cd.buffer);
    cv.setUint32(0,0x02014b50,true); cv.setUint16(4,20,true); cv.setUint16(6,20,true);
    cv.setUint16(12,0,true); cv.setUint16(14,0x21,true);
    cv.setUint32(16,crc,true); cv.setUint32(20,data.length,true); cv.setUint32(24,data.length,true);
    cv.setUint16(28,nm.length,true); cv.setUint32(42,off,true); cd.set(nm,46);
    central.push(cd);
    off+=head.length+data.length;
  }
  const cdLen=central.reduce((a,x)=>a+x.length,0);
  const end=new Uint8Array(22); const ev=new DataView(end.buffer);
  ev.setUint32(0,0x06054b50,true); ev.setUint16(8,files.length,true); ev.setUint16(10,files.length,true);
  ev.setUint32(12,cdLen,true); ev.setUint32(16,off,true);
  const total=off+cdLen+22, out=new Uint8Array(total); let p=0;
  for(const part of [...parts,...central,end]){ out.set(part,p); p+=part.length; }
  return out;
}
function dlXlsx(name, sheets){
  try{
    const b=new Blob([xlsxBytes(sheets)],{type:"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"});
    const a=document.createElement("a"); a.href=URL.createObjectURL(b); a.download=name;
    document.body.appendChild(a); a.click(); a.remove();
    setTimeout(()=>URL.revokeObjectURL(a.href), 4000);
    toast(`Fichier Excel « ${name} » téléchargé.`);
  }catch(e){ toast("Export Excel indisponible dans cet aperçu — utilisez l'export CSV ou ouvrez ddr-rca.html dans un navigateur."); }
}
function combRows(src){
  const rows=[["N° dossier","Nom","Prénom","Alias","Sexe","Date naissance","Nationalité","Téléphone","Préfecture","Sous-préfecture","Commune","Site","Groupe armé","Grade","Souhait","Statut","Carte démobilisé","Corps (mil.)","Matricule","Filière (socio-éco)","Appui FCFA","Fin de parcours"]];
  (src||DB.combattants).forEach(c=>rows.push([c.num,c.nom,c.prenom,c.alias||"",c.sexe,c.dn||"",c.nat,c.tel||"",c.prefecture,c.sousPref||"",c.commune||"",c.site||"",c.groupe,c.grade||"",c.souhait,STATUTS[c.statut].lbl,
    c.demobilisation?c.demobilisation.carte:"", c.reintMil?c.reintMil.corps:"", c.reintMil?c.reintMil.matricule:"",
    c.reintSocio?c.reintSocio.filiere:"", c.reintSocio?+c.reintSocio.appui||"":"", c.fin||""]));
  return rows;
}
function armesRowsX(){
  const rows=[["Type","Marque","Calibre","N° série","État","Garde","Remise par","N° dossier","Date","Lieu"]];
  allArmes().forEach(a=>rows.push([a.type,a.marque,a.calibre,a.serie,a.etat,GARDE_LBL[(a.garde&&a.garde.etat)||"depot"],a.nom,a.num,a.date,a.lieu]));
  return rows;
}
function exportXLSX(){
  dlXlsx(`pnddrr_registre_${today()}.xlsx`, [
    {nom:"Ex-combattants", rows:combRows()},
    {nom:"Armes", rows:armesRowsX()},
    {nom:"Munitions", rows:(()=>{const r=[["Nature / calibre","Quantité","Unité","Remise par","N° dossier","Date","Lieu"]];allMunitions().forEach(m=>r.push([m.nature,m.qte,m.unite,m.nom,m.num,m.date,m.lieu]));return r;})()}
  ]);
  log("Export Excel","Classeur registre (ex-combattants, armes, munitions)");
}
function csv(rows){ return "\uFEFF"+rows.map(r=>r.map(x=>`"${String(x==null?"":x).replace(/"/g,'""')}"`).join(";")).join("\r\n"); }
function exportCombCSV(fromSearch){
  const src = fromSearch && window._lastSearch ? window._lastSearch : DB.combattants;
  const rows=[["N° dossier","Nom","Prénom","Alias","Sexe","Date naissance","Nationalité","Téléphone","Préfecture","Sous-préfecture","Commune","Site","Groupe armé","Grade","Souhait","Statut","Carte démobilisé","Corps (mil.)","Matricule","Filière (socio-éco)","Appui FCFA","Fin de parcours"]];
  src.forEach(c=>rows.push([c.num,c.nom,c.prenom,c.alias||"",c.sexe,c.dn||"",c.nat,c.tel||"",c.prefecture,c.sousPref||"",c.commune||"",c.site||"",c.groupe,c.grade||"",c.souhait,STATUTS[c.statut].lbl,
    c.demobilisation?c.demobilisation.carte:"", c.reintMil?c.reintMil.corps:"", c.reintMil?c.reintMil.matricule:"",
    c.reintSocio?c.reintSocio.filiere:"", c.reintSocio?c.reintSocio.appui||"":"", c.fin||""]));
  dl("pnddrr_ex-combattants.csv", csv(rows), "text/csv"); log("Export CSV","Ex-combattants");
}
function exportArmesCSV(){
  const rows=[["Type","Marque","Calibre","N° série","État","Garde","Dépôt","N° scellé","PV destruction","Munitions","Remise par","N° dossier","Groupe","Date","Lieu"]];
  allArmes().forEach(a=>{const g=a.garde||{etat:"depot"};rows.push([a.type,a.marque,a.calibre,a.serie,a.etat,GARDE_LBL[g.etat],g.depot||"",g.scelle||"",g.destruction?g.destruction.pv||"":"",a.mun,a.nom,a.num,a.groupe,a.date,a.lieu]);});
  dl("pnddrr_registre_armes.csv", csv(rows), "text/csv"); log("Export CSV","Registre des armes");
}
function exportJournalCSV(){
  const rows=[["Horodatage","Utilisateur","Action","Détail"]];
  DB.journal.forEach(j=>rows.push([j.date,j.user,j.action,j.detail]));
  dl("pnddrr_journal.csv", csv(rows), "text/csv");
}
function askClearLocal(){
  openModal("Effacer les données locales", `<p>Toutes les données enregistrées sur <b>cet appareil</b> seront supprimées (dossiers, registres, journal, comptes créés). Téléchargez d'abord une sauvegarde JSON si nécessaire.</p>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn danger" onclick="doClearLocal()">Effacer définitivement</button>`);
}
function doClearLocal(){ clearPersisted(); try{ if(HAS_LS) localStorage.setItem("pnddrr_skip_demo","1"); }catch(e){} closeModal(); toast("Données locales effacées — rechargez la page."); }
function mergeJSONFile(inp){
  const f=inp.files[0]; if(!f) return;
  const r=new FileReader();
  r.onload=()=>{ try{
      const d=JSON.parse(r.result);
      if(!d.combattants||!d.users) throw 0;
      const rep=mergeDB(d);
      addSync("Fusion", f.name, `poste source : ${d.poste||"(non précisé)"} — ${rep.ajoutes} ajouté(s), ${rep.fusionnes} fusionné(s)`);
      log("Fusion multi-postes",`${f.name} — ${rep.ajoutes} dossier(s) ajouté(s), ${rep.fusionnes} fusionné(s), ${rep.armes} arme(s), ${rep.groupes} groupe(s)`);
      toast(`Fusion terminée : ${rep.ajoutes} ajouté(s), ${rep.fusionnes} fusionné(s).`);
      go("dashboard");
    }catch(e){ toast("Fichier de sauvegarde invalide."); } };
  r.readAsText(f); inp.value="";
}
function mergeDB(d){
  const rep={ajoutes:0,fusionnes:0,armes:0,groupes:0};
  (d.groupes||[]).forEach(g=>{ if(!GROUPES.some(x=>normTxt(x)===normTxt(g))){ GROUPES.splice(GROUPES.indexOf("Autre"),0,g); rep.groupes++; } });
  (d.users||[]).forEach(u=>{ if(!DB.users.some(x=>x.login===u.login)) DB.users.push(u); });
  const series=new Set(allArmes().map(a=>normTxt(a.serie)).filter(x=>x));
  for(const inc of d.combattants||[]){
    let ex=DB.combattants.find(c=>c.num===inc.num)
      ||DB.combattants.find(c=>c.nom===inc.nom&&normTxt(c.prenom)===normTxt(inc.prenom)&&(!inc.dn||!c.dn||c.dn===inc.dn));
    if(!ex){
      const m=String(inc.num||"").match(/^DDR-(\d{4})-(\d{1,5})$/);
      if(m) DB.seq.comb=Math.max(DB.seq.comb,+m[2]); else inc.num=numDossier();
      if(inc.demobilisation&&inc.demobilisation.carte){ const md=inc.demobilisation.carte.match(/^DEM-(\d{4})-(\d{1,5})$/); if(md) DB.seq.dem=Math.max(DB.seq.dem,+md[2]); }
      DB.combattants.push(inc); rep.ajoutes++;
      if(inc.desarmement) inc.desarmement.armes.forEach(a=>{ if(a.serie) series.add(normTxt(a.serie)); });
      continue;
    }
    // fusion : champs vides complétés, statut le plus avancé conservé
    ["alias","dn","ln","tel","sousPref","commune","site","grade","annees","zone","obs","photo"].forEach(f=>{ if(!ex[f]&&inc[f]) ex[f]=inc[f]; });
    if(STATUTS[inc.statut]&&STATUTS[inc.statut].ord>STATUTS[ex.statut].ord) ex.statut=inc.statut;
    if(inc.desarmement){
      if(!ex.desarmement) ex.desarmement={date:inc.desarmement.date,lieu:inc.desarmement.lieu,agent:inc.desarmement.agent,armes:[],munitions:[]};
      for(const a of inc.desarmement.armes||[]){
        if(a.serie&&series.has(normTxt(a.serie))) continue;
        ex.desarmement.armes.push(a); if(a.serie) series.add(normTxt(a.serie)); rep.armes++;
      }
      for(const m of inc.desarmement.munitions||[]){
        ex.desarmement.munitions=ex.desarmement.munitions||[];
        if(!ex.desarmement.munitions.some(x=>x.nature===m.nature&&x.qte===m.qte&&x.unite===m.unite)) ex.desarmement.munitions.push(m);
      }
    }
    if(inc.demobilisation&&!ex.demobilisation) ex.demobilisation=inc.demobilisation;
    if(inc.reintMil&&!ex.reintMil) ex.reintMil=inc.reintMil;
    if(inc.reintSocio){
      if(!ex.reintSocio) ex.reintSocio=inc.reintSocio;
      else (inc.reintSocio.visites||[]).forEach(v=>{ if(!ex.reintSocio.visites.some(x=>x.date===v.date&&x.obs===v.obs)) ex.reintSocio.visites.push(v); });
    }
    if(inc.fin&&!ex.fin) ex.fin=inc.fin;
    if(inc.abandon&&!ex.abandon&&ex.statut==="abandon") ex.abandon=inc.abandon;
    rep.fusionnes++;
  }
  (d.journal||[]).forEach(j=>{ if(!DB.journal.some(x=>x.date===j.date&&x.user===j.user&&x.action===j.action)) DB.journal.push(j); });
  DB.journal.sort((a,b)=>b.date.localeCompare(a.date));
  return rep;
}
function backupJSON(){
  const name=`pnddrr_sync_${posteSlug()}_${today()}.json`;
  dl(name, JSON.stringify(DB,null,1), "application/json");
  addSync("Export", name, `${DB.combattants.length} dossier(s), ${allArmes().length} arme(s)`);
  log("Sauvegarde",`Export du fichier de synchronisation ${name}`); toast("Fichier de synchronisation téléchargé.");
}
function restoreJSON(inp){
  const f=inp.files[0]; if(!f) return;
  const r=new FileReader();
  r.onload=()=>{
    try{
      const d=JSON.parse(r.result);
      if(!d.combattants||!d.users) throw 0;
      DB=d; migrateDB();
      DB._replace=true;
      (DB.users||[]).forEach(u=>{ u.passUpdated=true; });
      addSync("Restauration", f.name, `poste source : ${d.poste||"(non précisé)"} — ${DB.combattants.length} dossier(s)`);
      log("Restauration",`Fichier ${f.name} — ${DB.combattants.length} dossier(s)`);
      toast("Sauvegarde restaurée."); go("dashboard");
    }catch(e){ toast("Fichier de sauvegarde invalide."); }
  };
  r.readAsText(f); inp.value="";
}



/* module: documents/docs.js — PNDDRR engine (classic globals) */
/* ================= DOCUMENTS IMPRIMABLES ================= */
function docEntete(service){
  return `<div class="entete">
    <div class="rep">République Centrafricaine</div>
    <div class="devise">Unité — Dignité — Travail</div>
    <div class="armoiries">${ARM_SVG}</div>
    <div class="min">Unité d'exécution du Programme national de désarmement,<br>démobilisation, réintégration et rapatriement</div>
    ${service?`<div class="min" style="margin-top:3px;font-size:11.5px">${service}</div>`:""}
    <div class="trait"></div>
  </div>`;
}
function docWrap(inner){
  return `<div class="doc"><div class="wm">${ARM_SVG}</div><div class="inner">${inner}</div></div>`;
}
function doPrint(html, kind){
  document.body.classList.toggle("print-pvc", kind==="pvc");
  const zone=$("printZone");
  if(zone && zone.parentNode!==document.body) document.body.appendChild(zone);
  zone.innerHTML = html;
  zone.className = kind==="pvc" ? "pvc" : "";
  $("prevBody").innerHTML = html;
  $("prevBody").className = kind==="pvc" ? "pvc" : "";
  const ttl=document.querySelector("#printPrev .pp-bar b");
  if(ttl) ttl.textContent = kind==="pvc" ? "Aperçu carte PVC — 8,5 × 5,5 cm (une face par page)" : "Aperçu du document";
  $("printPrev").style.display = "flex";
  document.body.style.overflow = "hidden";
}
function prevClose(){
  $("printPrev").style.display="none";
  document.body.style.overflow="";
  document.body.classList.remove("print-pvc");
  $("prevBody").className="";
  $("printZone").innerHTML="";
  $("printZone").className="";
  const frame=document.getElementById("pnddrrPrintFrame");
  if(frame) frame.remove();
}
function prevPrint(){
  const pvc=document.body.classList.contains("print-pvc");
  const markup=($("printZone")&&$("printZone").innerHTML)||($("prevBody")&&$("prevBody").innerHTML);
  if(!markup){ toast("Rien à imprimer."); return; }
  let frame=document.getElementById("pnddrrPrintFrame");
  if(frame) frame.remove();
  frame=document.createElement("iframe");
  frame.id="pnddrrPrintFrame";
  frame.setAttribute("aria-hidden","true");
  frame.style.cssText="position:fixed;right:0;bottom:0;width:0;height:0;border:0;opacity:0;pointer-events:none";
  document.body.appendChild(frame);
  const doc=frame.contentDocument;
  const headBits=[...document.querySelectorAll("link[rel='stylesheet'], style")].map(n=>n.outerHTML).join("");
  const page=pvc?"size:85mm 55mm;margin:0":"size:A4 portrait;margin:12mm";
  doc.open();
  doc.write(`<!DOCTYPE html><html><head><meta charset="utf-8"><title>Impression PNDDRR</title>${headBits}
<style>
@page{${page}}
html,body{margin:0!important;padding:0!important;background:#fff!important;height:auto!important;overflow:visible!important}
body>*:not(#printZone){display:none!important}
#printZone{display:block!important;visibility:visible!important;position:static!important;left:auto!important;top:auto!important;width:${pvc?"85mm":"auto"}!important;height:auto!important}
#printZone *{visibility:visible!important}
</style></head>
<body class="${pvc?"print-pvc":""}"><div id="printZone" class="${pvc?"pvc":""}">${markup}</div></body></html>`);
  doc.close();
  let printed=false;
  const run=()=>{
    if(printed) return;
    printed=true;
    try{ frame.contentWindow.focus(); frame.contentWindow.print(); }
    catch(e){ toast("Impression indisponible dans cet aperçu — utilisez Chrome, Edge ou Firefox."); }
  };
  const links=[...doc.querySelectorAll("link[rel='stylesheet']")];
  if(!links.length){ setTimeout(run, 80); return; }
  let left=links.length;
  const one=()=>{ left-=1; if(left<=0) setTimeout(run, 80); };
  links.forEach(l=>{
    if(l.sheet){ one(); return; }
    l.addEventListener("load", one);
    l.addEventListener("error", one);
  });
  setTimeout(run, 1200);
}
function printFiche(id){
  const c=DB.combattants.find(x=>x.id===id);
  const arm = c.desarmement ? `<h3 style="font-size:13px;margin-top:14px;text-decoration:underline">Armement remis (${fmtD(c.desarmement.date)} — ${esc(c.desarmement.lieu)})</h3>
    <table class="dt"><tr><th>Type</th><th>Marque</th><th>Calibre</th><th>N° série</th><th>État</th><th>Munitions</th></tr>${
    c.desarmement.armes.map(a=>`<tr><td>${esc(a.type)}</td><td>${esc(a.marque)||"—"}</td><td>${esc(a.calibre)||"—"}</td><td>${esc(a.serie)||"—"}</td><td>${esc(a.etat)}</td><td>${esc(a.mun)||"—"}</td></tr>`).join("")}</table>${(c.desarmement.munitions||[]).length?`<p style="margin-top:4px"><b>Munitions et explosifs :</b> ${c.desarmement.munitions.map(m=>`${fmtN(m.qte)} ${esc(m.unite)}${m.nature?" ("+esc(m.nature)+")":""}`).join(" ; ")}</p>`:""}` : "";
  let reint="";
  if(c.reintMil) reint=`<h3 style="font-size:13px;margin-top:14px;text-decoration:underline">Réintégration militaire</h3>
    <table class="dt"><tr><th>Corps</th><td>${esc(c.reintMil.corps)}</td><th>Matricule</th><td>${esc(c.reintMil.matricule)||"—"}</td></tr>
    <tr><th>Unité</th><td>${esc(c.reintMil.unite)||"—"}</td><th>Incorporation</th><td>${fmtD(c.reintMil.date)}</td></tr></table>`;
  if(c.reintSocio) reint=`<h3 style="font-size:13px;margin-top:14px;text-decoration:underline">Réintégration socio-économique</h3>
    <table class="dt"><tr><th>Filière</th><td>${esc(c.reintSocio.filiere)}</td><th>Centre</th><td>${esc(c.reintSocio.centre)||"—"}</td></tr>
    <tr><th>Kit remis</th><td>${c.reintSocio.kit?"Oui — "+fmtD(c.reintSocio.kitDate):"Non"}</td><th>Appui</th><td>${c.reintSocio.appui?fmtN(c.reintSocio.appui)+" FCFA":"—"}</td></tr>
    <tr><th>Début</th><td>${fmtD(c.reintSocio.date)}</td><th>Visites de suivi</th><td>${c.reintSocio.visites.length}</td></tr></table>`;
  doPrint(docWrap(`${docEntete("Fiche individuelle d'ex-combattant")}
    <h2 class="titre">Fiche individuelle n° ${c.num}</h2>
    <table class="dt">
      <tr><th style="width:26%">Nom & prénom(s)</th><td>${esc(c.nom)} ${esc(c.prenom)}</td><th style="width:18%">Alias</th><td>${esc(c.alias)||"—"}</td></tr>
      <tr><th>Sexe</th><td>${c.sexe==="M"?"Masculin":"Féminin"}</td><th>Naissance</th><td>${fmtD(c.dn)} à ${esc(c.ln)||"—"}</td></tr>
      <tr><th>Nationalité</th><td>${esc(c.nat)}</td><th>Téléphone</th><td>${esc(c.tel)||"—"}</td></tr>
      <tr><th>Préfecture</th><td>${esc(c.prefecture)}</td><th>Site</th><td>${esc(c.site)||"—"}</td></tr>
      <tr><th>Groupe armé</th><td>${esc(c.groupe)}</td><th>Fonction</th><td>${esc(c.grade)||"—"}</td></tr>
      <tr><th>Statut actuel</th><td colspan="3"><b>${STATUTS[c.statut].lbl}</b>${c.demobilisation?` — carte de démobilisé n° ${c.demobilisation.carte}`:""}</td></tr>
    </table>
    ${arm}${reint}
    <div class="sig">
      <div class="c">L'intéressé(e)<div class="ligne">${esc(c.nom)} ${esc(c.prenom)}</div></div>
      <div class="c">Fait à ${esc(c.prefecture)}, le ${new Date().toLocaleDateString("fr-FR")}<br>L'agent du PNDDRR<div class="ligne">${esc(CUR.nom)}</div></div>
    </div>`));
  log("Impression",`Fiche individuelle ${c.num}`);
}
function attestationHtml(c){
  const d=c.desarmement;
  return docWrap(`${docEntete("")}
    <h2 class="titre">Attestation de désarmement</h2>
    <p>Le Programme National de Désarmement, Démobilisation, Réintégration et Rapatriement atteste que :</p>
    <p style="text-align:center;font-size:14.5px"><b>${esc(c.nom)} ${esc(c.prenom)}</b>${c.alias?`, alias « ${esc(c.alias)} »`:""},<br>
    né(e) le ${fmtD(c.dn)} à ${esc(c.ln)||"—"}, de nationalité ${esc(c.nat)},<br>
    anciennement membre du groupe <b>${esc(c.groupe)}</b>,</p>
    <p>a volontairement remis aux autorités du Programme, le <b>${fmtD(d.date)}</b> à <b>${esc(d.lieu)}</b>, l'armement et le matériel ci-après :</p>
    <table class="dt"><tr><th>Type</th><th>Marque</th><th>Calibre</th><th>N° série</th><th>État</th></tr>${
      d.armes.map(a=>`<tr><td>${esc(a.type)}</td><td>${esc(a.marque)||"—"}</td><td>${esc(a.calibre)||"—"}</td><td>${esc(a.serie)||"—"}</td><td>${esc(a.etat)}</td></tr>`).join("")}</table>
    ${(d.munitions||[]).length?`<p style="margin-top:6px"><b>Munitions et explosifs :</b></p>
    <table class="dt"><tr><th>Nature / calibre</th><th>Quantité</th><th>Unité</th><th>Observations</th></tr>${
      d.munitions.map(m=>`<tr><td>${esc(m.nature)||"—"}</td><td>${fmtN(m.qte)}</td><td>${esc(m.unite)}</td><td>${esc(m.obs)||"—"}</td></tr>`).join("")}</table>`:""}
    <p>En foi de quoi, la présente attestation lui est délivrée pour servir et valoir ce que de droit, dans le cadre du dossier n° <b>${c.num}</b>.</p>
    <div class="sig">
      <div class="c">L'intéressé(e)<div class="ligne">${esc(c.nom)} ${esc(c.prenom)}</div></div>
      <div class="c">Fait à ${esc(d.lieu)}, le ${new Date().toLocaleDateString("fr-FR")}<br>Pour le PNDDRR<div class="ligne">${esc(d.agent)}</div></div>
    </div>`);
}
function printAttestation(id){
  const c=DB.combattants.find(x=>x.id===id);
  doPrint(attestationHtml(c));
  log("Impression",`Attestation de désarmement ${c.num}`);
}
function authCode(c, legacy){
  const sel = legacy||!DB.secret ? "UEPNDDR-RCA" : DB.secret;
  const base=[c.num,c.demobilisation.carte,c.nom,c.prenom,c.dn||"",c.demobilisation.date,sel].join("|");
  let h1=5381, h2=0;
  for(let i=0;i<base.length;i++){ const ch=base.charCodeAt(i); h1=((h1<<5)+h1+ch)>>>0; h2=(ch+(h2<<6)+(h2<<16)-h2)>>>0; }
  const hex=(h1.toString(16)+h2.toString(16)+((h1^h2)>>>0).toString(16)).toUpperCase().padEnd(12,"0").slice(0,12);
  return hex.slice(0,4)+"-"+hex.slice(4,8)+"-"+hex.slice(8,12);
}
/* Générateur de QR code embarqué (qrcode-generator, licence MIT, minifié) */
var qrcode=function(){var t=function(t,r){var e=t,n=g[r],o=null,i=0,a=null,u=[],f={},c=function(t,r){o=function(t){for(var r=new Array(t),e=0;e<t;e+=1){r[e]=new Array(t);for(var n=0;n<t;n+=1)r[e][n]=null}return r}(i=4*e+17),l(0,0),l(i-7,0),l(0,i-7),s(),h(),d(t,r),e>=7&&v(t),null==a&&(a=p(e,n,u)),w(a,r)},l=function(t,r){for(var e=-1;e<=7;e+=1)if(!(t+e<=-1||i<=t+e))for(var n=-1;n<=7;n+=1)r+n<=-1||i<=r+n||(o[t+e][r+n]=0<=e&&e<=6&&(0==n||6==n)||0<=n&&n<=6&&(0==e||6==e)||2<=e&&e<=4&&2<=n&&n<=4)},h=function(){for(var t=8;t<i-8;t+=1)null==o[t][6]&&(o[t][6]=t%2==0);for(var r=8;r<i-8;r+=1)null==o[6][r]&&(o[6][r]=r%2==0)},s=function(){for(var t=B.getPatternPosition(e),r=0;r<t.length;r+=1)for(var n=0;n<t.length;n+=1){var i=t[r],a=t[n];if(null==o[i][a])for(var u=-2;u<=2;u+=1)for(var f=-2;f<=2;f+=1)o[i+u][a+f]=-2==u||2==u||-2==f||2==f||0==u&&0==f}},v=function(t){for(var r=B.getBCHTypeNumber(e),n=0;n<18;n+=1){var a=!t&&1==(r>>n&1);o[Math.floor(n/3)][n%3+i-8-3]=a}for(n=0;n<18;n+=1){a=!t&&1==(r>>n&1);o[n%3+i-8-3][Math.floor(n/3)]=a}},d=function(t,r){for(var e=n<<3|r,a=B.getBCHTypeInfo(e),u=0;u<15;u+=1){var f=!t&&1==(a>>u&1);u<6?o[u][8]=f:u<8?o[u+1][8]=f:o[i-15+u][8]=f}for(u=0;u<15;u+=1){f=!t&&1==(a>>u&1);u<8?o[8][i-u-1]=f:u<9?o[8][15-u-1+1]=f:o[8][15-u-1]=f}o[i-8][8]=!t},w=function(t,r){for(var e=-1,n=i-1,a=7,u=0,f=B.getMaskFunction(r),c=i-1;c>0;c-=2)for(6==c&&(c-=1);;){for(var g=0;g<2;g+=1)if(null==o[n][c-g]){var l=!1;u<t.length&&(l=1==(t[u]>>>a&1)),f(n,c-g)&&(l=!l),o[n][c-g]=l,-1==(a-=1)&&(u+=1,a=7)}if((n+=e)<0||i<=n){n-=e,e=-e;break}}},p=function(t,r,e){for(var n=A.getRSBlocks(t,r),o=b(),i=0;i<e.length;i+=1){var a=e[i];o.put(a.getMode(),4),o.put(a.getLength(),B.getLengthInBits(a.getMode(),t)),a.write(o)}var u=0;for(i=0;i<n.length;i+=1)u+=n[i].dataCount;if(o.getLengthInBits()>8*u)throw"code length overflow. ("+o.getLengthInBits()+">"+8*u+")";for(o.getLengthInBits()+4<=8*u&&o.put(0,4);o.getLengthInBits()%8!=0;)o.putBit(!1);for(;!(o.getLengthInBits()>=8*u||(o.put(236,8),o.getLengthInBits()>=8*u));)o.put(17,8);return function(t,r){for(var e=0,n=0,o=0,i=new Array(r.length),a=new Array(r.length),u=0;u<r.length;u+=1){var f=r[u].dataCount,c=r[u].totalCount-f;n=Math.max(n,f),o=Math.max(o,c),i[u]=new Array(f);for(var g=0;g<i[u].length;g+=1)i[u][g]=255&t.getBuffer()[g+e];e+=f;var l=B.getErrorCorrectPolynomial(c),h=k(i[u],l.getLength()-1).mod(l);for(a[u]=new Array(l.getLength()-1),g=0;g<a[u].length;g+=1){var s=g+h.getLength()-a[u].length;a[u][g]=s>=0?h.getAt(s):0}}var v=0;for(g=0;g<r.length;g+=1)v+=r[g].totalCount;var d=new Array(v),w=0;for(g=0;g<n;g+=1)for(u=0;u<r.length;u+=1)g<i[u].length&&(d[w]=i[u][g],w+=1);for(g=0;g<o;g+=1)for(u=0;u<r.length;u+=1)g<a[u].length&&(d[w]=a[u][g],w+=1);return d}(o,n)};f.addData=function(t,r){var e=null;switch(r=r||"Byte"){case"Numeric":e=M(t);break;case"Alphanumeric":e=x(t);break;case"Byte":e=m(t);break;case"Kanji":e=L(t);break;default:throw"mode:"+r}u.push(e),a=null},f.isDark=function(t,r){if(t<0||i<=t||r<0||i<=r)throw t+","+r;return o[t][r]},f.getModuleCount=function(){return i},f.make=function(){if(e<1){for(var t=1;t<40;t++){for(var r=A.getRSBlocks(t,n),o=b(),i=0;i<u.length;i++){var a=u[i];o.put(a.getMode(),4),o.put(a.getLength(),B.getLengthInBits(a.getMode(),t)),a.write(o)}var g=0;for(i=0;i<r.length;i++)g+=r[i].dataCount;if(o.getLengthInBits()<=8*g)break}e=t}c(!1,function(){for(var t=0,r=0,e=0;e<8;e+=1){c(!0,e);var n=B.getLostPoint(f);(0==e||t>n)&&(t=n,r=e)}return r}())},f.createTableTag=function(t,r){t=t||2;var e="";e+='<table style="',e+=" border-width: 0px; border-style: none;",e+=" border-collapse: collapse;",e+=" padding: 0px; margin: "+(r=void 0===r?4*t:r)+"px;",e+='">',e+="<tbody>";for(var n=0;n<f.getModuleCount();n+=1){e+="<tr>";for(var o=0;o<f.getModuleCount();o+=1)e+='<td style="',e+=" border-width: 0px; border-style: none;",e+=" border-collapse: collapse;",e+=" padding: 0px; margin: 0px;",e+=" width: "+t+"px;",e+=" height: "+t+"px;",e+=" background-color: ",e+=f.isDark(n,o)?"#000000":"#ffffff",e+=";",e+='"/>';e+="</tr>"}return e+="</tbody>",e+="</table>"},f.createSvgTag=function(t,r,e,n){var o={};"object"==typeof arguments[0]&&(t=(o=arguments[0]).cellSize,r=o.margin,e=o.alt,n=o.title),t=t||2,r=void 0===r?4*t:r,(e="string"==typeof e?{text:e}:e||{}).text=e.text||null,e.id=e.text?e.id||"qrcode-description":null,(n="string"==typeof n?{text:n}:n||{}).text=n.text||null,n.id=n.text?n.id||"qrcode-title":null;var i,a,u,c,g=f.getModuleCount()*t+2*r,l="";for(c="l"+t+",0 0,"+t+" -"+t+",0 0,-"+t+"z ",l+='<svg version="1.1" xmlns="http://www.w3.org/2000/svg"',l+=o.scalable?"":' width="'+g+'px" height="'+g+'px"',l+=' viewBox="0 0 '+g+" "+g+'" ',l+=' preserveAspectRatio="xMinYMin meet"',l+=n.text||e.text?' role="img" aria-labelledby="'+y([n.id,e.id].join(" ").trim())+'"':"",l+=">",l+=n.text?'<title id="'+y(n.id)+'">'+y(n.text)+"</title>":"",l+=e.text?'<description id="'+y(e.id)+'">'+y(e.text)+"</description>":"",l+='<rect width="100%" height="100%" fill="white" cx="0" cy="0"/>',l+='<path d="',a=0;a<f.getModuleCount();a+=1)for(u=a*t+r,i=0;i<f.getModuleCount();i+=1)f.isDark(a,i)&&(l+="M"+(i*t+r)+","+u+c);return l+='" stroke="transparent" fill="black"/>',l+="</svg>"},f.createDataURL=function(t,r){t=t||2,r=void 0===r?4*t:r;var e=f.getModuleCount()*t+2*r,n=r,o=e-r;return I(e,e,function(r,e){if(n<=r&&r<o&&n<=e&&e<o){var i=Math.floor((r-n)/t),a=Math.floor((e-n)/t);return f.isDark(a,i)?0:1}return 1})},f.createImgTag=function(t,r,e){t=t||2,r=void 0===r?4*t:r;var n=f.getModuleCount()*t+2*r,o="";return o+="<img",o+=' src="',o+=f.createDataURL(t,r),o+='"',o+=' width="',o+=n,o+='"',o+=' height="',o+=n,o+='"',e&&(o+=' alt="',o+=y(e),o+='"'),o+="/>"};var y=function(t){for(var r="",e=0;e<t.length;e+=1){var n=t.charAt(e);switch(n){case"<":r+="&lt;";break;case">":r+="&gt;";break;case"&":r+="&amp;";break;case'"':r+="&quot;";break;default:r+=n}}return r};return f.createASCII=function(t,r){if((t=t||1)<2)return function(t){t=void 0===t?2:t;var r,e,n,o,i,a=1*f.getModuleCount()+2*t,u=t,c=a-t,g={"██":"█","█ ":"▀"," █":"▄","  ":" "},l={"██":"▀","█ ":"▀"," █":" ","  ":" "},h="";for(r=0;r<a;r+=2){for(n=Math.floor((r-u)/1),o=Math.floor((r+1-u)/1),e=0;e<a;e+=1)i="█",u<=e&&e<c&&u<=r&&r<c&&f.isDark(n,Math.floor((e-u)/1))&&(i=" "),u<=e&&e<c&&u<=r+1&&r+1<c&&f.isDark(o,Math.floor((e-u)/1))?i+=" ":i+="█",h+=t<1&&r+1>=c?l[i]:g[i];h+="\n"}return a%2&&t>0?h.substring(0,h.length-a-1)+Array(a+1).join("▀"):h.substring(0,h.length-1)}(r);t-=1,r=void 0===r?2*t:r;var e,n,o,i,a=f.getModuleCount()*t+2*r,u=r,c=a-r,g=Array(t+1).join("██"),l=Array(t+1).join("  "),h="",s="";for(e=0;e<a;e+=1){for(o=Math.floor((e-u)/t),s="",n=0;n<a;n+=1)i=1,u<=n&&n<c&&u<=e&&e<c&&f.isDark(o,Math.floor((n-u)/t))&&(i=0),s+=i?g:l;for(o=0;o<t;o+=1)h+=s+"\n"}return h.substring(0,h.length-1)},f.renderTo2dContext=function(t,r){r=r||2;for(var e=f.getModuleCount(),n=0;n<e;n++)for(var o=0;o<e;o++)t.fillStyle=f.isDark(n,o)?"black":"white",t.fillRect(o*r,n*r,r,r)},f};t.stringToBytes=(t.stringToBytesFuncs={default:function(t){for(var r=[],e=0;e<t.length;e+=1){var n=t.charCodeAt(e);r.push(255&n)}return r}}).default,t.createStringToBytes=function(t,r){var e=function(){for(var e=S(t),n=function(){var t=e.read();if(-1==t)throw"eof";return t},o=0,i={};;){var a=e.read();if(-1==a)break;var u=n(),f=n()<<8|n();i[String.fromCharCode(a<<8|u)]=f,o+=1}if(o!=r)throw o+" != "+r;return i}(),n="?".charCodeAt(0);return function(t){for(var r=[],o=0;o<t.length;o+=1){var i=t.charCodeAt(o);if(i<128)r.push(i);else{var a=e[t.charAt(o)];"number"==typeof a?(255&a)==a?r.push(a):(r.push(a>>>8),r.push(255&a)):r.push(n)}}return r}};var r,e,n,o,i,a=1,u=2,f=4,c=8,g={L:1,M:0,Q:3,H:2},l=0,h=1,s=2,v=3,d=4,w=5,p=6,y=7,B=(r=[[],[6,18],[6,22],[6,26],[6,30],[6,34],[6,22,38],[6,24,42],[6,26,46],[6,28,50],[6,30,54],[6,32,58],[6,34,62],[6,26,46,66],[6,26,48,70],[6,26,50,74],[6,30,54,78],[6,30,56,82],[6,30,58,86],[6,34,62,90],[6,28,50,72,94],[6,26,50,74,98],[6,30,54,78,102],[6,28,54,80,106],[6,32,58,84,110],[6,30,58,86,114],[6,34,62,90,118],[6,26,50,74,98,122],[6,30,54,78,102,126],[6,26,52,78,104,130],[6,30,56,82,108,134],[6,34,60,86,112,138],[6,30,58,86,114,142],[6,34,62,90,118,146],[6,30,54,78,102,126,150],[6,24,50,76,102,128,154],[6,28,54,80,106,132,158],[6,32,58,84,110,136,162],[6,26,54,82,110,138,166],[6,30,58,86,114,142,170]],e=1335,n=7973,i=function(t){for(var r=0;0!=t;)r+=1,t>>>=1;return r},(o={}).getBCHTypeInfo=function(t){for(var r=t<<10;i(r)-i(e)>=0;)r^=e<<i(r)-i(e);return 21522^(t<<10|r)},o.getBCHTypeNumber=function(t){for(var r=t<<12;i(r)-i(n)>=0;)r^=n<<i(r)-i(n);return t<<12|r},o.getPatternPosition=function(t){return r[t-1]},o.getMaskFunction=function(t){switch(t){case l:return function(t,r){return(t+r)%2==0};case h:return function(t,r){return t%2==0};case s:return function(t,r){return r%3==0};case v:return function(t,r){return(t+r)%3==0};case d:return function(t,r){return(Math.floor(t/2)+Math.floor(r/3))%2==0};case w:return function(t,r){return t*r%2+t*r%3==0};case p:return function(t,r){return(t*r%2+t*r%3)%2==0};case y:return function(t,r){return(t*r%3+(t+r)%2)%2==0};default:throw"bad maskPattern:"+t}},o.getErrorCorrectPolynomial=function(t){for(var r=k([1],0),e=0;e<t;e+=1)r=r.multiply(k([1,C.gexp(e)],0));return r},o.getLengthInBits=function(t,r){if(1<=r&&r<10)switch(t){case a:return 10;case u:return 9;case f:case c:return 8;default:throw"mode:"+t}else if(r<27)switch(t){case a:return 12;case u:return 11;case f:return 16;case c:return 10;default:throw"mode:"+t}else{if(!(r<41))throw"type:"+r;switch(t){case a:return 14;case u:return 13;case f:return 16;case c:return 12;default:throw"mode:"+t}}},o.getLostPoint=function(t){for(var r=t.getModuleCount(),e=0,n=0;n<r;n+=1)for(var o=0;o<r;o+=1){for(var i=0,a=t.isDark(n,o),u=-1;u<=1;u+=1)if(!(n+u<0||r<=n+u))for(var f=-1;f<=1;f+=1)o+f<0||r<=o+f||0==u&&0==f||a==t.isDark(n+u,o+f)&&(i+=1);i>5&&(e+=3+i-5)}for(n=0;n<r-1;n+=1)for(o=0;o<r-1;o+=1){var c=0;t.isDark(n,o)&&(c+=1),t.isDark(n+1,o)&&(c+=1),t.isDark(n,o+1)&&(c+=1),t.isDark(n+1,o+1)&&(c+=1),0!=c&&4!=c||(e+=3)}for(n=0;n<r;n+=1)for(o=0;o<r-6;o+=1)t.isDark(n,o)&&!t.isDark(n,o+1)&&t.isDark(n,o+2)&&t.isDark(n,o+3)&&t.isDark(n,o+4)&&!t.isDark(n,o+5)&&t.isDark(n,o+6)&&(e+=40);for(o=0;o<r;o+=1)for(n=0;n<r-6;n+=1)t.isDark(n,o)&&!t.isDark(n+1,o)&&t.isDark(n+2,o)&&t.isDark(n+3,o)&&t.isDark(n+4,o)&&!t.isDark(n+5,o)&&t.isDark(n+6,o)&&(e+=40);var g=0;for(o=0;o<r;o+=1)for(n=0;n<r;n+=1)t.isDark(n,o)&&(g+=1);return e+=Math.abs(100*g/r/r-50)/5*10},o),C=function(){for(var t=new Array(256),r=new Array(256),e=0;e<8;e+=1)t[e]=1<<e;for(e=8;e<256;e+=1)t[e]=t[e-4]^t[e-5]^t[e-6]^t[e-8];for(e=0;e<255;e+=1)r[t[e]]=e;var n={glog:function(t){if(t<1)throw"glog("+t+")";return r[t]},gexp:function(r){for(;r<0;)r+=255;for(;r>=256;)r-=255;return t[r]}};return n}();function k(t,r){if(void 0===t.length)throw t.length+"/"+r;var e=function(){for(var e=0;e<t.length&&0==t[e];)e+=1;for(var n=new Array(t.length-e+r),o=0;o<t.length-e;o+=1)n[o]=t[o+e];return n}(),n={getAt:function(t){return e[t]},getLength:function(){return e.length},multiply:function(t){for(var r=new Array(n.getLength()+t.getLength()-1),e=0;e<n.getLength();e+=1)for(var o=0;o<t.getLength();o+=1)r[e+o]^=C.gexp(C.glog(n.getAt(e))+C.glog(t.getAt(o)));return k(r,0)},mod:function(t){if(n.getLength()-t.getLength()<0)return n;for(var r=C.glog(n.getAt(0))-C.glog(t.getAt(0)),e=new Array(n.getLength()),o=0;o<n.getLength();o+=1)e[o]=n.getAt(o);for(o=0;o<t.getLength();o+=1)e[o]^=C.gexp(C.glog(t.getAt(o))+r);return k(e,0).mod(t)}};return n}var A=function(){var t=[[1,26,19],[1,26,16],[1,26,13],[1,26,9],[1,44,34],[1,44,28],[1,44,22],[1,44,16],[1,70,55],[1,70,44],[2,35,17],[2,35,13],[1,100,80],[2,50,32],[2,50,24],[4,25,9],[1,134,108],[2,67,43],[2,33,15,2,34,16],[2,33,11,2,34,12],[2,86,68],[4,43,27],[4,43,19],[4,43,15],[2,98,78],[4,49,31],[2,32,14,4,33,15],[4,39,13,1,40,14],[2,121,97],[2,60,38,2,61,39],[4,40,18,2,41,19],[4,40,14,2,41,15],[2,146,116],[3,58,36,2,59,37],[4,36,16,4,37,17],[4,36,12,4,37,13],[2,86,68,2,87,69],[4,69,43,1,70,44],[6,43,19,2,44,20],[6,43,15,2,44,16],[4,101,81],[1,80,50,4,81,51],[4,50,22,4,51,23],[3,36,12,8,37,13],[2,116,92,2,117,93],[6,58,36,2,59,37],[4,46,20,6,47,21],[7,42,14,4,43,15],[4,133,107],[8,59,37,1,60,38],[8,44,20,4,45,21],[12,33,11,4,34,12],[3,145,115,1,146,116],[4,64,40,5,65,41],[11,36,16,5,37,17],[11,36,12,5,37,13],[5,109,87,1,110,88],[5,65,41,5,66,42],[5,54,24,7,55,25],[11,36,12,7,37,13],[5,122,98,1,123,99],[7,73,45,3,74,46],[15,43,19,2,44,20],[3,45,15,13,46,16],[1,135,107,5,136,108],[10,74,46,1,75,47],[1,50,22,15,51,23],[2,42,14,17,43,15],[5,150,120,1,151,121],[9,69,43,4,70,44],[17,50,22,1,51,23],[2,42,14,19,43,15],[3,141,113,4,142,114],[3,70,44,11,71,45],[17,47,21,4,48,22],[9,39,13,16,40,14],[3,135,107,5,136,108],[3,67,41,13,68,42],[15,54,24,5,55,25],[15,43,15,10,44,16],[4,144,116,4,145,117],[17,68,42],[17,50,22,6,51,23],[19,46,16,6,47,17],[2,139,111,7,140,112],[17,74,46],[7,54,24,16,55,25],[34,37,13],[4,151,121,5,152,122],[4,75,47,14,76,48],[11,54,24,14,55,25],[16,45,15,14,46,16],[6,147,117,4,148,118],[6,73,45,14,74,46],[11,54,24,16,55,25],[30,46,16,2,47,17],[8,132,106,4,133,107],[8,75,47,13,76,48],[7,54,24,22,55,25],[22,45,15,13,46,16],[10,142,114,2,143,115],[19,74,46,4,75,47],[28,50,22,6,51,23],[33,46,16,4,47,17],[8,152,122,4,153,123],[22,73,45,3,74,46],[8,53,23,26,54,24],[12,45,15,28,46,16],[3,147,117,10,148,118],[3,73,45,23,74,46],[4,54,24,31,55,25],[11,45,15,31,46,16],[7,146,116,7,147,117],[21,73,45,7,74,46],[1,53,23,37,54,24],[19,45,15,26,46,16],[5,145,115,10,146,116],[19,75,47,10,76,48],[15,54,24,25,55,25],[23,45,15,25,46,16],[13,145,115,3,146,116],[2,74,46,29,75,47],[42,54,24,1,55,25],[23,45,15,28,46,16],[17,145,115],[10,74,46,23,75,47],[10,54,24,35,55,25],[19,45,15,35,46,16],[17,145,115,1,146,116],[14,74,46,21,75,47],[29,54,24,19,55,25],[11,45,15,46,46,16],[13,145,115,6,146,116],[14,74,46,23,75,47],[44,54,24,7,55,25],[59,46,16,1,47,17],[12,151,121,7,152,122],[12,75,47,26,76,48],[39,54,24,14,55,25],[22,45,15,41,46,16],[6,151,121,14,152,122],[6,75,47,34,76,48],[46,54,24,10,55,25],[2,45,15,64,46,16],[17,152,122,4,153,123],[29,74,46,14,75,47],[49,54,24,10,55,25],[24,45,15,46,46,16],[4,152,122,18,153,123],[13,74,46,32,75,47],[48,54,24,14,55,25],[42,45,15,32,46,16],[20,147,117,4,148,118],[40,75,47,7,76,48],[43,54,24,22,55,25],[10,45,15,67,46,16],[19,148,118,6,149,119],[18,75,47,31,76,48],[34,54,24,34,55,25],[20,45,15,61,46,16]],r=function(t,r){var e={};return e.totalCount=t,e.dataCount=r,e},e={};return e.getRSBlocks=function(e,n){var o=function(r,e){switch(e){case g.L:return t[4*(r-1)+0];case g.M:return t[4*(r-1)+1];case g.Q:return t[4*(r-1)+2];case g.H:return t[4*(r-1)+3];default:return}}(e,n);if(void 0===o)throw"bad rs block @ typeNumber:"+e+"/errorCorrectionLevel:"+n;for(var i=o.length/3,a=[],u=0;u<i;u+=1)for(var f=o[3*u+0],c=o[3*u+1],l=o[3*u+2],h=0;h<f;h+=1)a.push(r(c,l));return a},e}(),b=function(){var t=[],r=0,e={getBuffer:function(){return t},getAt:function(r){var e=Math.floor(r/8);return 1==(t[e]>>>7-r%8&1)},put:function(t,r){for(var n=0;n<r;n+=1)e.putBit(1==(t>>>r-n-1&1))},getLengthInBits:function(){return r},putBit:function(e){var n=Math.floor(r/8);t.length<=n&&t.push(0),e&&(t[n]|=128>>>r%8),r+=1}};return e},M=function(t){var r=a,e=t,n={getMode:function(){return r},getLength:function(t){return e.length},write:function(t){for(var r=e,n=0;n+2<r.length;)t.put(o(r.substring(n,n+3)),10),n+=3;n<r.length&&(r.length-n==1?t.put(o(r.substring(n,n+1)),4):r.length-n==2&&t.put(o(r.substring(n,n+2)),7))}},o=function(t){for(var r=0,e=0;e<t.length;e+=1)r=10*r+i(t.charAt(e));return r},i=function(t){if("0"<=t&&t<="9")return t.charCodeAt(0)-"0".charCodeAt(0);throw"illegal char :"+t};return n},x=function(t){var r=u,e=t,n={getMode:function(){return r},getLength:function(t){return e.length},write:function(t){for(var r=e,n=0;n+1<r.length;)t.put(45*o(r.charAt(n))+o(r.charAt(n+1)),11),n+=2;n<r.length&&t.put(o(r.charAt(n)),6)}},o=function(t){if("0"<=t&&t<="9")return t.charCodeAt(0)-"0".charCodeAt(0);if("A"<=t&&t<="Z")return t.charCodeAt(0)-"A".charCodeAt(0)+10;switch(t){case" ":return 36;case"$":return 37;case"%":return 38;case"*":return 39;case"+":return 40;case"-":return 41;case".":return 42;case"/":return 43;case":":return 44;default:throw"illegal char :"+t}};return n},m=function(r){var e=f,n=t.stringToBytes(r),o={getMode:function(){return e},getLength:function(t){return n.length},write:function(t){for(var r=0;r<n.length;r+=1)t.put(n[r],8)}};return o},L=function(r){var e=c,n=t.stringToBytesFuncs.SJIS;if(!n)throw"sjis not supported.";!function(){var t=n("友");if(2!=t.length||38726!=(t[0]<<8|t[1]))throw"sjis not supported."}();var o=n(r),i={getMode:function(){return e},getLength:function(t){return~~(o.length/2)},write:function(t){for(var r=o,e=0;e+1<r.length;){var n=(255&r[e])<<8|255&r[e+1];if(33088<=n&&n<=40956)n-=33088;else{if(!(57408<=n&&n<=60351))throw"illegal char at "+(e+1)+"/"+n;n-=49472}n=192*(n>>>8&255)+(255&n),t.put(n,13),e+=2}if(e<r.length)throw"illegal char at "+(e+1)}};return i},D=function(){var t=[],r={writeByte:function(r){t.push(255&r)},writeShort:function(t){r.writeByte(t),r.writeByte(t>>>8)},writeBytes:function(t,e,n){e=e||0,n=n||t.length;for(var o=0;o<n;o+=1)r.writeByte(t[o+e])},writeString:function(t){for(var e=0;e<t.length;e+=1)r.writeByte(t.charCodeAt(e))},toByteArray:function(){return t},toString:function(){var r="";r+="[";for(var e=0;e<t.length;e+=1)e>0&&(r+=","),r+=t[e];return r+="]"}};return r},S=function(t){var r=t,e=0,n=0,o=0,i={read:function(){for(;o<8;){if(e>=r.length){if(0==o)return-1;throw"unexpected end of file./"+o}var t=r.charAt(e);if(e+=1,"="==t)return o=0,-1;t.match(/^\s$/)||(n=n<<6|a(t.charCodeAt(0)),o+=6)}var i=n>>>o-8&255;return o-=8,i}},a=function(t){if(65<=t&&t<=90)return t-65;if(97<=t&&t<=122)return t-97+26;if(48<=t&&t<=57)return t-48+52;if(43==t)return 62;if(47==t)return 63;throw"c:"+t};return i},I=function(t,r,e){for(var n=function(t,r){var e=t,n=r,o=new Array(t*r),i={setPixel:function(t,r,n){o[r*e+t]=n},write:function(t){t.writeString("GIF87a"),t.writeShort(e),t.writeShort(n),t.writeByte(128),t.writeByte(0),t.writeByte(0),t.writeByte(0),t.writeByte(0),t.writeByte(0),t.writeByte(255),t.writeByte(255),t.writeByte(255),t.writeString(","),t.writeShort(0),t.writeShort(0),t.writeShort(e),t.writeShort(n),t.writeByte(0);var r=a(2);t.writeByte(2);for(var o=0;r.length-o>255;)t.writeByte(255),t.writeBytes(r,o,255),o+=255;t.writeByte(r.length-o),t.writeBytes(r,o,r.length-o),t.writeByte(0),t.writeString(";")}},a=function(t){for(var r=1<<t,e=1+(1<<t),n=t+1,i=u(),a=0;a<r;a+=1)i.add(String.fromCharCode(a));i.add(String.fromCharCode(r)),i.add(String.fromCharCode(e));var f,c,g,l=D(),h=(f=l,c=0,g=0,{write:function(t,r){if(t>>>r!=0)throw"length over";for(;c+r>=8;)f.writeByte(255&(t<<c|g)),r-=8-c,t>>>=8-c,g=0,c=0;g|=t<<c,c+=r},flush:function(){c>0&&f.writeByte(g)}});h.write(r,n);var s=0,v=String.fromCharCode(o[s]);for(s+=1;s<o.length;){var d=String.fromCharCode(o[s]);s+=1,i.contains(v+d)?v+=d:(h.write(i.indexOf(v),n),i.size()<4095&&(i.size()==1<<n&&(n+=1),i.add(v+d)),v=d)}return h.write(i.indexOf(v),n),h.write(e,n),h.flush(),l.toByteArray()},u=function(){var t={},r=0,e={add:function(n){if(e.contains(n))throw"dup key:"+n;t[n]=r,r+=1},size:function(){return r},indexOf:function(r){return t[r]},contains:function(r){return void 0!==t[r]}};return e};return i}(t,r),o=0;o<r;o+=1)for(var i=0;i<t;i+=1)n.setPixel(i,o,e(i,o));var a=D();n.write(a);for(var u=function(){var t=0,r=0,e=0,n="",o={},i=function(t){n+=String.fromCharCode(a(63&t))},a=function(t){if(t<0);else{if(t<26)return 65+t;if(t<52)return t-26+97;if(t<62)return t-52+48;if(62==t)return 43;if(63==t)return 47}throw"n:"+t};return o.writeByte=function(n){for(t=t<<8|255&n,r+=8,e+=1;r>=6;)i(t>>>r-6),r-=6},o.flush=function(){if(r>0&&(i(t<<6-r),t=0,r=0),e%3!=0)for(var o=3-e%3,a=0;a<o;a+=1)n+="="},o.toString=function(){return n},o}(),f=a.toByteArray(),c=0;c<f.length;c+=1)u.writeByte(f[c]);return u.flush(),"data:image/gif;base64,"+u};return t}();qrcode.stringToBytesFuncs["UTF-8"]=function(t){return function(t){for(var r=[],e=0;e<t.length;e++){var n=t.charCodeAt(e);n<128?r.push(n):n<2048?r.push(192|n>>6,128|63&n):n<55296||n>=57344?r.push(224|n>>12,128|n>>6&63,128|63&n):(e++,n=65536+((1023&n)<<10|1023&t.charCodeAt(e)),r.push(240|n>>18,128|n>>12&63,128|n>>6&63,128|63&n))}return r}(t)},function(t){"function"==typeof define&&define.amd?define([],t):"object"==typeof exports&&(module.exports=t())}(function(){return qrcode});
function qrSvg(txt, taille){
  try{
    const qr=qrcode(0,"M"); qr.addData(String(txt)); qr.make();
    const n=qr.getModuleCount(), t=taille||96;
    let r="";
    for(let y=0;y<n;y++) for(let x=0;x<n;x++) if(qr.isDark(y,x)) r+='<rect x="'+x+'" y="'+y+'" width="1" height="1"/>';
    return '<svg class="cqr" viewBox="0 0 '+n+' '+n+'" width="'+t+'" height="'+t+'" xmlns="http://www.w3.org/2000/svg" shape-rendering="crispEdges"><rect width="'+n+'" height="'+n+'" fill="#fff"/><g fill="#000">'+r+"</g></svg>";
  }catch(e){ return ""; }
}
const C39={"0":"nnnwwnwnn","1":"wnnwnnnnw","2":"nnwwnnnnw","3":"wnwwnnnnn","4":"nnnwwnnnw","5":"wnnwwnnnn","6":"nnwwwnnnn","7":"nnnwnnwnw","8":"wnnwnnwnn","9":"nnwwnnwnn","A":"wnnnnwnnw","B":"nnwnnwnnw","C":"wnwnnwnnn","D":"nnnnwwnnw","E":"wnnnwwnnn","F":"nnwnwwnnn","G":"nnnnnwwnw","H":"wnnnnwwnn","I":"nnwnnwwnn","J":"nnnnwwwnn","K":"wnnnnnnww","L":"nnwnnnnww","M":"wnwnnnnwn","N":"nnnnwnnww","O":"wnnnwnnwn","P":"nnwnwnnwn","Q":"nnnnnnwww","R":"wnnnnnwwn","S":"nnwnnnwwn","T":"nnnnwnwwn","U":"wwnnnnnnw","V":"nwwnnnnnw","W":"wwwnnnnnn","X":"nwnnwnnnw","Y":"wwnnwnnnn","Z":"nwwnwnnnn","-":"nwnnnnwnw",".":"wwnnnnwnn"," ":"nwwnnnwnn","*":"nwnnwnwnn"};
function barcode39(txt, h){
  h=h||44;
  const chars=("*"+String(txt).toUpperCase().replace(/[^0-9A-Z\-\. ]/g,"")+"*").split("");
  let x=0, bars=[];
  for(const ch of chars){
    const pat=C39[ch]; if(!pat) continue;
    for(let i=0;i<9;i++){ const wdt=pat[i]==="w"?3:1; if(i%2===0) bars.push([x,wdt]); x+=wdt; }
    x+=1; /* espace inter-caractère */
  }
  const W=x-1;
  return `<svg class="cbar" viewBox="0 0 ${W} ${h}" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg" shape-rendering="crispEdges">${
    bars.map(([bx,bw])=>`<rect x="${bx}" y="0" width="${bw}" height="${h}" fill="#000"/>`).join("")}</svg>`;
}
function carteHtml(c){
  const dm=c.demobilisation;
  const qr=cfg("carteQR")!==false?qrSvg("PNDDRR|"+dm.carte+"|"+authCode(c),48):"";
  const cb=cfg("carteCodeBarres")!==false?`${barcode39(dm.carte,22)}<div class="cbar-lbl">${esc(dm.carte)}</div>`:"";
  return `
    <div class="carte-pvc recto">
      <div class="ch chc"><div class="emb">${ARM_SVG}</div>
        <div class="tx">
          <b>RÉPUBLIQUE CENTRAFRICAINE</b>
          <small>UEPNDDR</small>
          <div class="ct">CARTE DE DÉMOBILISÉ</div>
          <small>N° ${esc(dm.carte)}</small>
        </div>
      </div>
      <div class="cb">
        <div class="ph">${c.photo?`<img src="${c.photo}">`:"PHOTO"}</div>
        <table>
          <tr><td>Nom</td><td>${esc(c.nom)}</td></tr>
          <tr><td>Prénom(s)</td><td>${esc(c.prenom)}</td></tr>
          <tr><td>Né(e) le</td><td>${fmtD(c.dn)}</td></tr>
          <tr><td>Sexe</td><td>${c.sexe==="M"?"M":"F"}</td></tr>
          <tr><td>Dossier</td><td>${esc(c.num)}</td></tr>
          <tr><td>Démobilisé</td><td>${fmtD(dm.date)}</td></tr>
        </table>
      </div>
      <div class="cauth">AUTH. <b>${authCode(c)}</b></div>
      <div class="cf"><span>${esc(dm.lieu)}</span><span>${esc(cfg("signataireCarte")||"Le Coordonnateur")}</span></div>
    </div>
    <div class="carte-pvc verso">
      <div class="ch"><b>DISPOSITIONS</b></div>
      <div class="cb verso-body">
        <p>Carte personnelle attestant la démobilisation officielle au titre du PNDDRR. En cas de perte, prévenir l'antenne la plus proche.</p>
        ${qr||cb?`<div class="cbar-box">${qr?`<div class="cqr-wrap">${qr}</div>`:""}${cb?`<div class="cbar-wrap">${cb}</div>`:""}</div>`:""}
      </div>
      <div class="cf"><span>N° ${esc(dm.carte)}</span><span>République Centrafricaine</span></div>
    </div>`;
}
function printCarte(id){
  const c=DB.combattants.find(x=>x.id===id);
  doPrint(carteHtml(c), "pvc");
  log("Impression",`Carte de démobilisé ${c.demobilisation.carte}`);
}
var DOC_Q="";
function docSearch(v){ DOC_Q=v; rDocs(); const el=$("doc_q"); if(el){ el.focus(); const n=el.value.length; try{el.setSelectionRange(n,n);}catch(e){} } }
function docMatch(c){
  const q=DOC_Q.trim().toLowerCase(); if(!q) return true;
  return [c.num,c.nom,c.prenom,c.alias,c.vague,c.prefecture,c.groupe,c.demobilisation?c.demobilisation.carte:""].join(" ").toLowerCase().includes(q);
}
function rDocs(){
  const cartes=DB.combattants.filter(c=>c.demobilisation&&docMatch(c)).sort((a,b)=>a.demobilisation.carte.localeCompare(b.demobilisation.carte));
  const attests=DB.combattants.filter(c=>c.desarmement&&docMatch(c)).sort((a,b)=>a.num.localeCompare(b.num));
  $("view").innerHTML = `
  <div class="toolbar">
    <div class="field" style="flex:1;max-width:520px"><label>Recherche</label><input id="doc_q" value="${esc(DOC_Q)}" oninput="docSearch(this.value)" placeholder="Nom, n° dossier, n° de carte, vague, préfecture…"></div>
    ${DOC_Q?`<button class="btn ghost" style="align-self:end" onclick="DOC_Q='';rDocs()">✕ Effacer</button>`:""}
  </div>
  <div class="cards">
    <div class="kpi c-dem"><div class="n">${cartes.length}</div><div class="l">Cartes de démobilisé délivrées</div></div>
    <div class="kpi c-des"><div class="n">${attests.length}</div><div class="l">Attestations de désarmement</div></div>
  </div>
  <div class="panel"><div class="ph"><h3>Vérifier l'authenticité d'une carte</h3></div><div class="pb">
    <div class="toolbar" style="margin-bottom:0">
      <div class="field" style="max-width:230px"><label>N° de carte</label><input id="ver_num" placeholder="DEM-2026-0001"></div>
      <div class="field" style="max-width:230px"><label>Code d'authentification</label><input id="ver_code" placeholder="XXXX-XXXX-XXXX"></div>
      <button class="btn" style="align-self:end" onclick="verifCarte()">Vérifier</button>
    </div>
    <div id="verRes" style="margin-top:12px"></div>
    <p class="small muted" style="margin-top:10px">Le code figure au recto de chaque carte. La vérification fonctionne hors ligne, sur tout poste disposant de l'application, et est consignée au journal.</p>
  </div></div>
  <div class="panel"><div class="ph"><h3>Partie 1 — Cartes de démobilisé (${cartes.length})</h3>
    <span style="display:flex;gap:7px">
      <button class="btn sm sec" onclick="exportCartesCSV()">Exporter CSV</button>
      <button class="btn sm sec" ${cartes.length?"":"disabled"} onclick="printToutesCartes()">Imprimer toutes les cartes</button>
    </span></div>
    <p class="small muted" style="margin:0;padding:10px 16px 0">Impression : <b>8,5 × 5,5 cm</b> (carte PVC, une face par page). Dans la boîte d'impression, choisir le format 85 × 55 mm et les marges à zéro.</p>
    <div class="pb nopad">${
    cartes.length?`<table><thead><tr><th>N° de carte</th><th>Titulaire</th><th>Dossier</th><th>Démobilisé le</th><th>Lieu</th><th>Vague</th><th>Actions</th></tr></thead><tbody>${
      cartes.map(c=>`<tr><td><b>${esc(c.demobilisation.carte)}</b></td>
      <td><span class="link" onclick="go('fiche','${c.id}')">${esc(c.nom)} ${esc(c.prenom)}</span></td>
      <td>${c.num}</td><td>${fmtD(c.demobilisation.date)}</td><td class="small">${esc(c.demobilisation.lieu)}</td>
      <td class="small">${c.vague?`<span class="tag">${esc(c.vague)}</span>`:"—"}</td>
      <td class="actions-cell"><button class="btn sm sec" onclick="printCarte('${c.id}')">Voir / imprimer la carte</button></td></tr>`).join("")
    }</tbody></table>`:`<div class="empty">Aucune carte de démobilisé${DOC_Q?" ne correspond à la recherche":" délivrée pour le moment"}.</div>`}</div></div>
  <div class="panel"><div class="ph"><h3>Partie 2 — Attestations de désarmement (${attests.length})</h3>
    <span style="display:flex;gap:7px">
      <button class="btn sm sec" onclick="exportAttestsCSV()">Exporter CSV</button>
      <button class="btn sm sec" ${attests.length?"":"disabled"} onclick="printToutesAttests()">Imprimer toutes les attestations</button>
    </span></div><div class="pb nopad">${
    attests.length?`<table><thead><tr><th>Dossier</th><th>Titulaire</th><th>Désarmé le</th><th>Lieu</th><th>Armes</th><th>Munitions</th><th>Vague</th><th>Actions</th></tr></thead><tbody>${
      attests.map(c=>`<tr><td><b>${c.num}</b></td>
      <td><span class="link" onclick="go('fiche','${c.id}')">${esc(c.nom)} ${esc(c.prenom)}</span></td>
      <td>${fmtD(c.desarmement.date)}</td><td class="small">${esc(c.desarmement.lieu)}</td>
      <td>${c.desarmement.armes.length}</td><td>${(c.desarmement.munitions||[]).length}</td>
      <td class="small">${c.vague?`<span class="tag">${esc(c.vague)}</span>`:"—"}</td>
      <td class="actions-cell"><button class="btn sm sec" onclick="printAttestation('${c.id}')">Voir / imprimer l'attestation</button></td></tr>`).join("")
    }</tbody></table>`:`<div class="empty">Aucune attestation${DOC_Q?" ne correspond à la recherche":" — les attestations sont produites au désarmement"}.</div>`}</div></div>`;
}
function savePoste(){
  DB.poste=$("posteNom").value.trim();
  DB.posteCode=$("posteCodeInp").value.trim().toUpperCase().replace(/[^A-Z0-9]/g,"").slice(0,5);
  persist(); log("Synchronisation",`Poste : ${DB.poste||"(vide)"} — code ${DB.posteCode||"(aucun)"}`);
  toast("Identification du poste enregistrée."); rSauvegarde();
}
function genSecret(){
  openModal(DB.secret?"Renouveler le secret d'installation":"Générer le secret d'installation", `
    <p>Le secret est une clé aléatoire propre à votre installation. ${DB.secret?"Le renouveler change les codes des <b>futures</b> cartes ; les cartes déjà imprimées restent vérifiables (repli sur les codes antérieurs au secret).":"Une fois généré, les nouveaux codes d'authentification en dépendent."}</p>
    <p class="small muted" style="margin-top:8px">Pensez à exporter une sauvegarde JSON vers les autres postes après cette opération, afin qu'ils vérifient les mêmes codes.</p>`,
    `<button class="btn ghost" onclick="closeModal()">Annuler</button><button class="btn" onclick="doGenSecret()">Générer</button>`);
}
function doGenSecret(){
  if(CUR.role!=="admin"){ toast("Réservé à l'administrateur."); return; }
  let al="ABCDEFGHJKLMNPQRSTUVWXYZ23456789", sec="";
  const rnd=new Uint32Array(24);
  try{ crypto.getRandomValues(rnd); }catch(e){ for(let i=0;i<24;i++) rnd[i]=Math.floor(Math.random()*4294967296); }
  for(let i=0;i<24;i++) sec+=al[rnd[i]%al.length];
  DB.secret=sec; persist();
  log("Sécurité","Secret d'installation "+(DB.secret?"renouvelé":"généré"));
  closeModal(); toast("Secret d'installation enregistré."); rSauvegarde();
}
function verifCarte(){
  const num=$("ver_num").value.trim().toUpperCase();
  const code=$("ver_code").value.trim().toUpperCase().replace(/[^A-Z0-9]/g,"");
  if(!num||!code){ toast("Saisissez le n° de carte et le code d'authentification."); return; }
  const c=DB.combattants.find(x=>x.demobilisation&&x.demobilisation.carte.toUpperCase()===num);
  /* accepte le code du secret courant, ou l'ancien pour les cartes imprimées avant sa définition */
  const ok = c && (authCode(c).replace(/-/g,"")===code || authCode(c,true).replace(/-/g,"")===code);
  if(!c){
    $("verRes").innerHTML=`<div style="background:#FDF1F1;border:1px solid #E3B4B4;border-radius:8px;padding:12px"><b style="color:var(--danger)">✘ Carte inconnue.</b> Aucune carte n° <b>${esc(num)}</b> n'existe dans ce registre. Vérifiez le numéro ou synchronisez les données du poste émetteur.</div>`;
  } else if(!ok){
    $("verRes").innerHTML=`<div style="background:#FDF1F1;border:1px solid #E3B4B4;border-radius:8px;padding:12px"><b style="color:var(--danger)">✘ Code invalide.</b> Le code saisi ne correspond pas à la carte n° <b>${esc(num)}</b>. La carte présentée est susceptible d'être falsifiée — retenir la carte et en référer à l'UEPNDDR.</div>`;
  } else {
    $("verRes").innerHTML=`<div style="background:#EAF7EE;border:1px solid #9ED0AC;border-radius:8px;padding:12px;display:flex;gap:14px;align-items:center">
      <div style="width:64px;height:76px;border:1px solid var(--line);flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:9px;color:var(--muted);overflow:hidden">${c.photo?`<img src="${c.photo}" style="width:100%;height:100%;object-fit:cover">`:"Photo"}</div>
      <div><b style="color:var(--ok)">✔ Carte authentique.</b><br>
      <b>${esc(c.nom)} ${esc(c.prenom)}</b>${c.alias?` — alias « ${esc(c.alias)} »`:""}, ${c.sexe==="M"?"né":"née"} le ${fmtD(c.dn)}<br>
      <span class="small">Dossier ${c.num} · démobilisé(e) le ${fmtD(c.demobilisation.date)} à ${esc(c.demobilisation.lieu)} · statut actuel : <span class="badge st-${c.statut}">${STATUTS[c.statut].lbl}</span></span><br>
      <span class="link small" onclick="go('fiche','${c.id}')">Ouvrir le dossier complet</span></div></div>`;
  }
  log("Vérification de carte",`${num} — ${!c?"carte inconnue":ok?"authentique ("+c.num+")":"code invalide"}`);
}
function exportCartesCSV(){
  const rows=[["N° de carte","Nom","Prénom","N° dossier","Démobilisé le","Lieu","Vague","Groupe armé","Préfecture"]];
  DB.combattants.filter(c=>c.demobilisation&&docMatch(c)).forEach(c=>rows.push([c.demobilisation.carte,c.nom,c.prenom,c.num,c.demobilisation.date,c.demobilisation.lieu,c.vague||"",c.groupe,c.prefecture]));
  dl(`pnddrr_cartes_demobilise_${today()}.csv`, csv(rows), "text/csv"); log("Export CSV","Cartes de démobilisé");
}
function exportAttestsCSV(){
  const rows=[["N° dossier","Nom","Prénom","Désarmé le","Lieu","Armes","Lots de munitions","Vague","Groupe armé","Préfecture"]];
  DB.combattants.filter(c=>c.desarmement&&docMatch(c)).forEach(c=>rows.push([c.num,c.nom,c.prenom,c.desarmement.date,c.desarmement.lieu,c.desarmement.armes.length,(c.desarmement.munitions||[]).length,c.vague||"",c.groupe,c.prefecture]));
  dl(`pnddrr_attestations_desarmement_${today()}.csv`, csv(rows), "text/csv"); log("Export CSV","Attestations de désarmement");
}
function printToutesCartes(){
  const L=DB.combattants.filter(c=>c.demobilisation&&docMatch(c));
  doPrint(L.map(c=>carteHtml(c)).join(""), "pvc");
  log("Impression",`${L.length} carte(s) de démobilisé (lot)`);
}
function printToutesAttests(){
  const L=DB.combattants.filter(c=>c.desarmement&&docMatch(c));
  doPrint(L.map(c=>attestationHtml(c)).join('<div style="page-break-after:always"></div>'));
  log("Impression",`${L.length} attestation(s) de désarmement (lot)`);
}
function printRegArmes(){
  const A=allArmes(), MU=allMunitions();
  doPrint(docWrap(`${docEntete("Registre des armes et matériels collectés")}
    <h2 class="titre">Registre des armes collectées</h2>
    <p class="small">Arrêté au ${new Date().toLocaleDateString("fr-FR")} — ${A.length} arme(s) et lot(s) enregistrés.</p>
    <table class="dt"><tr><th>N°</th><th>Type</th><th>Marque</th><th>N° série</th><th>État</th><th>Garde</th><th>Remise par</th><th>Dossier</th><th>Date</th></tr>${
      A.map((a,i)=>`<tr><td>${i+1}</td><td>${esc(a.type)}</td><td>${esc(a.marque)||"—"}</td><td>${esc(a.serie)||"—"}</td><td>${esc(a.etat)}</td><td>${GARDE_LBL[(a.garde&&a.garde.etat)||"depot"]}${a.garde&&a.garde.scelle?" · "+esc(a.garde.scelle):""}${a.garde&&a.garde.destruction&&a.garde.destruction.pv?" · "+esc(a.garde.destruction.pv):""}</td><td>${esc(a.nom)}</td><td>${a.num}</td><td>${fmtD(a.date)}</td></tr>`).join("")}</table>
    ${MU.length?`<h3 style="font-size:13px;margin-top:14px;text-decoration:underline">Munitions et explosifs collectés (${MU.length} lot(s))</h3>
    <table class="dt"><tr><th>N°</th><th>Nature / calibre</th><th>Quantité</th><th>Unité</th><th>Remise par</th><th>Dossier</th><th>Date</th></tr>${
      MU.map((m,i)=>`<tr><td>${i+1}</td><td>${esc(m.nature)||"—"}</td><td>${fmtN(m.qte)}</td><td>${esc(m.unite)}</td><td>${esc(m.nom)}</td><td>${m.num}</td><td>${fmtD(m.date)}</td></tr>`).join("")}</table>`:""}
    <div class="sig"><div class="c"></div><div class="c">Fait à ${esc(cfg("villeSignature")||"Bangui")}, le ${new Date().toLocaleDateString("fr-FR")}<br>Pour le PNDDRR<div class="ligne">${esc(CUR.nom)}</div></div></div>`));
  log("Impression","Registre des armes");
}



/* module: reintegration/suivi.js — PNDDRR engine (classic globals) */
/* ================= PARTIE 2 — SUIVI DES RÉINTÉGRATIONS ================= */
var ATT_F="", REI_Q="";
function attSet(v){ ATT_F=v; rReint(); }
function reiSearch(v){
  REI_Q=v; rReint();
  const el=$("rei_q"); if(el){ el.focus(); const n=el.value.length; try{el.setSelectionRange(n,n);}catch(e){} }
}
function reiMatch(c){
  const q=REI_Q.trim().toLowerCase(); if(!q) return true;
  const r=reintOf(c);
  const txt=[c.num,c.nom,c.prenom,c.alias,c.vague,c.prefecture,c.groupe,c.souhait,
    r&&r.promo||"", c.reintMil?c.reintMil.corps:"", c.reintMil?c.reintMil.unite:"", c.reintMil?c.reintMil.matricule:"",
    c.reintSocio?c.reintSocio.filiere:"", c.reintSocio?c.reintSocio.centre:""].join(" ").toLowerCase();
  return txt.includes(q);
}
function attFiltre(){
  const attente=DB.combattants.filter(c=>["desarme","demobilise"].includes(c.statut));
  return ATT_F?attente.filter(c=>c.souhait===ATT_F):attente;
}
function exportAttenteCSV(){
  const rows=[["N° dossier","Nom","Prénom","Sexe","Âge","Statut","Souhait de réintégration","Vague","Groupe armé","Préfecture","Sous-préfecture","Commune","Désarmé le","Téléphone"]];
  attFiltre().forEach(c=>rows.push([c.num,c.nom,c.prenom,c.sexe,age(c.dn),STATUTS[c.statut].lbl,c.souhait,c.vague||"",c.groupe,c.prefecture,c.sousPref||"",c.commune||"",c.desarmement?c.desarmement.date:"",c.tel||""]));
  dl(`pnddrr_attente_orientation${ATT_F?"_"+ATT_F.toLowerCase().replace(/[^a-z]+/g,"-"):""}_${today()}.csv`, csv(rows), "text/csv");
  log("Export CSV",`En attente d'orientation${ATT_F?" — souhait "+ATT_F:""} (${attFiltre().length})`);
  toast("Liste d'attente exportée.");
}
function printAttente(){
  const L=attFiltre();
  doPrint(docWrap(`${docEntete("Liste des ex-combattants en attente d'orientation")}
    <h2 class="titre">En attente d'orientation${ATT_F?` — souhait : ${esc(ATT_F)}`:""}</h2>
    <p>${L.length} ex-combattant(s) désarmé(s) ou démobilisé(s) en attente d'orientation${ATT_F?` ayant exprimé le souhait « ${esc(ATT_F)} »`:""}, arrêté au ${new Date().toLocaleDateString("fr-FR")}.</p>
    <table class="dt"><tr><th>N°</th><th>Dossier</th><th>Nom & prénom</th><th>Statut</th><th>Souhait</th><th>Vague</th><th>Préfecture</th></tr>${
      L.map((c,i)=>`<tr><td>${i+1}</td><td>${c.num}</td><td>${esc(c.nom)} ${esc(c.prenom)}</td><td>${STATUTS[c.statut].lbl}</td><td>${esc(c.souhait)}</td><td>${esc(c.vague||"—")}</td><td>${esc(c.prefecture)}</td></tr>`).join("")}</table>
    <div class="sig"><div class="c"></div><div class="c">Fait à ${esc(cfg("villeSignature")||"Bangui")}, le ${new Date().toLocaleDateString("fr-FR")}<br>Pour le PNDDRR<div class="ligne">${esc(CUR.nom)}</div></div></div>`));
  log("Impression",`Liste d'attente d'orientation${ATT_F?" — "+ATT_F:""}`);
}
function vagueDe(c){ const r=reintOf(c); return (r&&r.promo)||c.vague||"(sans vague)"; }
function grpVagues(list){
  const m={}; list.forEach(c=>{ const v=vagueDe(c); (m[v]=m[v]||[]).push(c); });
  return Object.keys(m).sort().map(v=>[v,m[v]]);
}
function vgRow(v,n,cols){ return `<tr style="background:var(--teal-light)"><td colspan="${cols}" style="font-weight:700;color:var(--teal-dark);padding:7px 12px">▸ ${esc(v)} — ${n} dossier(s)</td></tr>`; }
function rReint(){
  const C=DB.combattants;
  const attente=attFiltre().filter(reiMatch);
  const enCours=C.filter(c=>["reintegration_militaire","reintegration_socio"].includes(c.statut)&&reiMatch(c));
  const enFormation=enCours.filter(c=>!reintOf(c).formFin);
  const enReint=enCours.filter(c=>reintOf(c).formFin);
  const acheves=C.filter(c=>c.statut==="reintegre"&&reiMatch(c));
  const visites=C.reduce((a,c)=>a+(c.reintSocio?c.reintSocio.visites.length:0),0);
  const A=hasPerm("orienter");
  $("view").innerHTML = `
  <div class="toolbar">
    <div class="field" style="flex:1;max-width:520px"><label>Recherche</label><input id="rei_q" value="${esc(REI_Q)}" oninput="reiSearch(this.value)" placeholder="Nom, n° dossier, matricule, filière, unité, vague, groupe, préfecture…"></div>
    ${REI_Q?`<button class="btn ghost" style="align-self:end" onclick="REI_Q='';rReint()">✕ Effacer (${attente.length+enCours.length+acheves.length} résultat(s))</button>`:""}
  </div>
  <div class="cards">
    <div class="kpi c-dem"><div class="n">${attente.length}</div><div class="l">En attente d'orientation</div></div>
    <div class="kpi c-rm"><div class="n">${enFormation.length}</div><div class="l">En formation</div></div>
    <div class="kpi c-rs"><div class="n">${enReint.length}</div><div class="l">En réintégration (formation achevée)</div></div>
    <div class="kpi c-ok"><div class="n">${acheves.length}</div><div class="l">Parcours achevés</div></div>
    <div class="kpi"><div class="n">${visites}</div><div class="l">Visites de suivi réalisées</div></div>
    <div class="kpi c-dem"><div class="n">${enCours.filter(c=>reintOf(c).formFin).length}/${enCours.length}</div><div class="l">Formations achevées (en cours)</div></div>
    <div class="kpi c-ok"><div class="n">${enCours.filter(c=>reintOf(c).vieDate).length}/${enCours.length}</div><div class="l">Intégrés vie militaire / civile</div></div>
  </div>

  <div class="panel"><div class="ph"><h3>En attente d'orientation (${attente.length}${ATT_F?` — souhait : ${esc(ATT_F)}`:""})</h3>
    <span style="display:flex;gap:7px;align-items:center">
      <select onchange="attSet(this.value)" style="padding:6px 9px;border:1px solid var(--line);border-radius:7px;font-size:12.5px">
        <option value="">Tous les souhaits</option>
        <option value="Militaire" ${ATT_F==="Militaire"?"selected":""}>Militaire</option>
        <option value="Socio-économique" ${ATT_F==="Socio-économique"?"selected":""}>Socio-économique</option>
        <option value="Indécis" ${ATT_F==="Indécis"?"selected":""}>Indécis</option>
      </select>
      <button class="btn sm sec" onclick="exportAttenteCSV()">Exporter CSV</button>
      <button class="btn sm sec" onclick="printAttente()">Imprimer / PDF</button>
    </span></div><div class="pb nopad">${
    attente.length?`<table><thead><tr><th>N° dossier</th><th>Nom & prénom</th><th>Statut</th><th>Souhait exprimé</th><th>Préfecture</th><th>Actions</th></tr></thead><tbody>${
      grpVagues(attente).map(([v,L])=>vgRow(v,L.length,6)+L.map(c=>`<tr><td><b>${c.num}</b></td><td><span class="link" onclick="go('fiche','${c.id}')">${esc(c.nom)} ${esc(c.prenom)}</span></td>
      <td><span class="badge st-${c.statut}">${STATUTS[c.statut].lbl}</span></td><td>${esc(c.souhait)}</td><td class="small">${esc(c.prefecture)}</td>
      <td class="actions-cell">${A?`<button class="btn sm" onclick="mReintMil('${c.id}')">Militaire</button><button class="btn sm sec" onclick="mReintSocio('${c.id}')">Socio-éco.</button><button class="btn sm ghost" onclick="mRapatriement('${c.id}')">Rapatr.</button>`:'<span class="muted small">—</span>'}</td></tr>`).join("")).join("")
    }</tbody></table>`:`<div class="empty">Aucun dossier en attente d'orientation${ATT_F?` avec le souhait « ${esc(ATT_F)} » — <span class="link" onclick="attSet('')">afficher tous</span>`:""}.</div>`}</div></div>

  ${[["En formation — par vague",enFormation,"Les formations en cours (fin de formation non validée), regroupées par vague ; validation possible dossier par dossier ici ou par promotion entière depuis la page « Formation & intégration »."],
     ["En réintégration — par vague (formation achevée)",enReint,"Formation validée : confirmation de l'intégration à la vie militaire ou civile, visites de suivi et clôture, par vague."]].map(([titre,liste,note])=>`
  <div class="panel"><div class="ph"><h3>${titre} (${liste.length})</h3></div><div class="pb nopad">${
    liste.length?`<table><thead><tr><th>N° dossier</th><th>Nom & prénom</th><th>Type</th><th>Détail</th><th>Formation</th><th>Intégration</th><th>Visites</th><th>Actions</th></tr></thead><tbody>${
      grpVagues(liste).map(([v,L])=>vgRow(v,L.length,8)+L.map(c=>{
        const mil=c.statut==="reintegration_militaire";
        const r=reintOf(c);
        const det=mil?`${esc(c.reintMil.corps)}${c.reintMil.matricule?" — matricule "+esc(c.reintMil.matricule):""}`:`Filière ${esc(c.reintSocio.filiere)}${c.reintSocio.centre?" · "+esc(c.reintSocio.centre):""}`;
        const [bF,bV]=jalonBadge(c);
        const P=hasPerm("orienter");
        return `<tr><td><b>${c.num}</b></td><td><span class="link" onclick="go('fiche','${c.id}')">${esc(c.nom)} ${esc(c.prenom)}</span></td>
        <td><span class="badge st-${c.statut}">${mil?"Militaire":"Socio-éco."}</span></td><td class="small">${det}</td>
        <td>${bF}</td><td>${bV}</td>
        <td>${mil?"—":c.reintSocio.visites.length}</td>
        <td class="actions-cell">${!r.formFin&&P?`<button class="btn sm sec" title="Valider la fin de formation" onclick="mFormationFin('${c.id}')">Fin formation</button>`:""}${r.formFin&&!r.vieDate&&P?`<button class="btn sm sec" title="Confirmer l'intégration à la vie ${mil?"militaire":"civile"}" onclick="mIntegration('${c.id}')">Intégration</button>`:""}${!mil&&hasPerm("visites")?`<button class="btn sm sec" onclick="mVisite('${c.id}')">+ Visite</button>`:""}${hasPerm("cloturer")?`<button class="btn sm" onclick="markReintegre('${c.id}')">Clôturer</button>`:""}</td></tr>`;
      }).join("")).join("")
    }</tbody></table><div class="small muted" style="padding:9px 14px">${note}</div>`:`<div class="empty">Aucun parcours à cette étape.</div>`}</div></div>`).join("")}

  <div class="panel"><div class="ph"><h3>Parcours achevés (${acheves.length})</h3></div><div class="pb nopad">${
    acheves.length?`<table><thead><tr><th>N° dossier</th><th>Nom & prénom</th><th>Type de réintégration</th><th>Détail</th><th>Formation</th><th>Intégration</th><th>Achevé le</th></tr></thead><tbody>${
      grpVagues(acheves).map(([v,L])=>vgRow(v,L.length,7)+L.map(c=>{const [bF,bV]=jalonBadge(c);return `<tr><td><b>${c.num}</b></td><td><span class="link" onclick="go('fiche','${c.id}')">${esc(c.nom)} ${esc(c.prenom)}</span></td>
      <td>${c.reintMil?"Militaire":c.reintSocio?"Socio-économique":"—"}</td>
      <td class="small">${c.reintMil?esc(c.reintMil.corps):c.reintSocio?esc(c.reintSocio.filiere):"—"}</td>
      <td>${bF||"—"}</td><td>${bV||"—"}</td>
      <td>${fmtD(c.fin)}</td></tr>`;}).join("")).join("")
    }</tbody></table>`:`<div class="empty">Aucun parcours achevé pour le moment.</div>`}</div></div>`;
}


/* module: cartographie/carto.js — PNDDRR engine (classic globals) */
/* ================= PARTIE 3 — CARTOGRAPHIE DES ZONES DE DÉSARMEMENT ================= */
/* Coordonnées approchées (longitude, latitude) des chefs-lieux des 20 préfectures */
const PREF_COORD = {
  "Bangui":[18.55,4.36], "Bamingui-Bangoran":[20.65,8.41], "Basse-Kotto":[21.18,4.32],
  "Haut-Mbomou":[26.50,5.40], "Haute-Kotto":[21.99,6.54], "Kémo":[19.08,5.72],
  "Lim-Pendé":[16.44,7.24], "Lobaye":[17.99,3.87], "Mambéré":[15.87,4.94],
  "Mambéré-Kadéï":[15.79,4.26], "Mbomou":[22.82,4.74], "Nana-Grébizi":[19.19,6.99],
  "Nana-Mambéré":[15.60,5.93], "Ombella-M'Poko":[18.12,4.80], "Ouaka":[20.67,5.77],
  "Ouham":[17.45,6.49], "Ouham-Fafa":[18.30,7.30], "Ouham-Pendé":[16.38,6.32],
  "Sangha-Mbaéré":[16.04,3.53], "Vakaga":[22.79,10.28]
};
/* Contours réels des 20 préfectures — fond de carte Natural Earth (données ouvertes),
   simplifié pour usage hors-ligne. Les tracés des trois préfectures créées par la
   loi 21-001 (Lim-Pendé, Ouham-Fafa, Mambéré) sont indicatifs (marqués a:1),
   en attente du tracé officiel. */
const RCA_GEO=[{"n":"Vakaga","p":[[[22.861,10.919],[23.006,10.687],[23.291,10.44],[23.624,9.908],[23.674,9.69],[23.606,9.537],[23.646,9.425],[23.632,9.278],[23.549,9.185],[23.463,9.154],[23.461,9.236],[23.373,9.213],[23.314,9.028],[23.114,8.917],[23.05,8.756],[22.647,8.685],[22.5,8.582],[22.391,8.592],[22.375,8.667],[22.212,8.706],[22.123,8.778],[22.087,8.969],[21.852,9.185],[21.776,9.399],[21.702,9.5],[21.617,9.589],[21.523,9.602],[21.486,9.768],[21.451,9.77],[21.253,9.583],[21.055,9.524],[20.897,9.383],[20.814,9.419],[20.841,9.484],[20.983,9.604],[21.02,9.748],[21.105,9.774],[21.132,9.849],[21.271,9.987],[21.374,9.973],[21.513,10.201],[21.656,10.234],[21.719,10.297],[21.751,10.412],[21.705,10.532],[21.723,10.637],[21.864,10.668],[22.004,10.743],[22.049,10.837],[22.176,10.816],[22.253,10.915],[22.447,10.998],[22.683,10.974],[22.861,10.919]]]},{"n":"Haute-Kotto","p":[[[23.463,9.154],[23.438,8.995],[23.478,8.959],[23.543,8.997],[23.567,8.975],[23.554,8.883],[23.482,8.783],[23.505,8.711],[23.596,8.734],[23.721,8.702],[23.803,8.722],[24.236,8.682],[24.211,8.627],[24.251,8.58],[24.193,8.532],[24.122,8.372],[24.18,8.298],[24.407,8.166],[24.473,8.074],[24.453,7.933],[24.497,7.634],[24.594,7.485],[24.512,7.308],[24.516,7.249],[24.56,7.205],[24.545,7.012],[24.346,6.958],[24.322,6.933],[24.346,6.882],[24.287,6.82],[24.303,6.778],[23.452,6.302],[23.396,6.215],[23.319,6.209],[23.291,6.104],[22.905,6.185],[22.821,6.27],[22.56,6.245],[22.457,6.08],[22.37,6.039],[22.248,6.063],[22.107,5.98],[22.052,5.708],[22.086,5.643],[22.03,5.637],[22.034,5.601],[21.901,5.475],[21.663,5.549],[21.673,5.598],[21.751,5.67],[21.85,5.62],[21.898,5.672],[21.94,5.911],[21.885,6.033],[21.822,6.057],[21.791,6.168],[21.744,6.215],[21.809,6.34],[21.795,6.545],[21.704,6.593],[21.61,6.772],[21.653,6.881],[21.606,7.003],[21.577,7.286],[21.422,7.32],[21.4,7.527],[21.131,7.703],[21.104,7.849],[21.132,7.958],[21.153,7.985],[21.419,8.018],[21.393,8.163],[21.49,8.248],[21.597,8.239],[21.62,8.279],[21.741,8.329],[21.773,8.326],[21.864,8.205],[21.981,8.221],[21.96,8.292],[22.001,8.404],[21.958,8.525],[22.005,8.605],[22.075,8.618],[22.349,8.567],[22.436,8.598],[22.507,8.583],[22.647,8.685],[23.05,8.756],[23.114,8.917],[23.314,9.028],[23.373,9.213],[23.461,9.236],[23.463,9.154]]]},{"n":"Haut-Mbomou","p":[[[24.263,8.269],[24.332,8.246],[24.431,8.271],[24.513,8.207],[24.8,8.18],[24.918,8.087],[24.953,7.997],[25.029,7.919],[25.23,7.852],[25.279,7.659],[25.165,7.58],[25.19,7.501],[25.316,7.417],[25.36,7.336],[25.786,7.143],[25.801,7.105],[26.026,6.997],[26.091,6.83],[26.378,6.653],[26.27,6.466],[26.29,6.387],[26.452,6.28],[26.456,6.23],[26.509,6.205],[26.425,6.072],[26.481,6.105],[26.544,6.031],[26.776,5.982],[26.819,5.895],[26.894,5.89],[26.916,5.85],[26.981,5.859],[27.036,5.785],[27.124,5.769],[27.218,5.645],[27.217,5.585],[27.261,5.578],[27.218,5.426],[27.264,5.26],[27.441,5.071],[27.074,5.203],[26.962,5.151],[26.867,5.038],[26.808,5.039],[26.739,5.094],[26.503,5.047],[26.391,5.149],[26.272,5.159],[26.198,5.238],[26.13,5.259],[26.088,5.245],[26.095,5.211],[26.027,5.19],[25.972,5.228],[25.911,5.17],[25.872,5.217],[25.826,5.195],[25.792,5.263],[25.758,5.244],[25.656,5.318],[25.623,5.31],[25.581,5.375],[25.371,5.317],[25.308,5.185],[25.349,5.142],[25.317,5.042],[25.228,5.01],[25.129,5.019],[25.076,4.952],[24.958,4.991],[24.789,4.92],[24.664,4.924],[24.671,4.957],[24.601,5.026],[24.481,5.102],[24.557,5.118],[24.539,5.182],[24.571,5.179],[24.605,5.273],[24.696,5.26],[24.783,5.287],[24.833,5.345],[24.871,5.333],[24.914,5.435],[24.972,5.448],[24.963,5.479],[25.046,5.564],[25.02,5.574],[25.043,5.592],[25.044,5.762],[25.118,5.806],[25.144,5.773],[25.184,5.91],[24.85,6.219],[24.925,6.208],[24.997,6.24],[25.012,6.283],[25.072,6.304],[25.05,6.375],[24.867,6.494],[24.796,6.485],[24.694,6.4],[24.612,6.391],[24.261,6.753],[24.307,6.782],[24.287,6.82],[24.346,6.882],[24.322,6.933],[24.346,6.958],[24.545,7.012],[24.56,7.205],[24.516,7.249],[24.512,7.308],[24.594,7.485],[24.497,7.634],[24.453,7.933],[24.473,8.074],[24.407,8.166],[24.263,8.269]]]},{"n":"Kémo","p":[[[19.858,5.06],[19.72,5.136],[19.432,5.136],[19.237,5.011],[19.196,4.95],[19.107,4.932],[19.091,5.036],[18.991,5.186],[19.025,5.334],[18.992,5.412],[18.898,5.493],[18.865,5.627],[18.687,5.668],[18.635,5.731],[18.627,6.001],[18.911,6.2],[18.866,6.241],[18.86,6.357],[18.759,6.462],[18.752,6.514],[19.014,6.543],[19.09,6.465],[19.176,6.437],[19.468,6.498],[19.743,6.476],[19.754,6.42],[19.852,6.361],[19.871,6.323],[19.839,6.12],[19.784,6.041],[19.717,6.034],[19.596,5.918],[19.607,5.769],[19.662,5.743],[19.662,5.677],[19.471,5.541],[19.522,5.49],[20.015,5.465],[19.962,5.346],[19.934,5.133],[19.858,5.06]]]},{"n":"Ouaka","p":[[[20.457,4.525],[20.456,4.622],[20.339,4.772],[20.237,4.806],[20.172,4.878],[20.004,4.974],[19.891,5.002],[19.858,5.06],[19.934,5.133],[19.962,5.346],[20.015,5.465],[19.522,5.49],[19.471,5.549],[19.558,5.582],[19.662,5.677],[19.662,5.743],[19.607,5.769],[19.596,5.918],[19.717,6.034],[19.784,6.041],[19.839,6.12],[19.867,6.34],[19.754,6.42],[19.743,6.476],[19.877,6.451],[19.871,6.626],[20.102,6.88],[20.156,7.097],[20.24,7.034],[20.349,7.02],[20.445,7.021],[20.572,7.075],[20.644,7.138],[20.687,7.241],[20.794,7.232],[20.894,7.289],[21.075,7.507],[21.056,7.582],[21.131,7.703],[21.4,7.527],[21.422,7.32],[21.577,7.286],[21.606,7.003],[21.653,6.881],[21.61,6.772],[21.704,6.593],[21.795,6.545],[21.809,6.34],[21.744,6.215],[21.791,6.168],[21.822,6.057],[21.893,6.02],[21.94,5.911],[21.938,5.814],[21.858,5.621],[21.751,5.67],[21.673,5.598],[21.634,5.491],[21.588,5.465],[21.518,5.51],[21.482,5.63],[21.395,5.728],[21.232,5.65],[21.122,5.656],[21.037,5.715],[21.057,5.65],[21.025,5.553],[20.94,5.523],[20.988,5.42],[20.957,5.377],[20.957,5.231],[20.903,5.123],[20.937,5.023],[20.899,4.953],[20.895,4.813],[20.712,4.748],[20.57,4.59],[20.457,4.525]]]},{"n":"Basse-Kotto","p":[[[20.603,4.41],[20.457,4.525],[20.57,4.59],[20.712,4.748],[20.895,4.813],[20.899,4.953],[20.937,5.023],[20.903,5.123],[20.957,5.231],[20.957,5.377],[20.988,5.42],[20.94,5.523],[21.025,5.553],[21.057,5.65],[21.037,5.715],[21.122,5.656],[21.232,5.65],[21.395,5.728],[21.57,5.466],[21.634,5.491],[21.663,5.549],[21.901,5.475],[21.881,5.174],[21.792,5.088],[21.833,5.005],[21.782,4.929],[21.736,4.714],[21.916,4.643],[21.956,4.566],[21.942,4.533],[22.099,4.542],[22.147,4.512],[22.065,4.363],[22.037,4.229],[21.868,4.242],[21.723,4.295],[21.634,4.294],[21.538,4.245],[21.375,4.278],[21.288,4.333],[21.211,4.292],[21.114,4.341],[21.076,4.395],[20.872,4.453],[20.603,4.41]]]},{"n":"Mbomou","p":[[[24.431,5.067],[24.396,5.122],[24.361,5.063],[24.399,5.036],[24.297,5.003],[24.259,4.93],[24.21,4.956],[24.153,4.903],[24.09,4.92],[23.978,4.854],[23.951,4.868],[23.948,4.818],[23.817,4.821],[23.588,4.734],[23.448,4.659],[23.415,4.591],[23.265,4.635],[23.169,4.738],[23.099,4.712],[23.017,4.751],[22.977,4.834],[22.898,4.824],[22.854,4.711],[22.786,4.725],[22.724,4.629],[22.689,4.492],[22.592,4.474],[22.614,4.374],[22.539,4.278],[22.546,4.237],[22.423,4.135],[22.207,4.15],[22.037,4.229],[22.065,4.363],[22.147,4.512],[22.099,4.542],[21.942,4.533],[21.956,4.566],[21.916,4.643],[21.736,4.714],[21.782,4.929],[21.833,5.005],[21.792,5.088],[21.881,5.174],[21.902,5.488],[22.034,5.601],[22.03,5.637],[22.086,5.643],[22.052,5.708],[22.114,5.985],[22.248,6.063],[22.401,6.047],[22.472,6.093],[22.541,6.232],[22.686,6.269],[22.821,6.27],[22.905,6.185],[23.291,6.104],[23.319,6.209],[23.396,6.215],[23.452,6.302],[24.261,6.753],[24.612,6.391],[24.694,6.4],[24.796,6.485],[24.867,6.494],[25.05,6.375],[25.072,6.304],[25.012,6.283],[24.997,6.24],[24.925,6.208],[24.85,6.219],[25.184,5.91],[25.144,5.773],[25.118,5.806],[25.044,5.762],[25.043,5.592],[25.02,5.574],[25.045,5.561],[24.963,5.479],[24.972,5.448],[24.88,5.392],[24.871,5.333],[24.837,5.347],[24.783,5.287],[24.699,5.26],[24.605,5.273],[24.571,5.179],[24.539,5.182],[24.557,5.118],[24.437,5.101],[24.431,5.067]]]},{"n":"Ombella-M'Poko","p":[[[19.107,4.932],[19.012,4.765],[18.828,4.56],[18.753,4.401],[18.634,4.356],[18.561,4.438],[18.497,4.399],[18.633,4.13],[18.645,3.982],[18.612,3.867],[18.499,3.911],[18.411,3.994],[18.342,4.086],[18.342,4.224],[18.305,4.302],[18.205,4.344],[18.132,4.448],[17.95,4.478],[17.891,4.456],[17.765,4.534],[17.643,4.511],[17.586,4.534],[17.465,4.794],[17.32,4.874],[17.144,5.028],[17.037,5.024],[16.996,5.092],[17.118,5.165],[17.033,5.288],[16.932,5.362],[16.708,5.379],[16.526,5.275],[16.497,5.388],[16.515,5.568],[16.749,5.779],[16.979,5.859],[16.981,5.794],[17.044,5.72],[17.583,5.721],[17.913,5.896],[17.998,5.853],[18.027,5.801],[18.244,5.8],[18.275,5.749],[18.397,5.738],[18.373,5.686],[18.413,5.563],[18.601,5.652],[18.622,5.735],[18.687,5.668],[18.865,5.627],[18.898,5.493],[19.022,5.352],[18.991,5.186],[19.091,5.036],[19.107,4.932]]]},{"n":"Bangui","p":[[[18.596,4.371],[18.537,4.306],[18.493,4.388],[18.561,4.438],[18.596,4.371]]]},{"n":"Lobaye","p":[[[18.612,3.867],[18.593,3.71],[18.626,3.477],[18.57,3.49],[18.472,3.632],[18.38,3.574],[18.249,3.575],[18.182,3.477],[18.082,3.563],[17.969,3.538],[17.919,3.566],[17.856,3.537],[17.793,3.618],[17.587,3.632],[17.482,3.705],[17.414,3.683],[17.322,3.812],[16.934,3.977],[16.738,4.245],[16.869,4.427],[16.997,4.347],[17.067,4.346],[16.846,4.68],[16.83,4.868],[17.027,4.969],[17.037,5.024],[17.144,5.028],[17.32,4.874],[17.465,4.794],[17.586,4.534],[17.643,4.511],[17.765,4.534],[17.845,4.467],[17.95,4.478],[18.147,4.436],[18.205,4.344],[18.305,4.302],[18.342,4.224],[18.342,4.086],[18.411,3.994],[18.499,3.911],[18.612,3.867]]]},{"n":"Bamingui-Bangoran","p":[[[20.802,9.426],[20.897,9.383],[21.055,9.524],[21.253,9.583],[21.451,9.77],[21.495,9.765],[21.523,9.602],[21.617,9.589],[21.702,9.5],[21.776,9.399],[21.852,9.185],[22.087,8.969],[22.137,8.76],[22.228,8.699],[22.375,8.667],[22.391,8.592],[22.349,8.567],[22.075,8.618],[21.997,8.6],[21.958,8.525],[22.001,8.404],[21.96,8.292],[21.99,8.233],[21.96,8.212],[21.864,8.205],[21.76,8.332],[21.62,8.279],[21.597,8.239],[21.49,8.248],[21.384,8.146],[21.419,8.018],[21.153,7.985],[21.132,7.958],[21.104,7.849],[21.131,7.703],[21.056,7.582],[21.075,7.507],[20.876,7.271],[20.794,7.232],[20.687,7.241],[20.633,7.125],[20.463,7.027],[20.24,7.034],[19.921,7.247],[19.885,7.353],[19.763,7.448],[19.729,7.548],[19.592,7.568],[19.599,7.651],[19.566,7.67],[19.592,7.699],[19.386,7.774],[19.357,7.756],[19.321,7.805],[19.352,7.842],[19.32,7.882],[19.352,7.898],[19.345,8.021],[19.38,8.048],[19.354,8.064],[19.359,8.13],[19.235,8.278],[19.249,8.301],[19.154,8.405],[19.064,8.583],[19.072,8.632],[19.124,8.675],[18.87,8.864],[19.101,9.015],[19.7,9.021],[19.889,9.046],[20.11,9.151],[20.155,9.116],[20.213,9.145],[20.257,9.116],[20.378,9.12],[20.507,9.213],[20.496,9.271],[20.542,9.322],[20.668,9.302],[20.655,9.343],[20.758,9.377],[20.802,9.426]]]},{"n":"Ouham","p":[[[17.896,5.898],[17.583,5.721],[17.021,5.726],[16.907,6.099],[16.953,6.135],[16.955,6.297],[17.015,6.309],[17.024,6.368],[17.091,6.397],[17.099,6.45],[17.041,6.488],[16.909,6.456],[16.793,6.558],[16.679,6.986],[16.767,7.114],[16.783,7.347],[16.832,7.427],[16.815,7.55],[16.881,7.633],[17.039,7.662],[17.059,7.697],[17.102,7.678],[17.251,7.823],[17.419,7.898],[17.466,7.884],[17.621,7.979],[17.859,7.96],[17.95,7.978],[17.95,5.874],[17.896,5.898]]],"a":1},{"n":"Ouham-Pendé","p":[[[16.764,6.614],[16.899,6.46],[17.041,6.488],[17.1,6.443],[17.091,6.397],[17.024,6.368],[17.015,6.309],[16.955,6.297],[16.953,6.135],[16.908,6.106],[16.979,5.859],[16.749,5.779],[16.515,5.568],[16.236,5.906],[16.195,5.906],[16.142,5.819],[16.064,5.792],[15.994,5.802],[15.824,5.925],[15.789,6.153],[15.705,6.278],[15.784,6.368],[15.759,6.417],[15.541,6.311],[15.48,6.328],[15.424,6.302],[15.35,6.383],[15.301,6.579],[15.189,6.695],[15.183,6.755],[14.922,6.686],[15.042,6.791],[15.064,6.85],[16.729,6.85],[16.764,6.614]]],"a":1},{"n":"Sangha-Mbaéré","p":[[[17.414,3.683],[17.334,3.619],[17.259,3.626],[17.197,3.582],[17.009,3.538],[16.868,3.562],[16.833,3.523],[16.686,3.542],[16.599,3.502],[16.465,3.157],[16.491,3.068],[16.446,2.958],[16.479,2.836],[16.197,2.236],[16.09,2.51],[16.11,2.701],[16.059,2.712],[16.095,2.837],[16.055,2.94],[16.026,2.979],[15.978,2.981],[15.914,3.098],[15.786,3.108],[15.262,3.663],[15.459,3.783],[15.506,3.742],[15.562,3.753],[15.619,3.84],[15.887,3.969],[16.085,3.991],[16.373,3.889],[16.449,3.832],[16.585,4.0],[16.601,4.142],[16.678,4.259],[16.761,4.228],[16.97,3.948],[17.322,3.812],[17.414,3.683]]]},{"n":"Nana-Mambéré","p":[[[14.576,6.19],[14.719,6.258],[14.772,6.318],[14.922,6.686],[15.183,6.755],[15.189,6.695],[15.301,6.579],[15.35,6.383],[15.424,6.302],[15.48,6.328],[15.541,6.311],[15.751,6.419],[15.775,6.404],[15.784,6.368],[15.705,6.278],[15.789,6.153],[15.816,5.936],[15.866,5.888],[15.994,5.802],[16.084,5.793],[16.156,5.832],[16.195,5.906],[16.236,5.906],[16.316,5.784],[16.437,5.691],[16.515,5.568],[16.497,5.388],[16.526,5.275],[16.001,5.068],[15.929,5.114],[15.806,5.082],[15.769,5.229],[15.71,5.233],[15.666,5.161],[15.686,5.018],[15.498,5.053],[15.406,5.0],[15.114,4.997],[15.019,4.943],[14.945,5.006],[14.66,5.011],[14.652,5.188],[14.52,5.291],[14.617,5.495],[14.59,5.608],[14.631,5.738],[14.617,5.865],[14.585,5.923],[14.466,5.921],[14.387,6.039],[14.535,6.19],[14.576,6.19]]]},{"n":"Mambéré-Kadéï","p":[[[15.064,4.293],[14.994,4.413],[14.78,4.545],[14.719,4.62],[16.886,4.62],[17.067,4.346],[17.002,4.345],[16.86,4.423],[16.738,4.245],[16.658,4.242],[16.601,4.142],[16.585,4.0],[16.449,3.832],[16.373,3.889],[16.085,3.991],[15.887,3.969],[15.619,3.84],[15.562,3.753],[15.506,3.742],[15.459,3.783],[15.262,3.663],[15.085,3.886],[15.026,4.026],[15.092,4.016],[15.192,4.052],[15.103,4.112],[15.064,4.293]]],"a":1},{"n":"Nana-Grébizi","p":[[[19.064,8.583],[19.154,8.405],[19.249,8.301],[19.235,8.278],[19.359,8.13],[19.354,8.064],[19.38,8.048],[19.345,8.021],[19.352,7.898],[19.32,7.882],[19.352,7.842],[19.321,7.805],[19.373,7.747],[19.386,7.774],[19.592,7.699],[19.566,7.67],[19.599,7.651],[19.592,7.568],[19.729,7.548],[19.763,7.448],[19.885,7.353],[19.921,7.247],[20.156,7.097],[20.102,6.88],[19.871,6.626],[19.874,6.448],[19.534,6.502],[19.176,6.437],[19.09,6.465],[19.014,6.543],[18.752,6.514],[18.776,6.583],[18.872,6.662],[18.752,6.75],[18.755,6.811],[18.655,6.941],[18.744,7.079],[18.712,7.117],[18.841,7.243],[18.732,7.571],[18.793,7.646],[19.014,7.763],[18.981,7.775],[18.98,7.856],[19.056,8.067],[19.057,8.229],[19.018,8.26],[19.072,8.41],[19.064,8.583]]]},{"n":"Lim-Pendé","p":[[[16.549,7.795],[16.613,7.748],[16.614,7.68],[16.822,7.523],[16.832,7.427],[16.795,7.385],[16.755,7.195],[16.767,7.114],[16.679,6.986],[16.724,6.881],[16.729,6.85],[15.064,6.85],[15.136,7.045],[15.188,7.089],[15.224,7.248],[15.433,7.39],[15.419,7.437],[15.481,7.523],[15.669,7.516],[15.758,7.456],[15.925,7.488],[16.043,7.584],[16.371,7.673],[16.392,7.784],[16.451,7.792],[16.549,7.87],[16.549,7.795]]],"a":1},{"n":"Ouham-Fafa","p":[[[18.589,8.048],[18.639,8.178],[18.813,8.276],[19.072,8.632],[19.072,8.41],[19.019,8.268],[19.057,8.229],[19.056,8.067],[18.98,7.856],[18.981,7.775],[19.014,7.763],[18.745,7.604],[18.732,7.571],[18.841,7.243],[18.712,7.117],[18.744,7.079],[18.655,6.953],[18.755,6.811],[18.752,6.75],[18.868,6.677],[18.867,6.644],[18.776,6.583],[18.743,6.492],[18.86,6.357],[18.866,6.241],[18.907,6.192],[18.627,6.001],[18.635,5.731],[18.608,5.663],[18.423,5.565],[18.374,5.678],[18.389,5.743],[18.275,5.749],[18.244,5.8],[18.027,5.801],[17.998,5.853],[17.95,5.874],[17.95,7.978],[18.175,8.022],[18.589,8.048]]],"a":1},{"n":"Mambéré","p":[[[14.717,4.622],[14.66,5.011],[14.945,5.006],[15.019,4.943],[15.114,4.997],[15.406,5.0],[15.498,5.053],[15.686,5.018],[15.666,5.161],[15.721,5.235],[15.769,5.229],[15.806,5.082],[15.929,5.114],[16.027,5.072],[16.73,5.384],[16.8,5.353],[16.932,5.362],[17.033,5.288],[17.118,5.165],[16.995,5.09],[17.037,5.024],[17.027,4.969],[16.83,4.868],[16.846,4.68],[16.886,4.62],[14.717,4.622]]],"a":1}];
const MAP_W=760, MAP_H=520;
function projGeo(lon,lat){ return [ (lon-14.3)/(27.55-14.3)*MAP_W, (11.05-lat)/(11.05-2.15)*MAP_H ]; }
function hexMix(hex,t){ /* blanc -> couleur selon l'intensité t (0..1) */
  const r=parseInt(hex.slice(1,3),16),g=parseInt(hex.slice(3,5),16),b=parseInt(hex.slice(5,7),16);
  const m=x=>Math.round(255+(x-255)*(0.15+0.85*t));
  return `rgb(${m(r)},${m(g)},${m(b)})`;
}
const CARTO_METRICS = {
  desarmes:{lbl:"Ex-combattants désarmés", fn:c=>!!c.desarmement, color:"#0E7C7B"},
  armes:{lbl:"Armes & lots collectés", fn:null, color:"#003082"},
  enroles:{lbl:"Ex-combattants enrôlés", fn:c=>true, color:"#5E7175"},
  reintegres:{lbl:"Réintégrés (parcours achevé)", fn:c=>c.statut==="reintegre", color:"#289728"},
  abandons:{lbl:"Abandons", fn:c=>c.statut==="abandon", color:"#C0242B"}
};
var CARTO_METRIC="desarmes";
var CARTO_LEVEL="pref";
var LAST_CARTO_SVG="";
const REGION_COLORS={"Plateaux":"#0E7C7B","Équateur":"#289728","Yadé":"#B87A00","Kagas":"#6A3FA0","Fertit":"#C0242B","Haut-Oubangui":"#003082","Bas-Oubangui":"#7A5E00"};
function cartoCounts(metric){
  const m={}; PREFECTURES.forEach(p=>m[p]=0);
  if(metric==="armes"){ DB.combattants.forEach(c=>{ if(c.desarmement) m[c.prefecture]=(m[c.prefecture]||0)+c.desarmement.armes.length; }); }
  else{ const fn=CARTO_METRICS[metric].fn; DB.combattants.forEach(c=>{ if(fn(c)) m[c.prefecture]=(m[c.prefecture]||0)+1; }); }
  return m;
}
function regionCounts(metric){
  const pc=cartoCounts(metric), m={};
  for(const [r,v] of Object.entries(REGIONS)) m[r]=v.prefs.reduce((a,p)=>a+(pc[p]||0),0);
  return m;
}
function rCarto(){
  $("view").innerHTML = `
  <div class="toolbar">
    <div class="field"><label>Niveau</label><select id="cLevel" onchange="CARTO_LEVEL=this.value;drawCarto()"><option value="pref" ${CARTO_LEVEL==="pref"?"selected":""}>20 préfectures</option><option value="region" ${CARTO_LEVEL==="region"?"selected":""}>7 régions</option></select></div>
    <div class="field"><label>Indicateur représenté</label><select id="cMetric" onchange="CARTO_METRIC=this.value;drawCarto()">${
      Object.entries(CARTO_METRICS).map(([k,v])=>`<option value="${k}" ${k===CARTO_METRIC?"selected":""}>${v.lbl}</option>`).join("")}</select></div>
    <div class="muted small" style="flex:1;align-self:center">Découpage loi 21-001 (2021), modèle ICASEES : 7 régions, 20 préfectures, 84 sous-préfectures — cliquez sur une zone pour le détail.</div>
    <button class="btn sec" onclick="exportCartoSVG()">Exporter SVG</button>
    <button class="btn sec" onclick="exportCartoPNG()">Exporter PNG</button>
    <button class="btn sec" onclick="exportCartoCSV()">Synthèse CSV</button>
    <button class="btn sec" onclick="printCarto()">Imprimer / PDF</button>
  </div>
  <div style="display:grid;grid-template-columns:1fr 330px;gap:16px;align-items:start">
    <div class="panel" style="margin-bottom:0"><div class="pb" id="mapBox" style="padding:8px"></div></div>
    <div id="cartoSide">
      <div class="panel" style="margin-bottom:16px"><div class="ph"><h3>Synthèse territoriale</h3><span class="muted small">région → préfecture → sous-préf. → commune</span></div><div class="pb nopad" style="max-height:340px;overflow-y:auto" id="cartoTable"></div></div>
      <div class="panel" style="margin-bottom:0"><div class="ph"><h3 id="zoneTitle">Détail de la zone</h3></div><div class="pb" id="zoneDetail"><span class="muted small">Sélectionnez une zone sur la carte.</span></div></div>
    </div>
  </div>`;
  drawCarto();
}
const CARTO_HDR=80;   /* hauteur de l'en-tête officiel du programme sur la carte */
function armInline(x,y,w,h){ return ARM_SVG.replace("<svg ", `<svg x="${x}" y="${y}" width="${w}" height="${h}" `); }
function cartoSvg(forExport){
  const M=CARTO_METRICS[CARTO_METRIC];
  const byRegion=(CARTO_LEVEL==="region");
  const counts=byRegion?regionCounts(CARTO_METRIC):cartoCounts(CARTO_METRIC);
  const max=Math.max(1,...Object.values(counts));
  const H_TOT=MAP_H+32+CARTO_HDR;
  const cx=MAP_W/2;
  let svg=`<svg viewBox="0 0 ${MAP_W} ${H_TOT}" xmlns="http://www.w3.org/2000/svg" ${forExport?`width="${MAP_W*2}" height="${H_TOT*2}"`:'style="width:100%;height:auto;display:block"'}>`;
  if(forExport) svg+=`<rect x="0" y="0" width="${MAP_W}" height="${H_TOT}" fill="#FFFFFF"/>`;
  /* En-tête officiel du programme : armoiries à gauche, République Centrafricaine en haut du bloc texte */
  svg+=`${armInline(16,8,58,55)}
  <text x="${cx}" y="22" font-size="15" font-weight="bold" text-anchor="middle" fill="#0A5D5C" font-family="Arial" letter-spacing="1">RÉPUBLIQUE CENTRAFRICAINE</text>
  <text x="${cx}" y="35" font-size="8.5" font-style="italic" text-anchor="middle" fill="#B8860B" font-family="Arial" letter-spacing="1.6">Unité — Dignité — Travail</text>
  <text x="${cx}" y="51" font-size="9" text-anchor="middle" fill="#33484C" font-family="Arial" letter-spacing="0.4">UNITÉ D'EXÉCUTION DU PROGRAMME NATIONAL DE DÉSARMEMENT,</text>
  <text x="${cx}" y="62" font-size="9" text-anchor="middle" fill="#33484C" font-family="Arial" letter-spacing="0.4">DÉMOBILISATION, RÉINTÉGRATION ET RAPATRIEMENT</text>
  <line x1="${cx-90}" y1="${CARTO_HDR-6}" x2="${cx+90}" y2="${CARTO_HDR-6}" stroke="#0A5D5C" stroke-width="1.6"/>
  <g transform="translate(0,${CARTO_HDR})">`;
  svg+=`<text x="14" y="24" font-size="15" font-weight="bold" fill="#0A5D5C" font-family="Arial">${M.lbl} ${byRegion?"par région":"par préfecture"} — carte des zones de désarmement</text>
  <g transform="translate(0,32)">`;
  for(const z of RCA_GEO){
    const zone=byRegion?regionOf(z.n):z.n;
    const n=counts[zone]||0;
    const base=byRegion?REGION_COLORS[zone]:M.color;
    const fill=n?hexMix(base,n/max):(byRegion?hexMix(base,0.06):"#F2F6F4");
    const paths=z.p.map(r=>"M"+r.map(pt=>projGeo(pt[0],pt[1]).map(v=>v.toFixed(1)).join(",")).join("L")+"Z").join(" ");
    const dash=(!byRegion&&z.a)?'stroke-dasharray="4,3"':"";
    svg+=`<path d="${paths}" fill="${fill}" stroke="${byRegion?"#7A8F8E":"#0A5D5C"}" stroke-width="${byRegion?0.7:1.1}" ${dash} stroke-linejoin="round" ${forExport?"":`style="cursor:pointer" onclick="zoneDetail('${zone.replace(/'/g,"\\'")}')"`}><title>${zone}${byRegion?" — "+z.n:""} — ${n}</title></path>`;
  }
  if(byRegion){
    for(const [r,v] of Object.entries(REGIONS)){
      const [x,y]=projGeo(...v.coord); const n=counts[r]||0;
      if(n){
        const rad=9+Math.sqrt(n/max)*17;
        svg+=`<g ${forExport?"":`style="cursor:pointer" onclick="zoneDetail('${r.replace(/'/g,"\\'")}')"`}><circle cx="${x.toFixed(1)}" cy="${y.toFixed(1)}" r="${rad.toFixed(1)}" fill="${REGION_COLORS[r]}" fill-opacity="0.9" stroke="#fff" stroke-width="1.8"/><text x="${x.toFixed(1)}" y="${(y+4).toFixed(1)}" font-size="12" font-weight="bold" text-anchor="middle" fill="#fff" font-family="Arial">${n}</text></g>`;
      }
      svg+=`<text x="${x.toFixed(1)}" y="${(y-(n?(9+Math.sqrt(n/max)*17):5)-4).toFixed(1)}" font-size="11" font-weight="bold" text-anchor="middle" fill="#33484C" font-family="Arial">${REGIONS[r].num}. ${r}</text>`;
    }
  } else {
    for(const p of PREFECTURES){
      const [x,y]=projGeo(...PREF_COORD[p]); const n=counts[p]||0;
      if(n){
        const r=8+Math.sqrt(n/max)*16;
        svg+=`<g ${forExport?"":`style="cursor:pointer" onclick="zoneDetail('${p.replace(/'/g,"\\'")}')"`}>
        <circle cx="${x.toFixed(1)}" cy="${y.toFixed(1)}" r="${r.toFixed(1)}" fill="${M.color}" fill-opacity="0.85" stroke="#fff" stroke-width="1.6"/>
        <text x="${x.toFixed(1)}" y="${(y+4).toFixed(1)}" font-size="12" font-weight="bold" text-anchor="middle" fill="#fff" font-family="Arial">${n}</text></g>`;
      }
      svg+=`<text x="${x.toFixed(1)}" y="${(y-(n?(8+Math.sqrt(n/max)*16):5)-3).toFixed(1)}" font-size="9.5" text-anchor="middle" fill="#33484C" font-family="Arial" ${forExport?"":`style="pointer-events:none"`}>${p}</text>`;
    }
  }
  svg+=`<text x="14" y="${MAP_H-6}" font-size="9" fill="#5E7175" font-family="Arial">${byRegion?"Chaque région est teintée selon l'indicateur ; le cercle chiffré marque son chef-lieu.":"Tracés en tirets : préfectures créées par la loi 21-001 (Lim-Pendé, Ouham-Fafa, Mambéré) — délimitation indicative."} · ${new Date().toLocaleDateString("fr-FR")} · Découpage : loi 21-001 · Modèle : ICASEES · Fond : Natural Earth</text></g></g></svg>`;
  return svg;
}
function metricWeight(c){
  if(CARTO_METRIC==="armes") return c.desarmement?c.desarmement.armes.length:0;
  return CARTO_METRICS[CARTO_METRIC].fn(c)?1:0;
}
var ZONE_NODES=[];
function nodeRef(filter,label){ ZONE_NODES.push({f:filter,l:label}); return ZONE_NODES.length-1; }
function drawCarto(){
  LAST_CARTO_SVG=cartoSvg(false);
  $("mapBox").innerHTML=LAST_CARTO_SVG;
  const M=CARTO_METRICS[CARTO_METRIC];
  ZONE_NODES=[];
  const NP="(non précisée)";
  const wsum=arr=>arr.reduce((a,c)=>a+metricWeight(c),0);
  const val=(n)=>`<b style="color:${n?M.color:"var(--muted)"};margin-left:auto">${n}</b>`;
  const row=(idx,label,n,pad,cls)=>`<div class="zrow ${cls||""}" style="padding-left:${pad}px" onclick="zoneDetailNode(${idx});event.stopPropagation()">${label}${val(n)}</div>`;
  let h="";
  for(const [rName,rV] of Object.entries(REGIONS)){
    const rC=DB.combattants.filter(c=>rV.prefs.includes(c.prefecture));
    const rN=wsum(rC);
    const rIdx=nodeRef({prefs:rV.prefs},`Région ${rName}`);
    let inner="";
    for(const p of rV.prefs){
      const pC=rC.filter(c=>c.prefecture===p);
      const pN=wsum(pC);
      const pIdx=nodeRef({prefs:[p]},`Préfecture : ${p}`);
      let spH="";
      if(pC.length){
        const sps=[...new Set(pC.map(c=>c.sousPref||NP))].sort();
        for(const sp of sps){
          const spC=pC.filter(c=>(c.sousPref||NP)===sp);
          const spIdx=nodeRef({prefs:[p],sp},`S/P ${sp} (${p})`);
          const coms=[...new Set(spC.map(c=>c.commune||NP))].sort();
          let comH="";
          for(const co of coms){
            const coC=spC.filter(c=>(c.commune||NP)===co);
            const coIdx=nodeRef({prefs:[p],sp,com:co},`Commune ${co} — S/P ${sp} (${p})`);
            comH+=row(coIdx,`<span class="muted">Commune</span> ${esc(co)}`,wsum(coC),46);
          }
          spH+=`<details><summary class="zsum" style="padding-left:30px"><span onclick="zoneDetailNode(${spIdx});event.stopPropagation()">S/P ${esc(sp)}</span>${val(wsum(spC))}</summary>${comH}</details>`;
        }
      }
      inner+=`<details><summary class="zsum" style="padding-left:16px"><span onclick="zoneDetailNode(${pIdx});event.stopPropagation()">${esc(p)}</span>${val(pN)}</summary>${spH||`<div class="zrow muted small" style="padding-left:32px">Aucun dossier.</div>`}</details>`;
    }
    h+=`<details ${rN?"open":""}><summary class="zsum zreg"><span onclick="zoneDetailNode(${rIdx});event.stopPropagation()" style="font-weight:700">Région ${esc(rName)}</span><span class="muted small" style="margin-left:6px">(${rV.prefs.length} préf.)</span>${val(rN)}</summary>${inner}</details>`;
  }
  $("cartoTable").innerHTML=`<div class="small" style="padding:6px 10px;border-bottom:1px solid var(--line);background:#FBFDFC;color:var(--muted)">${M.lbl} — cliquez sur un intitulé pour le détail, sur ▸ pour déplier.</div>${h}`;
}
function zoneDetailNode(i){
  const nd=ZONE_NODES[i]; if(!nd) return;
  const NP="(non précisée)";
  const C=DB.combattants.filter(c=>nd.f.prefs.includes(c.prefecture)
    &&(nd.f.sp===undefined||(c.sousPref||NP)===nd.f.sp)
    &&(nd.f.com===undefined||(c.commune||NP)===nd.f.com));
  renderZoneDetail(nd.l, C, nd.f.prefs.length>1?nd.f.prefs:null);
}
function zoneDetail(zone){
  const isRegion=!!REGIONS[zone] && CARTO_LEVEL==="region";
  const prefs=isRegion?REGIONS[zone].prefs:[zone];
  const C=DB.combattants.filter(c=>prefs.includes(c.prefecture));
  const titre=isRegion?`Région ${zone} (chef-lieu : ${REGIONS[zone].chef})`:`Préfecture : ${zone} (chef-lieu : ${CHEF_LIEUX[zone]||"—"}, région ${regionOf(zone)})`;
  renderZoneDetail(titre, C, isRegion?prefs:null);
}
function renderZoneDetail(titre, C, prefsList){
  $("zoneTitle").textContent=titre;
  if(!C.length){ $("zoneDetail").innerHTML=`<span class="muted small">Aucun dossier enregistré dans cette zone.</span>`; return; }
  const armes=C.reduce((a,c)=>a+(c.desarmement?c.desarmement.armes.length:0),0);
  const sites=[...new Set(C.map(c=>c.site).filter(Boolean))];
  const grp=countBy(C,c=>c.groupe);
  const stat=countBy(C,c=>STATUTS[c.statut].lbl);
  $("zoneDetail").innerHTML=`
    <table style="background:transparent;margin-bottom:8px">
      <tr><td style="color:var(--muted);font-weight:600">Dossiers</td><td style="text-align:right"><b>${C.length}</b></td></tr>
      <tr><td style="color:var(--muted);font-weight:600">Armes collectées</td><td style="text-align:right"><b>${armes}</b></td></tr>
      ${stat.map(([k,v])=>`<tr><td class="small" style="padding-left:14px">${k}</td><td style="text-align:right">${v}</td></tr>`).join("")}
    </table>
    ${prefsList?`<div class="small" style="margin-bottom:6px"><b>Préfectures :</b> ${prefsList.map(p=>`<span class="tag">${esc(p)} · ${C.filter(c=>c.prefecture===p).length}</span>`).join(" ")}</div>`:""}
    <div class="small" style="margin-bottom:6px"><b>Groupes armés :</b> ${grp.map(([k,v])=>`<span class="tag">${esc(k)} · ${v}</span>`).join(" ")||"—"}</div>
    <div class="small" style="margin-bottom:8px"><b>Sites de regroupement :</b> ${sites.map(x=>`<span class="tag">${esc(x)}</span>`).join(" ")||"—"}</div>
    <div class="small" style="max-height:150px;overflow-y:auto">${C.map(c=>`<div style="padding:3px 0;border-bottom:1px dashed var(--line)"><span class="link" onclick="go('fiche','${c.id}')">${c.num}</span> — ${esc(c.nom)} ${esc(c.prenom)} <span class="badge st-${c.statut}" style="float:right">${STATUTS[c.statut].lbl}</span></div>`).join("")}</div>`;
}
/* ---------- Exports de la cartographie ---------- */
function exportCartoSVG(){
  dl(`pnddrr_carte_${CARTO_LEVEL}_${CARTO_METRIC}_${today()}.svg`, '<?xml version="1.0" encoding="UTF-8"?>\n'+cartoSvg(true), "image/svg+xml");
  log("Export cartographie",`Carte SVG — ${CARTO_METRICS[CARTO_METRIC].lbl} (${CARTO_LEVEL==="region"?"régions":"préfectures"})`); toast("Carte SVG téléchargée.");
}
function exportCartoPNG(){
  try{
    const svg=cartoSvg(true);
    const img=new Image();
    img.onload=function(){
      const cv=document.createElement("canvas"); cv.width=MAP_W*2; cv.height=(MAP_H+32+CARTO_HDR)*2;
      const ctx=cv.getContext("2d"); ctx.fillStyle="#fff"; ctx.fillRect(0,0,cv.width,cv.height);
      ctx.drawImage(img,0,0,cv.width,cv.height);
      const a=document.createElement("a"); a.href=cv.toDataURL("image/png");
      a.download=`pnddrr_carte_${CARTO_LEVEL}_${CARTO_METRIC}_${today()}.png`; a.click();
      log("Export cartographie",`Carte PNG — ${CARTO_METRICS[CARTO_METRIC].lbl}`); toast("Carte PNG téléchargée.");
    };
    img.onerror=()=>toast("Export PNG indisponible — utilisez l'export SVG.");
    img.src="data:image/svg+xml;charset=utf-8,"+encodeURIComponent(svg);
  }catch(e){ toast("Export PNG indisponible — utilisez l'export SVG."); }
}
function exportCartoCSV(){
  const NP="(non précisée)";
  const mkeys=Object.keys(CARTO_METRICS);
  const w=(c,k)=>k==="armes"?(c.desarmement?c.desarmement.armes.length:0):(CARTO_METRICS[k].fn(c)?1:0);
  const sums=C=>mkeys.map(k=>C.reduce((a,c)=>a+w(c,k),0));
  const rows=[["Niveau","Région","Préfecture","Sous-préfecture","Commune",...mkeys.map(k=>CARTO_METRICS[k].lbl)]];
  for(const [rName,rV] of Object.entries(REGIONS)){
    const rC=DB.combattants.filter(c=>rV.prefs.includes(c.prefecture));
    rows.push(["Région",rName,"","","",...sums(rC)]);
    for(const p of rV.prefs){
      const pC=rC.filter(c=>c.prefecture===p);
      rows.push(["Préfecture",rName,p,"","",...sums(pC)]);
      for(const sp of [...new Set(pC.map(c=>c.sousPref||NP))].sort()){
        const spC=pC.filter(c=>(c.sousPref||NP)===sp);
        rows.push(["Sous-préfecture",rName,p,sp,"",...sums(spC)]);
        for(const co of [...new Set(spC.map(c=>c.commune||NP))].sort()){
          rows.push(["Commune",rName,p,sp,co,...sums(spC.filter(c=>(c.commune||NP)===co))]);
        }
      }
    }
  }
  dl(`pnddrr_synthese_territoriale_${today()}.csv`, csv(rows), "text/csv");
  log("Export cartographie","Synthèse territoriale CSV (région/préfecture/sous-préfecture/commune)"); toast("Synthèse territoriale CSV téléchargée.");
}
function printCarto(){
  const M=CARTO_METRICS[CARTO_METRIC];
  const byRegion=(CARTO_LEVEL==="region");
  const counts=byRegion?regionCounts(CARTO_METRIC):cartoCounts(CARTO_METRIC);
  const rows=(byRegion?Object.keys(REGIONS):PREFECTURES).map(p=>[p,counts[p]||0]).sort((a,b)=>b[1]-a[1]);
  doPrint(docWrap(`${docEntete("Cartographie des zones de désarmement")}
    <h2 class="titre">Carte des zones de désarmement</h2>
    <div style="border:1px solid #000;padding:6px">${cartoSvg(true).replace(/width="\d+" height="\d+"/,'style="width:100%;height:auto"')}</div>
    <table class="dt" style="margin-top:14px"><tr><th>${byRegion?"Région":"Préfecture"}</th><th style="text-align:right">${M.lbl}</th></tr>${
      rows.filter(r=>r[1]).map(([p,n])=>`<tr><td>${p}</td><td style="text-align:right"><b>${n}</b></td></tr>`).join("")||"<tr><td colspan=2>Aucune donnée</td></tr>"}</table>
    <div class="sig"><div class="c"></div><div class="c">Fait à ${esc(cfg("villeSignature")||"Bangui")}, le ${new Date().toLocaleDateString("fr-FR")}<br>Pour le PNDDRR<div class="ligne">${esc(CUR.nom)}</div></div></div>`));
  log("Impression","Carte des zones de désarmement");
}

/* module: admin/import.js — PNDDRR engine (classic globals) */
/* ================= IMPORTATION DE DONNÉES (CSV / Excel / Word / PDF) =================
   Assistant en 3 étapes : lecture du fichier → correspondance des colonnes →
   aperçu, contrôle et synchronisation avec le registre des ex-combattants.
   CSV : traité intégralement hors connexion. XLSX / DOCX / PDF : bibliothèques
   chargées à la demande depuis cdnjs (connexion Internet requise pour ces formats). */

var IMP = {kind:"comb", grid:null, headers:[], map:{}, fileName:"", report:null};

const IMP_FIELDS_ARMES = {
  num:"N° dossier", nom:"Nom (si pas de n° dossier)", prenom:"Prénom",
  type:"Type d'arme *", marque:"Marque / modèle", calibre:"Calibre", serie:"N° de série",
  etat:"État", mun:"Munitions", munQte:"Munitions — quantité", munUnite:"Munitions — unité", dateRemise:"Date de remise", lieu:"Lieu de collecte",
  prefecture:"Préfecture (si création de dossier)", groupe:"Groupe armé (si création)"
};
const IMP_SYNONYMS_ARMES = {
  num:["numdossier","ndossier","dossier","numero","reference","ref","id"],
  nom:["nom","noms","remisepar","detenteur","excombattant","proprietaire"],
  prenom:["prenom","prenoms"],
  type:["type","typedarme","typearme","arme","categorie","nature"],
  marque:["marque","modele","marquemodele","fabricant"],
  calibre:["calibre","cal"],
  serie:["nserie","numserie","numerodeserie","serie","serial","sn"],
  etat:["etat","condition","fonctionnement"],
  mun:["munitions","mun"],
  munQte:["munitionsquantite","quantitemunitions","qtemunitions","quantite","qte","nombre","cartouches"],
  munUnite:["munitionsunite","unitemunitions","unite"],
  dateRemise:["dateremise","date","datecollecte","remisele"],
  lieu:["lieu","lieudecollecte","site","lieuremise"],
  prefecture:["prefecture","pref"],
  groupe:["groupearme","groupe","faction"]
};

const IMP_FIELDS = {
  num:"N° dossier", nom:"Nom *", prenom:"Prénom *", alias:"Alias", sexe:"Sexe",
  dn:"Date de naissance", ln:"Lieu de naissance", nat:"Nationalité", tel:"Téléphone",
  prefecture:"Préfecture *", sousPref:"Sous-préfecture", commune:"Commune", site:"Site",
  groupe:"Groupe armé", grade:"Grade / fonction", annees:"Années de service", zone:"Zone d'opération",
  souhait:"Souhait de réintégration", instr:"Instruction", obs:"Observations",
  statut:"Statut", carte:"N° carte démobilisé", corps:"Corps (militaire)", matricule:"Matricule",
  filiere:"Filière (socio-éco.)"
};
const IMP_SYNONYMS = {
  num:["numdossier","ndossier","dossier","numero","reference","ref","id"],
  nom:["nom","noms","nomfamille","lastname","surname"],
  prenom:["prenom","prenoms","firstname"],
  alias:["alias","nomdeguerre","surnom","pseudo"],
  sexe:["sexe","genre","sex","gender"],
  dn:["datenaissance","datedenaissance","nele","dob","naissance"],
  ln:["lieunaissance","lieudenaissance","nea"],
  nat:["nationalite"],
  tel:["telephone","tel","contact","phone","portable"],
  prefecture:["prefecture","pref"],
  sousPref:["sousprefecture","sp","souspref"],
  commune:["commune"],
  site:["site","siteregroupement","cantonnement","camp"],
  groupe:["groupearme","groupe","faction","mouvement"],
  grade:["grade","fonction","rang"],
  annees:["annees","anneesservice","anciennete"],
  zone:["zone","zoneoperation"],
  souhait:["souhait","orientation","voeu","choix"],
  instr:["instruction","niveau","scolarite","education"],
  obs:["observations","obs","remarques","notes","commentaires"],
  statut:["statut","status","etape","phase","situation"],
  carte:["carte","cartedemobilise","numcarte","dem"],
  corps:["corps","armee","unite"],
  matricule:["matricule"],
  filiere:["filiere","metier","activite","formation"]
};
const normTxt = s => String(s==null?"":s).toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]/g,"");

/* ---------- Vue ---------- */
function rImport(kind){
  IMP={kind:kind||IMP.kind||"comb",grid:null,headers:[],map:{},fileName:"",report:null};
  const armes=IMP.kind==="armes";
  $("view").innerHTML = `
  <div class="toolbar">
    <button class="btn ghost" onclick="go('parametres')">← Retour aux paramètres</button>
    <button class="btn ghost" onclick="go('registre')">← Registre</button>
    <button class="btn ghost" onclick="go('dashboard')">← Tableau de bord</button>
  </div>
  <div class="panel"><div class="ph"><h3>Étape 1 — Fichier source</h3></div><div class="pb">
    <div class="field" style="max-width:420px"><label>Type de registre à importer</label>
      <select onchange="rImport(this.value)"><option value="comb" ${armes?"":"selected"}>Ex-combattants (dossiers individuels)</option><option value="armes" ${armes?"selected":""}>Registre des armes (rattaché aux dossiers)</option></select></div>
    <p class="small muted" style="margin-bottom:10px">Formats acceptés : <b>CSV</b> (recommandé, hors connexion), <b>Excel</b> (.xlsx/.xls), <b>Word</b> (.docx — première table du document) et <b>PDF</b> (extraction de tableau, au mieux). La première ligne doit contenir les en-têtes de colonnes. ${armes?"Chaque ligne du fichier correspond à <b>une arme ou un lot</b>, rattaché à un dossier par son n° (sinon par nom + prénom).":"Modèle : utilisez l'export CSV du registre comme gabarit."}</p>
    <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
      <label class="btn" style="display:inline-flex;align-items:center">Choisir un fichier…<input type="file" id="impFile" accept=".csv,.txt,.xlsx,.xls,.docx,.pdf" style="display:none" onchange="impRead(this)"></label>
      <span class="small muted" id="impFileLbl">Aucun fichier sélectionné.</span>
      <button class="btn sec sm" onclick="impGabarit()">Télécharger le gabarit CSV</button>
      ${armes?`<button class="btn ghost sm" onclick="go('armes')">← Retour au registre des armes</button>`:""}
    </div>
  </div></div>
  <div id="impStep2"></div>
  <div id="impStep3"></div>
  <div id="impReport"></div>`;
}
function impGabarit(){
  if(IMP.kind==="armes"){
    const rows=[Object.values(IMP_FIELDS_ARMES).map(x=>x.replace(" *","")),
      ["DDR-2026-0001","","","Fusil d'assaut AK-47 / dérivés","Type 56","7,62 mm","AK-556677","Fonctionnelle","2 chargeurs","15/06/2026","Bambari","",""],
      ["","YAKETE","Fidèle","Grenade","","","","Fonctionnelle","1","15/06/2026","Bambari","",""]];
    dl("gabarit_import_registre_armes.csv", csv(rows), "text/csv"); return;
  }
  const rows=[Object.values(IMP_FIELDS).map(x=>x.replace(" *","")),
    ["","YAKETE","Fidèle","","M","11/04/1992","Bambari","Centrafricaine","72000000","Ouaka","Bambari","Ippy","Site A","UPC","Chef de groupe","5","Bambari","Militaire","Primaire","","Enregistré","","","",""]];
  dl("gabarit_import_ex-combattants.csv", csv(rows), "text/csv");
}

/* ---------- Lecture des fichiers ---------- */
function impLoadLib(url){
  return new Promise((ok,ko)=>{
    if(document.querySelector(`script[src="${url}"]`)) return ok();
    const sc=document.createElement("script"); sc.src=url;
    sc.onload=()=>ok(); sc.onerror=()=>ko(new Error("chargement impossible"));
    document.head.appendChild(sc);
  });
}
function impRead(inp){
  const f=inp.files[0]; if(!f) return;
  IMP.fileName=f.name;
  $("impFileLbl").textContent=f.name;
  const ext=f.name.toLowerCase().split(".").pop();
  const fail=msg=>{ toast(msg); $("impStep2").innerHTML=`<div class="panel"><div class="pb" style="color:var(--danger)">${esc(msg)}</div></div>`; };
  if(ext==="csv"||ext==="txt"){
    const r=new FileReader();
    r.onload=()=>{ try{ impSetGrid(impParseCSV(r.result)); }catch(e){ fail("Lecture CSV impossible : "+e.message); } };
    r.readAsText(f,"utf-8");
  } else if(ext==="xlsx"||ext==="xls"){
    const r=new FileReader();
    r.onload=async()=>{ try{
      await impLoadLib("https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js");
      const wb=XLSX.read(new Uint8Array(r.result),{type:"array",cellDates:true});
      const ws=wb.Sheets[wb.SheetNames[0]];
      const grid=XLSX.utils.sheet_to_json(ws,{header:1,raw:false,defval:""});
      impSetGrid(grid.filter(row=>row.some(c=>String(c).trim()!=="")));
    }catch(e){ fail("Lecture Excel impossible (connexion Internet requise pour ce format) — exportez en CSV depuis Excel si besoin."); } };
    r.readAsArrayBuffer(f);
  } else if(ext==="docx"){
    const r=new FileReader();
    r.onload=async()=>{ try{
      await impLoadLib("https://cdnjs.cloudflare.com/ajax/libs/mammoth/1.8.0/mammoth.browser.min.js");
      const res=await mammoth.convertToHtml({arrayBuffer:r.result});
      const doc=new DOMParser().parseFromString(res.value,"text/html");
      const table=doc.querySelector("table");
      if(!table) throw new Error("aucune table trouvée dans le document Word");
      const grid=[...table.querySelectorAll("tr")].map(tr=>[...tr.querySelectorAll("td,th")].map(td=>td.textContent.trim()));
      impSetGrid(grid);
    }catch(e){ fail("Lecture Word impossible : "+e.message+" (connexion Internet requise pour ce format)."); } };
    r.readAsArrayBuffer(f);
  } else if(ext==="pdf"){
    const r=new FileReader();
    r.onload=async()=>{ try{
      await impLoadLib("https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js");
      pdfjsLib.GlobalWorkerOptions.workerSrc="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js";
      const pdf=await pdfjsLib.getDocument({data:new Uint8Array(r.result)}).promise;
      let grid=[];
      for(let i=1;i<=pdf.numPages;i++){
        const tc=await(await pdf.getPage(i)).getTextContent();
        const lines={};
        tc.items.forEach(it=>{ const y=Math.round(it.transform[5]/3)*3; (lines[y]=lines[y]||[]).push({x:it.transform[4],t:it.str}); });
        Object.keys(lines).sort((a,b)=>b-a).forEach(y=>{
          const items=lines[y].sort((a,b)=>a.x-b.x);
          const cells=[]; let cur=items[0].t; let lastEnd=items[0].x+items[0].t.length*4;
          for(let k=1;k<items.length;k++){
            if(items[k].x-lastEnd>18){ cells.push(cur.trim()); cur=items[k].t; }
            else cur+=" "+items[k].t;
            lastEnd=items[k].x+items[k].t.length*4;
          }
          cells.push(cur.trim());
          if(cells.filter(c=>c).length>1) grid.push(cells);
        });
      }
      if(grid.length<2) throw new Error("aucun tableau détecté dans le PDF");
      impSetGrid(grid);
      toast("Extraction PDF « au mieux » — vérifiez soigneusement l'aperçu.");
    }catch(e){ fail("Lecture PDF impossible : "+e.message+" (connexion Internet requise ; préférez le CSV)."); } };
    r.readAsArrayBuffer(f);
  } else fail("Format non pris en charge : "+ext);
  inp.value="";
}
function impParseCSV(text){
  text=text.replace(/^\uFEFF/,"");
  const firstLine=text.split(/\r?\n/)[0]||"";
  const sep=(firstLine.match(/;/g)||[]).length>=(firstLine.match(/,/g)||[]).length?";":",";
  const rows=[]; let row=[], cell="", inQ=false;
  for(let i=0;i<text.length;i++){
    const ch=text[i];
    if(inQ){
      if(ch==='"'){ if(text[i+1]==='"'){cell+='"';i++;} else inQ=false; }
      else cell+=ch;
    } else {
      if(ch==='"') inQ=true;
      else if(ch===sep){ row.push(cell); cell=""; }
      else if(ch==="\n"||ch==="\r"){ if(ch==="\r"&&text[i+1]==="\n") i++; row.push(cell); if(row.some(c=>c.trim()!=="")) rows.push(row); row=[]; cell=""; }
      else cell+=ch;
    }
  }
  row.push(cell); if(row.some(c=>c.trim()!=="")) rows.push(row);
  if(rows.length<2) throw new Error("le fichier doit contenir une ligne d'en-têtes et au moins une ligne de données");
  return rows;
}

/* ---------- Étape 2 : correspondance des colonnes ---------- */
function impFields(){ return IMP.kind==="armes"?IMP_FIELDS_ARMES:IMP_FIELDS; }
function impSyns(){ return IMP.kind==="armes"?IMP_SYNONYMS_ARMES:IMP_SYNONYMS; }
function impSetGrid(grid){
  IMP.grid=grid; IMP.headers=grid[0].map(h=>String(h).trim());
  IMP.map={};
  IMP.headers.forEach((h,i)=>{
    const n=normTxt(h);
    for(const [f,syns] of Object.entries(impSyns())){
      if(Object.values(IMP.map).includes(i)) break;
      if(IMP.map[f]===undefined && (syns.includes(n)||n===normTxt(impFields()[f].replace(" *","")))) { IMP.map[f]=i; break; }
    }
  });
  $("impReport").innerHTML="";
  $("impStep2").innerHTML=`<div class="panel"><div class="ph"><h3>Étape 2 — Correspondance des colonnes</h3><span class="small muted">${IMP.grid.length-1} ligne(s) de données détectée(s)</span></div><div class="pb">
    <div class="grid4">${Object.entries(impFields()).map(([f,lbl])=>`
      <div class="field"><label>${lbl}</label><select id="map_${f}" onchange="IMP.map['${f}']=this.value===''?undefined:+this.value">
        <option value="">— Ignorer —</option>
        ${IMP.headers.map((h,i)=>`<option value="${i}" ${IMP.map[f]===i?"selected":""}>${esc(h)||("Colonne "+(i+1))}</option>`).join("")}
      </select></div>`).join("")}
    </div>
    <div style="display:flex;gap:9px">
      <button class="btn ghost" type="button" onclick="rImport()">← Choisir un autre fichier</button>
      <button class="btn" onclick="IMP.kind==='armes'?impPreviewArmes():impPreview()">Contrôler et prévisualiser</button>
    </div>
  </div></div>`;
  $("impStep3").innerHTML="";
}

/* ---------- Analyse d'une ligne ---------- */
function impDate(v){
  v=String(v||"").trim(); if(!v) return "";
  let m=v.match(/^(\d{1,2})[\/\.\-](\d{1,2})[\/\.\-](\d{4})$/);
  if(m) return `${m[3]}-${m[2].padStart(2,"0")}-${m[1].padStart(2,"0")}`;
  m=v.match(/^(\d{4})-(\d{1,2})-(\d{1,2})/);
  if(m) return `${m[1]}-${m[2].padStart(2,"0")}-${m[3].padStart(2,"0")}`;
  if(/^\d{4,5}$/.test(v)){ const d=new Date(Date.UTC(1899,11,30)+(+v)*86400000); return d.toISOString().slice(0,10); }
  return "";
}
function impMatchRef(v,list){
  const n=normTxt(v); if(!n) return "";
  for(const x of list) if(normTxt(x)===n) return x;
  for(const x of list) if(normTxt(x).includes(n)||n.includes(normTxt(x))) return x;
  return null;
}
function impStatut(v){
  const n=normTxt(v);
  if(!n) return "enregistre";
  if(n.includes("abandon")) return "abandon";
  if(n.includes("acheve")||n==="reintegre"||n.includes("reintegre")&&!n.includes("militaire")&&!n.includes("socio")) return "reintegre";
  if(n.includes("militaire")) return "reintegration_militaire";
  if(n.includes("socio")||n.includes("economique")) return "reintegration_socio";
  if(n.includes("demobil")) return "demobilise";
  if(n.includes("desarm")) return "desarme";
  if(n.includes("enregistr")) return "enregistre";
  return "enregistre";
}
function impRow(row, idx){
  const g=f=>IMP.map[f]===undefined?"":String(row[IMP.map[f]]||"").trim();
  const err=[];
  const nom=g("nom").toUpperCase(), prenom=g("prenom");
  if(!nom||!prenom) err.push("nom/prénom manquant");
  let prefecture=null;
  if(g("prefecture")){ prefecture=impMatchRef(g("prefecture"),PREFECTURES); if(prefecture===null) err.push("préfecture inconnue : « "+g("prefecture")+" »"); }
  if(!g("prefecture")) err.push("préfecture manquante");
  const sexeRaw=normTxt(g("sexe"));
  const sexe=sexeRaw.startsWith("f")?"F":"M";
  const groupe=g("groupe")?(impMatchRef(g("groupe"),GROUPES)||"Autre"):"Autre";
  const statut=impStatut(g("statut"));
  const d={
    num:g("num").toUpperCase(), nom, prenom, alias:g("alias"), sexe, dn:impDate(g("dn")), ln:g("ln"),
    nat:g("nat")||"Centrafricaine", tel:g("tel"),
    prefecture:prefecture||"", sousPref:g("sousPref"), commune:g("commune"), site:g("site"),
    groupe, groupeLibre:g("groupe"), grade:g("grade"), annees:g("annees").replace(/\D/g,""), zone:g("zone"),
    souhait:impMatchRef(g("souhait"),["Socio-économique","Militaire","Indécis"])||"Indécis",
    instr:impMatchRef(g("instr"),["Aucun","Primaire","Secondaire","Supérieur","Coranique"])||"Aucun",
    obs:g("obs"), statut, carte:g("carte").toUpperCase(), corps:g("corps"), matricule:g("matricule"), filiere:g("filiere")
  };
  return {ligne:idx+2, d, err};
}

/* ---------- Étape 3 : aperçu ---------- */
function impPreview(){
  const rows=IMP.grid.slice(1).map((r,i)=>impRow(r,i));
  IMP.rows=rows;
  const bad=rows.filter(r=>r.err.length), ok=rows.length-bad.length;
  $("impStep3").innerHTML=`<div class="panel"><div class="ph"><h3>Étape 3 — Contrôle et synchronisation</h3>
    <span class="small">${ok} ligne(s) valide(s) · <span style="color:${bad.length?"var(--danger)":"var(--ok)"}">${bad.length} en erreur</span></span></div>
  <div class="pb">
    <div class="pb nopad" style="max-height:300px;overflow:auto;border:1px solid var(--line);border-radius:8px;margin-bottom:12px">
    <table><thead><tr><th>Ligne</th><th>N° dossier</th><th>Nom & prénom</th><th>Sexe</th><th>Préfecture</th><th>Groupe</th><th>Statut</th><th>Anomalies</th></tr></thead><tbody>
    ${rows.slice(0,200).map(r=>`<tr style="${r.err.length?"background:#FDF1F1":""}"><td>${r.ligne}</td><td>${esc(r.d.num)||"<span class='muted'>auto</span>"}</td>
      <td>${esc(r.d.nom)} ${esc(r.d.prenom)}</td><td>${r.d.sexe}</td><td>${esc(r.d.prefecture)||"—"}</td>
      <td class="small">${esc(r.d.groupe)}${r.d.groupe==="Autre"&&r.d.groupeLibre?` <span class="muted">(${esc(r.d.groupeLibre)})</span>`:""}</td>
      <td><span class="badge st-${r.d.statut}">${STATUTS[r.d.statut].lbl}</span></td>
      <td class="small" style="color:var(--danger)">${r.err.join(" · ")||""}</td></tr>`).join("")}
    </tbody></table>${rows.length>200?`<div class="small muted" style="padding:8px">… ${rows.length-200} ligne(s) supplémentaires non affichées.</div>`:""}</div>
    <div class="grid2" style="margin-bottom:12px">
      <div class="field"><label>Mode de synchronisation</label><select id="impMode">
        <option value="fusion">Créer les nouveaux + mettre à jour les existants (fusion)</option>
        <option value="create">Créer uniquement les nouveaux (ignorer les doublons)</option>
      </select></div>
      <div class="small muted" style="align-self:end;padding-bottom:8px">Doublons détectés par n° de dossier, sinon par nom + prénom + date de naissance. La fusion complète les champs vides et fait progresser le statut sans jamais le faire reculer.</div>
    </div>
    <div style="display:flex;gap:9px">
      <button class="btn ghost" type="button" onclick="impBack()">← Modifier la correspondance</button>
      <button class="btn" ${ok?"":"disabled"} onclick="impSync()">Synchroniser ${ok} dossier(s) avec le registre</button>
    </div>
  </div></div>`;
  $("impReport").innerHTML="";
}

function impBack(){ $("impStep3").innerHTML=""; $("impReport").innerHTML=""; $("view").scrollTop=0; }

/* ---------- Registre des armes : analyse, aperçu, synchronisation ---------- */
function impRowArme(row, idx){
  const g=f=>IMP.map[f]===undefined?"":String(row[IMP.map[f]]||"").trim();
  const err=[];
  const num=g("num").toUpperCase(), nom=g("nom").toUpperCase(), prenom=g("prenom");
  if(!num&&!nom) err.push("aucun identifiant (n° dossier ou nom)");
  const typeRaw=g("type");
  if(!typeRaw) err.push("type d'arme manquant");
  const type=impMatchRef(typeRaw,TYPES_ARMES)||"Autre";
  let prefecture="";
  if(g("prefecture")){ const m=impMatchRef(g("prefecture"),PREFECTURES); if(m===null) err.push("préfecture inconnue : « "+g("prefecture")+" »"); else prefecture=m; }
  const d={ num, nom, prenom, type, typeLibre:typeRaw,
    marque:g("marque"), calibre:g("calibre"), serie:g("serie").toUpperCase(),
    etat:impMatchRef(g("etat"),["Fonctionnelle","Défectueuse","Hors d'usage"])||"Fonctionnelle",
    mun:g("mun"), munQte:+g("munQte").replace(/\D/g,"")||0, munUnite:impMatchRef(g("munUnite"),UNITES_MUN)||"cartouches", dateRemise:impDate(g("dateRemise")), lieu:g("lieu"),
    prefecture, groupe:g("groupe")?(impMatchRef(g("groupe"),GROUPES)||"Autre"):"Autre" };
  return {ligne:idx+2, d, err};
}
function impPreviewArmes(){
  const rows=IMP.grid.slice(1).map((r,i)=>impRowArme(r,i));
  IMP.rows=rows;
  const bad=rows.filter(r=>r.err.length), ok=rows.length-bad.length;
  $("impStep3").innerHTML=`<div class="panel"><div class="ph"><h3>Étape 3 — Contrôle et synchronisation (registre des armes)</h3>
    <span class="small">${ok} arme(s) valide(s) · <span style="color:${bad.length?"var(--danger)":"var(--ok)"}">${bad.length} en erreur</span></span></div>
  <div class="pb">
    <div class="pb nopad" style="max-height:300px;overflow:auto;border:1px solid var(--line);border-radius:8px;margin-bottom:12px">
    <table><thead><tr><th>Ligne</th><th>Rattachement</th><th>Type</th><th>Marque</th><th>N° série</th><th>État</th><th>Date / lieu</th><th>Anomalies</th></tr></thead><tbody>
    ${rows.slice(0,200).map(r=>`<tr style="${r.err.length?"background:#FDF1F1":""}"><td>${r.ligne}</td>
      <td>${esc(r.d.num)||`${esc(r.d.nom)} ${esc(r.d.prenom)}`}</td>
      <td class="small">${esc(r.d.type)}${r.d.type==="Autre"&&r.d.typeLibre?` <span class="muted">(${esc(r.d.typeLibre)})</span>`:""}</td>
      <td>${esc(r.d.marque)||"—"}</td><td>${esc(r.d.serie)||"—"}</td><td>${esc(r.d.etat)}</td>
      <td class="small">${r.d.dateRemise?fmtD(r.d.dateRemise):"—"}${r.d.lieu?" · "+esc(r.d.lieu):""}</td>
      <td class="small" style="color:var(--danger)">${r.err.join(" · ")||""}</td></tr>`).join("")}
    </tbody></table>${rows.length>200?`<div class="small muted" style="padding:8px">… ${rows.length-200} ligne(s) supplémentaires non affichées.</div>`:""}</div>
    <div class="grid2" style="margin-bottom:12px">
      <div class="field"><label>Dossiers introuvables</label><select id="impModeArmes">
        <option value="attach">Ignorer les armes sans dossier correspondant</option>
        <option value="create">Créer automatiquement le dossier (statut Désarmé — préfecture requise)</option>
      </select></div>
      <div class="small muted" style="align-self:end;padding-bottom:8px">Rattachement par n° de dossier, sinon par nom + prénom. Les n° de série déjà présents dans le registre sont ignorés (anti-doublon). Un dossier « Enregistré » qui reçoit une arme passe au statut « Désarmé ».</div>
    </div>
    <div style="display:flex;gap:9px">
      <button class="btn ghost" type="button" onclick="impBack()">← Modifier la correspondance</button>
      <button class="btn" ${ok?"":"disabled"} onclick="impSyncArmes()">Synchroniser ${ok} arme(s) avec les registres</button>
    </div>
  </div></div>`;
  $("impReport").innerHTML="";
}
function impSyncArmes(){
  const mode=$("impModeArmes").value;
  const series=new Set(allArmes().map(a=>normTxt(a.serie)).filter(x=>x));
  const ajoutees=[], crees=[], skipped=[];
  for(const r of IMP.rows){
    if(r.err.length){ skipped.push({ligne:r.ligne, motif:r.err.join(" · ")}); continue; }
    const d=r.d;
    if(d.serie&&series.has(normTxt(d.serie))){ skipped.push({ligne:r.ligne, motif:"n° de série déjà enregistré : "+d.serie}); continue; }
    let c=null;
    if(d.num) c=DB.combattants.find(x=>x.num===d.num);
    if(!c&&d.nom) c=DB.combattants.find(x=>x.nom===d.nom&&(!d.prenom||normTxt(x.prenom)===normTxt(d.prenom)));
    if(!c){
      if(mode!=="create"){ skipped.push({ligne:r.ligne, motif:"dossier introuvable : "+(d.num||`${d.nom} ${d.prenom}`)}); continue; }
      if(!d.prefecture){ skipped.push({ligne:r.ligne, motif:"création impossible sans préfecture"}); continue; }
      if(!d.nom){ skipped.push({ligne:r.ligne, motif:"création impossible sans nom"}); continue; }
      c={ id:"c"+Date.now()+Math.random().toString(36).slice(2,6), num:numDossier(), statut:"enregistre",
        creele:new Date().toISOString(), agent:CUR.login+" (import armes)",
        nom:d.nom, prenom:d.prenom, alias:"", sexe:"M", dn:"", ln:"", nat:"Centrafricaine", tel:"",
        fam:"Célibataire", prefecture:d.prefecture, sousPref:"", commune:"", site:d.lieu||"",
        groupe:d.groupe, grade:"", annees:"", zone:"", souhait:"Indécis", instr:"Aucun",
        obs:"Dossier créé lors de l'importation du registre des armes", photo:null,
        desarmement:null, demobilisation:null, reintMil:null, reintSocio:null, fin:null, abandon:null };
      DB.combattants.push(c); crees.push(c.num);
    }
    if(!c.desarmement) c.desarmement={date:d.dateRemise||"", lieu:d.lieu||"(importé)", agent:"Importation", armes:[]};
    if(!c.desarmement.date&&d.dateRemise) c.desarmement.date=d.dateRemise;
    if(d.type==="Munitions (lot)"){
      c.desarmement.munitions=c.desarmement.munitions||[];
      c.desarmement.munitions.push({nature:d.calibre||d.mun||d.typeLibre, qte:d.munQte||(+String(d.mun).replace(/\D/g,"")||1), unite:d.munUnite, obs:d.marque});
    } else {
      c.desarmement.armes.push({type:d.type, marque:d.marque, calibre:d.calibre, serie:d.serie, etat:d.etat, mun:d.mun});
      if(d.munQte){ c.desarmement.munitions=c.desarmement.munitions||[]; c.desarmement.munitions.push({nature:d.calibre||"", qte:d.munQte, unite:d.munUnite, obs:"remises avec "+(d.serie||d.type)}); }
    }
    if(d.serie) series.add(normTxt(d.serie));
    if(c.statut==="enregistre") c.statut="desarme";
    ajoutees.push((d.serie||d.type)+" → "+c.num);
  }
  IMP.report={ajoutees,crees,skipped};
  log("Importation registre des armes",`${IMP.fileName} — ${ajoutees.length} arme(s) ajoutée(s), ${crees.length} dossier(s) créé(s), ${skipped.length} ignorée(s)`);
  toast(`Synchronisation terminée : ${ajoutees.length} arme(s) ajoutée(s).`);
  $("impReport").innerHTML=`<div class="panel"><div class="ph"><h3>Rapport de synchronisation — ${esc(IMP.fileName)}</h3>
    <span style="display:flex;gap:7px"><button class="btn sm ghost" onclick="rImport('armes')">← Nouvelle importation</button>
    <button class="btn sm sec" onclick="go('armes')">Ouvrir le registre des armes</button></span></div><div class="pb">
    <div class="cards" style="margin-bottom:12px">
      <div class="kpi c-ok"><div class="n">${ajoutees.length}</div><div class="l">Armes ajoutées</div></div>
      <div class="kpi c-des"><div class="n">${crees.length}</div><div class="l">Dossiers créés</div></div>
      <div class="kpi c-ab"><div class="n">${skipped.length}</div><div class="l">Lignes ignorées</div></div>
    </div>
    ${ajoutees.length?`<div class="small" style="margin-bottom:6px"><b>Ajoutées :</b> ${ajoutees.map(n=>`<span class="tag">${esc(n)}</span>`).join(" ")}</div>`:""}
    ${crees.length?`<div class="small" style="margin-bottom:6px"><b>Dossiers créés :</b> ${crees.map(n=>`<span class="tag">${n}</span>`).join(" ")}</div>`:""}
    ${skipped.length?`<table style="margin-top:8px"><thead><tr><th>Ligne</th><th>Motif</th></tr></thead><tbody>${skipped.map(x=>`<tr><td>${x.ligne}</td><td class="small">${esc(x.motif)}</td></tr>`).join("")}</tbody></table>`:""}
  </div></div>`;
}
/* ---------- Synchronisation ---------- */
function impStubs(c){
  const ord=STATUTS[c.statut].ord;
  if(ord>=2&&!c.desarmement) c.desarmement={date:"",lieu:"(importé)",agent:"Importation",armes:[]};
  if(ord>=3&&!c.demobilisation) c.demobilisation={date:"",lieu:"(importé)",carte:c._carte||numDem()};
  if(c.statut==="reintegration_militaire"&&!c.reintMil) c.reintMil={corps:c._corps||"—",unite:"",matricule:c._matricule||"—",date:"",formation:""};
  if(c.statut==="reintegration_socio"&&!c.reintSocio) c.reintSocio={filiere:c._filiere||"—",centre:"",duree:"",date:"",kit:false,kitDate:"",appui:"",visites:[]};
  if(c.statut==="reintegre"&&!c.reintMil&&!c.reintSocio){
    if(c._matricule||c._corps) c.reintMil={corps:c._corps||"—",unite:"",matricule:c._matricule||"—",date:"",formation:""};
    else c.reintSocio={filiere:c._filiere||"—",centre:"",duree:"",date:"",kit:false,kitDate:"",appui:"",visites:[]};
  }
  if(c.statut==="abandon"&&!c.abandon) c.abandon={date:today(),motif:"Importé"};
  delete c._carte; delete c._corps; delete c._matricule; delete c._filiere;
}
function impSync(){
  const mode=$("impMode").value;
  const created=[], updated=[], skipped=[];
  for(const r of IMP.rows){
    if(r.err.length){ skipped.push({ligne:r.ligne, motif:r.err.join(" · ")}); continue; }
    const d=r.d;
    let ex=null;
    if(d.num) ex=DB.combattants.find(c=>c.num===d.num);
    if(!ex) ex=DB.combattants.find(c=>c.nom===d.nom&&normTxt(c.prenom)===normTxt(d.prenom)&&(!d.dn||!c.dn||c.dn===d.dn));
    if(ex){
      if(mode==="create"){ skipped.push({ligne:r.ligne, motif:"doublon de "+ex.num}); continue; }
      const fill=["alias","dn","ln","tel","sousPref","commune","site","grade","annees","zone","obs"];
      fill.forEach(f=>{ if(!ex[f]&&d[f]) ex[f]=d[f]; });
      if(d.groupe!=="Autre") ex.groupe=ex.groupe&&ex.groupe!=="Autre"?ex.groupe:d.groupe;
      if(!ex.prefecture||ex.prefecture!==d.prefecture&&d.prefecture) ex.prefecture=d.prefecture||ex.prefecture;
      if(d.souhait!=="Indécis"&&ex.souhait==="Indécis") ex.souhait=d.souhait;
      if(STATUTS[d.statut].ord>STATUTS[ex.statut].ord||d.statut==="abandon"&&ex.statut!=="reintegre"){
        ex.statut=d.statut;
        ex._carte=d.carte; ex._corps=d.corps; ex._matricule=d.matricule; ex._filiere=d.filiere;
        impStubs(ex);
      }
      updated.push(ex.num);
    } else {
      let num=d.num;
      const mnum=num.match(/^DDR-(\d{4})-(\d{1,5})$/);
      if(mnum){ DB.seq.comb=Math.max(DB.seq.comb,+mnum[2]); }
      else num=numDossier();
      const nc={ id:"c"+Date.now()+Math.random().toString(36).slice(2,6), num, statut:d.statut,
        creele:new Date().toISOString(), agent:CUR.login+" (import)",
        nom:d.nom, prenom:d.prenom, alias:d.alias, sexe:d.sexe, dn:d.dn, ln:d.ln, nat:d.nat, tel:d.tel,
        fam:"Célibataire", prefecture:d.prefecture, sousPref:d.sousPref, commune:d.commune, site:d.site,
        groupe:d.groupe, grade:d.grade, annees:d.annees, zone:d.zone, souhait:d.souhait, instr:d.instr,
        obs:d.obs||(d.groupe==="Autre"&&d.groupeLibre?("Groupe déclaré : "+d.groupeLibre):""), photo:null,
        desarmement:null, demobilisation:null, reintMil:null, reintSocio:null, fin:null, abandon:null,
        _carte:d.carte, _corps:d.corps, _matricule:d.matricule, _filiere:d.filiere };
      impStubs(nc);
      DB.combattants.push(nc);
      created.push(nc.num);
    }
  }
  IMP.report={created,updated,skipped};
  log("Importation",`${IMP.fileName} — ${created.length} créé(s), ${updated.length} mis à jour, ${skipped.length} ignoré(s)`);
  toast(`Synchronisation terminée : ${created.length} créé(s), ${updated.length} mis à jour.`);
  $("impReport").innerHTML=`<div class="panel"><div class="ph"><h3>Rapport de synchronisation — ${esc(IMP.fileName)}</h3>
    <span style="display:flex;gap:7px"><button class="btn sm ghost" onclick="rImport()">← Nouvelle importation</button>
    <button class="btn sm sec" onclick="go('registre')">Ouvrir le registre</button></span></div><div class="pb">
    <div class="cards" style="margin-bottom:12px">
      <div class="kpi c-ok"><div class="n">${created.length}</div><div class="l">Dossiers créés</div></div>
      <div class="kpi c-des"><div class="n">${updated.length}</div><div class="l">Dossiers mis à jour</div></div>
      <div class="kpi c-ab"><div class="n">${skipped.length}</div><div class="l">Lignes ignorées</div></div>
    </div>
    ${created.length?`<div class="small" style="margin-bottom:6px"><b>Créés :</b> ${created.map(n=>`<span class="tag">${n}</span>`).join(" ")}</div>`:""}
    ${updated.length?`<div class="small" style="margin-bottom:6px"><b>Mis à jour :</b> ${updated.map(n=>`<span class="tag">${n}</span>`).join(" ")}</div>`:""}
    ${skipped.length?`<table style="margin-top:8px"><thead><tr><th>Ligne</th><th>Motif</th></tr></thead><tbody>${skipped.map(x=>`<tr><td>${x.ligne}</td><td class="small">${esc(x.motif)}</td></tr>`).join("")}</tbody></table>`:""}
  </div></div>`;
}



/* module: stats/stats.js — PNDDRR engine (classic globals) */
/* ================= STATISTIQUES DU PROGRAMME ================= */
function trancheAge(c){
  const a=age(c.dn);
  if(a==="—") return "Âge inconnu";
  if(a<18) return "Moins de 18 ans";
  if(a<=25) return "18–25 ans";
  if(a<=35) return "26–35 ans";
  if(a<=45) return "36–45 ans";
  return "46 ans et plus";
}
function moisFR(k){ const [a,m]=k.split("-"); return ["janv.","févr.","mars","avr.","mai","juin","juil.","août","sept.","oct.","nov.","déc."][+m-1]+" "+a; }
function statsData(){
  const C=DB.combattants, A=allArmes(), MU=allMunitions();
  const ord=c=>STATUTS[c.statut].ord;
  const desTot=C.filter(c=>c.desarmement||ord(c)>=2&&c.statut!=="abandon").length;
  const demTot=C.filter(c=>c.demobilisation).length;
  const enReint=C.filter(c=>["reintegration_militaire","reintegration_socio"].includes(c.statut)).length;
  const reint=C.filter(c=>c.statut==="reintegre").length;
  const aband=C.filter(c=>c.statut==="abandon").length;
  const appui=C.reduce((a,c)=>a+(c.reintSocio&&c.reintSocio.appui?+c.reintSocio.appui:0),0);
  const visites=C.reduce((a,c)=>a+(c.reintSocio?c.reintSocio.visites.length:0),0);
  const pct=(n,d)=>d?Math.round(n/d*1000)/10:0;
  const parMois=(fn)=>{ const m={}; C.forEach(c=>{ const v=fn(c); if(v) m[v.slice(0,7)]=(m[v.slice(0,7)]||0)+1; }); return Object.entries(m).sort((a,b)=>a[0].localeCompare(b[0])).map(([k,v])=>[moisFR(k),v]); };
  return {C,A,MU,desTot,demTot,enReint,reint,aband,appui,visites,pct,
    sexe:countBy(C,c=>c.sexe==="M"?"Hommes":"Femmes"),
    ages:countBy(C,trancheAge),
    groupes:countBy(C,c=>c.groupe),
    regions:countBy(C,c=>regionOf(c.prefecture)),
    prefs:countBy(C,c=>c.prefecture),
    statuts:countBy(C,c=>STATUTS[c.statut].lbl),
    voies:countBy(C.filter(c=>c.reintMil||c.reintSocio),c=>c.reintMil?"Militaire":"Socio-économique"),
    filieres:countBy(C.filter(c=>c.reintSocio),c=>c.reintSocio.filiere),
    corps:countBy(C.filter(c=>c.reintMil),c=>c.reintMil.corps),
    armesType:countBy(A,a=>a.type),
    armesEtat:countBy(A,a=>a.etat),
    munUnites:(()=>{const m={};MU.forEach(x=>m[x.unite]=(m[x.unite]||0)+x.qte);return Object.entries(m).sort((a,b)=>b[1]-a[1]);})(),
    moisEnr:parMois(c=>c.creele),
    moisDes:parMois(c=>c.desarmement&&c.desarmement.date)};
}
function rStats(){
  const S=statsData(); const T=S.C.length;
  $("view").innerHTML = `
  <div class="toolbar">
    <div class="muted small" style="flex:1;align-self:center">Statistiques calculées en temps réel sur ${T} dossier(s) — arrêtées au ${new Date().toLocaleDateString("fr-FR")}.</div>
    <button class="btn sec" onclick="exportStatsCSV()">Exporter CSV</button>
    <button class="btn sec" onclick="printStats()">Imprimer le rapport / PDF</button>
  </div>
  <div class="cards">
    <div class="kpi"><div class="n">${T}</div><div class="l">Ex-combattants enrôlés</div></div>
    <div class="kpi c-des"><div class="n">${S.desTot}</div><div class="l">Désarmés (${S.pct(S.desTot,T)} %)</div></div>
    <div class="kpi c-dem"><div class="n">${S.demTot}</div><div class="l">Démobilisés (${S.pct(S.demTot,T)} %)</div></div>
    <div class="kpi c-rm"><div class="n">${S.enReint}</div><div class="l">Réintégration en cours</div></div>
    <div class="kpi c-ok"><div class="n">${S.reint}</div><div class="l">Réintégrés (${S.pct(S.reint,T)} %)</div></div>
    <div class="kpi c-ab"><div class="n">${S.aband}</div><div class="l">Abandons (${S.pct(S.aband,T)} %)</div></div>
    <div class="kpi"><div class="n">${S.A.length}</div><div class="l">Armes collectées</div></div>
    <div class="kpi c-dem"><div class="n">${S.MU.length}</div><div class="l">Lots de munitions</div></div>
    <div class="kpi c-rs"><div class="n">${fmtN(S.appui)}</div><div class="l">Appui financier (FCFA)</div></div>
    <div class="kpi"><div class="n">${S.visites}</div><div class="l">Visites de suivi</div></div>
  </div>
  <div class="grid2">
    <div class="panel"><div class="ph"><h3>Répartition par statut</h3></div><div class="pb">${barChart(S.statuts)}</div></div>
    <div class="panel"><div class="ph"><h3>Répartition par sexe</h3></div><div class="pb">${barChart(S.sexe)}</div></div>
    <div class="panel"><div class="ph"><h3>Répartition par tranche d'âge</h3></div><div class="pb">${barChart(S.ages)}</div></div>
    <div class="panel"><div class="ph"><h3>Répartition par groupe armé</h3></div><div class="pb">${barChart(S.groupes)}</div></div>
    <div class="panel"><div class="ph"><h3>Répartition par région</h3></div><div class="pb">${barChart(S.regions)}</div></div>
    <div class="panel"><div class="ph"><h3>Répartition par préfecture</h3></div><div class="pb">${barChart(S.prefs)}</div></div>
    <div class="panel"><div class="ph"><h3>Voies de réintégration</h3></div><div class="pb">${barChart(S.voies)}</div></div>
    <div class="panel"><div class="ph"><h3>Filières socio-économiques</h3></div><div class="pb">${barChart(S.filieres)}</div></div>
    <div class="panel"><div class="ph"><h3>Corps d'incorporation (voie militaire)</h3></div><div class="pb">${barChart(S.corps)}</div></div>
    <div class="panel"><div class="ph"><h3>Armes par type</h3></div><div class="pb">${barChart(S.armesType)}</div></div>
    <div class="panel"><div class="ph"><h3>Armes par état</h3></div><div class="pb">${barChart(S.armesEtat)}</div></div>
    <div class="panel"><div class="ph"><h3>Munitions par unité (quantités)</h3></div><div class="pb">${barChart(S.munUnites)}</div></div>
    <div class="panel"><div class="ph"><h3>Enregistrements par mois</h3></div><div class="pb">${barChart(S.moisEnr)}</div></div>
    <div class="panel"><div class="ph"><h3>Désarmements par mois</h3></div><div class="pb">${barChart(S.moisDes)}</div></div>
  </div>`;
}
function statsSections(){
  const S=statsData();
  return [["Statuts",S.statuts],["Sexe",S.sexe],["Tranches d'âge",S.ages],["Groupes armés",S.groupes],
    ["Régions",S.regions],["Préfectures",S.prefs],["Voies de réintégration",S.voies],
    ["Filières socio-économiques",S.filieres],["Corps d'incorporation",S.corps],
    ["Armes par type",S.armesType],["Armes par état",S.armesEtat],["Munitions par unité",S.munUnites],
    ["Enregistrements par mois",S.moisEnr],["Désarmements par mois",S.moisDes]];
}
function exportStatsCSV(){
  const S=statsData();
  const rows=[["Section","Catégorie","Valeur"]];
  rows.push(["Indicateurs","Ex-combattants enrôlés",S.C.length],["Indicateurs","Désarmés",S.desTot],
    ["Indicateurs","Démobilisés",S.demTot],["Indicateurs","Réintégration en cours",S.enReint],
    ["Indicateurs","Réintégrés",S.reint],["Indicateurs","Abandons",S.aband],
    ["Indicateurs","Armes collectées",S.A.length],["Indicateurs","Lots de munitions",S.MU.length],
    ["Indicateurs","Appui financier (FCFA)",S.appui],["Indicateurs","Visites de suivi",S.visites]);
  statsSections().forEach(([sec,pairs])=>pairs.forEach(([k,v])=>rows.push([sec,k,v])));
  dl(`pnddrr_statistiques_${today()}.csv`, csv(rows), "text/csv");
  log("Export CSV","Statistiques du programme"); toast("Statistiques CSV téléchargées.");
}
function printStats(){
  const S=statsData(); const T=S.C.length;
  const tbl=(titre,pairs)=>pairs.length?`<h3 style="font-size:13px;margin-top:12px;text-decoration:underline">${titre}</h3>
    <table class="dt"><tr><th>Catégorie</th><th style="text-align:right">Effectif</th></tr>${
    pairs.map(([k,v])=>`<tr><td>${esc(String(k))}</td><td style="text-align:right">${fmtN(v)}</td></tr>`).join("")}</table>`:"";
  doPrint(docWrap(`${docEntete("Rapport statistique du programme")}
    <h2 class="titre">Rapport statistique</h2>
    <p>Arrêté au ${new Date().toLocaleDateString("fr-FR")}, le programme compte <b>${T}</b> ex-combattant(s) enrôlé(s), dont
    <b>${S.desTot}</b> désarmé(s) (${S.pct(S.desTot,T)} %), <b>${S.demTot}</b> démobilisé(s), <b>${S.enReint}</b> en parcours de réintégration,
    <b>${S.reint}</b> réintégré(s) (${S.pct(S.reint,T)} %) et <b>${S.aband}</b> abandon(s). <b>${S.A.length}</b> arme(s) et <b>${S.MU.length}</b> lot(s)
    de munitions ont été collectés. L'appui financier versé s'élève à <b>${fmtN(S.appui)} FCFA</b> et <b>${S.visites}</b> visite(s) de suivi ont été réalisées.</p>
    ${statsSections().map(([t,p])=>tbl(t,p)).join("")}
    <div class="sig"><div class="c"></div><div class="c">Fait à ${esc(cfg("villeSignature")||"Bangui")}, le ${new Date().toLocaleDateString("fr-FR")}<br>Pour le PNDDRR<div class="ligne">${esc(CUR.nom)}</div></div></div>`));
  log("Impression","Rapport statistique");
}

window.__PNDDRR_READY = true;
document.dispatchEvent(new Event("pnddrr-ready"));
